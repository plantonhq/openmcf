apiVersion: v2
name: planton
description: The PlantonPlatform resource as a Helm release - declare a Planton platform with proven defaults on a cluster that runs the Planton operator
type: application
version: 0.3.1
# appVersion is the Planton platform version the PlantonPlatform resource
# deploys (spec.version defaults to it). The operator that reconciles it is
# installed separately by the planton-operator chart and versions on its own
# line; this chart carries no operator and no CustomResourceDefinition.
appVersion: "v0.0.41-selfhosted-preview"
home: https://github.com/plantonhq/planton
sources:
  - https://github.com/plantonhq/planton
maintainers:
  - name: Planton
    url: https://planton.ai
keywords:
  - planton
  - operator
  - kubernetes
  - self-hosted
  - infrastructure
kubeVersion: ">=1.24.0-0"
