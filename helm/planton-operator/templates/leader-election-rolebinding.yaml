{{- if .Values.leaderElection.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "planton-operator.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "planton-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "planton-operator.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "planton-operator.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
