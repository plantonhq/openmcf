---
alwaysApply: false
---

# Delete: Safely Remove Components

## Purpose

This rule safely removes components from the Planton codebase, ensuring complete cleanup of all artifacts while providing safety features like dry-run and backup.

## Role

You are the Planton Code Partner. This rule orchestrates the safe deletion of components, removing all traces from proto files, IaC modules, documentation, and registry entries.

## When to Use Delete

Use delete when you need to:
- ✅ **Remove obsolete components** (deprecated, replaced, no longer supported)
- ✅ **Clean up test components** (experimental, POC, no longer needed)
- ✅ **Remove duplicates** (consolidating similar components)
- ✅ **Decommission providers** (discontinuing support for a cloud provider)

**Don't use delete when:**
- ❌ Component needs updates (use **update** instead)
- ❌ Component needs fixes (use **update** instead)
- ❌ Just checking status (use **audit** instead)
- ❌ Component doesn't exist (nothing to delete)

## Safety First

Delete is **irreversible** without backups. Safety features:

1. **🔍 Dry-Run Mode** - Preview what would be deleted
2. **💾 Automatic Backup** - Creates timestamped backup before deletion
3. **🔎 Reference Check** - Warns if component is referenced elsewhere
4. **✋ Confirmation Required** - Must explicitly confirm (unless --force)
5. **📋 Detailed Report** - Shows exactly what was deleted

## Usage

### Basic Syntax

```
@delete-planton-component <ComponentName> [flags]
```

### Safety Workflow (Recommended)

```bash
# Step 1: Dry-run to see what would be deleted
@delete-planton-component CloudflareD1Database --dry-run

# Step 2: Review the plan, then delete with backup
@delete-planton-component CloudflareD1Database --backup

# Step 3: Verify deletion
# Check that component folder is gone
```

### Quick Delete (Use with Caution)

```bash
# Delete with force (skips confirmation)
@delete-planton-component ObsoleteComponent --force --backup
```

## Deletion Workflow

### Step 1: Validate Component Exists

1. Lookup component in `cloud_resource_kind.proto`
2. Verify component path exists
3. Extract metadata (provider, enum value, id_prefix)
4. If not found, report error and exit

**Important:** Before proceeding with deletion, read the component's `GUIDE.md` (component root) if it exists, along with its `README.md` and `catalog.md`. This helps understand:
- Component's purpose and use cases
- Why it was created (was it well-researched or experimental?)
- Impact of removing it (what users might be affected)
- Whether it's truly obsolete or just needs updates

**A well-documented component with a grounded GUIDE.md might warrant reconsideration vs quick deletion.**

### Step 2: Discover All Artifacts

Identify everything related to the component:

**Core Files:**
- Component folder: `catalog/<provider>/<component>/`
- All contents: version dir (protos, stubs, tests), IaC modules, presets, e2e manifests, docs

**Registry Entry:**
- Enum entry in `cloud_resource_kind.proto`

**Generated Files:**
- `.pb.go` stubs (will be regenerated by `make protos`)

**Potentially Related:**
- References in other components (imports, dependencies)
- References in documentation
- References in examples

### Step 3: Check for References

Search codebase for references to the component:

**Search patterns:**
- Component name in Go imports
- Component name in proto imports
- Component enum value references
- Component in documentation

**Inbound-link sweep (mandatory — links INTO the component, not just its own
files):** deleting the component's folder removes its pages, but every
OTHER page that links to those pages becomes a dead link. Grep for the
kind name AND its catalog slug (the `/docs/catalog/<provider>/<kind>`
form) across:
- sibling kinds' `presets/*.md`, `catalog.md`, `README.md`, and
  `GUIDE.md` under `catalog/` — prose like "pair this with X" outlives X
- `charts/**` templates and READMEs

Fix each hit: remove the dead link, or repoint the prose to the replacing
component(s). Prose that names the cloud provider's own product/feature
(which still exists) is fine — only pointers to the retired Planton kind
are defects.

**If references found:**
```
⚠️  Warning: CloudflareD1Database is referenced in other files

Found 2 references:
  1. catalog/cloudflare/backup/v1alpha1/spec.proto
     Line 15: import "catalog/cloudflare/cloudflared1database/v1alpha1/api.proto"
     
  2. catalog/aws/awsrdsinstance/GUIDE.md
     Line 45: CloudflareD1Database vs AwsRdsInstance

Action Required:
  - Remove or update these references before deleting
  - Or use --force to delete anyway (may break builds)

Proceed? (y/n): _
```

### Step 4: Create Backup (Optional but Recommended)

If `--backup` flag is used:

```bash
# Create timestamped backup
TIMESTAMP=$(date +"%Y-%m-%d-%H%M%S")
BACKUP_DIR="catalog/<provider>/<component>-backup-$TIMESTAMP"

# Copy entire component folder
cp -r catalog/<provider>/<component> $BACKUP_DIR

# Save enum entry
grep -A 5 "^  <ComponentName> = " cloud_resource_kind.proto > $BACKUP_DIR/enum_entry.txt
```

**Backup location shown:**
```
💾 Backup created at:
  catalog/cloudflare/cloudflared1database-backup-2025-11-13-143022/

To restore:
  cp -r cloudflared1database-backup-2025-11-13-143022 cloudflared1database
  # Manually restore enum entry to cloud_resource_kind.proto
```

### Step 5: Confirm Deletion (Unless --force)

Show deletion plan and ask for confirmation:

```
🗑️  Deletion Plan for CloudflareD1Database

Enum Entry:
  CloudflareD1Database = 7005 [(kind_meta) = {
    provider: cloudflare
    version: "v1alpha1"
    id_prefix: "cfd1db"
  }]

Component Path:
  catalog/cloudflare/cloudflared1database/

Files to Delete (25 files):
  📄 README.md, catalog.md, logo.svg (component root)
  📁 v1alpha1/ (api.proto, spec.proto, input.proto, outputs.proto,
     *.pb.go, spec_test.go, BUILD.bazel, reference.md)
  📁 presets/ (2 files)
  📁 e2e/ (2 files)
  📁 iac/pulumi/ (5 files)
  📁 iac/tf/ (6 files)

Registry Update:
  Remove CloudflareD1Database enum entry from cloud_resource_kind.proto

⚠️  This action is irreversible!
Backup created: Yes (cloudflared1database-backup-2025-11-13-143022)

Type 'DELETE CloudflareD1Database' to confirm: _
```

### Step 6: Execute Deletion

If confirmed:

1. **Remove component folder**
   ```bash
   rm -rf catalog/<provider>/<component>/
   ```

2. **Remove enum entry from cloud_resource_kind.proto**
   - Find and remove the enum entry
   - Maintain file formatting
   - Preserve surrounding entries

3. **Regenerate proto stubs** (optional)
   ```bash
   make protos
   ```
   This removes stale .pb.go files for deleted component

4. **Verify deletion**
   - Component folder gone
   - Enum entry removed
   - No stale references

### Step 7: Generate Deletion Report

Show what was deleted:

```
✅ Deletion Complete: CloudflareD1Database

Removed:
  ✅ Component folder (23 files deleted)
     catalog/cloudflare/cloudflared1database/v1alpha1/
     
  ✅ Enum entry removed
     cloud_resource_kind.proto: CloudflareD1Database = 7005
     
  ✅ Proto stubs regenerated
     make protos completed successfully

Backup:
  💾 Saved to: cloudflared1database-backup-2025-11-13-143022/
  
References Found:
  ⚠️  3 references in other files (review recommended)

Next Steps:
  1. Update files that referenced CloudflareD1Database
  2. Run: go build (verify component no import errors)
  3. Run: go test (verify component no test failures)
  4. Commit changes:
     git add -A
     git commit -m "Remove CloudflareD1Database component"

Duration: 15 seconds
```

## Flags and Options

### --dry-run (Recommended First Step)

Preview deletion without making changes:

```bash
@delete-planton-component CloudflareD1Database --dry-run
```

**Output:**
```
🔍 Dry-Run: CloudflareD1Database Deletion

Would Delete:
  📁 catalog/cloudflare/cloudflared1database/v1alpha1/
     (23 files, 450 KB)
     
  📝 Enum entry: CloudflareD1Database = 7005
  
Would Check:
  🔎 References in codebase
  
Would Create:
  💾 Backup (if --backup flag used)

No files will be modified (dry-run mode)

To proceed: remove --dry-run flag
To create backup: add --backup flag
```

### --backup (Strongly Recommended)

Create backup before deleting:

```bash
@delete-planton-component CloudflareD1Database --backup
```

Creates timestamped backup folder with all component files and enum entry.

### --force (Use with Caution)

Skip confirmation prompt:

```bash
@delete-planton-component ObsoleteComponent --force --backup
```

**Use when:**
- Scripting/automation
- Certain about deletion
- Already verified no issues

**Don't use when:**
- Unsure about references
- Component might be needed later
- First time deleting

### --skip-references

Skip reference checking (faster but riskier):

```bash
@delete-planton-component TestComponent --skip-references --force
```

**Warning:** May break builds if component is referenced elsewhere.

## Examples

### Example 1: Safe Deletion (Recommended)

```bash
# Step 1: Preview
@delete-planton-component ObsoleteComponent --dry-run

# Step 2: Review output, then delete with backup
@delete-planton-component ObsoleteComponent --backup

# Step 3: Confirm when prompted
# Type: DELETE ObsoleteComponent

# Step 4: Verify
ls catalog/aws/obsoletecomponent/
# ls: cannot access ... : No such file or directory ✅

# Step 5: Check for broken references
go build ./catalog/<provider>/<component>/...
go test -v ./catalog/<provider>/<component>/...
```

### Example 2: Quick Deletion (Known Safe)

```bash
# Delete test component (known to have no references)
@delete-planton-component TestCloudResourceGeneric --force --backup

# Verify and commit
go build ./catalog/<provider>/<component>/... && go test -v ./catalog/<provider>/<component>/... && git add -A && git commit -m "Remove test component"
```

### Example 3: Delete After Moving

```bash
# Scenario: Consolidated KubernetesPostgres and KubernetesPostgresOperator
# Now removing deprecated KubernetesPostgres

# Step 1: Verify migration complete (all users moved to new component)

# Step 2: Delete old component
@delete-planton-component KubernetesPostgres --backup

# Step 3: Update references
# Find any docs/examples mentioning old component
grep -r "KubernetesPostgres" catalog/
# Update to reference KubernetesPostgresOperator

# Step 4: Commit
git add -A
git commit -m "Remove deprecated KubernetesPostgres (consolidated into KubernetesPostgresOperator)"
```

### Example 4: Bulk Deletion

```bash
# Delete multiple test components
for component in TestCloudResourceGeneric TestCloudResourceKubernetes TestCloudResourceKubernetes; do
  @delete-planton-component $component --force --backup
done

# Verify
go build ./catalog/<provider>/<component>/... && go test -v ./catalog/<provider>/<component>/...
```

## Reference Checking

Delete searches for references in:

**Go Code:**
- Import statements
- Type references
- Variable declarations

**Proto Files:**
- Import statements
- Field types
- Message references

**Documentation:**
- Markdown files
- README files
- Examples

**Configuration:**
- Manifest files
- Build configurations

**If references found:**
- Shows all locations
- Suggests fixes
- Requires explicit confirmation to proceed

## Error Handling

### Component Not Found

```
❌ CloudflareD1Database not found

Checked:
  ✓ cloud_resource_kind.proto - No enum entry
  ✓ File system - No component folder

Possible reasons:
  1. Component name misspelled
  2. Component already deleted
  3. Component never existed

Available components:
  - CloudflareDnsZone
  - CloudflareKvNamespace
  - CloudflareR2Bucket
  ...
```

### References Prevent Deletion

```
❌ Cannot delete: CloudflareD1Database is referenced in 3 files

Critical References:
  1. catalog/cloudflare/backup/v1alpha1/spec.proto
     - Uses CloudflareD1Database as field type
     - Must update before deletion
     
Options:
  1. Fix references first, then delete
  2. Use --force to delete anyway (may break builds)
  3. Cancel deletion

Recommendation: Fix references first for safety
```

### Deletion Failed

```
❌ Deletion failed: Permission denied

Error: cannot remove 'catalog/cloudflare/cloudflared1database/'
Reason: Directory not writable

Fix:
  chmod -R u+w catalog/cloudflare/cloudflared1database/
  
Then retry deletion.
```

## Safety Checklist

Before deleting, verify:

- [ ] Component is truly obsolete (not just needs updates)
- [ ] No active users of the component
- [ ] References have been updated or documented
- [ ] Backup will be created
- [ ] Team is aware of deletion
- [ ] Alternative component exists (if applicable)

## Restoring Deleted Components

If you need to restore:

### From Backup

```bash
# Find backup
ls catalog/<provider>/*-backup-*/

# Restore component folder
cp -r cloudflared1database-backup-2025-11-13-143022/v1 cloudflared1database/

# Restore enum entry
cat cloudflared1database-backup-2025-11-13-143022/enum_entry.txt
# Manually add back to cloud_resource_kind.proto

# Regenerate stubs
make protos

# Verify
go build ./catalog/<provider>/<component>/... && go test -v ./catalog/<provider>/<component>/...
```

### From Git History

```bash
# Find deletion commit
git log --all --full-history -- "catalog/<provider>/<component>/"

# Restore from before deletion
git checkout <commit-before-deletion> -- catalog/<provider>/<component>/

# Restore enum entry from cloud_resource_kind.proto
git show <commit-before-deletion>:shared/cloudresourcekind/cloud_resource_kind.proto | grep -A 5 "<ComponentName>"
# Manually add back to current cloud_resource_kind.proto

# Regenerate and verify
make protos && go build ./catalog/<provider>/<component>/... && go test -v ./catalog/<provider>/<component>/...
```

## Integration with Other Rules

### After Audit

```bash
# Audit reveals component is incomplete and obsolete
@audit-planton-component OldComponent
# Result: 40% complete, not worth updating

# Decision: Delete instead of update
@delete-planton-component OldComponent --backup
```

### Before Forge

```bash
# Want to recreate component from scratch
@delete-planton-component OldImplementation --backup

# Create new implementation
@forge-planton-component NewImplementation --provider aws
```

## Best Practices

### Before Deletion

1. ✅ **Run audit** - Understand current state
2. ✅ **Check references** - Search codebase
3. ✅ **Notify team** - Communicate intent
4. ✅ **Document why** - Record rationale
5. ✅ **Use dry-run** - Preview changes

### During Deletion

1. ✅ **Always use --backup** - Safety first
2. ✅ **Review confirmation** - Read carefully
3. ✅ **Type component name** - Deliberate action
4. ✅ **Watch for errors** - Monitor output

### After Deletion

1. ✅ **Verify build** - Run go build
2. ✅ **Verify tests** - Run go test
3. ✅ **Update docs** - Remove references
4. ✅ **Commit with context** - Good commit message
5. ✅ **Keep backup** - Don't delete immediately

## Troubleshooting

### Can't Delete: Permission Denied

```bash
# Fix permissions
chmod -R u+w catalog/<provider>/<component>/

# Retry deletion
@delete-planton-component <ComponentName> --backup
```

### References Blocking Deletion

**Option 1: Fix references first (recommended)**
```bash
# Find all references
grep -r "CloudflareD1Database" catalog/

# Update each reference
# Then delete
```

**Option 2: Force delete (risky)**
```bash
@delete-planton-component CloudflareD1Database --force --backup

# Fix build errors after
go build ./catalog/<provider>/<component>/...  # Shows what broke
# Fix broken imports/references
```

### Accidentally Deleted

```bash
# Restore from backup (if created)
cp -r cloudflared1database-backup-2025-11-13-143022/v1 cloudflared1database/

# Or restore from git
git checkout HEAD~1 -- catalog/cloudflare/cloudflared1database/

# Restore enum entry
git show HEAD~1:shared/cloudresourcekind/cloud_resource_kind.proto | grep -A 5 "CloudflareD1Database"
# Add back to cloud_resource_kind.proto

# Regenerate
make protos
```

## Success Criteria

After deletion:

✅ Component folder removed
✅ Enum entry removed from registry
✅ No broken references (or documented)
✅ Build succeeds (go build ./catalog/...)
✅ Tests pass (go test -v ./catalog/...)
✅ Backup created (if --backup used)
✅ Deletion report generated
✅ Changes committed

## Reference

- **Ideal State:** `architecture/component.md`
- **Delete README:** `_rules/component/delete/README.md`
- **Other Rules:** `audit`, `forge`, `update`

---

**Ready to delete?** Remember: Always use `--dry-run` first, then `--backup` when deleting!
