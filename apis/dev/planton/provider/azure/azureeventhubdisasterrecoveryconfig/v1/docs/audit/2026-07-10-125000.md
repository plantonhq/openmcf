# Audit Report: AzureEventHubDisasterRecoveryConfig

**Date:** 2026-07-10-125000
**Component:** AzureEventHubDisasterRecoveryConfig (enum 520, provider azure, id_prefix `azehdr`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80
`azurerm_eventhub_namespace_disaster_recovery_config` surface: alias name
(ForceNew) + primary namespace reference (ForceNew; both engines parse
the provider's discrete-name addressing from the resolved ARM id) +
partner reference (updatable -- the provider break-pairs and re-pairs),
with the create-settle, partner-change, and
break-pair/delete/404-wait/name-release destroy choreography documented
in both modules. Faithful to Event Hubs' own surface: the resource
exposes no credential outputs (alias-addressed connection strings surface
on the namespace and authorization-rule kinds).

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 520 (opens the Event Hubs continuation block -- the messaging sub-band 470-479 is full), id_prefix `azehdr` unique; no registry prerequisite (two scenario-local namespaces) |
| 2 | Folder Structure | ✅ `provider/azure/azureeventhubdisasterrecoveryconfig/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 10 spec tests pass (alias contract boundaries, both required references); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; anchored primary-id parse with clear errors; release-equivalent build passes; the lifecycle choreography documented |
| 5 | IaC Terraform | ✅ canonical empty provider block; anchored named-group regex parse; the same choreography comments |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ metadata-vs-event-data replication, the alias identity model, the no-credential-outputs design, the pairing contracts, failover-from-secondary |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders the parse + partner pass-through) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 1 pair (geo-dr-pairing), validates |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- no secret-bearing fields (the outputs are the pairing identity only); `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `eventhub_namespace_disaster_recovery_config_resource.go`:

- **Create polls Accepted → Succeeded** (fails on DRFailed): documented
  in both modules.
- **Partner change breaks the existing pairing first** (Update calls
  BreakPairing, waits, re-pairs): documented on the field and in both
  modules.
- **Destroy choreography** (Delete: BreakPairing → wait Succeeded →
  Delete → poll GET to 404 → poll CheckNameAvailability until the alias
  name frees): documented -- destroys take minutes by the service's own
  design; the E2E profile budgets for it.
- **Pairing contracts** (different regions, same tier, empty partner)
  live in ARM, not the provider -- documented as apply-time
  cross-resource contracts, deliberately not CEL'd (references cannot be
  dereferenced at validation time; inventing a partial check would be a
  defect).
- **Alias name validator** (reuses ValidateEventHubAuthorizationRuleName,
  ≤60): front-loaded as a field CEL.
- No spec CEL invents a rule the provider does not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH -- identical anchored parse of the SAME resolved
  `primary_namespace_id`; partner id passes through untouched on both
- Naming basis: MATCH (`alias_name` both engines)
- Labels/tags: N/A (ARM does not support tags on this resource type)
- Feature coverage: MATCH -- three fields, all honored identically
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- id/alias byte-identical; `pkg/outputs`
  conformance case added

**PARITY ✅** (no exceptions needed).

## Recorded Skips

- Credential outputs -- Azure's Event Hubs DR resource exposes none;
  alias-addressed connection strings are the namespace and
  authorization-rule kinds' `*_connection_string_alias` outputs (the
  service's own credential surface).
- The wire-level DR role read-back (Primary/PrimaryNotReplicating/
  Secondary) -- not surfaced by the provider.

## Critical Gaps

None.
