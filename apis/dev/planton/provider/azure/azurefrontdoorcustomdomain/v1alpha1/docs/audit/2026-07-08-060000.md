# Audit Report: AzureFrontDoorCustomDomain

**Audit Date:** 2026-07-08 06:00:00
**Component Kind:** AzureFrontDoorCustomDomain
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurefrontdoorcustomdomain/v1alpha1/`
**Enum Value:** 486
**ID Prefix:** azfdcd

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ Enum `AzureFrontDoorCustomDomain = 486`;
`prerequisites: [AzureFrontDoorProfile]` with rationale (DNS zone and
secret are optional references, not deploy-order parents);
`id_prefix: "azfdcd"` unique.

### 2. Folder Structure (4.44%)

✅ Correct hierarchy.

### 3. Protobuf API Definitions (22.20%)

✅ Full azurerm v4.80 surface: parent FK; the provider's 2-260
resource-name regex; host_name with the provider's exact FQDN contract
(<=253, >=2 labels, each <=63 with letter/digit edges, wildcard only as
the whole first label); optional `dns_zone_id` FK → AzureDnsZone;
required `tls` with certificate-type enum (Managed default),
`secret_id` FK → AzureFrontDoorSecret CEL-paired both directions, and
the full cipher-suite block (predefined TLS12_2022/TLS12_2023 sets XOR
CUSTOMIZED with the provider's four-suite ECDHE-RSA TLS 1.2 allowlist
and the both-TLS 1.3-suites-when-pinned contract). Managed-cert CELs:
host_name <= 64 chars and never a wildcard. `spec_test.go`: 30 cases
(9 valid, 21 error paths incl. the CustomizeDiff contracts) -- all
PASS.

### 4. IaC Modules - Pulumi (13.32%)

✅ Single `cdn.FrontdoorCustomDomain` on the shared builder; comments
explain the pending-validation lifecycle, the constant TLS12 floor (the
recorded minimum-version skip), and the sent-only-when-configured
cipher policy. Release-equivalent build passes.

### 5. IaC Modules - Terraform (4.24%)

✅ Canonical provider block; `planton tofu plan` renders the
customer-certificate + secret + DNS-zone + CUSTOMIZED-ciphers seams
(certificate_type `CustomerCertificate`, both TLS 1.3 suites, the
camelCase `dnsZones` ARM segment).

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅** (`pkg/outputs`: 4/4 populate, zero
unmapped)

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `spec.profile_id.GetValue()` | `var.spec.profile_id` | ✅ MATCH |
| Naming basis | `spec.domain_name` | same | ✅ MATCH |
| Minimum TLS | constant `TLS12` via `minimum_version` (never the deprecated alias) | same | ✅ MATCH |
| Certificate type | sent only when chosen (ARM defaults Managed) | `ManagedCertificate` materialized when absent | ✅ MATCH (same wire result) |
| Secret / DNS zone refs | sent only when set | null passthrough | ✅ MATCH |
| Cipher suite | only when configured; empty tls13 omitted | dynamic block; empty tls13 → null | ✅ MATCH |
| Tags | none (ARM unsupported on custom domains) | none | ✅ MATCH |
| Stack outputs → proto | 4 exports | 4 outputs, same names | ✅ MATCH |

### 6-10. Documentation, Supporting Files, Presets

✅ `docs/README.md` (field map, the minimum-version recorded skip with
its re-enable trigger, the pending-validation lifecycle proof, provider
timeout rationale); user docs + module READMEs teaching the
validation workflow; hack manifest exercising customer-cert +
ciphers + DNS zone; presets ×3 (managed certificate, wildcard BYO,
hardened ciphers) -- validate green; E2E `scenarios/minimal.yaml`
(pending-validation token proof), `profile.yaml` with the recorded
validation-completion exclusion, verifier + test entrypoints.

### 11. Secure-by-Default Coverage (Critical GATE)

**COVERAGE ✅** -- no secret-bearing fields. The `validation_token`
output is a public DNS challenge by design (proves DNS control, not
secret possession); `secret_id` is an ARM reference, not key material.

---

## Apply-Time Validator Source-Diff

Walked `cdn_frontdoor_custom_domain_resource.go`'s TWO CustomizeDiff
functions and the create/update expand:

- `frontDoorCustomDomainHostNameCustomizeDiff` (managed-cert 64-char +
  no-wildcard) -- front-loaded as the spec's managed-cert message CEL.
- `frontDoorCustomDomainTlsCustomizeDiff` (customized-ciphers pairing,
  at-least-one-TLS 1.2, both-TLS 1.3-suites) -- all front-loaded.
- `expandAfdDomainTlsParameters` (secret required iff Customer,
  forbidden iff Managed) -- front-loaded both directions.
- The update path's approved-state wait poller and the always-resend
  secret quirk are provider-internal; both engines inherit them.
- The deprecated `minimum_tls_version`/TLS10 retirement error is
  unreachable (the alias is not modeled) -- the recorded skip.
- No invented rules found.

## Critical Gaps

None.

---

**Audit completed at:** 2026-07-08-060000
**Reference:** `architecture/deployment-component.md`
