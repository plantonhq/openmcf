# Audit Report: AzureApplicationInsightsStandardWebTest

**Date:** 2026-07-12-130505
**Component:** AzureApplicationInsightsStandardWebTest (enum 457, provider azure, id_prefix `azwebtest`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**
(after two audit-caught spec gaps were fixed -- see the source-diff
section)

Forged at the full azurerm v4.80
`azurerm_application_insights_standard_web_test` surface: the required
request block (URL, the seven-verb vocabulary, headers, body, redirect
and dependent-request dials), validation rules (expected status, SSL
certificate checks, content match), the 300/600/900 frequency
vocabulary, timeout, retry, geo-locations (min 1), and the
`synthetic_monitor_id` output that feeds the metric-alert web-test
criteria. The metric alert's `web_test_id` plain string was retrofitted
to an FK on this kind in the same change.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 457 (observability sub-band), id_prefix `azwebtest` unique, `prerequisites: [AzureApplicationInsights]` with comment |
| 2 | Folder Structure | ✅ |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; spec tests cover frequency/verb vocabularies, SSL pairings (added in-audit), header requireds -- happy + error paths |
| 4 | IaC Pulumi | ✅ shared builder; builds; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; dynamic validation-rules/content blocks |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ geo-location id semantics (web-test location ids, not region names), availability-metric pairing |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ READMEs, hack manifest (plan renders request + validation rules) |
| 9 | Build Files | ✅ gazelle |
| 10 | Presets | ✅ 2 pairs (endpoint-availability / content-check), validate |
| 11 | Sensitive Coverage | ✅ no secret-bearing fields (headers may carry tokens at the USER's discretion -- documented; the field itself is config, matching azurerm) |

## Apply-Time Validator Source-Diff

Read `application_insights_standard_web_test_resource.go` schema +
CustomizeDiff + expand:

- **frequency IntInSlice {300,600,900}**: front-loaded as a field CEL.
- **http_verb vocabulary** (GET/POST/PUT/PATCH/DELETE/HEAD/OPTIONS):
  front-loaded as a field CEL.
- **ssl_cert_remaining_lifetime IntBetween(1,365)**: front-loaded as a
  field CEL.
- **SSL checks require an https URL** (CustomizeDiff: ssl_check_enabled
  true or a non-zero lifetime on a non-https request URL is rejected):
  **audit-caught gap, fixed** -- front-loaded as the
  `web_test_ssl_checks_require_https` root-message CEL (both fields live
  in the same spec message, so the cross-block contract is expressible).
- **the lifetime rides the SSL check** (expand sends
  SSLCertRemainingLifetimeCheck only when SSLCheck is true -- a lifetime
  without the check is SILENTLY DROPPED): **audit-caught gap, fixed** --
  front-loaded as the `web_test_ssl_lifetime_requires_check` message CEL
  (the anti-silent-drop treatment), and the field comment corrected (it
  had claimed setting the lifetime enables the check -- false).
- **ignore_status_code**: skipped by the provider itself over an
  API-spec typo (its own commented-out block) -- recorded skip.
- No spec CEL invents a rule the provider and ARM do not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved resource group + component id)
- Naming basis: MATCH
- Labels/tags: family-wide tag-shape PARITY-EXCEPTION two-sided
- Feature coverage: MATCH -- every optional presence-guarded identically;
  provider defaults (GET, follow-redirects, parse-dependent, 300s
  frequency, 30s timeout, 200 status) applied by omission on both
- Stack outputs: MATCH -- `web_test_id`/`web_test_name`/
  `synthetic_monitor_id`; `pkg/outputs` conformance case added
- Retrofit call site: the metric alert's web-test criteria resolves
  `web_test_id` via `GetValue()` on Pulumi; Terraform converter-flattened

**PARITY ✅** (tag-shape exception only).

## Recorded Skips

- `ignore_status_code` -- the provider skips it itself (API-spec typo,
  their commented-out schema block). Re-enable trigger: the upstream
  issue closing.

## Critical Gaps

None remaining. Two spec gaps found and fixed in-audit (the https gate
and the lifetime-requires-check pairing, both now CELs with error-path
tests).

## Live E2E

Dual-engine green (Pulumi 277s / Terraform 288s, all 8 phases, zero
orphans) through the composed chain: Application Insights prerequisite
(bringing its workspace and resource group) → web test pinging
https://microsoft.com with SSL checks on → ARM verify → destroy →
verify-gone.
