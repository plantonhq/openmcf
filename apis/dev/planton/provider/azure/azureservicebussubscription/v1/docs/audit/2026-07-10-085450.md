# Audit Report: AzureServiceBusSubscription

**Date:** 2026-07-10-085450
**Component:** AzureServiceBusSubscription (enum 474, provider azure, id_prefix `azsbsub`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_servicebus_subscription` +
`azurerm_servicebus_subscription_rule` surface with the rules FOLDED
(the fold test passes on every axis): required `max_delivery_count`
(azurerm's own contract), consumer dials, the client-scoped (JMS 2.0)
block, name-referenced forwarding as StringValueOrRef seams, and both
filter families with the provider's XOR + at-least-one contracts as
CELs. **Live-caught and fixed in-session:** the original design let a
declared rule named `$Default` "overwrite" the service-created
catch-all -- both providers' create paths refuse to adopt an existing
resource, so the name is now CEL-reserved, declared rules are
documented as ADDITIVE, and the one-time out-of-band catch-all removal
is taught on the spec and presets.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 474, prefix `azsbsub`; NO registry prerequisite (scenario-local fixtures) |
| 2 | Folder Structure | ✅ `provider/azure/azureservicebussubscription/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 29 spec tests pass (both filter families, every XOR error path, the empty-matcher rejection, the reserved-$Default rejection, name rules, gate states); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; rules parented to the subscription; the `$`-stripped LOGICAL name (pulumi URN constraint) with the ARM name preserved; release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; rules keyed by name (duplicate names fail the plan); richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ the rules-fold verdict, the $Default contract, recorded skips |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (BOTH filter families + $Default overwrite + action + client-scoped block) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 3 pairs (catch-all / filtered / fanout-to-work-queue), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (no secret-bearing fields) |

## Apply-Time Validator Source-Diff

Read `servicebus_subscription_resource.go` +
`servicebus_subscription_rule_resource.go` Create/expand:

- **Rule correlation-filter requirements**
  (`expandAzureRmServiceBusCorrelationFilter`: block required with
  CorrelationFilter; at least one matcher non-empty) -- front-loaded as
  the filter-type XOR CELs + the at-least-one-matcher message CEL.
- **sql_filter/correlation_filter ConflictsWith** -- front-loaded as the
  two XOR CELs.
- **Client-scoped entity-name transformation** (`{name}${client_id}$D`)
  -- provider round-trips it; documented on the block.
- **max_delivery_count Required** -- mirrored as a required proto field.
- **The create-path import-existence check** (shared provider behavior):
  refuses to adopt the service-created `$Default` rule -- live-confirmed
  on both engines; front-loaded as the reserved-name CEL.
- No spec CEL invents a rule the provider does not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `topic_id` both engines)
- Naming basis: MATCH (`subscription_name` + rule names both engines --
  the pulumi LOGICAL name strips `$` for URN validity, the ARM-facing
  name argument keeps it; output-neutral)
- Labels/tags: N/A (no ARM tags on Service Bus entities)
- Feature coverage: MATCH -- all 15 spec fields + 5 rule fields + 9
  correlation matchers wired on both engines;
  `dead_lettering_on_filter_evaluation_error` presence-guarded to true
  on both
- Dependency ordering: MATCH (rules depend on the subscription on both
  engines -- TF by reference, Pulumi by parent)
- Stack outputs: MATCH -- 4 outputs; both engines parse
  namespace/topic names with the same anchored contract; `pkg/outputs`
  conformance case added

**PARITY ✅** (zero exceptions).

## Recorded Skips

- `sql_filter_compatibility_level` -- a Computed, service-reserved
  output (hard-coded 20); not a knob.
- Rule-name uniqueness within the list -- outside protovalidate's CEL
  subset; both modules key by name so duplicates fail the plan.

## Critical Gaps

None.
