# Audit Report: AzureServiceBusTopic

**Date:** 2026-07-10-085450
**Component:** AzureServiceBusTopic (enum 473, provider azure, id_prefix `azsbtop`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_servicebus_topic` surface:
parent-by-ARM-id addressing, the fixed size ladder, Premium large
messages, the ForceNew pair (partitioning/dedup), publish ordering,
express with its dedup conflict as CEL, and the provider's own
restricted Active/Disabled gate set. Consumer semantics deliberately
live on the subscription kind -- Azure's own grain.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 473, prefix `azsbtop` (deliberately not `azsbt` -- one edit from the storage table's `azst`); NO registry prerequisite (scenario-local fixtures) |
| 2 | Folder Structure | ✅ `provider/azure/azureservicebustopic/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 19 spec tests pass; messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; unspecified-enum row; release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; anchored parse regex; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ incl. the no-consumer-semantics design note and the express-block asymmetry vs the queue |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (dedup+window, ordering, DISABLED gate) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 2 pairs (event-broadcast / ordered-dedup-topic), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (no secret-bearing fields) |

## Apply-Time Validator Source-Diff

Read `servicebus_topic_resource.go` Create/Update (no CustomizeDiff):

- **Premium partition-layout pairing** -- cross-resource; documented.
- **max_message_size Premium-only** -- cross-resource; documented.
- **The provider has NO express-on-Premium block for topics** (unlike the
  queue) -- deliberately NOT invented as a CEL; ARM's own rejection
  surfaces (documented in the research doc).
- **The 16x partitioned-size read normalization** -- documented.
- No spec CEL invents a rule the provider does not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `namespace_id` both engines)
- Naming basis: MATCH (`topic_name` both engines)
- Labels/tags: N/A (no ARM tags on Service Bus entities; noted in both modules)
- Feature coverage: MATCH -- all 13 spec fields wired on both engines
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- 3 outputs; `pkg/outputs` conformance case added

**PARITY ✅** (zero exceptions).

## Recorded Skips

- The gate enum models azurerm's own restricted Active/Disabled set for
  topics (direction gating is a subscription concern).

## Critical Gaps

None.
