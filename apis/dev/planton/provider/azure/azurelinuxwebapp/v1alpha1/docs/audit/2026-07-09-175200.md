# Audit Report: AzureLinuxWebApp

**Audit Date:** 2026-07-09 17:52:00
**Component Kind:** AzureLinuxWebApp
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurelinuxwebapp/v1alpha1/`
**Enum Value:** 444
**ID Prefix:** azweb

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `AzureLinuxWebApp = 444` with `kind_meta` (`id_prefix: azweb`); `prerequisites: [AzureServicePlan]` — the app runs on a referenced plan, the resource group arrives transitively.

### 2. Folder Structure (4.44%)

✅ Complete component anatomy present.

### 3. Protobuf API Definitions (22.20%)

✅ The spec covers the complete `azurerm_linux_web_app` argument surface (azurerm v4.80):

- Top level: `web_app_name` (globally unique, the provider's 2-60 contract), plan FK, `sticky_settings`, `backup` (+ schedule), `auth_settings_v2` (the full Easy Auth v2 model: platform, global gating, login/session, and ALL NINE provider blocks incl. repeated `custom_oidc_v2`), client-certificate trio (mode as a closed enum), the two basic-auth publishing toggles, `zip_deploy_file`, `vnet_image_pull_enabled`, `virtual_network_backup_restore_enabled`, `client_affinity_enabled`, connection strings (closed 11-value type enum), storage mounts (closed type enum + `AzureStorageAccount` FKs), dual-destination `logs` (file system XOR blob storage on HTTP logs; blob-storage application logs), user `tags`.
- site_config: application stack (all 8 runtimes; nested docker; Java version/server/server-version travel-together CEL), `auto_heal_setting` (all four Linux trigger kinds; Recycle implicit), `api_management_api_id`, `api_definition_url`, `default_documents`, TLS floors as enums + `minimum_tls_cipher_suite` (the 17-suite closed vocabulary as CEL), `managed_pipeline_mode`, `local_mysql_enabled`, `remote_debugging_enabled`, load balancing, both IP-restriction surfaces with enum actions/default actions and Front Door header filters (≤8 caps), ACR managed-identity pulls.
- 12 message-level CELs front-load the provider contracts (VNet-toggle pairings, health-eviction-requires-path, CORS wildcard-credentials, sticky non-empty, HTTP-logs XOR, auto-heal trigger non-empty, auth pairings ×3, identity pairing).
- Outputs gain `possible_outbound_ip_addresses`, `hosting_environment_id`, and the secret-bearing `site_credential_name`/`site_credential_password` pair.
- Spec test: 63 Ginkgo cases (every CEL error path + block happy paths), `go test` green.

**Recorded skips (in `docs/README.md`):** legacy `auth_settings` v1 (superseded by v2 — modeling both creates conflicting expressions of the same intent); deployment slots + custom hostname bindings/certificates (independent-lifecycle standalone-kind candidates); Windows variant (Linux-first platform).

### 4/5. IaC Modules (17.56%)

✅ **Pulumi**: migrated from inline `azure.NewProvider` to the shared `pulumiazureprovider.Get` builder (keyless-ready). Builders for every block; enum wire maps row for row; presence-guarded defaults; outbound IP strings split into real lists; conditional identity + site-credential exports.
✅ **Terraform**: full resource with every block, `locals.tf` enum wire maps identical to Pulumi's. Fixed a pre-existing input-shape defect: the docker variables were flattened (mismatching the proto's nested `docker` message), so a container manifest failed type-checking — the module now takes the nested shape and maps onto azurerm's flattened `docker_*` attributes (image and tag combine into `docker_image_name`). Full `tofu plan` renders the composed hack manifest (auth v2 + auto-heal + sticky + IP restrictions + logs) — verified green.

### 5b. Cross-Engine IaC Parity — PARITY ✅

Field-by-field walk over the ~120-field surface: identical arguments and identical unset-behavior on both engines; the outbound IP list normalization makes the repeated outputs byte-identical (conformance case green). The docker `image:tag` combination is identical in both modules.

**PARITY-EXCEPTION (recorded in both modules):** the family-wide metadata tag shape (output-neutral).

**Apply-time validator source-diff** (`linux_web_app_resource.go` + helpers vs the spec):

| Provider contract | Disposition |
|---|---|
| `always_on` rejected on F1/D1 plans | Apply-time (plan SKU invisible to manifest validation) — documented on the field |
| auth v2: Entra secret-setting XOR certificate thumbprint | `auth_v2_aad_one_credential` CEL |
| auth v2: token store path XOR SAS setting | `auth_v2_token_store_one_backing` CEL |
| auth v2: custom proxy headers require CUSTOM convention | `auth_v2_custom_proxy_headers` CEL |
| sticky settings AtLeastOneOf | `web_app_sticky_settings_not_empty` CEL |
| IP restriction ip/service-tag/subnet exactly-one | Documented (subnet is a ref whose sub-fields CEL cannot inspect — the catalog's standing constraint) |
| vnet_image_pull ASE/Consumption constraints | Apply-time cross-resource — documented on the field |

### 6/7. Documentation (26.27%)

✅ `docs/README.md` rewritten timeless (landscape, 8 design decisions, provider analysis, FK table); README + catalog-page updated to the current shape with enum spellings, the auth example, and the new outputs.

### 8. Supporting Files (10.09%)

✅ Hack manifest exercises auth v2 (custom OIDC), auto-heal, sticky settings, enum dials, hardened publishing, tags; `validate-manifest` + full `tofu plan` green.

### 9. Presets (5.00%)

✅ 3 presets (node-web-api, docker-container, enterprise-private-web-app) refreshed to the new shape (enum spellings, auto-heal in the enterprise preset, hardened publishing), all validating.

### 10. Nice to Have (10.00%)

✅ Registry prerequisites; conformance case; site catalog regenerated.

### Secure-by-Default Coverage — COVERAGE ✅

Secret-bearing fields annotated: docker `registry_password`, storage-mount `access_key`, blob-log `sas_url` ×2, backup `storage_account_url`. Look-alikes exempted with reasons: `application_insights_connection_string` (FK), `client_secret_certificate_thumbprint` (public fingerprint), `sticky_settings.connection_string_names` (names, not values). The auth v2 model carries no secret values by design (setting-name references). `planton secret-coverage --check` green.
