# Audit Report: AzureMonitorDiagnosticSetting

**Date:** 2026-07-10-034017
**Component:** AzureMonitorDiagnosticSetting (enum 452, provider azure, id_prefix `azmondiag`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_monitor_diagnostic_setting`
surface (v5-clean: the deprecated `metric`/`retention_policy` blocks are
recorded skips): polymorphic target, category/category-group log selections,
metric selections, and all four destination families, with the provider's
at-least-one-category, at-least-one-destination, and category-XOR-group
contracts front-loaded as CELs.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 452 (observability sub-band), unique prefix `azmondiag` (deliberately not `azdiag` -- two edits from `azdisk`), `prerequisites: [AzureLogAnalyticsWorkspace]` |
| 2 | Folder Structure | ✅ `provider/azure/azuremonitordiagnosticsetting/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 17 spec tests pass (every CEL error path); messages user-facing |
| 4 | IaC Pulumi | ✅ module + root entrypoint; release-equivalent build passes; richly commented; shared builder |
| 5 | IaC Terraform | ✅ dynamic blocks per selection family; richly commented; canonical empty provider block |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ field map, v5-clean skips, the composite-ID trap, front-loaded contracts |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders three destinations + both log-entry shapes at once) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 3 pairs (logs-to-workspace / archive-to-storage / stream-to-siem), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (no secret-bearing fields; no gaps) |

## Apply-Time Validator Source-Diff

Read `monitor_diagnostic_setting_resource.go` Create/Update/Delete and
referenced validators:

- **At-least-one-enabled contract** re-enforced at create AND update in the
  provider (the API "creates" an empty setting, then 404s on read) --
  front-loaded as the at-least-one-category message CEL.
- **category XOR category_group** -- the provider's expand-time error --
  front-loaded as the per-entry message CEL.
- **Destination AtLeastOneOf** -- schema-level -- front-loaded as message CEL.
- **Eventual-consistency polling** after create and delete -- provider
  behavior, documented in both modules.
- **`log_analytics_destination_type` unconfigurable for some target types**
  (Key Vault et al.) -- Azure decides; documented on the field, not CEL'd
  (target-type-dependent, not statically checkable).
- No spec CEL invents a rule the provider does not enforce (the name
  charset CEL mirrors `validate.MonitorDiagnosticSettingName`).

## Parity Gate Walk (5b)

- Target source: MATCH (resolved `target_resource_id` both engines)
- Naming basis: MATCH (`spec.setting_name`)
- Labels/tags: n/a (ARM does not support tags on diagnostic settings)
- Feature coverage: MATCH -- selections and all four destinations wired on
  both engines; the destination-type enum guarded identically (unspecified
  sends nothing)
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- both engines export the CONSTRUCTED ARM
  extension-resource id (never the provider's `{target}|{name}` composite);
  `pkg/outputs` conformance case added (3 fields)

**PARITY ✅** (no exceptions needed).

## Live Evidence

Live dual-engine E2E GREEN: Pulumi 277s / Terraform 291s (all 8 phases;
zero orphans). The scenario routes the fixture workspace's own audit logs
back into itself -- one fixture proving the polymorphic target seam, the
audit category group, and the workspace destination seam; the verifier
reads the CONSTRUCTED extension-resource ARM id.

## Critical Gaps

None.
