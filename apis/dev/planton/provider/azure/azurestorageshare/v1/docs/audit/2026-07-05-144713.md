# Audit Report: AzureStorageShare

**Audit Date:** 2026-07-05 14:47:13
**Component Kind:** AzureStorageShare
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurestorageshare/v1/`
**Enum Value:** 491
**ID Prefix:** azshare

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureStorageShare = 491` in the Azure band (400-599), storage-data grouping
- `id_prefix: "azshare"` is unique across the registry
- `kind_meta` carries provider/version/id_prefix
- Deliberately NO registry prerequisite on AzureStorageAccount (globally-unique account
  names + Azure's post-delete name reservation make a recreate-per-scenario fixture hang;
  the shared registry comment above the storage-data group records this; E2E declares a
  scenario-local account fixture instead)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:** `apis/dev/planton/provider/azure/azurestorageshare/v1/` with the standard anatomy.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- Four protos present and substantial (spec.proto 7.6 KB with dense intent-explaining
  comments; api/stack_input/stack_outputs conform to the four-proto anatomy)
- Generated stubs present (`make protos` run this session)
- Every CEL message is a complete user-facing sentence with the regex explained in plain
  English (`share_name` format, `acl` permission ordering)
- `spec_test.go` (7.1 KB): 17 cases covering the happy path and every error path
  (name rules ×7, quota bounds, ACL count/ID/permission-order, undefined enums)
- `go test ./apis/dev/planton/provider/azure/azurestorageshare/v1/` — ALL PASS

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present; comments explain
  why/trade-offs (ARM-id addressing rationale, presence-guarded tier/ACL windows, the
  two-id output design, no-URL rationale)
- Root `main.go` (`package main`), `Pulumi.yaml`, `Makefile`; no misplaced entrypoint dirs
- Release-equivalent build passes:
  `go build -o /dev/null ./apis/dev/planton/provider/azure/azurestorageshare/v1/iac/pulumi`
- Shared provider builder (`pulumiazureprovider.Get`) — never inline `NewProvider`

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files present; `main.tf` and `variables.tf` substantial and comment-dense
- `azurerm ~> 4.0` with the canonical empty provider block
- `tofu init`/`validate` clean; full `planton tofu plan` on the hack manifest renders
  1-to-add with ACL blocks, tier and protocol enum seams, and all four outputs

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `StorageAccountId` (ARM id) | `storage_account_id` (ARM id) | ✅ MATCH |
| Resource naming basis | `{account}-{share_name}` pulumi name; ARM name = `spec.share_name` | ARM name = `var.spec.share_name` | ✅ MATCH |
| Quota | `pulumi.Int(spec.QuotaGb)` | `var.spec.quota_gb` | ✅ MATCH |
| Protocol default | unset → `"SMB"` | unset → `"SMB"` (locals map) | ✅ MATCH |
| Access tier | sent only when chosen | null when unset (locals) | ✅ MATCH |
| ACLs + windows | presence-guarded start/expiry | optional() start/expiry (tfvars drops empty strings) | ✅ MATCH |
| Metadata | sent when non-empty | `{}` default (equivalent: no metadata) | ✅ MATCH |
| Stack outputs → proto | share_id, rbac_scope_id (provider attribute), share_name, storage_account_name | identical (rbac_scope_id from the provider attribute) | ✅ MATCH |
| Azure tags | none (ARM: unsupported on shares) | none | ✅ MATCH |

Verified with `planton validate-outputs` (schema check, generic reflection transformer)
and the `pkg/outputs/conformance_test.go` case for AzureStorageShare (PASS, 4/4 fields).

**Effect on score:** none.

---

### 6-9. Documentation, Supporting Files, Nice to Have

✅ All present and substantial: `docs/README.md` (field mapping, the rbac-vs-management-id
distinction, recorded skips with reasons), `README.md`, `catalog-page.md`, both module
READMEs, `iac/hack/manifest.yaml` (exercises protocol/tier/ACL seams), Gazelle-generated
BUILD files, `e2e/` profile + scenario + scenario-local account fixture.

---

### 10. Presets (5.00%)

✅ **Passed:** Three pairs — `01-team-file-share`, `02-nfs-premium-share`,
`03-policy-anchored-access` — each teaching a distinct posture (standard SMB, premium
NFS, policy-anchored SAS). `my-` names, `valueFrom` reference forms, all validate via
`planton validate-manifest` with placeholders substituted.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` green; Azure slice 100%; no field of this kind is
secret-bearing (ACL permissions/windows are policy metadata, not credentials; SAS tokens
are minted at runtime, never stored in the spec).

---

**Audit completed at:** 2026-07-05-144713
**Reference:** `architecture/deployment-component.md`
