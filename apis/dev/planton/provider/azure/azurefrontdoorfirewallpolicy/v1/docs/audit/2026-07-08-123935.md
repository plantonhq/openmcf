# Audit Report: AzureFrontDoorFirewallPolicy

**Audit Date:** 2026-07-08 12:39:35
**Component Kind:** AzureFrontDoorFirewallPolicy
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurefrontdoorfirewallpolicy/v1/`
**Enum Value:** 488
**ID Prefix:** azfdwaf

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureFrontDoorFirewallPolicy = 488` exists with `kind_meta`
- 488 is in the Azure band (400-599), filling the Front Door sub-band beside 480-487
- `id_prefix: azfdwaf` is unique across the registry
- `prerequisites: [AzureResourceGroup]` -- honest: the policy is RG-scoped and global,
  NOT a profile child (security policies attach it to profiles)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- `apis/dev/planton/provider/azure/azurefrontdoorfirewallpolicy/v1/` exists; folder name
  is the lowercase enum name

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files

✅ **Passed:**

- `spec.proto` (31.1 KB): the full azurerm v4.80 surface -- policy settings (mode,
  enabled, body check, redirect URL, block-response customization, the Premium
  challenge lifetimes), custom rules (types, 6 actions, 9 match variables, 12
  operators, 6 transforms, rate-limit dials), managed rule sets (open type/version
  strings, 3-value set action, exclusions at all three scopes, group + per-rule
  overrides with the 7-value override-action vocabulary), and log scrubbing (7
  variables, Equals/EqualsAny)
- `api.proto`, `stack_input.proto`, `stack_outputs.proto` complete and non-empty
- Every CEL message is a complete user-facing sentence; regexes explained in prose

#### 3.2 Generated Stubs

✅ All four `.pb.go` files regenerated and current.

#### 3.3 / 3.4 Unit Tests

✅ `spec_test.go` -- 40 cases (happy paths across both tiers + an error path per CEL);
`go test ./apis/dev/planton/provider/azure/azurefrontdoorfirewallpolicy/v1/` ALL PASS.

#### Apply-Time Validator Source-Diff (mandatory)

Read in full: `cdn_frontdoor_firewall_policy_resource.go` (schema, all three
CustomizeDiff blocks, Create/Update bodies, expand helpers) plus
`validate.FrontDoorFirewallPolicyName` and the 2025-03-01 waf SDK constants.

Contracts found OUTSIDE the schema and their disposition:

- **CustomizeDiff #1 (Standard-sku gates):** managed rules, JS-challenge/CAPTCHA
  expirations, and JSChallenge/CAPTCHA custom-rule actions rejected on Standard ->
  front-loaded as four message CELs (`front_door_firewall_policy_managed_rules_premium_only`,
  `_js_challenge_premium_only`, `_captcha_premium_only`, `_challenge_actions_premium_only`).
- **CustomizeDiff #1 (downgrade block):** Premium -> Standard rejected outright ->
  cross-update contract, not statically checkable; documented on the `sku` field,
  README, and docs.
- **CustomizeDiff #3 (Premium default reset):** removed challenge lifetimes drift back
  to 30 -> realized behaviorally: both engines always send value-or-30 on Premium.
- **Create/Update body (managed-rules Premium check):** duplicate of the CustomizeDiff
  gate -> covered by the same CEL.
- **expand managed rules (version pairing):** DefaultRuleSet <= 1.0 XOR
  Microsoft_DefaultRuleSet >= 1.1 -> two message CELs.
- **expand rule overrides (action gates):** AnomalyScoring only on 2.0+; 2.0+ only
  AnomalyScoring/Log; JSChallenge only on bot-manager types (case-insensitive
  `strings.Contains`, mirrored with `lowerAscii().contains`) -> two message CELs.
- **expand scrubbing rules:** RequestIPAddress/RequestUri require EqualsAny; Equals
  requires selector; EqualsAny forbids selector -> field CEL + two message CELs.
- **No invented rules:** `match_values` is required here (min 1) because THIS
  provider marks it Required -- the regional WAF's "ANY = empty values" rule was
  deliberately NOT copied. Custom-rule priority uniqueness is NOT enforced (the
  provider enforces none). Selector-per-variable requiredness is ARM-side
  (documented, not CEL'd).

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present; comments explain
  the Premium-conditional lifetime pinning, provider-default passthroughs, the
  proto-local enum prefixes, and the disable-gesture override default -- the
  module-comment bar
- Entrypoint at `iac/pulumi/main.go` root (`package main`), `Pulumi.yaml` + `Makefile`
  present, no subdir
- Release-equivalent build passes: `go build -o /dev/null ./.../iac/pulumi`
- Shared provider builder (`pulumiazureprovider.Get`) -- no inline `NewProvider`

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files present and substantial (`variables.tf` 6.9 KB richly commented,
  `main.tf` 7.3 KB); canonical empty `provider "azurerm" { features {} }` with
  `~> 4.0`
- Full `planton tofu plan` against `iac/hack/manifest.yaml` renders the Premium
  depth (managed rules ×2, JS-challenge overrides, scrubbing, block response) --
  1 to add, outputs wired

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with the `pkg/outputs` conformance case (full proto population, zero
unmapped: yes).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.resource_group` | `var.spec.resource_group` | ✅ MATCH |
| Resource naming basis | `spec.policy_name` | `var.spec.policy_name` | ✅ MATCH |
| Tags | merged metadata + user tags | same | 🔵 EXCEPTION (family-wide tag-shape drift, documented in BOTH modules, output-neutral) |
| Sku/mode/settings | value-or-documented-default | same | ✅ MATCH |
| Premium challenge lifetimes | value-or-30 on Premium, never on Standard | same | ✅ MATCH |
| Custom rules (types, actions, conditions, transforms) | full vocabulary maps | same maps in `locals.tf` | ✅ MATCH |
| Managed rules (sets, exclusions ×3 scopes, overrides) | full | full | ✅ MATCH |
| Log scrubbing | operator sent only when chosen | same | ✅ MATCH |
| Optional-field presence semantics | sent only when explicitly set | null lets provider default through; proto3 `optional` presence survives the tfvars converter (explicit false/0 serialize) | ✅ MATCH |
| Stack outputs | `firewall_policy_id`, `firewall_policy_name` | same | ✅ MATCH |

**Effect on score:** none.

---

### 6-9. Documentation and Supporting Files

✅ `docs/README.md` (6.7 KB): field mapping, shape decisions (incl. the three
do-not-copy-the-regional-WAF calls), the validator source-diff disposition, recorded
skips (`groupBy` with its re-enable trigger, `frontend_endpoint_ids`), preview-status
notes. ✅ `README.md` (2.8 KB), `catalog-page.md`, both module READMEs, hack
manifest. ✅ BUILD.bazel trees build via Bazel.

---

### 10. Presets (5.00%)

✅ Three ranked pairs (`01-standard-rate-limit`, `02-premium-managed-prevention`,
`03-detection-rollout`) covering the kind's decision space (tier × mode × managed
tuning); all validate via `planton validate-manifest`; `my-` naming; `valueFrom`
wrapper forms correct.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` passes; the kind carries no secret-bearing fields
(`custom_block_response_body` is a public error page, base64-encoded for transport,
not secrecy) and no name-heuristic look-alikes needing exemption.

---

## Recorded Skips (with reasons)

- **ARM `groupBy` rate-limit grouping** -- absent from the azurerm schema; neither
  engine can express it. Re-enable trigger: the field landing in the provider.
- **`frontend_endpoint_ids` computed attribute** -- classic-Front-Door read-back, not
  configuration or a composition seam.

---

**Audit completed at:** 2026-07-08 12:39:35
**Reference:** `architecture/deployment-component.md`
