# Audit Report: AzureFrontDoorOriginGroup

**Audit Date:** 2026-07-07 21:00:00
**Component Kind:** AzureFrontDoorOriginGroup
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurefrontdoororigingroup/v1alpha1/`
**Enum Value:** 482
**ID Prefix:** azfdog

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ Enum `AzureFrontDoorOriginGroup = 482`;
`prerequisites: [AzureFrontDoorProfile]` with rationale;
`id_prefix: "azfdog"` unique.

### 2. Folder Structure (4.44%)

✅ Correct hierarchy.

### 3. Protobuf API Definitions (22.20%)

✅ Full azurerm v4.80 surface: parent FK, name (provider's 2-90 regex),
`load_balancing` (all three dials with azurerm's exact bounds and
defaults), explicit-optional `health_probe` (protocol enum required,
interval required 1-255, request-type enum defaulting HEAD, path
starts-with-/ CEL), `session_affinity_enabled` default true,
`restore_traffic_time_...` 0-50 default 10. `spec_test.go`: 23 cases
(8 valid, 15 error paths) -- all PASS.

### 4. IaC Modules - Pulumi (13.32%)

✅ Single `cdn.FrontdoorOriginGroup`; shared builder; comments explain
the always-send load-balancing contract, probe-absence semantics, and
the provider's PATCH workaround inheritance. Release-equivalent build
passes.

### 5. IaC Modules - Terraform (4.24%)

✅ Canonical provider block; `planton tofu plan` renders the full
probe + dials + affinity-off + restore-ramp seams.

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅** (`pkg/outputs`: 2/2 populate, zero
unmapped)

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `spec.profile_id.GetValue()` | `var.spec.profile_id` | ✅ MATCH |
| Naming basis | `spec.origin_group_name` | `var.spec.origin_group_name` | ✅ MATCH |
| Load balancing | ALWAYS sent; defaults 4/3/50 materialized | same (`try(coalesce(...))`) | ✅ MATCH |
| Health probe | only when set; HEAD + "/" defaults materialized | dynamic block, same coalesces | ✅ MATCH |
| Affinity / restore ramp | sent only when set | null passthrough | ✅ MATCH |
| Tags | none (ARM does not support them on origin groups) | none | ✅ MATCH |
| Stack outputs → proto | 2 exports | 2 outputs, same names | ✅ MATCH |

### 6-10. Documentation, Supporting Files, Presets

✅ `docs/README.md` (field map incl. the required-block/null-to-disable
notes, zero recorded skips); user docs + module READMEs; hack manifest;
presets ×3 (single origin, multi-region probed, stateful web app) --
validate green; E2E `prerequisite.yaml`, `scenarios/minimal.yaml`,
`profile.yaml`, verifier + test entrypoints.

### 11. Secure-by-Default Coverage (Critical GATE)

**COVERAGE ✅** -- no secret-bearing fields.

---

## Apply-Time Validator Source-Diff

`cdn_frontdoor_origin_group_resource.go`: the update path's
`azuresdkhacks` workaround client (PATCH would null probe settings) is
provider-internal behavior both engines inherit -- documented in both
module READMEs. No unmodeled user-facing contracts.

## Critical Gaps

None.

---

**Audit completed at:** 2026-07-07-210000
**Reference:** `architecture/deployment-component.md`
