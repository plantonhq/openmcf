# Audit Report: AzureEventHubCluster

**Date:** 2026-07-10-125000
**Component:** AzureEventHubCluster (enum 522, provider azure, id_prefix `azehclu`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_eventhub_cluster` surface: the
dedicated single-tenant cluster namespaces join via dedicated_cluster_id.
Both engines compose the ARM sku string `Dedicated_{n}` from
`capacity_units` (the tier name is a one-value constant -- Dedicated is
the only sku family Azure sells), with the 4-hour deletion moratorium and
per-CU-per-hour billing warnings carried prominently in both modules.
Live E2E is excluded by the service's own rule (the moratorium makes an
ephemeral create-verify-destroy cycle impossible), recorded in the E2E
profile with the exact reason.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 522, id_prefix `azehclu` unique; `prerequisites: [AzureResourceGroup]`; the registry comment carries the moratorium/offline-gated note |
| 2 | Folder Structure | ✅ `provider/azure/azureeventhubcluster/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 9 spec tests pass (name boundaries, capacity floor, required trio); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; presence-guarded capacity default (1); release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; coalesce-composed sku; the full tag merge (base/org/env/user); richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ sku composition, the moratorium, billing, the E2E exclusion rationale |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders Dedicated_1 + tags) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 1 pair (dedicated-cluster), validates |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- no secret-bearing fields; `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `eventhub_cluster_resource.go`:

- **sku_name regex** (`^Dedicated_[1-9][0-9]*$`): the spec models the
  capacity count and the modules compose the string -- mismatch is
  unrepresentable (the one-value-constant treatment for the tier name).
- **The 4-hour deletion moratorium** (Delete retries against
  ClusterMoratoriumInEffect/400/429 with a 5-hour timeout, per the
  provider's own comment): documented prominently in the spec, both
  modules, the README, and the E2E profile's exclusion reason.
- **No CustomizeDiff** -- stated explicitly as a finding.
- No spec CEL invents a rule the provider does not enforce (capacity has
  no documented upper bound in the provider; the spec enforces only the
  regex's ≥1 floor).

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `resource_group` value both engines)
- Naming basis: MATCH (`cluster_name` both engines)
- Labels/tags: EXCEPTION (family-wide tag-shape note, documented in BOTH
  modules)
- Feature coverage: MATCH -- sku composition identical
  (coalesce/1 on TF, nil-guard/1 on Pulumi -- same wire result)
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- id/name byte-identical; `pkg/outputs`
  conformance case added

**PARITY ✅** (one documented exception: the family-wide tag shape).

## Recorded Skips

- The sku family name -- one legal value ("Dedicated"); both modules
  compose it unconditionally. Re-enable trigger: Azure selling a second
  cluster sku family.
- **Live E2E excluded** (E2E profile `status: deferred` with the reason):
  Azure forbids deleting a cluster for 4 hours after creation, so the
  ephemeral create-verify-destroy pattern is impossible by the service's
  own rule; the component closes on this offline gate.

## Critical Gaps

None.
