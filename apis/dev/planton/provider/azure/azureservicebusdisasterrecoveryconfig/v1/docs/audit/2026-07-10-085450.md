# Audit Report: AzureServiceBusDisasterRecoveryConfig

**Date:** 2026-07-10-085450
**Component:** AzureServiceBusDisasterRecoveryConfig (enum 476, provider azure, id_prefix `azsbdr`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80
`azurerm_servicebus_namespace_disaster_recovery_config` surface: the
alias identity (ForceNew), primary (ForceNew) + partner namespace FKs,
and the optional alias-rule FK consuming
`AzureServiceBusAuthorizationRule.authorization_rule_id` with zero
translation. The four alias credential faces are sensitive outputs.
Azure's pairing contracts (both Premium, different regions, empty
partner) are cross-resource live-state checks -- documented verbatim,
never invented as CELs.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 476, prefix `azsbdr`; NO registry prerequisite (scenario-local Premium fixtures) |
| 2 | Folder Structure | ✅ `provider/azure/azureservicebusdisasterrecoveryconfig/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 9 spec tests pass; messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; release-equivalent build passes; richly commented (the provider's break-pair/name-release choreography explained) |
| 5 | IaC Terraform | ✅ canonical empty provider block; the optional alias rule sent only when non-empty; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ pairing contracts, failover-is-operational rationale, the E2E deferral note |
| 7 | User README | ✅ incl. the metadata-not-messages boundary |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (both namespace refs + the scoped alias rule) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 1 pair (geo-dr-pairing -- the kind has exactly one meaningful shape), validates |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- all four alias credential faces annotated sensitive |

## Apply-Time Validator Source-Diff

Read `servicebus_namespace_disaster_recovery_config_resource.go`
Create/Update/Delete:

- **Create waits** Accepted → Succeeded (fails on Failed) --
  provider-internal.
- **Partner change = BreakPairing → wait → re-pair** under a namespace
  lock -- provider-internal; documented on the field.
- **Delete = BreakPairing → delete → poll-404 → poll name availability**
  -- provider-internal; documented in both modules (why immediate alias
  re-creation is safe).
- **Both-Premium / different-region / empty-partner** -- ARM's own
  apply-time contracts over two referenced namespaces' live state;
  documented on the spec, deliberately not CEL-able.
- No spec CEL invents a rule the provider does not enforce (the alias
  name deliberately carries NO format regex -- azurerm has no validator;
  min 1/max 50 with the DNS reality documented).

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved primary/partner ids both engines)
- Naming basis: MATCH (`alias_name` both engines)
- Labels/tags: N/A (no ARM tags on disasterRecoveryConfigs)
- Feature coverage: MATCH -- all 4 spec fields wired on both engines
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- 6 outputs byte-identical; `pkg/outputs`
  conformance case added

**PARITY ✅** (zero exceptions).

## Live Evidence

Live E2E DEFERRED by owner decision (recorded in the E2E profile): the
chain needs two PREMIUM namespaces in two regions plus
pairing/break-pairing settling -- an estimated 40-70 minute dual-engine
lane past the pre-approved window. The scenario and both Premium
fixtures ship ready-to-run; the offline gate (this audit, spec tests,
the full plan rendering both references and the alias-rule seam,
outputs conformance) is fully green.

## Critical Gaps

None.
