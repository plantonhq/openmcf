# Audit Report: AzureEventHubSchemaGroup

**Date:** 2026-07-10-125000
**Component:** AzureEventHubSchemaGroup (enum 521, provider azure, id_prefix `azehsg`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_eventhub_namespace_schema_group`
surface: parent by ARM id, the provider-exact 256-char name contract,
closed compatibility (NONE/BACKWARD/FORWARD) and format (AVRO/JSON)
enums with exact wire maps, and the all-ForceNew/no-update-surface
lifecycle documented throughout (the provider registers no Update
function -- every change replaces the group and drops its schemas).

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 521 (Event Hubs continuation block), id_prefix `azehsg` unique; no registry prerequisite |
| 2 | Folder Structure | ✅ `provider/azure/azureeventhubschemagroup/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 14 spec tests pass (every enum value, both unspecified rejections, name boundaries); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ registry value proposition, policy semantics with upgrade-order guidance, the "Unknown" skip, the all-ForceNew verdict |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders BACKWARD + AVRO) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 1 pair (backward-compatible-avro), validates |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- no secret-bearing fields; `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `eventhub_namespace_schema_group_resource.go`:

- **No Update function exists** -- the resource is create/read/delete
  only; every field ForceNew. Documented as the kind's lifecycle
  contract (a change drops registered schemas).
- **Name contract** (ValidateSchemaGroupName, ≤256 start/end-alnum):
  front-loaded as a field CEL.
- **schema_type "Json" is a provider-added literal** (the SDK enum
  carries only Unknown/Avro): both engines' wire maps send the exact
  strings the provider validates ("Avro"/"Json").
- **The STANDARD+ tier contract** lives in ARM (BASIC namespaces reject
  schema groups) -- documented, not CEL'd (cross-resource).
- **No CustomizeDiff** -- stated explicitly as a finding.
- No spec CEL invents a rule the provider does not enforce (the SDK's
  "Unknown" placeholder type is deliberately not modeled -- it is not a
  real format; recorded skip).

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `namespace_id` passed directly on both
  engines -- this resource takes the ARM id natively)
- Naming basis: MATCH (`schema_group_name` both engines)
- Labels/tags: N/A (ARM does not support tags on Event Hubs entities)
- Feature coverage: MATCH -- both enum wire maps mechanically identical
  across engines, no unspecified fallback rows (both enums are
  required/not_in:[0] in the spec, so an unmapped zero value is
  unreachable behind validation)
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- id/name byte-identical; `pkg/outputs`
  conformance case added

**PARITY ✅** (no exceptions needed).

## Recorded Skips

- The SDK's "Unknown" schema type -- an API placeholder, not a real
  serialization format; deliberately not modeled.

## Critical Gaps

None.
