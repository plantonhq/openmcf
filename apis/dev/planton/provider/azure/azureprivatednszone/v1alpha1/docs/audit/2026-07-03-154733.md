# Audit Report: AzurePrivateDnsZone

**Audit Date:** 2026-07-03 15:47:33
**Component Kind:** AzurePrivateDnsZone
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azureprivatednszone/v1alpha1/`
**Enum Value:** 415
**ID Prefix:** azpdns

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.18% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Both gates pass and every critical category is complete.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzurePrivateDnsZone = 415` in `cloud_resource_kind.proto`
- `id_prefix: "azpdns"` is unique across the registry
- `kind_meta` carries `prerequisites: [AzureResourceGroup]` — the honest direct dependency (added with this rework; the link kind chains through it)

**Score:** 4.44% / 4.44%

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy, lowercase folder name, `v1/` subfolder.

**Score:** 4.44% / 4.44%

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos exist with dense intent-explaining comments, rewritten timeless — no design-decision breadcrumbs, no bundled-link narration
- The zone is zone-only: the previously bundled network link (with its one-link-per-zone limitation) is the standalone `AzurePrivateDnsZoneVirtualNetworkLink` kind; this spec's comments direct composition there
- Completed to the azurerm v4.80 floor: `soa_record` block (email + five timer fields with Azure's defaults declared) and user `tags` added; fields renumbered contiguously
- Generated `.pb.go` stubs present and current (`make protos` green)
- `spec_test.go`: 16 ginkgo cases covering zone-name shapes (privatelink, custom, single-label, invalid, uppercase), SOA requiredness and timer bounds, tags, and the KRM constants
- `go test ./apis/dev/planton/provider/azure/azureprivatednszone/v1alpha1/` — **ok**

**Score:** 22.20% / 22.20%

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- Module rewritten zone-only, richly commented (rename blast radius; SOA written-at-creation; the zone-answers-nobody-without-links model)
- Migrated off inline `azure.NewProvider` onto the shared `pulumiazureprovider.Get` (keyless-compatible)
- Canonical `Pulumi.yaml` restored (was a non-canonical variant)
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`

**Score:** 13.32% / 13.32%

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- Module rewritten zone-only on the canonical variable shape (was a pre-standard metadata contract); `tofu init`/`validate`/`fmt` green; full `planton tofu plan` on the hack manifest: 1 to add, all 3 outputs
- Canonical empty `provider "azurerm" { features {} }` block on `~> 4.0`
- SOA expressed as a dynamic block absent when unset

**Score:** 4.24% / 4.24%

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.resource_group` resolved literal | same | ✅ MATCH |
| Resource naming basis | `spec.name` (the zone's DNS name) | `spec.name` | ✅ MATCH |
| Labels/tags | metadata-derived merged under user tags (user wins) | identical merge order (previously divergent locals conformed) | ✅ MATCH |
| Spec feature coverage | all 4 spec fields honored | all 4 spec fields honored | ✅ MATCH |
| SOA absent-field semantics | block omitted when unset; unset timers omitted | dynamic block; null timers omitted | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 3/3 fields | 3/3 fields | ✅ MATCH |

- `pkg/outputs/conformance_test.go` carries an AzurePrivateDnsZone case — passing
- Live dual-engine E2E: `TestAzurePrivateDnsZone_Pulumi/minimal` and `_Terraform/minimal` — tagged custom internal zone composed on the fixture RG, all 8 phases, zero orphans

**Effect on score:** none.

### 6. Documentation - Research (11.18% / 13.18%)

⚠️ `docs/README.md` is in the moderate size band. Content quality is complete: full azurerm→spec field map, the link decomposition argued against the fold tests, the SOA modeling decision, the new `resource_group_name` output rationale, and rename blast-radius notes.

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` rewritten for the zone-only design with the privatelink-name reference table and the three-kind composition.

### 8. Supporting Files (10.09%)

✅ `iac/pulumi/README.md`, `iac/tf/README.md` rewritten; `iac/hack/manifest.yaml` carries the `value:` wrapper on the reference field.

### 9. Nice to Have (10.00%)

✅ Gazelle-generated BUILD.bazel files present; E2E profile + scenario + prerequisite fixture profile + verifier + test entrypoints wired (the zone previously had no E2E coverage at all).

### 10. Presets (5.00%)

✅ Two ranked pairs: `01-privatelink-zone` (with the full privatelink-name table), `02-internal-zone` (SOA tuning + auto-registration pointer) — the removed bundled-link presets' service-specific variants collapsed into the name table, since the zone spec no longer varies per service.

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` reports zero gaps for this kind. Zone names and SOA timers are public configuration; the SOA `email` is a published DNS contact, not a credential — nothing to annotate, nothing to exempt.

---

## Next Steps

None required for this component. `AzurePrivateEndpoint` (which registers records in privatelink zones) is a later networking-wave audit.

---

**Audit completed at:** 2026-07-03 15:47:33
**Reference:** `architecture/deployment-component.md`
