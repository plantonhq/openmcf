# Audit Report: AzureFrontDoorRuleSet

**Audit Date:** 2026-07-08 06:00:00
**Component Kind:** AzureFrontDoorRuleSet
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurefrontdoorruleset/v1/`
**Enum Value:** 485
**ID Prefix:** azfdrs

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ Enum `AzureFrontDoorRuleSet = 485` (CDN sub-band);
`prerequisites: [AzureFrontDoorProfile]` with rationale;
`id_prefix: "azfdrs"` unique.

### 2. Folder Structure (4.44%)

✅ Correct hierarchy.

### 3. Protobuf API Definitions (22.20%)

✅ Full azurerm v4.80 surface over BOTH resources (`rule_set` + `rule`):
parent FK; the set's letters-and-digits-only 1-60 name regex; folded
`rules[]` with the rule-name regex (1-260, letter start), order >= 0,
behavior enum, ALL 19 condition types (shared 13-value operator enum
with per-condition `in` subsets -- WILDCARD url_path-only,
GEO_MATCH/IP_MATCH address-only; Equal-only conditions carry NO
operator field, a one-value knob being a constant), all 5 action types
with the singular redirect/rewrite/override shape making the provider's
only-once contracts structural. ~30 CELs mirror the provider's
expand-time validators (condition/action caps, redirect XOR rewrite,
operator↔match_values incl. the ANY-excluded vocabularies where the
provider requires values, header-action value pairing, GeoMatch country
codes, the override's full cache/forwarding matrix, the d.HH:MM:SS
duration format, fold-derived unique rule names). `spec_test.go`: 65
cases (15 valid, 50 error paths) -- all PASS.

### 4. IaC Modules - Pulumi (13.32%)

✅ `cdn.FrontdoorRuleSet` + one `cdn.FrontdoorRule` per rule on the
shared builder; comments explain the two protocol dialects, the IPMatch
address default, DELETE-header value omission, and order-vs-creation
semantics. Release-equivalent build passes.

### 5. IaC Modules - Terraform (4.24%)

✅ Canonical provider block; `for_each` keyed on spec-unique rule
names; `planton tofu plan` renders 5 resources with every dialect seam
verified in the plan output (`Https` on the redirect vs `HttpsOnly` on
the override, `GeoMatch`, `Wildcard`, `IPMatch` default, `Delete`
header without value, `OverrideAlways` + duration + specified query
strings, order 0 through the tfvars zero-drop guard).

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅** (`pkg/outputs`: 2/2 populate, zero
unmapped)

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `spec.profile_id.GetValue()` | `var.spec.profile_id` | ✅ MATCH |
| Naming basis | set + rule names from spec | same | ✅ MATCH |
| Rule fan-out | loop, one resource per rule | `for_each` on `rules_by_name` | ✅ MATCH |
| Operator/transform vocab | shared wire maps | `locals.tf` maps, same strings | ✅ MATCH |
| Protocol dialects | redirect Http/Https; override HttpOnly/HttpsOnly | same two maps | ✅ MATCH |
| Address operator default | IPMatch materialized | same | ✅ MATCH |
| Scheme default | "HTTP" materialized | `coalesce(..., "HTTP")` | ✅ MATCH |
| Optional enums / values | sent only when chosen | null passthrough | ✅ MATCH |
| Header DELETE value | omitted | null | ✅ MATCH |
| Tags | none (ARM unsupported on rule sets/rules) | none | ✅ MATCH |
| Stack outputs → proto | 2 exports | 2 outputs, same names | ✅ MATCH |

### 6-10. Documentation, Supporting Files, Presets

✅ `docs/README.md` (field map, shape decisions incl. the
typed-lists-vs-heterogeneous-array rationale, front-loaded CEL
inventory, recorded non-rules: order uniqueness and CIDR parsing
deliberately NOT invented); user docs + module READMEs; hack manifest
exercising every dialect-sensitive enum seam; presets ×3
(security headers, HTTPS + tiered caching, rewrite + cookie canary) --
validate green; E2E `prerequisite.yaml` (empty set for the route's
attach scenario), `scenarios/minimal.yaml` (three-rule policy),
`profile.yaml`, verifier + test entrypoints.

### 11. Secure-by-Default Coverage (Critical GATE)

**COVERAGE ✅** -- no secret-bearing fields (header values, match
values, and redirect targets are routing configuration, not
credentials).

---

## Apply-Time Validator Source-Diff

Walked `cdn_frontdoor_rule_resource.go` create/update bodies,
`frontdoorruleconditions/`, `frontdoorruleactions/`, and
`validate/front_door_validation_helpers.go` (neither resource has a
CustomizeDiff):

- Condition/action caps, singleton actions, redirect XOR rewrite,
  operator↔match_values, header value pairing, the override's cache
  matrix, GeoMatch code shape, duration format -- ALL front-loaded as
  CELs (§3).
- CIDR validity/duplicate/overlap on address conditions: provider
  parses with `net.ParseCIDR` -- left apply-time and documented (a
  regex cannot honestly replicate CIDR parsing).
- Rule deletion polls to true absence (custom poller) -- provider
  behavior both engines inherit.
- NO SKU gating exists anywhere in the provider (Premium-only
  operators/conditions are ARM-rejected on Standard) -- documented in
  docs/README, not invented as CELs.
- No invented rules found: the spec's only beyond-schema rule
  (unique rule names) mirrors ARM child-resource identity.

## Critical Gaps

None.

---

**Audit completed at:** 2026-07-08-060000
**Reference:** `architecture/deployment-component.md`
