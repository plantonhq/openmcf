# Audit Report: AzureRoleDefinition

**Audit Date:** 2026-07-03 12:52:29
**Component Kind:** AzureRoleDefinition
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azureroledefinition/v1alpha1/`
**Enum Value:** 462
**ID Prefix:** azrd

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Fully Complete

**Cross-Engine Parity:** PARITY ✅
**Secure-by-Default Coverage:** COVERAGE ✅

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.68% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. All critical categories pass, both gates are green, and live dual-engine
E2E ran against the real test subscription (see Detailed Findings §5b and the
component's `e2e/profile.yaml`).

---

## Quick Wins

1. **Grow `docs/README.md` past the 10KB comprehensive bar**
   - **Why:** The research doc is 9.4KB — the "moderate" band (5–10KB). Content
     already covers landscape, design decisions, and skip reasons; a deeper
     built-in-role comparison table would clear the bar.
   - **File:** `docs/README.md`

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureRoleDefinition = 462` exists in `cloud_resource_kind.proto`
- 462 is in the Azure band (400–599), identity sub-band (461–469)
- `id_prefix` `azrd` is unique across the registry
- `kind_meta` carries provider, version, id_prefix, and an honest
  `prerequisites: [AzureResourceGroup]` (the default scope reference)
- `pkg/crkreflect/kind_map_gen.go` regenerated; kind resolves via reflection

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- `apis/dev/planton/provider/azure/azureroledefinition/v1alpha1/` exists
- Folder name is the lowercase enum name; `v1/` in place

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- `api.proto` (1.3KB), `spec.proto` (9.3KB), `stack_input.proto` (0.6KB),
  `stack_outputs.proto` (1.6KB) — all present and substantial
- All four `.pb.go` stubs generated (`make protos` green)
- Field comments are dense, intent-explaining, LLM-consumable (control-plane
  vs data-plane, carve-out vs deny semantics, tenant limits, scope trade-offs)
- No CEL message-level rules needed (no XOR pairs); field-level rules
  (`required`, `min_len`, `string.uuid` with `IGNORE_IF_ZERO_VALUE`) carry
  buf.validate's standard user-facing messages
- `spec_test.go` (7.2KB): 18 ginkgo cases covering every field and rule —
  happy paths (minimal, RG/MG/subscription scopes, ref scope, empty
  permissions, all four operation lists, multi-block, literal+ref assignable
  scopes, description, pinned GUID) and error paths (missing name/scope,
  non-UUID GUID, wrong apiVersion/kind, missing metadata/spec)
- `go test ./apis/dev/planton/provider/azure/azureroledefinition/v1alpha1/` — ok

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present
- Provider built via the shared `pulumiazureprovider.Get` builder (keyless
  web-identity compatible; never inline `NewProvider`)
- Comments explain lifecycle (in-place vs replace fields), eventual
  consistency, empty-optional omission, server-side assignable-scopes
  defaulting, and the output-naming contract — the why, not narration
- Entrypoint `iac/pulumi/main.go` at root, `package main`; no `main/` or
  `entrypoint/` subdir; `Pulumi.yaml` + `Makefile` present
- Release-equivalent build passes:
  `go build -o /dev/null ./apis/.../v1/iac/pulumi`

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `variables.tf` (typed object spec with per-field comments), `main.tf`
  (dynamic permissions blocks), `locals.tf` (empty-string→null normalization,
  empty-list→null for provider defaulting), `outputs.tf` (5 outputs with the
  naming rationale), `provider.tf` (canonical empty `azurerm ~> 4.0` block)
- `tofu init && tofu validate && tofu fmt -check` clean
- Full `planton tofu plan` against `iac/hack/manifest.yaml`: 1 to add, all
  5 outputs wired

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines against the proto contract; verified outputs with
`planton validate-outputs` semantics via the `pkg/outputs` conformance case
(zero unmapped, all 5 fields populate) AND live E2E output verification
(5/5 proto fields populated on both engines).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.scope.GetValue()` | `var.spec.scope` | ✅ MATCH |
| Resource naming basis | role name from `spec.name` | same | ✅ MATCH |
| Labels / tags | none (ARM unsupported) | none | ✅ MATCH |
| Selectors | n/a (no workload) | n/a | ✅ MATCH |
| Spec feature coverage | all 6 fields honored | all 6 fields honored | ✅ MATCH |
| Empty assignable_scopes | argument omitted → server default `[scope]` | `null` → server default `[scope]` | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 5 outputs, resource-sourced | 5 outputs, resource-sourced | ✅ MATCH |

**Effect on score:** none — engines in parity, zero exceptions needed.

---

### 6. Documentation - Research (13.18%)

✅ **Passed:**

- `docs/README.md` exists (9.4KB — moderate band)
- Covers: RBAC anatomy, control-plane vs data-plane, carve-out vs deny,
  authorization-model nuances (permissions, limits, uniqueness, scope
  mechanics, eventual consistency), the full azurerm→spec field map with
  parity verification, design decisions (output naming, reference defaults,
  ARM list-of-blocks fidelity, empty permissions), operational notes,
  verification procedure
- Nothing skipped from the provider surface (recorded explicitly)

⚠️ **Warnings:**

- Size is in the 5–10KB moderate band rather than >10KB comprehensive

**Score:** 11.68% / 13.18%

---

### 7. Documentation - User-Facing (13.09%)

✅ **Passed:**

- `README.md` at v1/ (4.8KB): overview, first-class rationale, features,
  when-to-use, field/output tables, composed example, required permissions
- `catalog-page.md` (6.8KB) with quick start, config reference, 3 examples,
  stack outputs, related components; site catalog regenerated
  (`yarn copy-docs && yarn generate-structure`)

**Score:** 13.09% / 13.09%

---

### 8. Supporting Files (10.09%)

✅ **Passed:**

- `iac/pulumi/README.md` (1.9KB), `iac/tf/README.md` (2.9KB)
- `iac/hack/manifest.yaml` (offline plan target, exercised by `planton tofu plan`)

**Score:** 10.09% / 10.09%

---

### 9. Nice to Have (10%)

✅ **Passed:**

- Gazelle-generated `BUILD.bazel` files for the component package, the pulumi
  module, and the entrypoint

**Score:** 10.00% / 10.00%

---

### 10. Presets (5.00%)

✅ **Passed:**

- 3 ranked pairs, each YAML with companion MD, no duplicate ranks, kebab-case:
  `01-vm-operator-role` (explicit-actions least privilege),
  `02-blob-data-reader-role` (data-plane teaching),
  `03-project-admin-carve-out-role` (wildcard-minus-carve-out +
  assignable-scope governance)
- `apiVersion`/`kind` match `api.proto` constants; `metadata.name` prefixed
  `my-`; `StringValueOrRef` fields use `value:` wrapper; all field names exist
  in `spec.proto`; no `status` blocks

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

Ran `go run . secret-coverage --output json`; zero `gaps[]` entries for
`AzureRoleDefinition:` (the kind has no secret-bearing fields — role
definitions are permission metadata). `--check` gate passes repo-wide; the
Azure slice stays at 100%.

**Effect on score:** none.

---

## Live E2E (beyond the audit's offline scope)

Live dual-engine E2E ran against the real test subscription
(`8158df85-…`): fixture resource group deployed (registry prerequisite),
custom role `planton-oss-e2e-smoke-rg-reader` created at the fixture-RG
scope, verified via `armauthorization` GetByID on the fully-scoped ARM ID,
destroyed (Azure's eventual-consistency delete polling ~4.5 min), verified
absent, fixtures torn down. Both `TestAzureRoleDefinition_Pulumi/minimal`
and `TestAzureRoleDefinition_Terraform/minimal` — all 8 phases PASS.

Live nuance captured: ARM returns the definition's fully-scoped ID at
SUBSCRIPTION level (`/subscriptions/{sub}/providers/.../roleDefinitions/{guid}`)
even when the creation scope is a resource group — the verifier keys on the
module's exported ID, so verification is scope-representation agnostic.

---

## Next Steps

1. None blocking. Optional: deepen `docs/README.md` past 10KB with a
   built-in-vs-custom role decision table.

---

**Audit completed at:** 2026-07-03-125229
**Reference:** See `architecture/deployment-component.md` for ideal state definition
