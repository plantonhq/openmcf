# Audit Report: AzureMonitorActionGroup

**Date:** 2026-07-10-034017
**Component:** AzureMonitorActionGroup (enum 453, provider azure, id_prefix `azmonag`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_monitor_action_group` surface:
all eleven receiver families with `use_common_alert_schema` modeled on
exactly the seven payload-aware families, Entra-authenticated webhooks,
the ITSM ticket-configuration key contract front-loaded, and composable
FK seams (Function App, Role Definition, Event Hub namespace).

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 453, unique prefix `azmonag` (deliberately not `azag` -- one edit from the App Gateway's `azagw`), `prerequisites: [AzureResourceGroup]` |
| 2 | Folder Structure | ✅ `provider/azure/azuremonitoractiongroup/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 15 spec tests pass (every receiver family + error paths); messages user-facing |
| 4 | IaC Pulumi | ✅ module + root entrypoint; release-equivalent build passes; richly commented; shared builder |
| 5 | IaC Terraform | ✅ one dynamic block per receiver family; richly commented; canonical empty provider block |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ per-family notes, the global-location constant skip, ITSM addressing |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders ALL ELEVEN families at once, incl. aad_auth and the ITSM JSON) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 3 pairs (oncall-team / automation-hooks / role-fanout), all validate (the automation preset's placeholder was corrected to a valid example UUID -- angle-bracket placeholders fail the UUID rule) |
| 11 | Sensitive Coverage | ✅ GATE PASSED (webhook URLs/UUIDs/JSON configs are not credentials; no gaps) |

## Apply-Time Validator Source-Diff

Read `monitor_action_group_resource.go` Create and expand functions:

- **ITSM ticket_configuration key requirement** (`expandMonitorActionGroupItsmReceiver`
  rejects JSON lacking PayloadRevision/WorkItemType) -- front-loaded as a
  contains-check CEL (CEL cannot parse JSON; the check catches the
  documented failure class) with the exact example in the message.
- **Webhook aad_auth tenant defaulting** -- tenant/identifier derived from the
  account when unset -- mirrored by sending them only when set.
- **Location** -- Optional with default "global"; the one-value-knob rule
  applies (recorded skip; both engines let the provider default).
- **Receiver-name case-insensitive uniqueness** -- an Azure-side contract
  spanning eleven repeated fields; documented, not CEL'd.
- No spec CEL invents a rule the provider does not enforce (short-name
  length and URL schemes are the provider's own validators).

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `resource_group` value both engines)
- Naming basis: MATCH (`metadata.name` both engines -- the catalog grain)
- Labels/tags: EXCEPTION (family-wide tag-shape note, documented in BOTH modules)
- Feature coverage: MATCH -- all eleven receiver families wired
  field-by-field on both engines; optional UUIDs sent only when set on both
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- `pkg/outputs` conformance case added (2 fields)

**PARITY ✅** (one documented exception: the family-wide tag shape).

## Live Evidence

Live dual-engine E2E GREEN: Pulumi 115s / Terraform 132s (all 8 phases;
zero orphans) -- a three-family group (email + HTTPS webhook + ARM-role
fan-out to the built-in Owner GUID). The first attempt FAILED on both
engines with a live-caught FIXTURE defect: Azure validates webhook receiver
URIs server-side and rejects placeholder domains
(WebhookServiceUriBlocked on example.com). The scenario, preset, and hack
manifest now carry domain-you-own URI shapes; the class is recorded in
e2e/README.

## Critical Gaps

None.
