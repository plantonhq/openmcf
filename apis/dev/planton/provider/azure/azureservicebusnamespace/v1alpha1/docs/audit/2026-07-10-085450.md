# Audit Report: AzureServiceBusNamespace

**Date:** 2026-07-10-085450
**Component:** AzureServiceBusNamespace (enum 470, provider azure, id_prefix `azsb`)
**Mode:** full audit with `--parity` focus (breaking depth rework)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Reworked breaking to the full azurerm v4.80 `azurerm_servicebus_namespace`
surface: the bundled queues/topics lists dissolved into first-class kinds,
`name`→`namespace_name` with the provider-exact regex + reserved-suffix
bans, a closed sku enum with the Premium capacity/partitions pairings
front-loaded as CELs in both directions, all three managed-identity
models, folded CMK (Premium-gated, identity-paired) and network rule set
(Premium-gated, DENY-requires-sources), the keyless-posture
`local_auth_enabled` dial, user tags, and the root SAS rule's four
credential faces as sensitive outputs.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 470 kept; gains `prerequisites: [AzureResourceGroup]`; the child-kinds-carry-no-prerequisite decision documented in the registry comment |
| 2 | Folder Structure | ✅ `provider/azure/azureservicebusnamespace/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 45 spec tests pass (every sku pairing, identity model, CMK gate, firewall contract, name rule -- happy + error paths); messages user-facing |
| 4 | IaC Pulumi | ✅ shared `pulumiazureprovider.Get` builder (inline NewProvider retired); release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block (azurerm ~> 4.0); dynamic identity/CMK/firewall blocks; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ field mapping, the CMK fold-vs-standalone verdict, recorded skips with reasons |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders the FULL Premium surface at once: capacity+partitions, combined identity, CMK, DENY firewall with both source lists, keyless, tags) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 3 pairs (standard-namespace / premium-isolated / keyless-entra), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- the four root-rule credential outputs annotated sensitive; `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `servicebus_namespace_resource.go` Create/Update + CustomizeDiff:

- **capacity vs sku** (Create AND Update): non-Premium requires 0, Premium
  requires {1,2,4,8,16} -- front-loaded as two message CELs (the spec's
  optional presence maps to azurerm's 0-default).
- **premium_messaging_partitions vs sku** (Create): same two-direction
  pairing as CELs; ForceNew documented.
- **CMK-removal ForceNew** (CustomizeDiff): documented on the field and in
  both modules -- not CEL-able (state-transition, not shape).
- **Premium-boundary sku migration ForceNew** (CustomizeDiff): documented
  on the sku enum.
- **DENY-without-sources rejection** (`createNetworkRuleSetForNamespace`):
  front-loaded as a message CEL.
- **network-rule-set public-access must match the namespace-level dial**
  (Create/Update): a cross-dial server-side check -- documented in both
  modules; the block's dial is modeled independently as azurerm does.
- **minimum_tls_version unset-after-set error** (CustomizeDiff): moot --
  the field is a recorded one-value-constant skip (v5 restricts to "1.2").
- No spec CEL invents a rule the provider does not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `resource_group` value both engines)
- Naming basis: MATCH (`namespace_name` for the ARM name both engines)
- Labels/tags: EXCEPTION (family-wide tag-shape note, documented in BOTH modules)
- Feature coverage: MATCH -- sku map (unspecified→Standard both sides),
  capacity/partitions presence-sent, identity three models, CMK triple,
  firewall block incl. presence-guarded public-access dial, local-auth +
  public-access presence-guarded to true on both
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- 8 outputs byte-identical; `pkg/outputs`
  conformance case added (8 fields incl. 4 sensitive)

**PARITY ✅** (one documented exception: the family-wide tag shape).

## Recorded Skips

- `minimum_tls_version` -- one-value constant ("1.2"); both modules leave
  the provider default.
- `zone_redundant` -- not in the azurerm v4 schema.

## Critical Gaps

None.
