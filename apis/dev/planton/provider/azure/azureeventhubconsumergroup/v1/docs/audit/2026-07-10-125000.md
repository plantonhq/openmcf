# Audit Report: AzureEventHubConsumerGroup

**Date:** 2026-07-10-125000
**Component:** AzureEventHubConsumerGroup (enum 478, provider azure, id_prefix `azehcg`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_eventhub_consumer_group`
surface: single parent `event_hub_id` reference (both engines parse the
provider's legacy discrete-name addressing from the resolved ARM id with
identical anchored semantics), the provider-exact 50-char name contract,
the `$Default` reservation (the service-created catch-all cannot be
adopted declaratively), and `user_metadata` as the operator-facing
ownership surface.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 478 (messaging sub-band), id_prefix `azehcg` unique; no registry prerequisite (scenario-local parents per the globally-unique-name discipline) |
| 2 | Folder Structure | ✅ `provider/azure/azureeventhubconsumergroup/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 13 spec tests pass (name contract boundaries, the `$Default` reservation, metadata cap -- happy + error paths); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; anchored ARM-id parse with clear malformed-id errors; release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; anchored named-group regex parse; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ one-group-per-application discipline, the `$Default` verdict, tier quotas as apply-time contracts |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders the parse + metadata) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 1 pair (per-application-group), validates |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- no secret-bearing fields; `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `eventhub_consumer_group_resource.go` (a typed sdk.Resource):

- **Name contract** (ValidateEventHubConsumerName, ≤50 start/end-alnum):
  front-loaded as a field CEL.
- **user_metadata 1-1024** (StringLenBetween): front-loaded as a max-len
  rule (the 1-minimum applies only when the provider sends a set value;
  the spec's optional presence matches).
- **No CustomizeDiff, no create-path checks beyond the schema** -- stated
  explicitly as a finding.
- **Tier quotas** (BASIC rejects additional groups; STANDARD caps at 20
  per hub) live in ARM, not the provider -- documented on the spec and in
  docs/README.md, deliberately not CEL'd (cross-resource).
- The `$Default` reservation CEL mirrors the service's own behavior
  (providers refuse to adopt service-created resources -- the
  import-existence check class); not an invented rule.

## Parity Gate Walk (5b)

- Scope source: MATCH -- identical anchored parse of the SAME resolved
  `event_hub_id` (TF named-group regex; Go regexp), same accepted shapes,
  both fail loudly on malformed ids
- Naming basis: MATCH (`consumer_group_name` both engines)
- Labels/tags: N/A (ARM does not support tags on Event Hubs entities)
- Feature coverage: MATCH -- user_metadata presence-guarded on Pulumi /
  null-passed on TF (same wire result)
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- id/name byte-identical; `pkg/outputs`
  conformance case added

**PARITY ✅** (no exceptions needed).

## Recorded Skips

None -- the provider surface is fully modeled.

## Critical Gaps

None.
