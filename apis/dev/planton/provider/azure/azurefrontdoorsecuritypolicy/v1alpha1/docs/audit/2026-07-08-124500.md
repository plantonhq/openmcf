# Audit Report: AzureFrontDoorSecurityPolicy

**Audit Date:** 2026-07-08 12:45:00
**Component Kind:** AzureFrontDoorSecurityPolicy
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurefrontdoorsecuritypolicy/v1alpha1/`
**Enum Value:** 489
**ID Prefix:** azfdsecpol

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureFrontDoorSecurityPolicy = 489` exists with `kind_meta` -- closes
  the Front Door 480-489 sub-band
- `id_prefix: azfdsecpol` unique (deliberately not `azfdsp`, one edit away from the
  secret kind's `azfdsec`)
- `prerequisites: [AzureFrontDoorEndpoint, AzureFrontDoorFirewallPolicy]` -- direct
  parents only; the profile arrives transitively through the endpoint (the route
  kind's precedent)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Folder exists and matches the lowercase enum name.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files

✅ **Passed:**

- `spec.proto` (4.4 KB): the honest flat shape -- `profile_id` FK,
  `security_policy_name` (provider-exact regex), `firewall_policy_id` FK,
  `domain_ids` repeated FK (1-500, endpoint default_kind with the custom-domain
  alternative documented). The provider's triple-nested one-item blocks
  (`security_policies` -> `firewall` -> `association`) are TF ergonomics for a
  one-choice ARM union and are deliberately not mirrored.
- `api.proto`, `stack_input.proto`, `stack_outputs.proto` complete
- Every CEL message is a complete user-facing sentence

#### 3.2 Generated Stubs

✅ All four `.pb.go` files regenerated and current.

#### 3.3 / 3.4 Unit Tests

✅ `spec_test.go` -- 11 cases (mixed-ID happy path, name-format error paths, required
fields, the 500-domain cap); ALL PASS.

#### Apply-Time Validator Source-Diff (mandatory)

Read in full: `cdn_frontdoor_security_policy_resource.go` (schema, Create/Update
bodies, `expandCdnFrontdoorFirewallPolicyParameters`) plus
`validate.FrontDoorSecurityPolicyName` and `validate.FrontDoorSecurityPolicyDomainID`.

Contracts found OUTSIDE the schema and their disposition:

- **Create/Update body fetches the PROFILE to read its sku** and enforces the
  100 (Standard) / 500 (Premium) domain cap -> cross-resource, not statically
  checkable; the universal 500 ceiling is a spec bound, the sku-dependent 100 cap is
  documented on `domain_ids`, the README, and docs.
- **`FrontDoorSecurityPolicyDomainID` accepts BOTH custom-domain and endpoint IDs**
  (with case-insensitive fallbacks) -> realized as the mixed-type `domain_ids`
  design; both accepted shapes documented and exercised in the hack manifest.
- **`patterns_to_match` accepts exactly `/*`** (schema in-list with one value) ->
  a constant, not a field: both modules send it unconditionally; recorded skip with
  its re-enable trigger.
- **The WAF-policy-sku-must-match-profile-sku pairing** is enforced by ARM at
  association time -> cross-resource, documented in this kind and the WAF kind.
- **No invented rules:** no uniqueness or shape constraints beyond the provider's
  regex and counts.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ Module + entrypoint complete; comments explain the wrapper-nesting rebuild, the
patterns constant, and the bridge's list-to-string dialect. Release-equivalent build
passes. Shared provider builder used.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files present; canonical empty provider block, `~> 4.0`. Full
`planton tofu plan` against the hack manifest renders both domain shapes and the
`["/*"]` pattern -- 1 to add, outputs wired.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with the `pkg/outputs` conformance case (full proto population, zero
unmapped: yes).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent / scope source | `spec.profile_id` | `var.spec.profile_id` | ✅ MATCH |
| Resource naming basis | `spec.security_policy_name` | same | ✅ MATCH |
| WAF policy reference | `spec.firewall_policy_id` | same | ✅ MATCH |
| Domain list | resolved literals, order preserved | same | ✅ MATCH |
| `patterns_to_match` | constant `"/*"` (bridge flattens the one-item list to a string) | constant `["/*"]` | ✅ MATCH (same ARM payload; dialect documented in BOTH modules) |
| Tags | none -- ARM does not support tags here (noted in both modules) | none | ✅ MATCH |
| Stack outputs | `security_policy_id`, `security_policy_name` | same | ✅ MATCH |

**Effect on score:** none.

---

### 6-9. Documentation and Supporting Files

✅ `docs/README.md` (3.9 KB): field mapping, the flattening/one-association/constant
decisions, recorded skips, the E2E boundary note. ✅ `README.md`, `catalog-page.md`,
both module READMEs, hack manifest. ✅ Bazel trees build.

---

### 10. Presets (5.00%)

✅ Two ranked pairs (`01-endpoint-association`, `02-custom-domain-association`)
covering the kind's decision space (default-domain vs custom-domain protection); all
validate; `my-` naming; `valueFrom` forms correct.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` passes; the kind is pure ARM references -- no
secret-bearing fields and no look-alikes needing exemption.

---

## Recorded Skips (with reasons)

- **`patterns_to_match`** -- the service accepts exactly `/*`; a one-value knob is a
  constant. Re-enable trigger: Azure accepting a second pattern.
- **Per-domain `active` computed flag** -- read-back association status; nothing
  composes on it.

---

**Audit completed at:** 2026-07-08 12:45:00
**Reference:** `architecture/deployment-component.md`
