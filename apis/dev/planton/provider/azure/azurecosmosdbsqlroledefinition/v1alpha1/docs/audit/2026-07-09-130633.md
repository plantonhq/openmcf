# Audit Report: AzureCosmosdbSqlRoleDefinition

**Audit Date:** 2026-07-09 13:06:33
**Component Kind:** AzureCosmosdbSqlRoleDefinition
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurecosmosdbsqlroledefinition/v1alpha1/`
**Enum Value:** 504
**ID Prefix:** azcsqlrdef

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureCosmosdbSqlRoleDefinition = 504` exists in `cloud_resource_kind.proto`
- Enum value 504 is in the Azure band (400-599), inside the Cosmos data-services sub-band (500-509) beside its family (account 432, SQL database 500, SQL container 501)
- ID prefix `azcsqlrdef` is unique across all registered prefixes and not within one edit distance of a neighbor
- `kind_meta` includes provider (azure), version (v1), id_prefix
- No registry prerequisites, deliberately: Cosmos account names are globally unique DNS labels (the recorded sub-band convention); the E2E scenario declares a scenario-local account fixture

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists at `apis/dev/planton/provider/azure/azurecosmosdbsqlroledefinition/v1alpha1/`
- Folder name is the lowercase enum name; `v1/` subfolder present

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files

✅ **Passed:**

- `api.proto`, `spec.proto`, `stack_input.proto`, `stack_outputs.proto` all present and substantial
- Spec models the FULL azurerm v4.80 surface of `azurerm_cosmosdb_sql_role_definition`: single ARM-id parent FK (RG/account derived), `role_name`, closed `type` enum (CustomRole default / BuiltInRole), repeated-ref `assignable_scopes` (min 1, account-defaulting), `permissions[].data_actions` (min 1 / min 1, allow-only per Cosmos's model), optional pinned UUID `role_definition_id`
- Every CEL message is a complete user-facing sentence; standard constraints (required, min_items, uuid) carry protovalidate's own messages
- Field comments teach the Cosmos-RBAC-vs-ARM-RBAC boundary, the built-in role IDs, the data-action vocabulary, and the scope grammar at LLM-configurable density

#### 3.2 Generated Stubs

✅ All four `.pb.go` files regenerated and committed alongside the protos.

#### 3.3 + 3.4 Unit Tests

✅ `spec_test.go` present; 12 ginkgo cases covering the happy path plus every validation rule's error path (missing account/name/scopes/permissions, empty data actions, non-UUID GUID, both type values, database-scoped scope literal). `go test ./apis/dev/planton/provider/azure/azurecosmosdbsqlroledefinition/v1alpha1/` → **ok**.

#### Apply-Time Validator Source-Diff (mandatory)

Source read: `internal/services/cosmos/cosmosdb_sql_role_definition_resource.go` (create/read/update/delete paths) plus the `2024-08-15/rbacs` SDK id parsers. Findings:

- **Account-name locking** around create/update/delete (`locks.ByName(account, CosmosDbAccountResourceName)`) — Cosmos serializes control-plane writes per account. Module-internal provider behavior; nothing to model.
- **Client-side UUID generation** when `role_definition_id` is unset — both engines inherit it from the shared provider code; the spec documents the generated-vs-pinned posture.
- **No CustomizeDiff and no Validate\* helpers beyond schema-level string checks** (`validation.IsUUID` on the GUID — mirrored as the spec's uuid CEL; `validation.StringInSlice` on type — mirrored as the closed enum; `StringIsNotEmpty` on scopes/actions — mirrored as min_len items). Nothing beyond the schema is a finding and is stated explicitly.
- **Cross-resource contracts that stay apply-time** (documented on fields + docs/README.md, per the protovalidate ref-dereference constraint): assignable scopes must sit at or below the account; the account must be a SQL API account.
- **No invented rules**: the spec adds no constraint the provider/ARM does not enforce (permissions min-1 mirrors the provider's Required flags; scope-path shapes are documented, not CEL'd).

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `iac/pulumi/module/{main.go, locals.go, outputs.go, parse.go}` present; entrypoint `iac/pulumi/main.go` at the root declaring `package main`; no `main/`/`entrypoint/` subdir; `Pulumi.yaml` + `Makefile` present
- Release-equivalent build passes: `go build -o /dev/null ./apis/dev/planton/provider/azure/azurecosmosdbsqlroledefinition/v1alpha1/iac/pulumi`
- Provider built via the shared `pulumiazureprovider.Get` builder (keyless-safe); never inline
- Module comments explain the ARM-id parent parsing contract, the type-enum wire mapping, the send-only-when-pinned GUID posture, and the composition seam — the why, not narration

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five `.tf` files present; `azurerm ~> 4.0` with the canonical empty provider block
- `locals.tf` parses account/RG from the ARM id with named-group regexes that fail the plan loudly; the type map speaks ARM's exact wire vocabulary
- HCL comments carry the same why-level density as the Pulumi module
- Full `planton tofu plan` on the hack manifest renders every seam: both assignable-scope shapes, two permission blocks, the pinned GUID, `type = "CustomRole"`

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs --kind AzureCosmosdbSqlRoleDefinition --sample-outputs` → 4/4 proto fields populated, zero unmapped; the `pkg/outputs/conformance_test.go` case passes.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | account/RG parsed from `spec.cosmosdb_account_id` (`parse.go`) | same, `locals.tf` regexes (identical anchored semantics) | ✅ MATCH |
| Resource naming basis | ARM name = `spec.role_name`; GUID from `spec.role_definition_id` or generated | same | ✅ MATCH |
| Labels/tags | none — ARM does not support tags on Cosmos children (documented both modules) | same | ✅ MATCH |
| Spec feature coverage | every spec field honored (type mapped, GUID conditional, scopes, permissions) | same | ✅ MATCH |
| Enum wire mapping | `roleTypeMap` → CustomRole/BuiltInRole; unspecified sends nothing | `type_map` + `try(...)` → null when unspecified | ✅ MATCH |
| Dependency ordering | single resource; refs resolved pre-module | same | ✅ MATCH |
| Stack outputs → proto | `role_definition_id`/`_guid`/`role_name`/`cosmosdb_account_name` | identical names/shapes | ✅ MATCH |

**Effect on score:** none — engines in parity, zero exceptions needed (pulumi-azure v6.38 bridges the entire surface).

---

### 6. Documentation - Research (13.18%)

✅ `docs/README.md` carries the azurerm→spec field map with ForceNew/update semantics, the decomposition verdict, the apply-time contract sweep, the zero-skips statement, the Mongo-RBAC-not-here note, and operational guidance (readMetadata, built-ins, SQL-API-only, allow-only unions).

**Score:** 13.18% / 13.18%

---

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` (when-to-use incl. when NOT to create a definition, key configuration, composition snippets) and `catalog-page.md` (what gets created, quick start, outputs table, related resources) both present and substantial.

**Score:** 13.09% / 13.09%

---

### 8. Supporting Files (10.09%)

✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` (exercising the type seam, both scope shapes, multiple permission blocks, and the pinned GUID) all present.

**Score:** 10.09% / 10.09%

---

### 9. Nice to Have (10%)

✅ Gazelle-generated `BUILD.bazel` files present for the component and both Pulumi packages; Bazel builds green.

**Score:** 10.00% / 10.00%

---

### 10. Presets (5.00%)

✅ **Passed:**

- Three preset pairs, each `.yaml` with a companion `.md`: `01-read-only-role`, `02-writer-no-delete`, `03-database-scoped-role`
- All validate via `planton validate-manifest`; apiVersion/kind match `api.proto`; `metadata.name` carries the `my-` prefix; ref fields use `valueFrom`/`value` wrapper forms; no `status` blocks; no stale field names; no duplicate ranks
- The set covers the kind's real decision space: general read role, the allow-only-carve-out teaching case, and assignable-scope narrowing

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

Ran `go run . secret-coverage --check` → gate passed; zero `gaps[]` entries for `AzureCosmosdbSqlRoleDefinition`. The kind carries no secret-bearing fields (role names, scopes, and data-action patterns are not secret material), and no field name trips the analyzer's heuristic — no annotations or exemptions needed.

---

## Next Steps

None — the component is complete. Live dual-engine E2E is wired (`e2e/` profile, scenario-local account fixture, verifier, entrypoints) and runs per its profile.

---

**Audit completed at:** 2026-07-09 13:06:33
**Reference:** See `architecture/deployment-component.md` for ideal state definition
