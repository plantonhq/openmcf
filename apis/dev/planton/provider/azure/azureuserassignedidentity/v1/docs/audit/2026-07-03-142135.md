# Audit Report: AzureUserAssignedIdentity

**Audit Date:** 2026-07-03 14:21:35
**Component Kind:** AzureUserAssignedIdentity
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azureuserassignedidentity/v1/`
**Enum Value:** 460
**ID Prefix:** azid

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.18% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Both gates pass and every critical category is complete.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureUserAssignedIdentity = 460`, Azure band, identity sub-band anchor
- `id_prefix: "azid"` unique; `prerequisites: [AzureResourceGroup]` (the transitive root of the identity fixture chain)

**Score:** 4.44% / 4.44%

### 2. Folder Structure (4.44%)

✅ **Passed:** correct hierarchy, lowercase name, `v1/` subfolder.

**Score:** 4.44% / 4.44%

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos present; the spec models the FULL azurerm v4 `azurerm_user_assigned_identity` surface: name, resource group (FK), region, `isolation_scope` (enum mirroring ARM's opt-in Regional mode), and user `tags`
- The formerly embedded role-assignment block is gone — grants are `AzureRoleAssignment`'s job, trust rules are `AzureFederatedIdentityCredential`'s; fields renumbered contiguously (1-5)
- Comments are timeless and Azure-only; no design-history narration
- Generated stubs current; `spec_test.go` (5.6 KB): 15 ginkgo cases incl. boundary lengths (3/128), isolation scope, tags, and the KRM constants — `go test` **ok**

**Score:** 22.20% / 22.20%

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- Module files present, richly commented (replacement-mints-a-new-principal warning, tag-merge order, isolation-scope defaulting)
- Provider via the shared `pulumiazureprovider.Get`; entrypoint canonical; release-equivalent build passes
- `isolation_scope` supported natively by the pinned SDK (pulumi-azure v6.38.0 — bumped this session, repo-wide builds green)

**Score:** 13.32% / 13.32%

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five `.tf` files; `tofu init`/`validate`/`fmt` green; full `planton tofu plan` on the hack manifest: 1 to add, all 4 outputs
- Canonical empty `provider "azurerm" { features {} }` on `~> 4.0`
- Enum name string ("REGIONAL") mapped to ARM's "Regional" in locals, mirroring the established principal-type convention

**Score:** 4.24% / 4.24%

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.resource_group` resolved value | `var.spec.resource_group` | ✅ MATCH |
| Resource naming basis | `spec.name` | `spec.name` | ✅ MATCH |
| Tags | metadata-derived + user tags merged, user wins | identical merge order | ✅ MATCH |
| Spec feature coverage | region, RG, name, isolation_scope, tags | same | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 4/4 fields | 4/4 fields | ✅ MATCH |

- Both engines send `isolation_scope` only when REGIONAL, so an unspecified spec and ARM's default deploy identically
- `pkg/outputs/conformance_test.go` now carries an AzureUserAssignedIdentity case — passing
- Live dual-engine E2E: `TestAzureUserAssignedIdentity_Pulumi/minimal` PASS (135s) and `_Terraform/minimal` PASS (160s), all 8 phases each, zero orphans (`az group list` and `az identity list` both empty afterward)

**Effect on score:** none.

### 6. Documentation - Research (11.18% / 13.18%)

⚠️ `docs/README.md` is 5.3 KB (moderate band). Content quality is complete: identity
anatomy (the three identifiers and their consumers), the identity-only decomposition
rationale, the full azurerm→spec field map with recorded not-modeled reasons (embedded
grants, system-assigned identities), and operational notes (replacement blast radius, AAD
replication lag, regional isolation semantics).

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` (4.8 KB) rewritten identity-only with the composition seams; no
cross-provider references, no design-history breadcrumbs (the former "per DD03" citation
is gone).

### 8. Supporting Files (10.09%)

✅ Both module READMEs rewritten; `iac/hack/manifest.yaml` rewritten to the new spec
(tags exercised; proven by a full `planton tofu plan`).

### 9. Nice to Have (10.00%)

✅ BUILD.bazel current; first-class E2E profile + scenario added alongside the existing
prerequisite fixture profile; test entrypoints wired.

### 10. Presets (5.00%)

✅ Three ranked pairs: `01-standard`, `02-ci-deployer`, `03-governance-tagged` — the
former `02-multi-role` (bundled grants) removed; each preset teaches the composition
seam with `AzureRoleAssignment` / `AzureFederatedIdentityCredential`; correct
apiVersion/kind, `my-` names, `value:` wrappers, no stale fields.

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

Zero `gaps[]` for this kind; the identity carries no secret material by design (that is
the point of managed identities).

---

## Next Steps

None required for this component. The identity/RBAC wave (role assignment, role
definition, federated identity credential, identity rework) is complete; the networking
wave is next in the leaf-first order.

---

**Audit completed at:** 2026-07-03 14:21:35
**Reference:** `architecture/deployment-component.md`
