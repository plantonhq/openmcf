# Audit Report: AzureLogAnalyticsWorkspace

**Date:** 2026-07-10-034017
**Component:** AzureLogAnalyticsWorkspace (enum 450, provider azure, id_prefix `azlaw`)
**Mode:** full audit with `--parity` focus

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

The kind was reworked breaking from a 6-field 80/20 spec to the full azurerm
v4.80 `azurerm_log_analytics_workspace` surface: the closed 4-value SKU enum
with the commitment-tier pairing enforced in both directions, the full
security/network posture (Entra-only, AMPLS-only paths, resource-context
access, forced CMK for query artifacts, immediate purge), the managed
identity, the default DCR seam, and user tags. Both engines rewritten; the
Pulumi module migrated to the shared provider builder.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 450, azure band, unique prefix `azlaw`, `prerequisites: [AzureResourceGroup]` added |
| 2 | Folder Structure | ✅ `provider/azure/azureloganalyticsworkspace/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs regenerated; 24 spec tests pass (`go test` green); CEL messages user-facing |
| 4 | IaC Pulumi | ✅ module + root entrypoint; release-equivalent build passes; richly commented; shared builder |
| 5 | IaC Terraform | ✅ full variable surface; richly commented; canonical empty provider block |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ `docs/README.md` -- field map, skips with reasons, transition semantics, output design |
| 7 | User README | ✅ rewritten to the reworked surface |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (full `tofu plan` renders the commitment-tier + posture seams) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 3 pairs (pay-as-you-go / commitment-tier / private-hardened), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (no `AzureLogAnalyticsWorkspace:` gaps in `secret-coverage --output json`; shared keys are OUTPUTS, prose-documented per the established precedent) |

## Apply-Time Validator Source-Diff

Read `log_analytics_workspace_resource.go` Create/Update, the CustomizeDiff,
and referenced validators:

- **SKU ForceNew matrix** (CustomizeDiff): PerGB2018 <-> CapacityReservation
  in place, everything else ForceNew -- an update-behavior contract, documented
  on the spec field (not statically checkable).
- **Standard/Premium creation block** (CustomizeDiff error) -- front-loaded by
  EXCLUDING both values from the SKU enum (recorded skip).
- **LACluster-at-create block** (create path) -- front-loaded by excluding the
  value (recorded skip; cluster-linked state, not a choosable tier).
- **CapacityReservation <-> reservation_capacity pairing** (enforced in BOTH
  create and update paths) -- front-loaded as two message CELs.
- **`data_collection_rule_id` second-call application** (ARM rejects at
  create) and **`allow_resource_only_permissions` update+poll workaround** --
  provider behaviors, documented in both modules.
- No spec CEL invents a rule the provider does not enforce (verified rule by
  rule; the workspace-name regex is the provider's own).

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `resource_group` value both engines)
- Naming basis: MATCH (`spec.workspace_name` both engines)
- Labels/tags: EXCEPTION (family-wide tag-shape note, documented in BOTH modules; output-neutral)
- Selectors: n/a
- Feature coverage: MATCH -- every spec field wired on both engines, verified
  field-by-field; unspecified-enum handling identical (both deploy PerGB2018)
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- `pkg/outputs` conformance case added (7 fields incl.
  the ARM-id/customer-GUID split); both engines export identical keys

**PARITY ✅** (one documented exception: the family-wide tag shape).

## Live Evidence

Live dual-engine E2E GREEN: Pulumi 228s / Terraform 191s (all 8 phases;
zero orphans in the final sweep). Run 1 additionally live-caught the
unspecified-enum wire-map defect in the Pulumi module (unmapped enum sent
the empty string, provider-rejected) -- the unspecified row now maps to
PerGB2018 explicitly. The offline plan had earlier caught that workspaces
support only single identity models (SystemAssigned XOR UserAssigned) --
the spec enum was corrected from the generic three-model shape before any
live run.

## Critical Gaps

None.
