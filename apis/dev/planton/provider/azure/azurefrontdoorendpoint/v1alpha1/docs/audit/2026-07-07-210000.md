# Audit Report: AzureFrontDoorEndpoint

**Audit Date:** 2026-07-07 21:00:00
**Component Kind:** AzureFrontDoorEndpoint
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurefrontdoorendpoint/v1alpha1/`
**Enum Value:** 481
**ID Prefix:** azfde

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ Enum `AzureFrontDoorEndpoint = 481` in the CDN sub-band;
`prerequisites: [AzureFrontDoorProfile]` with rationale (ARM child of
the profile; RG chains transitively); `id_prefix: "azfde"` unique.

### 2. Folder Structure (4.44%)

✅ Correct hierarchy.

### 3. Protobuf API Definitions (22.20%)

✅ Full azurerm v4.80 surface (four attributes, all modeled): parent by
ARM id FK (`profile_id` → AzureFrontDoorProfile.profile_id, ForceNew),
`endpoint_name` with the provider's exact 2-46 regex, `enabled` optional
default true, user `tags`. `spec_test.go`: 15 cases (5 valid, 10 error
paths) -- all PASS.

### 4. IaC Modules - Pulumi (13.32%)

✅ Single `cdn.FrontdoorEndpoint`; shared `pulumiazureprovider.Get`
builder; comments explain ARM-id parent addressing and the
only-when-set `enabled` guard. Release-equivalent build passes.

### 5. IaC Modules - Terraform (4.24%)

✅ Canonical empty provider block; `planton tofu plan` on the hack
manifest renders the disabled-endpoint seam + merged tags.

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅** (`pkg/outputs`: 3/3 populate, zero
unmapped)

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `spec.profile_id.GetValue()` | `var.spec.profile_id` | ✅ MATCH |
| Naming basis | `spec.endpoint_name` | `var.spec.endpoint_name` | ✅ MATCH |
| Enabled guard | sent only when set | null passthrough | ✅ MATCH |
| Tags | merged, user wins (family PARITY-EXCEPTION documented both sides) | same | ✅ MATCH |
| Stack outputs → proto | 3 exports incl. `host_name` | 3 outputs, same names | ✅ MATCH |

### 6-10. Documentation, Supporting Files, Presets

✅ `docs/README.md` (field map, hostname-hash behavior, zero recorded
skips); `README.md` + `catalog-page.md` + module READMEs; hack manifest;
presets ×2 (production endpoint, pre-provisioned disabled) -- validate
green; E2E `prerequisite.yaml` (the route chain's fixture),
`scenarios/minimal.yaml`, `profile.yaml`, verifier + test entrypoints.

### 11. Secure-by-Default Coverage (Critical GATE)

**COVERAGE ✅** -- no secret-bearing fields.

---

## Apply-Time Validator Source-Diff

`cdn_frontdoor_endpoint_resource.go` Create/Update carry no cross-field
contracts beyond the schema (import-exists check only). Nothing to
model.

## Critical Gaps

None.

---

**Audit completed at:** 2026-07-07-210000
**Reference:** `architecture/deployment-component.md`
