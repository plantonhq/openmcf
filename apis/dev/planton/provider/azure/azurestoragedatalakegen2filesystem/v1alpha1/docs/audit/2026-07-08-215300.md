# Audit Report: AzureStorageDataLakeGen2Filesystem

**Audit Date:** 2026-07-08 21:53:00
**Component Kind:** AzureStorageDataLakeGen2Filesystem
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurestoragedatalakegen2filesystem/v1alpha1/`
**Enum Value:** 495
**ID Prefix:** azadlsfs

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `AzureStorageDataLakeGen2Filesystem = 495`, unique `id_prefix: "azadlsfs"`
(one-edit-distance clean against all existing Azure prefixes), storage-data
grouping; covered by the shared no-account-prerequisite rationale (globally
unique account names).

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Standard anatomy at
`apis/dev/planton/provider/azure/azurestoragedatalakegen2filesystem/v1alpha1/`.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- Four protos present; the filesystem-name CEL mirrors azurerm's exact
  validator (`$root` or lowercase alnum/hyphen 3-63, no leading hyphen) with a
  plain-English message
- `owner`/`group` mirror azurerm's `validation.Any(IsUUID, "$superuser")`
  contract as CELs; the ace message carries the permission regex
  (`[r-][w-][x-]`, giovanni's `ValidateACEPermissions`), closed scope/type
  enums, and the mask/other-take-no-qualifier contract (giovanni's
  `ACE.Validate`) as a message CEL
- `default_encryption_scope` is a `StringValueOrRef` →
  `AzureStorageEncryptionScope.encryption_scope_name` (the container-kind
  precedent); `storage_account_id` →
  `AzureStorageAccount.storage_account_id`, both output-backed
- Generated stubs present; 21 spec-test cases (happy + every error path incl.
  `$root`, all name-rule violations, both owner/group formats, all permission
  malformations, and the mask/other qualifier rejections); `go test` — ALL PASS

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ Module trio + root entrypoint + Pulumi.yaml + Makefile; release-equivalent
build passes; shared provider builder; owner/group/scope presence-guarded
(Computed on the provider); comments explain the data-plane create path, the
constructed container-proxy output id, and the HNS apply-time contract.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files; `azurerm ~> 4.0` + canonical empty provider block; full
`planton tofu plan` on the hack manifest renders every seam (all four ACE
types, both scopes, qualified entries, the encryption scope, owner as GUID +
group as `$superuser`, base64 properties) — 1 to add, 3/3 outputs.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `StorageAccountId` (ARM id) | `storage_account_id` (ARM id) | ✅ MATCH |
| Resource naming basis | ARM name = `spec.filesystem_name` | same | ✅ MATCH |
| ACE scope/type vocabularies | `aceScopeStrings`/`aceTypeStrings` → lowercase wire values; unset scope stays unset (provider default access) | `ace_scope_map`/`ace_type_map`, null-guarded scope | ✅ MATCH |
| ACE qualifier | `Id` sent only when `object_id` set | `id` null-guarded | ✅ MATCH |
| owner/group | sent only when set (Computed) | null-guarded locals | ✅ MATCH |
| Encryption scope | sent only when set | null-guarded local | ✅ MATCH |
| Properties map | direct pass-through | direct pass-through | ✅ MATCH |
| `filesystem_id` output | CONSTRUCTED container-proxy id (`{account}/blobServices/default/containers/{name}`) | identical construction in `locals.tf` | ✅ MATCH |
| Stack outputs → proto | filesystem_id, filesystem_name, storage_account_name | identical | ✅ MATCH |
| Azure tags | none (ARM: unsupported; properties is the metadata surface) | none | ✅ MATCH |

Verified with the `pkg/outputs/conformance_test.go` case (PASS, 3/3 fields).

**Effect on score:** none.

---

### 5c. Apply-Time Validator Source-Diff (per the audit rule)

Walked `storage_data_lake_gen2_filesystem_resource.go` create/update/read
bodies and the giovanni `accesscontrol` package beyond the schema:

- **ACL/owner/group require HNS on the ACCOUNT** — enforced at create AND
  update (`!account.IsHnsEnabled` errors). Cross-resource (the account is a
  reference), so deliberately documented on the spec/modules rather than
  CEL'd — not an invention, not a miss.
- **`ACE.Validate` (giovanni)**: mask/other reject a qualifier → mirrored as
  the message CEL; permissions regex → mirrored; tag-type closed set →
  mirrored by the closed enum.
- **`ACL.Validate` has NO duplicate-entry check** (an upstream TODO) — the
  spec deliberately invents no uniqueness rule.
- **Name validation** (`validateStorageDataLakeGen2FileSystemName`) →
  mirrored exactly, including `$root` and the no-leading-hyphen rule.
- Nothing else beyond the schema: no CustomizeDiff on this resource.

---

### 6-9. Documentation, Supporting Files, Nice to Have

✅ All present: research doc (data-plane resource semantics, the constructed
container-proxy output rationale, the `object_id` naming decision, the
gen2-path adoption-backlog verdict, the HNS apply-time skip reason), README,
catalog page, both module READMEs, hack manifest (every ACE shape), BUILD
files, E2E profile + scenario + scenario-local HNS account fixture, verifier
(the ARM container-proxy pattern — no data-plane client, no new SDK deps) +
both test entrypoints. The encryption-scope seam's live exclusion is recorded
in the profile with the specific reason.

---

### 10. Presets (5.00%)

✅ Three pairs — `01-data-lake-zone`, `02-team-scoped-workspace`,
`03-regulated-zone-cmk` — one per real lake posture (medallion zone,
Entra-group workspace, CMK compliance zone). All validate.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

No secret-bearing fields: owner/group/object_id are Entra OBJECT IDs
(identifiers), the encryption-scope field is a name reference, and properties
is documented not-for-secrets. The analyzer reports no gap and the Azure
slice stays 100%.

---

**Audit completed at:** 2026-07-08-215300
**Reference:** `architecture/deployment-component.md`
