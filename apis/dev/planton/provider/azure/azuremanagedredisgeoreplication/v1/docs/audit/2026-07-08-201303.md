# Audit Report: AzureManagedRedisGeoReplication

**Audit Date:** 2026-07-08 20:13:03
**Component Kind:** AzureManagedRedisGeoReplication
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azuremanagedredisgeoreplication/v1/`
**Enum Value:** 511
**ID Prefix:** azmredisgeo

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureManagedRedisGeoReplication = 511` exists with
  `kind_meta` in the Managed Redis sub-band (510-519)
- `id_prefix: azmredisgeo` is unique across the registry
- NO registry prerequisite on `AzureManagedRedis` -- deliberate and
  documented in the registry comment: clusters are expensive
  slow-provisioning parents, so E2E declares scenario-local cluster
  fixtures; deploy ordering in composed environments flows from the
  `managed_redis_id` references

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- `apis/dev/planton/provider/azure/azuremanagedredisgeoreplication/v1/`
  exists; folder name is the lowercase enum name

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files

✅ **Passed:**

- `spec.proto`: the full `azurerm_managed_redis_geo_replication`
  surface -- the managing member FK plus 1-4 linked member FKs; the
  header comment teaches the multi-primary model, the
  group-wide-operation rationale, deletion-is-unlink semantics, and the
  cross-member contracts Azure enforces at link time
- `api.proto`, `stack_input.proto`, `stack_outputs.proto` complete
- CEL/constraint messages are complete user-facing sentences

#### 3.2 Generated Stubs

✅ All four `.pb.go` files generated and current.

#### 3.3 / 3.4 Unit Tests

✅ `spec_test.go` -- 7 cases (pair, maximum mesh, kind-output
references; missing-manager, empty-list, over-max error paths);
`go test ./.../azuremanagedredisgeoreplication/v1/` ALL PASS.

#### Apply-Time Validator Source-Diff (mandatory)

Read in full: `managed_redis_geo_replication_resource.go` (schema,
Create/Read/Update/Delete, CustomizeDiff, the `databaselink`
link/unlink planner and its force-link/unlink invocations, the
geo-replication linking/unlinking custom pollers).

Contracts found OUTSIDE the schema and their disposition:

- **CustomizeDiff (self-link rejection):** `linked_managed_redis_ids`
  must not contain `managed_redis_id` -> the members are
  `StringValueOrRef`s and message-level CEL cannot dereference a ref's
  sub-fields (the protovalidate-java constraint), so the contract is
  DOCUMENTED on both fields and in docs/README.md; the provider rejects
  violations at plan/apply with its own diagnostic.
- **Set semantics (distinct members):** the provider's TypeSet dedupes;
  the same ref-dereference constraint applies -> documented.
- **Link-time cross-member contracts:** same group name on every
  member, BALANCED_B3+ SKUs, no persistence, geo-compatible modules ->
  cross-resource; the per-instance halves are CEL-enforced on
  AzureManagedRedisSpec, the group-level pairing is documented.
- **Out-of-band state churn:** ARM mutates every member's replication
  state on link/unlink -- the provider's own reason for modeling
  membership as a separate resource; adopted as this kind's split
  verdict.
- **No invented rules:** no ordering, role, or naming rules exist in
  the provider; none were added.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present;
  comments explain the group-wide-operation model, the
  one-resource-per-group rule, deletion-is-unlink, and the no-tags
  rationale (no ARM object of its own) -- the module-comment bar
- Entrypoint at `iac/pulumi/main.go` root (`package main`),
  `Pulumi.yaml` + `Makefile` present, no subdir
- Release-equivalent build passes:
  `go build -o /dev/null ./.../iac/pulumi`
- Shared provider builder (`pulumiazureprovider.Get`)

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files present; the two-field kind's `variables.tf` and
  `main.tf` are proportionally sized and richly commented; canonical
  empty provider block with `~> 4.0`
- Full `planton tofu plan` against `iac/hack/manifest.yaml` renders a
  three-member group -- 1 to add, output wired

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with the `pkg/outputs` conformance case.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Managing member | `spec.managed_redis_id` resolved value | `var.spec.managed_redis_id` | ✅ MATCH |
| Linked members | resolved ref values, declared order | same | ✅ MATCH |
| Tags | none (no ARM object) -- documented in both | none | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs | `geo_replication_id` | same | ✅ MATCH |

**Effect on score:** none.

---

### 6-9. Documentation and Supporting Files

✅ `docs/README.md`: field mapping, the split verdict (the provider's
own out-of-band-churn reasoning), the documented-not-CEL'd self-link
constraint with its protovalidate-java rationale, and the operational
notes (one resource per group, unlink-not-delete, the state-aware E2E
verifier, region evacuation, the contrast with classic linked-server
failover). ✅ `README.md`, `catalog-page.md`, both module READMEs,
hack manifest. ✅ BUILD.bazel trees build via Bazel.

---

### 10. Presets (5.00%)

✅ Two ranked pairs (`01-two-region-pair`, `02-global-mesh`) covering
the kind's decision space (pair vs maximum mesh); both validate;
`my-` naming; `valueFrom` forms correct.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` passes; the kind carries no
secret-bearing fields and no name-heuristic look-alikes.

---

## Recorded Skips (with reasons)

- None -- both provider fields are modeled.

---

**Audit completed at:** 2026-07-08 20:13:03
**Reference:** `architecture/deployment-component.md`
