# Audit Report: AzureMssqlElasticPool

**Audit Date:** 2026-07-05 01:15:00
**Component Kind:** AzureMssqlElasticPool
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azuremssqlelasticpool/v1/`
**Enum Value:** 436
**ID Prefix:** azmsqlpool
**Audit Mode:** `--parity` (parity-focused full audit)

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

Gates: **PARITY ✅** and **COVERAGE ✅**. All automated guards pass fresh:
spec tests (17 cases covering every CEL — the closed sku vocabulary in
both directions, the per-database min≤max rule, the storage-cap XOR, the
Hyperscale HA-replica gate, and the vCore-only license gate), targeted +
release-equivalent builds, the `pkg/outputs` conformance case (2/2 proto
fields), `secret-coverage --check` (no secret-bearing fields on this
surface), `validate-refs --check` (the parent server FK resolves), a
full `planton tofu plan` on the hack manifest proving the derived
tier + family (GP_Gen5 → GeneralPurpose/Gen5), fractional per-database
bounds, and the license enum seam (1 to add), and 3 presets validating.

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.44% | 4.44% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.34% | 13.34% | ✅ |
| Documentation - User-Facing | 13.33% | 13.33% | ✅ |
| Supporting Files | 13.33% | 13.33% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Category 5b: Cross-Engine IaC Parity (the gate this audit leads with)

Field-by-field sweep of `iac/tf/` vs `iac/pulumi/module/` against
`spec.proto` + `stack_outputs.proto`:

- **Resource surface identical**: one `azurerm_mssql_elasticpool` /
  `mssql.ElasticPool`.
- **Parent derivation identical**: the server NAME and RESOURCE GROUP
  the resource wants are derived from the spec's single parent FK (the
  server's ARM id) with the same split-index logic on both engines.
- **Sku derivation identical**: the service tier and hardware family are
  pure functions of `sku_name`, derived through the SAME exhaustive
  vocabularies on both engines (DTU pools carry no family) — a
  name/tier/family mismatch is unrepresentable on either engine.
- **Enum vocabularies exhaustive and identical**: enclave types (incl.
  ARM's explicit `Default`) and license types.
- **Conditional-send behavior identical**: both storage caps forwarded
  only when present; the maintenance window presence-guarded to
  `SQL_Default` on both engines.
- **Outputs identical**: `elastic_pool_id`, `elastic_pool_name`.
- **Documented PARITY-EXCEPTION (pre-existing, family-wide)**: the tag
  metadata shape; output-neutral, recorded in both modules.

**Verdict: PARITY ✅ — no undocumented divergence.**

---

## Secure-by-Default Coverage

No secret-bearing fields exist on this surface. `planton secret-coverage
--check` green.

**Verdict: COVERAGE ✅.**

---

## Recorded Skips

See `docs/README.md`: the retired Gen4 hardware families (Azure no longer
provisions them; the closed vocabulary carries only provisionable
families).
