# Audit Report: AzureVirtualNetwork

**Audit Date:** 2026-07-03 15:47:33
**Component Kind:** AzureVirtualNetwork
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurevirtualnetwork/v1/`
**Enum Value:** 406
**ID Prefix:** azvnet

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.18% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Both gates pass and every critical category is complete.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureVirtualNetwork = 406` in `cloud_resource_kind.proto` (Azure foundation/networking sub-band)
- `id_prefix: "azvnet"` is unique across the registry; `container_kind: true` (subnets live inside)
- `kind_meta` carries `prerequisites: [AzureResourceGroup]` — the honest direct dependency

**Score:** 4.44% / 4.44%

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy, lowercase folder name, `v1/` subfolder.

**Score:** 4.44% / 4.44%

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos exist with dense intent-explaining comments
- Generated `.pb.go` stubs present and current (`make protos` green)
- `spec_test.go`: 31 ginkgo cases covering every field, the address-source XOR (neither/both/valid), IPAM pool bounds and count format, BGP community shape and range boundaries, DDoS plan requiredness, flow-timeout 4-30 boundaries, name-format rules, and the KRM constants
- `go test ./apis/dev/planton/provider/azure/azurevirtualnetwork/v1/` — **ok**
- The full `azurerm_virtual_network` v4.80 surface is modeled: multi-CIDR `address_spaces` XOR `ip_address_pools` (message-level CEL mirroring azurerm's ExactlyOneOf), `dns_servers`, `bgp_community` (range-precise regex), `ddos_protection_plan` (id + enable), `encryption` enforcement enum, `flow_timeout_in_minutes`, `private_endpoint_vnet_policies` enum, `edge_zone`, user `tags`
- Deliberately not modeled, with recorded reasons in docs: inline `subnet` blocks (standalone `AzureSubnet` is the grain), the standalone DNS-servers resource (redundant with the inline field)

**Score:** 22.20% / 22.20%

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present, richly commented (lifecycle: in-place vs replace; address-space shrink constraint; IPAM output visibility; the honest-decomposition note)
- Provider built via the shared `pulumiazureprovider.Get` (keyless-compatible)
- Entrypoint at `iac/pulumi/main.go` root (`package main`), canonical `Pulumi.yaml` + `Makefile`
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`

**Score:** 13.32% / 13.32%

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five `.tf` files present; `tofu init`/`validate`/`fmt` green; full `planton tofu plan` on the hack manifest: 1 to add, all 4 outputs
- Canonical empty `provider "azurerm" { features {} }` block on `~> 4.0`
- HCL comments at the module-comment bar (dynamic-block absence semantics, DNS-resolver caveat, encryption GA note)

**Score:** 4.24% / 4.24%

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.resource_group` resolved literal | same | ✅ MATCH |
| Resource naming basis | `spec.name` (ARM name) | `spec.name` | ✅ MATCH |
| Labels/tags | metadata-derived merged under user tags (user wins) | identical merge order | ✅ MATCH |
| Spec feature coverage | all 13 spec fields honored | all 13 spec fields honored | ✅ MATCH |
| Enum → ARM mapping | AllowUnencrypted/DropUnencrypted, Basic; unspecified omits | identical mapping via locals | ✅ MATCH |
| Absent-field semantics | encryption/DDoS/policy blocks omitted when unspecified | dynamic blocks empty when unspecified | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 4/4 fields | 4/4 fields | ✅ MATCH |

- `pkg/outputs/conformance_test.go` carries an AzureVirtualNetwork case (incl. repeated `address_spaces`) — passing
- Live dual-engine E2E: `TestAzureVirtualNetwork_Pulumi/minimal` and `_Terraform/minimal` — composed on the fixture RG, all 8 phases, zero orphans

**Effect on score:** none.

### 6. Documentation - Research (11.18% / 13.18%)

⚠️ `docs/README.md` is in the moderate size band. Content quality is complete: full azurerm→spec field map, the two deliberate non-modelings with reasons, the flattened-enum and plain-string-DDoS-id design decisions, operational behavior (DHCP lease pickup, address-space shrink, flow-timeout guidance), and the composition seams.

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` with the network-is-just-the-network philosophy, spec/output tables, quick example, and lifecycle notes.

### 8. Supporting Files (10.09%)

✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all present and accurate.

### 9. Nice to Have (10.00%)

✅ Gazelle-generated BUILD.bazel files present; E2E profile + scenario + prerequisite fixture profile + verifier + test entrypoints wired.

### 10. Presets (5.00%)

✅ Three ranked pairs: `01-standard`, `02-hub-custom-dns`, `03-ddos-protected` — correct apiVersion/kind, `value:` wrapper on the reference field, each teaching the composition seams (subnet/NAT/DNS-link) and the operational trade-offs.

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` reports zero gaps for this kind. Address space, DNS servers, and plan IDs are public configuration, not secret material — nothing to annotate, nothing to exempt.

---

## Next Steps

None required for this component. Its natural companions in the networking wave are the `AzureSubnet` enrichment (route-table attach seam, delegations depth) and `AzureVnetPeering`.

---

**Audit completed at:** 2026-07-03 15:47:33
**Reference:** `architecture/deployment-component.md`
