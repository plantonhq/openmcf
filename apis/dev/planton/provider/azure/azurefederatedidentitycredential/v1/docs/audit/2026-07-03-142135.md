# Audit Report: AzureFederatedIdentityCredential

**Audit Date:** 2026-07-03 14:21:35
**Component Kind:** AzureFederatedIdentityCredential
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurefederatedidentitycredential/v1/`
**Enum Value:** 463
**ID Prefix:** azfic

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.18% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Both gates pass and every critical category is complete.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureFederatedIdentityCredential = 463` in `cloud_resource_kind.proto`
- Value 463 is in the Azure band (400-599), identity sub-band (461-469), contiguous after 461/462
- `id_prefix: "azfic"` is unique across the registry
- `kind_meta` carries provider, version, id_prefix, and `prerequisites: [AzureUserAssignedIdentity]` — the honest direct dependency (the resource group chains transitively through the identity's own prerequisite)

**Score:** 4.44% / 4.44%

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy, lowercase folder name, `v1/` subfolder.

**Score:** 4.44% / 4.44%

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos exist with dense intent-explaining comments (spec.proto 5.4 KB)
- Generated `.pb.go` stubs present and current (`make protos` green)
- `spec_test.go` (6.8 KB): 19 ginkgo cases covering every field, boundary lengths (3/120), URI validation, the optional-audience empty-string rejection, and the KRM constants
- `go test ./apis/dev/planton/provider/azure/azurefederatedidentitycredential/v1/` — **ok**
- Validation quality: `issuer` uses `string.uri`; boundary rules carry plain constraints; no raw regex; the full azurerm v4 surface is modeled (name, parent identity, issuer, subject, audience)
- `audience` is `optional string` with `(default) = "api://AzureADTokenExchange"`, the ARM token-exchange audience

**Score:** 22.20% / 22.20%

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present, richly commented (lifecycle: in-place vs replace; the per-identity write serialization quirk; the audience flattening)
- Provider built via the shared `pulumiazureprovider.Get` (keyless-compatible)
- Entrypoint at `iac/pulumi/main.go` root (`package main`), `Pulumi.yaml` + `Makefile` present, no subdir
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`
- The resource group is derived by parsing the parent identity's ARM ID (documented why)

**Score:** 13.32% / 13.32%

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five `.tf` files present; `tofu init`/`validate`/`fmt` green; full `planton tofu plan` on the hack manifest: 1 to add, all 6 outputs
- Canonical empty `provider "azurerm" { features {} }` block on `~> 4.0`
- Uses the v4-canonical `user_assigned_identity_id` argument (not the deprecated `parent_id`)
- HCL comments at the module-comment bar (trust-match semantics, audience wire shape, RG derivation)

**Score:** 4.24% / 4.24%

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | parent ARM id from `spec.user_assigned_identity` | same | ✅ MATCH |
| Resource naming basis | `spec.name` (ARM name), pulumi resource keyed on `metadata.name` | `spec.name` | ✅ MATCH |
| Labels/tags | none — ARM models the credential untagged | none | ✅ MATCH |
| Spec feature coverage | name, parent, issuer, subject, audience (defaulted) | same | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 6/6 fields | 6/6 fields (audience list flattened via `one()`) | ✅ MATCH |

- Audience default applied identically in both engines (module-level `api://AzureADTokenExchange` fallback mirroring the spec default)
- `pkg/outputs/conformance_test.go` carries an AzureFederatedIdentityCredential case — passing
- Live dual-engine E2E: `TestAzureFederatedIdentityCredential_Pulumi/minimal` PASS (174s) and `_Terraform/minimal` PASS (183s), all 8 phases each, zero orphans

**Effect on score:** none.

### 6. Documentation - Research (11.18% / 13.18%)

⚠️ `docs/README.md` is 6.5 KB (moderate band). Content quality is complete: trust-model anatomy, headline flows (GitHub OIDC, AKS workload identity), full azurerm→spec field map, engine parity notes, and recorded reasons for everything deliberately not modeled (deprecated `resource_group_name`, preview claims-matching expressions, system-assigned parents).

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` (4.8 KB) with decomposition rationale, spec/output tables, subject-format reference, and the three-kind keyless-CI composition.

### 8. Supporting Files (10.09%)

✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all present and accurate.

### 9. Nice to Have (10.00%)

✅ Gazelle-generated BUILD.bazel files present; E2E profile + scenario + verifier + test entrypoints wired.

### 10. Presets (5.00%)

✅ Three ranked pairs: `01-github-actions-oidc`, `02-aks-workload-identity`, `03-external-oidc-issuer` — correct apiVersion/kind, `my-` names, `value:` wrapper on the reference field, no stale fields, each teaching the consuming side (workflow YAML, service-account annotation, OIDC discovery).

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --output json` reports zero `gaps[]` for this kind. The trust
coordinates (issuer/subject/audience) are public configuration, not secret material —
nothing to annotate, nothing to exempt.

---

## Next Steps

None required for this component. Its natural companions are the identity rework (shipped
in the same session) and, later, the AKS cluster's OIDC-issuer output enrichment so the
AKS workload-identity preset can be wired fully by reference.

---

**Audit completed at:** 2026-07-03 14:21:35
**Reference:** `architecture/deployment-component.md`
