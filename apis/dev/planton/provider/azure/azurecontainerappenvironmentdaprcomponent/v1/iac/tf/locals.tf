locals {
  # The Dapr component carries no tags (ARM does not support them on
  # managedEnvironments/daprComponents) and every spec vocabulary is
  # passed through as-is, so no mapping locals exist here.
  placeholder = null
}
