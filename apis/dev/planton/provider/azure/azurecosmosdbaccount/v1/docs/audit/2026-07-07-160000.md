# Audit Report: AzureCosmosdbAccount

**Audit Date:** 2026-07-07 16:00:00
**Component Kind:** AzureCosmosdbAccount
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurecosmosdbaccount/v1/`
**Enum Value:** 432
**ID Prefix:** azcdb

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureCosmosdbAccount = 432` in the Azure band (400–599), databases sub-band
- `id_prefix: "azcdb"` unique across the registry
- `prerequisites: [AzureResourceGroup]` with rationale in the registry comment (account created inside a referenced resource group)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy (`apis/dev/planton/provider/azure/azurecosmosdbaccount/v1/`), lowercase folder name, `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos present and substantial (`spec.proto` 36 KB); generated stubs current
- 10 message-level CEL rules mirror ARM's conditional matrix: geo write-region uniqueness, BoundedStaleness multi-region floors, Mongo/SQL capability kind-pairing, MONGO_DB_V34→ENABLE_MONGO, create_mode↔continuous backup, restore↔RESTORE pairing, backup per-mode field pairings, identity ids-match-type, default-identity pairing
- Every CEL `message` is a complete user-facing sentence; regex patterns explained in plain English (`account_name`, `ip_range_filter`, CORS methods)
- `spec_test.go`: 47 cases (19 valid-path, 28 error-path) — all PASS (verified offline via `go test`)

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `locals.go`, `outputs.go` present; authoring comments explain presence-guarded proto defaults, capability non-injection, local-auth inversion, CMK versionless key, default-identity wire-string composition, restore/create_mode gating — the spec-comment bar
- Root `main.go` `package main`; no `main/` or `entrypoint/` subdir; `Pulumi.yaml` + `Makefile` present
- Release-equivalent build passes (verified offline via bazel pulumi build)
- Provider built via shared `pulumiazureprovider.Get` builder (keyless-auth compliant)

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files present and substantial (`variables.tf` 5 KB, `main.tf` 7.8 KB); canonical empty `provider "azurerm" { features {} }` block on `~> 4.0`
- Full `planton tofu plan` on `iac/hack/manifest.yaml` renders every enum seam: kind, BoundedStaleness dials, capabilities (including non-EnableX wire values), network posture, periodic backup with ZONE redundancy, USER_ASSIGNED identity + composed default identity, CMK key, analytical storage, capacity, CORS, burst/partition-merge (verified offline)

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with the `pkg/outputs` conformance case (18/18 fields populate, zero unmapped) and `planton validate-outputs` (verified offline).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.resource_group` | `var.spec.resource_group` | ✅ MATCH |
| Resource naming basis | `spec.account_name` | `var.spec.account_name` | ✅ MATCH |
| Kind default materialization | unspecified → GlobalDocumentDB | coalesce → GlobalDocumentDB | ✅ MATCH |
| Consistency defaults | Session + 5/100 staleness dials | same | ✅ MATCH |
| Capability mapping | exhaustive enum map, declared-only | same | ✅ MATCH |
| Network posture | VNet rules, IP filter, ACL bypass | same | ✅ MATCH |
| Backup block emission | presence-gated per-mode fields | dynamic on presence | ✅ MATCH |
| Identity + default identity | composed wire strings | same | ✅ MATCH |
| CMK encryption | versionless `key_vault_key_id` | same | ✅ MATCH |
| Local auth inversion | `local_authentication_disabled = !enabled` | same | ✅ MATCH |
| Restore / create_mode | presence-gated | same | ✅ MATCH |
| Offer type | hardcoded "Standard" | hardcoded "Standard" | ✅ MATCH |
| Labels/tags | lowered enum kind string | snake-case literal | 🔵 EXCEPTION (documented in TF `locals.tf`) |
| Stack outputs → proto | 18 exports | 18 outputs, same names | ✅ MATCH |

**Effect on score:** none — the single divergence is the family-wide, output-neutral tag-shape `PARITY-EXCEPTION`, documented in `iac/tf/locals.tf`.

---

### 6–10. Documentation, Supporting Files, Presets

✅ **Passed:**

- `docs/README.md` (8.3 KB): compact design-research convention — full azurerm→spec field map with six recorded skips (`minimal_tls_version`, `managed_hsm_key_id`, `local_authentication_disabled`, `Parse` kind, Cassandra/Gremlin/Table child kinds, dedicated gateway), decomposition verdicts (databases/containers as first-class kinds, default-identity decomposed, capabilities declared-not-injected), operational notes (capability recreate behavior, analytical one-way, backup direction, restore id form, slow provisioning)
- `README.md` (3.1 KB), `catalog-page.md`, both module READMEs, `iac/hack/manifest.yaml` present
- Presets: 3 pairs (`01-sql-api`, `02-mongodb-api`, `03-serverless`), `my-` names, apiVersion/kind correct, StringValueOrRef `valueFrom` wrapper forms, all field names match current `spec.proto`

**Score:** full marks in each category

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --output json` (verified offline): zero `AzureCosmosdbAccount:` gaps. Stack-output keys (account keys and connection strings) are secret-bearing and surfaced through sensitive IaC output paths; `validate-refs` green.

---

## Critical Gaps

None.

## Quick Wins

1. **Mirror `PARITY-EXCEPTION` comment in Pulumi `locals.go`**
   - **Why:** Aligns tag-shape documentation with `azurerediscache` and TF `locals.tf` for audit-checklist completeness (behavior already correct)
   - **How:** Add the same `PARITY-EXCEPTION` block above `AzureTags` in `iac/pulumi/module/locals.go`
   - **File:** `iac/pulumi/module/locals.go`

---

**Audit completed at:** 2026-07-07-160000
**Reference:** `architecture/deployment-component.md`
