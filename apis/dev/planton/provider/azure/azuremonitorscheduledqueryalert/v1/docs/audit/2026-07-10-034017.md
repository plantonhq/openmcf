# Audit Report: AzureMonitorScheduledQueryAlert

**Date:** 2026-07-10-034017
**Component:** AzureMonitorScheduledQueryAlert (enum 455, provider azure, id_prefix `azsqalert`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80
`azurerm_monitor_scheduled_query_rules_alert_v2` surface (the kind name
drops the provider's "v2" artifact -- ARM's type is scheduledQueryRules):
row-count and metric-measurement criteria, dimension splits, failing
periods with the min<=total bound, the auto-mitigation XOR mute contract,
the managed identity, query-time-range override, and action-group FKs.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 455, unique prefix `azsqalert`, `prerequisites: [AzureLogAnalyticsWorkspace, AzureMonitorActionGroup]` |
| 2 | Folder Structure | ✅ `provider/azure/azuremonitorscheduledqueryalert/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 25 spec tests pass (both styles, every vocabulary, the XOR, failing-period bounds, identity paths); messages user-facing |
| 4 | IaC Pulumi | ✅ module + root entrypoint; release-equivalent build passes; richly commented; shared builder |
| 5 | IaC Terraform | ✅ full criteria/identity/action surface; richly commented; canonical empty provider block |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below; one documented bridge-flattening note) |
| 6 | Research Docs | ✅ field map, kind naming, the "Equal" wire spelling, v1-superseded skip, documented apply-time contracts |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders the metric-measurement style, dimensions, failing periods, mute duration, override, identity, custom properties) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 3 pairs (error-spike / latency-threshold / missing-heartbeat), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (KQL queries and durations are not credentials; no gaps) |

## Apply-Time Validator Source-Diff

Read `monitor_scheduled_query_rules_alert_v2_resource.go` Create/Update:

- **auto_mitigation XOR mute duration** -- enforced in BOTH Create and
  Update ("auto mitigation must be disabled when mute action duration is
  set") -- front-loaded as a message CEL.
- **The "Equal" operator spelling** -- the provider fixed "Equals" as a
  breaking bug on this API; the enum maps EQUAL -> "Equal" in both engines
  (the vendor-constant-casing class).
- **evaluation_frequency <= window_duration (and <= mute duration)** --
  Azure validates at apply across two independently-set vocabularies;
  documented on the fields, not CEL'd (a CEL would duplicate a cross-field
  duration comparison ARM already diagnoses clearly).
- **metric_measure_column pairing** (required for non-COUNT, forbidden for
  COUNT) -- documented by the provider, enforced by Azure at apply;
  documented on the field.
- **failing-periods min <= total** -- front-loaded as message CEL (a
  violating rule can never fire).
- No spec CEL invents a rule the provider does not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH -- the provider caps scopes at one; the pulumi bridge
  flattens the one-item list to a scalar and the TF module wraps the same
  value in a one-item list (wire-identical; documented in both modules)
- Naming basis: MATCH (`spec.alert_name`)
- Labels/tags: EXCEPTION (family-wide tag-shape note, documented in BOTH modules)
- Feature coverage: MATCH -- criteria, dimensions, failing periods,
  identity, action, and every dial wired identically; presence-guarded
  defaults (severity 3, PT5M/PT5M) sent explicitly on both engines
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- `pkg/outputs` conformance case added (3 fields
  incl. the identity principal)

**PARITY ✅** (one documented exception: the family-wide tag shape).

## Live Evidence

Live dual-engine E2E GREEN: Pulumi 264s / Terraform 272s (all 8 phases;
zero orphans) -- a row-count rule (inverted heartbeat shape) against the
fixture workspace with actions into the fixture action group, proving the
workspace scope FK, the COUNT/GreaterThan wire maps, query validation on a
schema-present table, and the action FK through the composed chain.

## Critical Gaps

None.
