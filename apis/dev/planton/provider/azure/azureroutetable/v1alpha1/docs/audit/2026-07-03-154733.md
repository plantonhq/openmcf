# Audit Report: AzureRouteTable

**Audit Date:** 2026-07-03 15:47:33
**Component Kind:** AzureRouteTable
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azureroutetable/v1alpha1/`
**Enum Value:** 418
**ID Prefix:** azrt

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.18% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Both gates pass and every critical category is complete.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureRouteTable = 418` in `cloud_resource_kind.proto` — first slot of the free networking sub-band (418-429)
- `id_prefix: "azrt"` is unique across the registry
- `kind_meta` carries `prerequisites: [AzureResourceGroup]` — the honest direct dependency

**Score:** 4.44% / 4.44%

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy, lowercase folder name, `v1/` subfolder.

**Score:** 4.44% / 4.44%

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos exist with dense intent-explaining comments (routes teach prefix precedence and forced-tunneling semantics)
- Generated `.pb.go` stubs present and current (`make protos` green)
- `spec_test.go`: 21 ginkgo cases covering every field, all five next-hop shapes, the appliance-IP pairing in both error directions (missing when required; present when forbidden), service-tag prefixes, and the KRM constants
- `go test ./apis/dev/planton/provider/azure/azureroutetable/v1alpha1/` — **ok**
- The full `azurerm_route_table` v4.80 surface is modeled: repeated `routes` (name, `address_prefix` accepting CIDR or service tag, `next_hop_type` closed enum with `defined_only`, `next_hop_in_ip_address` with message-level CEL enforcing the VirtualAppliance pairing), `bgp_route_propagation_enabled` (`optional bool` default "true" mirroring azurerm), user `tags`
- The computed `subnets` attribute is deliberately not modeled — membership is subnet-side derived state (recorded in docs)

**Score:** 22.20% / 22.20%

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present, richly commented (inline-route ownership semantics — the always-send-the-list rule; the network-wide blast radius of route edits; the subnet-side attach model)
- Provider built via the shared `pulumiazureprovider.Get` (keyless-compatible)
- Entrypoint at `iac/pulumi/main.go` root (`package main`), canonical `Pulumi.yaml` + `Makefile`
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`

**Score:** 13.32% / 13.32%

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five `.tf` files present; `tofu init`/`validate`/`fmt` green; full `planton tofu plan` on the hack manifest: 1 to add, both outputs
- Canonical empty `provider "azurerm" { features {} }` block on `~> 4.0`
- Routes expressed as an attribute list comprehension (an explicitly empty list removes the last route rather than orphaning it) with the enum→ARM map in locals

**Score:** 4.24% / 4.24%

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.resource_group` resolved literal | same | ✅ MATCH |
| Resource naming basis | `spec.name` (ARM name) | `spec.name` | ✅ MATCH |
| Labels/tags | metadata-derived merged under user tags (user wins) | identical merge order | ✅ MATCH |
| Spec feature coverage | all 6 spec fields honored | all 6 spec fields honored | ✅ MATCH |
| Enum → ARM mapping | shared `nextHopTypeToArm` map | identical `next_hop_type_to_arm` local | ✅ MATCH |
| BGP default | true applied when unset | `optional(bool, true)` | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 2/2 fields | 2/2 fields | ✅ MATCH |

- `pkg/outputs/conformance_test.go` carries an AzureRouteTable case — passing
- Live dual-engine E2E: `TestAzureRouteTable_Pulumi/minimal` and `_Terraform/minimal` — two-route table (NONE black-hole + service-tag INTERNET) with BGP propagation disabled, composed on the fixture RG, all 8 phases, zero orphans

**Effect on score:** none.

### 6. Documentation - Research (11.18% / 13.18%)

⚠️ `docs/README.md` is in the moderate size band. Content quality is complete: full azurerm→spec field map, both decomposition decisions (routes fold; attach is subnet-side) argued against the split tests, route-precedence and default-outbound behavior, and the composition seam the subnet enrichment will consume.

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` with the one-policy-many-subnets rationale, spec/output tables, quick example, and lifecycle notes.

### 8. Supporting Files (10.09%)

✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all present and accurate.

### 9. Nice to Have (10.00%)

✅ Gazelle-generated BUILD.bazel files present; E2E profile + scenario + verifier + test entrypoints wired.

### 10. Presets (5.00%)

✅ Three ranked pairs: `01-firewall-egress`, `02-forced-tunnel`, `03-blackhole-guardrails` — correct apiVersion/kind, `value:` wrapper on the reference field, each teaching a real routing pattern and its hardening trade-offs.

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` reports zero gaps for this kind. Route prefixes and appliance IPs are network configuration, not secret material — nothing to annotate, nothing to exempt.

---

## Next Steps

None required for this component. The `AzureSubnet` enrichment session adds the subnet-side `route_table_id` reference this kind's output feeds.

---

**Audit completed at:** 2026-07-03 15:47:33
**Reference:** `architecture/deployment-component.md`
