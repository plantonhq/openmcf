apiVersion: azure.planton.dev/v1
kind: AzureStorageTable
metadata:
  name: my-app-entities
spec:
  storageAccountId:
    valueFrom:
      kind: AzureStorageAccount
      name: <storage-account-resource-name>
      fieldPath: status.outputs.storage_account_id
  # 3-63 alphanumerics starting with a letter -- no hyphens, and never
  # the literal word "table". PascalCase is the Table-storage
  # convention.
  tableName: <TableName>
