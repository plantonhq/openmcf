# Audit Report: AzurePrivateDnsZoneVirtualNetworkLink

**Audit Date:** 2026-07-03 15:47:33
**Component Kind:** AzurePrivateDnsZoneVirtualNetworkLink
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azureprivatednszonevirtualnetworklink/v1alpha1/`
**Enum Value:** 419
**ID Prefix:** azpdnslink

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.18% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Both gates pass and every critical category is complete.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzurePrivateDnsZoneVirtualNetworkLink = 419` in `cloud_resource_kind.proto` (networking sub-band, contiguous after 418)
- `id_prefix: "azpdnslink"` is unique across the registry
- `kind_meta` carries `prerequisites: [AzurePrivateDnsZone, AzureVirtualNetwork]` — both honest direct dependencies (the resource group chains transitively through each)

**Score:** 4.44% / 4.44%

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy, lowercase folder name, `v1/` subfolder.

**Score:** 4.44% / 4.44%

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos exist with dense intent-explaining comments (the link-adds-one-network-to-the-zone's-audience model; the one-registration-link-per-network rule)
- Generated `.pb.go` stubs present and current (`make protos` green)
- `spec_test.go`: 16 ginkgo cases covering every field, name-format boundaries, both-parents requiredness, references for both parents, and the KRM constants
- `go test ./apis/dev/planton/provider/azure/azureprivatednszonevirtualnetworklink/v1alpha1/` — **ok**
- The full `azurerm_private_dns_zone_virtual_network_link` v4.80 surface is modeled: `name`, `private_dns_zone_id` (the parent reference — see the design decision), `virtual_network_id`, `registration_enabled` (`optional bool` default "false"), `resolution_policy` (closed enum, unspecified = Azure's per-zone-type default), user `tags`
- Design decision recorded: azurerm's name+RG contract is its own marked-legacy shape; the spec models the parent by ARM ID and both modules derive the zone name and resource group from it — no redundant contradictable state

**Score:** 22.20% / 22.20%

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present, richly commented (lifecycle; the single-registration-link constraint; the parse-don't-restate rationale)
- Provider built via the shared `pulumiazureprovider.Get` (keyless-compatible)
- Entrypoint at `iac/pulumi/main.go` root (`package main`), canonical `Pulumi.yaml` + `Makefile`
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`

**Score:** 13.32% / 13.32%

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five `.tf` files present; `tofu init`/`validate`/`fmt` green; full `planton tofu plan` on the hack manifest: 1 to add, both outputs
- Canonical empty `provider "azurerm" { features {} }` block on `~> 4.0`
- The zone's RG and name derived via a named-group `regex()` in locals that fails the plan loudly on a malformed ID

**Score:** 4.24% / 4.24%

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | zone RG parsed from `spec.private_dns_zone_id` | same (regex in locals) | ✅ MATCH |
| Resource naming basis | `spec.name` (ARM name under the zone) | `spec.name` | ✅ MATCH |
| Labels/tags | metadata-derived merged under user tags (user wins) | identical merge order | ✅ MATCH |
| Spec feature coverage | all 6 spec fields honored | all 6 spec fields honored | ✅ MATCH |
| Enum → ARM mapping | Default/NxDomainRedirect; unspecified omits | identical mapping via locals | ✅ MATCH |
| Registration default | false applied when unset | `optional(bool, false)` | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 2/2 fields | 2/2 fields | ✅ MATCH |

- `pkg/outputs/conformance_test.go` carries an AzurePrivateDnsZoneVirtualNetworkLink case — passing
- Live dual-engine E2E: `TestAzurePrivateDnsZoneVirtualNetworkLink_Pulumi/minimal` and `_Terraform/minimal` — the two-parent composed chain (fixture RG → fixture zone + fixture network → link), exercising multi-prerequisite registry-driven resolution and the ARM-id parse on both engines, all 8 phases, zero orphans

**Effect on score:** none.

### 6. Documentation - Research (11.18% / 13.18%)

⚠️ `docs/README.md` is in the moderate size band. Content quality is complete: full azurerm→spec field map, the parent-reference design decision argued from ARM's child-resource model and the catalog's parent-child precedent, the resolution-policy platform-default behavior, and operational notes (propagation window, replacement blast radius).

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` with the many-per-zone decomposition rationale, spec/output tables, quick example, and the three-kind composition (network + zone + link).

### 8. Supporting Files (10.09%)

✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all present and accurate.

### 9. Nice to Have (10.00%)

✅ Gazelle-generated BUILD.bazel files present; E2E profile + scenario + verifier + test entrypoints wired.

### 10. Presets (5.00%)

✅ Three ranked pairs: `01-privatelink-zone-link`, `02-internal-zone-autoregistration`, `03-public-fallback` — correct apiVersion/kind, `value:` wrapper on the reference fields, each teaching one of the link's three real usage modes.

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` reports zero gaps for this kind. Zone and network IDs are public configuration, not secret material — nothing to annotate, nothing to exempt.

---

## Next Steps

None required for this component. Its zone-side parent (`AzurePrivateDnsZone`) was reworked to zone-only in the same session; downstream, `AzurePrivateEndpoint` remains the kind that populates privatelink zones this link makes resolvable.

---

**Audit completed at:** 2026-07-03 15:47:33
**Reference:** `architecture/deployment-component.md`
