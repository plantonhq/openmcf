# Audit Report: AzureServiceBusAuthorizationRule

**Date:** 2026-07-10-085450
**Component:** AzureServiceBusAuthorizationRule (enum 475, provider azure, id_prefix `azsbauth`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged as ONE polymorphic kind over azurerm v4.80's THREE
byte-identical SAS-rule resources (namespace/queue/topic scoped -- one
shared schema builder and one shared CustomizeDiff in the provider
source). The exactly-one-scope XOR dispatches to the right resource on
both engines; the rights contract (at-least-one; manage ⇒ listen+send)
is front-loaded as CELs; the six credential faces are sensitive
outputs, and `authorization_rule_id` is the zero-translation seam the
geo-DR pairing consumes.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 475, prefix `azsbauth`; NO registry prerequisite (scenario-local fixtures) |
| 2 | Folder Structure | ✅ `provider/azure/azureservicebusauthorizationrule/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 18 spec tests pass (every scope-count error path, every rights-combination error path, the reserved root-name rejection); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; three-way dispatch switch with shared export block; release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; count-gated resources with PER-ATTRIBUTE output coalescing (whole-object coalescing would sensitivity-taint the id/name outputs -- caught by the offline plan, fixed); richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ the one-kind-three-types verdict with its grounds; operational notes (rename-regenerates-keys, DR-pairing replication waits, keyless namespaces) |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (queue-scope dispatch + the full manage trio) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 2 pairs (queue-sender / namespace-operator), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- all six credential faces annotated sensitive; `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `internal.go` (`authorizationRuleSchemaFrom` +
`authorizationRuleCustomizeDiff`) + the three resource files:

- **At-least-one-right** (CustomizeDiff) -- front-loaded as a message CEL.
- **manage ⇒ listen ∧ send** (CustomizeDiff) -- front-loaded as a message CEL.
- **`waitForPairedNamespaceReplication`** after create/delete on
  geo-DR-paired Premium namespaces -- provider-internal; documented in
  the research doc.
- No spec CEL invents a rule the provider does not enforce (the reserved
  `RootManageSharedAccessKey` rejection mirrors ARM's reservation of the
  built-in rule; its keys already surface as namespace outputs).

## Parity Gate Walk (5b)

- Scope source: MATCH (the same XOR discriminator picks the same
  resource type on both engines for an identical stack input)
- Naming basis: MATCH (`rule_name` both engines)
- Labels/tags: N/A (no ARM tags on authorization rules)
- Feature coverage: MATCH -- rights trio wired identically; all three
  dispatch paths implemented on both engines
- Dependency ordering: MATCH (single resource per deploy)
- Stack outputs: MATCH -- 8 outputs byte-identical across all three
  dispatch paths on both engines (alias faces fall back to "" on both
  when no DR pairing exists); `pkg/outputs` conformance case added

**PARITY ✅** (zero exceptions).

## Recorded Skips

- None -- the surface is complete.

## Critical Gaps

None.
