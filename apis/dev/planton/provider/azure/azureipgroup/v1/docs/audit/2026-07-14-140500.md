# Audit Report: AzureIpGroup

**Date:** 2026-07-14-140500
**Component:** AzureIpGroup (enum 533, provider azure, id_prefix `azipg`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_ip_group` INPUT surface: name,
location, resource group, cidrs, tags. The IP Group is a passive,
reusable address set; consumption is declared rule-side (firewall policy
rule collection groups' source/destination ip-group fields and the
policy's IDPS traffic bypasses are `StringValueOrRef` FKs onto this
kind's `ip_group_id`). The kind exists exactly so rule graphs never
copy-paste address lists.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 533 (network-security sub-band 530-539), id_prefix `azipg` unique, `prerequisites: [AzureResourceGroup]` |
| 2 | Folder Structure | ✅ `provider/azure/azureipgroup/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 14 spec tests pass (name format happy + error paths, mixed cidr shapes, empty group) |
| 4 | IaC Pulumi | ✅ shared builder (`pulumiazureprovider.Get`); builds (targeted + release-equivalent); richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block, azurerm ~> 4.0; `tofu validate` green; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ field mapping, recorded skips, seams, provider locking behavior |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi/tf READMEs, hack manifest (`planton tofu plan` renders full surface) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle; Bazel tree builds |
| 10 | Presets | ✅ 2 pairs (branch-offices / on-prem-datacenter), validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- no secret-bearing fields |

## Apply-Time Validator Source-Diff

Read `internal/services/network/ip_group_resource.go` (schema +
create/update/delete) in full:

- Schema: `name` Required/ForceNew with NO validator; `cidrs` an optional
  set with `StringIsNotEmpty` elements; no CustomizeDiff. The spec
  mirrors the element non-emptiness and front-loads ARM's
  Microsoft.Network name contract (1-80, start alnum, end
  alnum/underscore) as the `ip_group_name_format` CEL.
- Create/update/delete take named locks on every firewall/policy that
  references the group -- behavioral only, documented in the research
  docs, no spec surface.
- No spec CEL invents a rule the provider and ARM do not enforce.

## Recorded Skips

- `firewall_ids` / `firewall_policy_ids` computed reverse indexes: not
  modeled (composition flows rule→group; exporting them would churn
  outputs on unrelated attachments).
- `azurerm_ip_group_cidr`: a Terraform-ergonomics per-entry resource with
  no independent Azure lifecycle; the `cidrs` list is the honest surface.

## Parity Gate Walk (5b)

- Scope source: MATCH (resource_group resolved value both engines)
- Naming basis: MATCH (`spec.name` both engines)
- Labels/tags: family-wide tag-shape PARITY-EXCEPTION documented in BOTH modules (output-neutral)
- Feature coverage: MATCH (all five arguments byte-for-byte; cidrs passed verbatim both engines)
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- `ip_group_id` / `ip_group_name`; `pkg/outputs` conformance case added and green
