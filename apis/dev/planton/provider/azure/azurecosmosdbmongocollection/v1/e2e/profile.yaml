apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurecosmosdbmongocollection
spec:
  # The harness deploys the fixture resource group, a scenario-local
  # Mongo account and database (declared via extra-prerequisites in
  # deploy order), creates the collection with a shard key and the _id
  # unique index Azure requires on every Mongo collection, verifies it
  # via the ARM API, destroys it, and verifies it is gone -- on both
  # provisioners. The scenario-local account pins westus3 (eastus
  # rejects new Cosmos accounts under zonal-capacity pressure -- see
  # e2e/README.md); the full chain runs ~15 min per engine.
  tier: 1
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 90
