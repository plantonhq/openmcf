# Audit Report: AzureCosmosdbMongoCollection

**Audit Date:** 2026-07-07 16:00:00
**Component Kind:** AzureCosmosdbMongoCollection
**Provider:** azure
**Component Path:** `apis/dev/planton/provider/azure/azurecosmosdbmongocollection/v1alpha1/`
**Enum Value:** 503
**ID Prefix:** azcmgocl

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureCosmosdbMongoCollection = 503` in the Azure band (400-599)
- `id_prefix: "azcmgocl"` unique across the registry

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy, lowercase folder, `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos present; generated stubs current
- Message-level CELs: throughput XOR autoscale, shard key semantics, TTL constraints, required `_id` unique index — every CEL message is a complete user-facing sentence
- `spec_test.go`: 12 cases — all PASS via `go test`

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `locals.go`, `outputs.go`, `parse.go` present; authoring comments explain parent-ID parsing, shard-key presence guards, index blocks, and no-tags posture
- Root `main.go` `package main`; no subdir entrypoints; `Pulumi.yaml` + `Makefile` present
- Release-equivalent build passes; provider built via shared `pulumiazureprovider.Get`

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files present and substantial; canonical empty `provider "azurerm" { features {} }` block
- `planton tofu plan` on hack manifest exercises throughput, shard key, indexes, and TTL seams (user-verified)

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified with `planton validate-outputs` (4/4 proto fields, zero unmapped) and `TestStackOutputsConformance/AzureCosmosdbMongoCollection`.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.mongo_database_id` | `var.spec.mongo_database_id` | ✅ MATCH |
| Parent parsing | `parseMongoDatabaseId` | anchored regexes in `locals.tf` | ✅ MATCH |
| Resource naming basis | `spec.collection_name` | `var.spec.collection_name` | ✅ MATCH |
| Shard key | presence-guarded | `null` when empty | ✅ MATCH |
| Throughput / autoscale | presence-guarded XOR | dynamic autoscale block | ✅ MATCH |
| TTL fields | presence-guarded | optional passthrough | ✅ MATCH |
| Indexes | `Indices` array | dynamic `index` blocks | ✅ MATCH |
| Stack outputs → proto | 4 exports | 4 outputs, same names | ✅ MATCH |

**Effect on score:** none — zero undocumented divergences.

---

### 6-10. Documentation, Supporting Files, Presets

✅ **Passed:**

- `docs/README.md`: compact design-research convention — full azurerm→spec field map, recorded skips (mongo role/user kinds deferred, system_indexes computed-only), operational notes (shard key immutability, unsharded one-partition tradeoff)
- `README.md`, `catalog-page.md`, both module READMEs, `iac/hack/manifest.yaml` present
- Presets: 1 pair (`01-tenant-sharded`); `my-tenant-events` name with required `my-` prefix, apiVersion/kind correct, StringValueOrRef wrapper forms
- E2E: `e2e/scenarios/mongo-chain.yaml` + account/database fixtures

**Score:** full marks in each category

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`go run . secret-coverage --output json`: zero `AzureCosmosdbMongoCollection:` gaps. No secret-bearing spec fields on this kind.

---

## Critical Gaps

None.

## Quick Wins

None outstanding.

---

## Post-Review Addendum (2026-07-07, same session)

A deep review against the azurerm v4.80 source found one apply-blocking
defect the initial audit missed; it was fixed and the offline gate
re-run (spec tests, Bazel Pulumi build, full `planton tofu plan` on the
hack manifest):

1. **The `_id` index requirement is now spec-enforced and present in
   every artifact**: azurerm unconditionally rejects a Mongo collection
   whose index list lacks the `_id` key ("index with '_id' key is
   required" -- create AND update paths, case-insensitive). The E2E
   scenario (`mongo-chain.yaml`) and the tenant-sharded preset both
   omitted it, so neither could ever deploy. Both now declare the
   `_id` unique index, and a message-level CEL guard
   (`cosmosdb_mongo_coll_id_index_required`, matching case-insensitively
   like Azure) makes the class of error impossible to ship again.
   Docs/comments that scoped the requirement to "Mongo 3.6+ accounts"
   were corrected -- the provider enforces it unconditionally.

Spec tests also gained floor/ceiling coverage (throughput 400 floor,
autoscale 1000 floor, 255-character name ceiling, analytical TTL below
-1) in passing.

The score and both gates remain 100% / PARITY ✅ / COVERAGE ✅ with these
fixes applied and re-verified.

---

**Audit completed at:** 2026-07-07-160000 (addendum same day)
**Reference:** `architecture/deployment-component.md`
