# Audit Report: AzureRoleAssignment

**Audit Date:** 2026-07-03 11:36:47
**Component Kind:** AzureRoleAssignment
**Provider:** azure
**Enum Value:** 461
**Component Path:** `apis/dev/planton/provider/azure/azureroleassignment/v1alpha1/`
**Overall Score:** 98% — Fully Complete
**Parity Gate (5b):** PARITY ✅
**Sensitive-Coverage Gate (11):** COVERAGE ✅

---

## Summary

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules — Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules — Terraform | 4.24% | 4.24% | ✅ |
| Documentation — Research | 13.18% | 12.18% | ✅ |
| Documentation — User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 9.09% | ⚠️ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have (BUILD files) | 10.00% | 10.00% | ✅ |

---

## Detailed Findings

### 1. Cloud Resource Registry ✅

- Enum entry `AzureRoleAssignment = 461` exists, inside the Azure band (400–599) in the identity sub-band (461–469)
- `id_prefix: "azra"` is unique across the registry
- `kind_meta` carries provider, version, id_prefix, and `prerequisites: [AzureResourceGroup, AzureUserAssignedIdentity]` (honest deploy-ordering metadata: an assignment grants at a referenced scope to a referenced principal)

### 2. Folder Structure ✅

- `apis/dev/planton/provider/azure/azureroleassignment/v1alpha1/` with the standard anatomy

### 3. Protobuf API Definitions ✅

- All four protos present and substantial (`spec.proto` 10.8KB with dense intent-explaining comments; `api.proto`, `stack_input.proto`, `stack_outputs.proto` complete)
- Generated stubs present (`*.pb.go`)
- Validation quality: both message-level CEL rules carry complete, empathetic user-facing messages (role exactly-one-of; condition_version-requires-condition); `condition_version` constrained to `1.0`/`2.0`; `name` validated as UUID when set
- `spec_test.go`: 21 cases — a case per rule and error path — all passing via `go test ./apis/dev/planton/provider/azure/azureroleassignment/v1alpha1/`

### 4. IaC Modules — Pulumi ✅

- `module/main.go`, `module/locals.go`, `module/outputs.go` present, commented to the module-comment bar (why/trade-offs/provider quirks: immutability lifecycle, empty-vs-unset optional semantics, condition-version defaulting, principal-type inference)
- Provider built through the shared `pulumiazureprovider.Get` builder (static / keyless web-identity / ambient dispatch) — never inline `NewProvider`
- Entrypoint `iac/pulumi/main.go` is `package main` at the root; `Pulumi.yaml` and `Makefile` present; no `main/` or `entrypoint/` subdir
- Release-equivalent build passes: `go build -o /dev/null ./apis/dev/planton/provider/azure/azureroleassignment/v1alpha1/iac/pulumi`

### 5. IaC Modules — Terraform ✅

- All five files present; `azurerm ~> 4.0` with the canonical empty `provider "azurerm" { features {} }` block (runtime ARM_* env injection; keyless-compatible)
- HCL commented to the same bar as the Pulumi module
- `tofu init` + `tofu validate` + `tofu fmt -check` clean; full `tofu plan` against `iac/hack/manifest.yaml` produced the expected single-resource create plan

### 5b. Cross-Engine Parity Gate — PARITY ✅

| Dimension | Verdict |
|-----------|---------|
| Scope source | MATCH — both read resolved `spec.scope` |
| Resource naming basis | MATCH — the ARM assignment name is `spec.name` (or Azure-generated GUID) on both sides; engine-internal resource names have no cloud effect |
| Labels/tags | MATCH — N/A by design: `Microsoft.Authorization` resources do not support ARM tags (documented in both modules) |
| Spec feature coverage | MATCH — all 11 spec fields honored by both engines; empty optionals uniformly mean "unset" |
| Dependency ordering | MATCH — single resource, no edges |
| Stack outputs | MATCH — identical six keys; verified by the `AzureRoleAssignment` case in `pkg/outputs/conformance_test.go` (green) and `planton validate-outputs` schema check (generic reflection path, no override) |

No `PARITY-EXCEPTION` needed anywhere.

### 6. Documentation — Research ✅ (12.18/13.18)

- `docs/README.md` 8.6KB (moderate-to-comprehensive): authorization model, ABAC nuances, cross-tenant delegation, provider field-surface table (no skipped fields), design decisions with rationale, operational notes, verification story
- Marginally below the 10KB "comprehensive" size band; content coverage is complete for a single-resource kind

### 7. Documentation — User-Facing ✅

- `README.md` 5.1KB; `catalog-page.md` 7.2KB with quick start, full configuration reference, three worked examples, outputs, related components; site catalog regenerated (`yarn copy-docs && yarn generate-structure`)

### 8. Supporting Files ⚠️ (9.09/10.09)

- `iac/pulumi/README.md` ✅, `iac/tf/README.md` ✅, `iac/hack/manifest.yaml` ✅
- `iac/pulumi/debug.sh` ✗ — noted: no component in the catalog ships this file (repo-wide grain)

### 9. Nice to Have ✅

- Gazelle-generated `BUILD.bazel` files present for the component and both Pulumi packages

### 10. Presets ✅

- Three ranked pairs: `01-identity-resource-group-grant`, `02-abac-conditioned-grant`, `03-custom-role-subscription-grant`
- All YAML: correct `apiVersion`/`kind`, `my-` names, `value:` wrapper form for `StringValueOrRef`, no `status`, all fields exist in `spec.proto`; companion `.md` files carry when-to-use, key choices, and placeholder tables

### 11. Sensitive-Coverage Gate — COVERAGE ✅

- `go run . secret-coverage --check` green; zero `gaps[]` entries for `AzureRoleAssignment` (the kind carries no secret-bearing fields, and no name-lookalike needed an exemption)

---

## Critical Gaps

None.

## Recommendations

- The `iac/pulumi/debug.sh` helper-file check fails for every component in the catalog; align the audit checklist with the actual repo grain rather than adding the file per component.

---

**Audit completed at:** 2026-07-03-113647
**Reference:** See `architecture/deployment-component.md` for ideal state definition
