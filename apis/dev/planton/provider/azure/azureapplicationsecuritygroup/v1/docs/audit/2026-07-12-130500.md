# Audit Report: AzureApplicationSecurityGroup

**Date:** 2026-07-12-130500
**Component:** AzureApplicationSecurityGroup (enum 428, provider azure, id_prefix `azasg`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_application_security_group`
surface -- which is deliberately tiny: name, location, resource group,
tags. The ASG is an empty, named composition anchor; membership is
declared member-side (NIC / VMSS ip-configuration / private endpoint
associations) and NSG rules target it by id. The value of the kind is the
FK seam it opens: the NSG rule source/destination ASG ids, the NIC ASG
ids, and the VMSS ip-configuration ASG ids all converted from plain
strings to `StringValueOrRef` with `default_kind` in the same change.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 428 (networking sub-band), id_prefix `azasg` unique, `prerequisites: [AzureResourceGroup]` with comment |
| 2 | Folder Structure | ✅ `provider/azure/azureapplicationsecuritygroup/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; spec tests pass (name format happy + error paths); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder (`pulumiazureprovider.Get`); builds; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ member-side membership model, ForceNew-everything-but-tags |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (`tofu plan` renders) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 2 pairs (workload-tier / tagged-governance), validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- no secret-bearing fields; `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `application_security_group_resource.go` (schema + create/update):

- The provider's schema has NO ValidateFunc on the name and NO
  CustomizeDiff; ARM enforces the 1-80 start/end character contract. The
  spec front-loads ARM's documented name rules as the `asg_name_format`
  CEL (same contract as azurerm's PrivateLinkName class used across
  Microsoft.Network).
- Everything except tags is ForceNew -- documented on the spec and both
  modules.
- No spec CEL invents a rule the provider and ARM do not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH (resource_group resolved value both engines)
- Naming basis: MATCH (`spec.name` both engines)
- Labels/tags: family-wide tag-shape PARITY-EXCEPTION documented in BOTH
  modules (output-neutral)
- Feature coverage: MATCH (all four arguments byte-for-byte)
- Stack outputs: MATCH -- `application_security_group_id` / `_name`;
  `pkg/outputs` conformance case added
- Retrofit call sites: NSG rules ×2 + NIC + VMSS ×3 resolve via
  `GetValue()` on Pulumi; Terraform unchanged (the tfvars converter
  flattens references before the module runs) -- behaviorally identical

**PARITY ✅** (tag-shape exception only, two-sided).

## Recorded Skips

- None -- the azurerm surface is fully covered.

## Critical Gaps

None.

## Live E2E

Dual-engine green (Pulumi 130s / Terraform 147s, all 8 phases, zero
orphans): create → ARM verify → destroy → verify-gone.
