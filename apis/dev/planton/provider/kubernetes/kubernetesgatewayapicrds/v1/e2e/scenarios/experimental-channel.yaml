# Experimental channel: every standard resource (with experimental fields)
# plus the x- experimental resources. Proves the channel arm end to end.
apiVersion: kubernetes.planton.dev/v1
kind: KubernetesGatewayApiCrds
metadata:
  name: e2e-gwapi-crds-exp
spec:
  version: v1.6.1
  install_channel:
    channel: experimental
