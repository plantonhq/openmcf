syntax = "proto3";

package dev.planton.provider.kubernetes.kubernetesflinkdeployment.v1alpha1;

import "buf/validate/validate.proto";
import "dev/planton/provider/kubernetes/kubernetesflinkdeployment/v1alpha1/spec.proto";
import "dev/planton/provider/kubernetes/kubernetesflinkdeployment/v1alpha1/stack_outputs.proto";
import "dev/planton/shared/metadata.proto";

//flink-deployment-kubernetes
message KubernetesFlinkDeployment {
  //api-version
  string api_version = 1 [(buf.validate.field).string.const = 'kubernetes.planton.dev/v1alpha1'];

  //resource-kind
  string kind = 2 [(buf.validate.field).string.const = 'KubernetesFlinkDeployment'];

  //metadata
  dev.planton.shared.CloudResourceMetadata metadata = 3 [(buf.validate.field).required = true];

  //spec
  KubernetesFlinkDeploymentSpec spec = 4 [(buf.validate.field).required = true];

  //status
  KubernetesFlinkDeploymentStatus status = 5;
}

//flink-deployment-kubernetes status.
message KubernetesFlinkDeploymentStatus {
  //stack-outputs
  KubernetesFlinkDeploymentStackOutputs outputs = 1;
}
