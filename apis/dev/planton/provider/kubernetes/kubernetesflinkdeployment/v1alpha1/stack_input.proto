syntax = "proto3";

package dev.planton.provider.kubernetes.kubernetesflinkdeployment.v1alpha1;

import "dev/planton/provider/kubernetes/kubernetesflinkdeployment/v1alpha1/api.proto";
import "dev/planton/provider/kubernetes/provider.proto";

//flink-deployment-kubernetes stack-input
message KubernetesFlinkDeploymentStackInput {
  //target cloud-resource
  KubernetesFlinkDeployment target = 1;
  //provider-config
  dev.planton.provider.kubernetes.KubernetesProviderConfig provider_config = 2;
}
