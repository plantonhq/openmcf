apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesaltinityoperator
spec:
  tier: 4
  # Rebuilt against chart 0.27.2 and offline-validated (spec tests,
  # both engines' plan/preview proofs); the live lanes have not run
  # against the rebuilt spec yet.
  status: pending_proof
  timeout_minutes: 20
