apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesgharunnerscalesetcontroller
spec:
  tier: 4
  # Rebuilt on the official gha-runner-scale-set-controller OCI chart
  # 0.14.2 (was 0.13.1). Offline-validated (spec tests, both engines'
  # plan/preview proofs) and live-proven on the kind lanes: the
  # controller rollout (both replicas on the full-surface lane) and the
  # actions.github.com CRDs established. Destroy KEEPS the CRDs — they
  # ride the chart's crds/ directory (install-once, never removed by
  # Helm; verified live — the pre-proof claim that they delete with the
  # release was wrong), the same designed keep the monitoring stack's
  # CRDs prove; destroy-asserted.
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 15
