apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesgharunnerscalesetcontroller
spec:
  tier: 4
  # Rebuilt on the official gha-runner-scale-set-controller OCI chart
  # 0.14.2 (was 0.13.1) and offline-validated (spec tests, both engines'
  # plan/preview proofs); the live lanes have not run yet — the
  # pre-rebuild green claim covered the old surface and was honestly
  # reset. The lanes prove the controller rollout (both replicas on the
  # full-surface lane) and the actions.github.com CRDs established;
  # destroy removes everything INCLUDING the CRDs (the chart-owned CRD
  # posture).
  status: pending_proof
  timeout_minutes: 15
