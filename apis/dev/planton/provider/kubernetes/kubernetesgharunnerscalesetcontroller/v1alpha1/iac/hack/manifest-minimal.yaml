# Minimal-shape hack manifest: namespace only (the chart defaults) —
# the plan proof must survive every optional block ABSENT.
apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesGhaRunnerScaleSetController
metadata:
  name: arc-min
spec:
  namespace:
    value: arc-min
  createNamespace: true
