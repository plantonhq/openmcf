apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesseaweedfs
spec:
  tier: 2
  # Forged as the catalog's in-cluster S3 flagship (the engine ruling that
  # replaced MinIO) against chart 4.40.0 and offline-validated (spec tests,
  # both engines' plan/preview proofs across full and minimal shapes); the
  # live lanes have not run yet.
  status: pending_proof
  timeout_minutes: 30
