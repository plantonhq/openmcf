name: kubernetes-valkey
runtime: go
description: Valkey installation from the official Helm chart with typed standalone/replication topology, module-materialized ACL credentials, and TLS via cert-manager Secrets
