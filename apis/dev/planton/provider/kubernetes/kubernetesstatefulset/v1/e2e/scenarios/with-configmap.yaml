# Composed configuration: the ConfigMap is a first-class KubernetesConfigMap
# fixture (declared via the e2e-prerequisites annotation) and the workload
# mounts it by name — configuration composes across kinds instead of being
# inlined into the workload spec.
apiVersion: kubernetes.planton.dev/v1
kind: KubernetesStatefulSet
metadata:
  name: e2e-sts-cm
  annotations:
    planton.dev/e2e-prerequisites: "KubernetesConfigMap"
spec:
  namespace:
    value: default
  container:
    app:
      image:
        repo: nginx
        tag: "1.27-alpine"
      resources:
        requests:
          cpu: "10m"
          memory: "32Mi"
        limits:
          cpu: "100m"
          memory: "64Mi"
      ports:
        - name: http
          container_port: 80
          network_protocol: TCP
          app_protocol: http
          service_port: 80
      volume_mounts:
        - name: app-config
          mount_path: /etc/app-config
          config_map:
            name: e2e-cfg-minimal
