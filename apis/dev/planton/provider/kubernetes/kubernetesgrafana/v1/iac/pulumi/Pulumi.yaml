name: kubernetes-grafana
runtime: go
description: Grafana installation from the official Helm chart with chart-owned admin credentials, secret-safe datasource and dashboard provisioning, an external-database HA arm, and cluster-wide dashboard-ConfigMap discovery
