apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesrabbitmqoperator
spec:
  tier: 4
  # Forged against the v2.22.3 release manifest and offline-validated
  # (spec tests, both engines' plan/preview proofs across full and
  # minimal shapes); the live lanes have not run yet.
  status: pending_proof
  timeout_minutes: 20
