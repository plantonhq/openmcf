# Minimal-shape hack manifest: the existing-Secret arm (NO materialized
# Secret — count 0) with every optional block ABSENT.
apiVersion: kubernetes.planton.dev/v1
kind: KubernetesGhaRunnerScaleSet
metadata:
  name: repo-runners-min
spec:
  namespace:
    value: ci-runners
  githubConfigUrl: https://github.com/my-org/my-repo
  auth:
    existingSecretName: github-credential
