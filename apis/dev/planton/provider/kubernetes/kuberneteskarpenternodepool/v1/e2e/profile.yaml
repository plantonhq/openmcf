apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kuberneteskarpenternodepool
spec:
  tier: 1
  status: real_cluster
  validated_provisioners:
    - pulumi
    - tofu
  timeout_minutes: 25
  deferred_reason: >-
    The NodePool CRD is installed by KubernetesKarpenter (the registry
    prerequisite), whose controller cannot start off AWS — so a local kind
    cluster can never serve the karpenter.sh API group. Offline plan proofs
    cover both engines' full rendering; object-grade and node-provisioning
    lanes run in the batched EKS real-cluster set (the aws-eks cluster
    profile).
