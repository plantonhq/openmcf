# Minimal offline-proof manifest: the component's defaults — a single
# monolithic replica on filesystem storage with the nginx gateway. Proves
# the minimal render path (the shape most likely to expose a nil-deref on
# an absent optional block). Placeholder values; never applied.
apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesLoki
metadata:
  name: loki-hack-min
spec:
  namespace:
    value: loki-hack-min
  createNamespace: true
