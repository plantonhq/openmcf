apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesrabbitmq
spec:
  tier: 3
  # Forged against the RabbitmqCluster CRD at operator v2.22.3 and
  # offline-validated (spec tests, both engines' plan/preview proofs
  # across full and minimal shapes); the live lanes have not run yet.
  status: pending_proof
  timeout_minutes: 35
