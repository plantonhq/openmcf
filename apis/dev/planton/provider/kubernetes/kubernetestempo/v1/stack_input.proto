syntax = "proto3";

package dev.planton.provider.kubernetes.kubernetestempo.v1;

import "dev/planton/provider/kubernetes/kubernetestempo/v1/api.proto";
import "dev/planton/provider/kubernetes/provider.proto";

//tempo-kubernetes stack-input
message KubernetesTempoStackInput {
  //target cloud-resource
  KubernetesTempo target = 1;
  //provider-config
  dev.planton.provider.kubernetes.KubernetesProviderConfig provider_config = 2;
}
