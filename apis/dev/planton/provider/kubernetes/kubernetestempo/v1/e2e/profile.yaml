apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetestempo
spec:
  tier: 2
  # Forged onto the official monolithic tempo chart 2.2.3 and
  # offline-validated (spec tests, both engines' plan/preview proofs across
  # full and minimal shapes); the live lanes have not run yet — they run
  # the OTLP-ingest→trace-by-ID round-trip and the trace-survives-pod-loss
  # durability proof.
  status: pending_proof
  timeout_minutes: 25
