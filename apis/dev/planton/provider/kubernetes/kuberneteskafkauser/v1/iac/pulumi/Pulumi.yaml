name: kubernetes-kafka-user
runtime: go
description: Declarative Kafka client identity on the Strimzi KafkaUser custom resource — credentials generated by the target cluster's user operator, ACLs enforced under simple authorization
