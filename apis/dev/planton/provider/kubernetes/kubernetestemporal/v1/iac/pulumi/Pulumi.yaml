name: kubernetes-temporal
runtime: go
description: Temporal installation from the official Helm chart on an external database (PostgreSQL/MySQL/Cassandra) with existingSecret credential wiring, per-service sizing, declarative namespaces, dynamic-config limits and archival
