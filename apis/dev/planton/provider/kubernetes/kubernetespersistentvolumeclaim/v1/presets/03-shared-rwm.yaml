apiVersion: kubernetes.planton.dev/v1
kind: KubernetesPersistentVolumeClaim
metadata:
  name: shared-assets
spec:
  namespace:
    value: "<your-namespace>"
  name: shared-assets
  access_modes:
    - ReadWriteMany
  storage_request: 100Gi
  storage_class_name:
    value: "<your-shared-fs-class>"
