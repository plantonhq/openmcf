apiVersion: iac.planton.dev/v1
kind: ComponentImportMap
metadata:
  name: kubernetespeerauthentication
spec:
  # One kubectl_manifest applying the Istio PeerAuthentication CR. The composed import ID
  # is apiVersion//kind//name//namespace — apiVersion and kind are constants
  # of this module (it renders exactly one GVK), declared as literals.
  values:
    - name: api_version
      derivations:
        - literal: security.istio.io/v1
      where_to_find: >-
        Always "security.istio.io/v1" — the module pins the served version.
    - name: kind
      derivations:
        - literal: PeerAuthentication
      where_to_find: Always "PeerAuthentication" — the CR kind this module applies.
    - name: name
      derivations:
        - from_metadata_name: true
        - from_stack_output: peer_authentication_name
      where_to_find: >-
        The PeerAuthentication's metadata.name (equals the Planton resource name) —
        `kubectl get peerauthentications -A` (NAME column).
    - name: namespace
      derivations:
        - from_stack_output: namespace
        - from_spec_field: namespace.value
      where_to_find: >-
        The namespace the resource lives in — `kubectl get peerauthentications -A`
        (NAMESPACE column).
