name: kubernetes-neo4j
runtime: go
description: Neo4j graph database installation from the official Helm chart with a module-materialized NEO4J_AUTH credentials Secret, typed memory/config tuning, and a deliberate ClusterIP service default
