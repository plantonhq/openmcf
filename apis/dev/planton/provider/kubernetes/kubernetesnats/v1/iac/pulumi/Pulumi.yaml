name: kubernetes-nats
runtime: go
description: NATS installation from the official Helm chart with JetStream persistence on by default, module-generated user credentials wired through env-var config references, clustering, WebSocket/MQTT/leafnode listeners and TLS seams
