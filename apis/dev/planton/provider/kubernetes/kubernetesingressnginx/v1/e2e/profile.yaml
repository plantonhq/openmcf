apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesingressnginx
spec:
  tier: 1
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  limitations:
    - "LoadBalancer service type needs a cloud LB controller — kind lanes run node_port; real LB provisioning rides the batched real-cluster lanes"
  timeout_minutes: 20
