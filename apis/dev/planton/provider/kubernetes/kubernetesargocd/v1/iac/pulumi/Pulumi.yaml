name: kubernetes-argocd
runtime: go
description: Argo CD installation from the official Helm chart with application-owned initial admin credentials, secret-safe SSO indirection, a three-arm Redis cache, public repository registration, and kept-by-default CRDs
