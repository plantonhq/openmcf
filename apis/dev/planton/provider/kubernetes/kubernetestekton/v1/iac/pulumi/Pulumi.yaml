name: kubernetes-tekton
runtime: go
description: The cluster's Tekton installation declared as the TektonConfig the Tekton Operator reconciles — profile, component configuration, pipeline feature flags, CloudEvents sink, resolvers, metrics shape and pruner policy
