name: kubernetes-tekton-operator
runtime: go
description: Tekton Operator installation from the released official manifest — the operator.tekton.dev CRDs, operator and webhook Deployments with auto-install disabled so a KubernetesTekton declaration owns the cluster's TektonConfig, and typed overrides for images, sizing and placement
