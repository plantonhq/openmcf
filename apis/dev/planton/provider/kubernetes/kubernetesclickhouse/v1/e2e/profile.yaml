apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesclickhouse
spec:
  tier: 3
  # Rebuilt to full configuration depth against operator 0.27.2 and
  # offline-validated (spec tests, both engines' plan/preview proofs);
  # the live lanes have not run against the rebuilt spec yet.
  status: pending_proof
  timeout_minutes: 35
