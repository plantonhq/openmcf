apiVersion: _test.planton.dev/v1alpha1
kind: TestCloudResourceGeneric
metadata:
  name: torture-default
spec:
  stringField: explicit-string
  int32Field: 7
  boolField: true
  nested:
    nestedString: explicit-nested
  requiredRef:
    value: literal-required-value
  annotatedRef:
    valueFrom:
      kind: TestCloudResourceGeneric
      name: torture-upstream
      fieldPath: status.outputs.id
  labels:
    purpose: certification
  commands:
    - first
    - second
  sensitiveString: not-a-real-secret
