# Audit Report: GcpGlobalForwardingRule

**Audit Date:** 2026-07-03 16:51:00
**Component Kind:** GcpGlobalForwardingRule
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpglobalforwardingrule/v1alpha1/`
**Enum Value:** 710
**ID Prefix:** gcpgfr

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpGlobalForwardingRule = 710` (opens the 710–719 networking/load-balancer overflow block; the 623–629 LB sub-band is fully allocated); `id_prefix` `gcpgfr` unique; `prerequisites: [GcpTargetHttpsProxy, GcpGlobalAddress]` drives the deepest composed E2E chain in the GCP harness (7 transitive prerequisites).

### Protobuf API Definitions

- Full released 6.50.0 GA surface on plain `google ~> 6.0`: required `target` ref (default_kind → `GcpTargetHttpsProxy`; accepts HTTP-proxy self-links, PSC bundles `all-apis`/`vpc-sc`, service attachment URIs); `ip_address` ref → `GcpGlobalAddress.address` (the literal IP keeps state drift-free); `ip_protocol` (middleware default TCP), `ip_version`, `port_range` (`n`/`n-m` CEL); `load_balancing_scheme` with the `NONE` sentinel mapping to the API's empty PSC scheme; `network`/`subnetwork` refs; `network_tier` (PREMIUM-only CEL — STANDARD is regional-only); Traffic Director `metadata_filters`; PSC `service_directory_registration` (provider caps the list at 1 → modeled singular) and `no_automate_dns_zone`; `labels`; the EXTERNAL→EXTERNAL_MANAGED backend-bucket migration canary.
- **Five message-level CEL coherence rules** enforce pre-deploy what GCP rejects later: network-requires-internal-or-PSC, metadata-filters-require-TD, service-directory-requires-PSC, no-automate-dns-requires-PSC, migration-percentage-requires-TEST_BY_PERCENTAGE.
- **Deliberately unmodeled (recorded in `docs/README.md`):** `source_ip_ranges` (in the provider's global schema but API-documented as regional-EXTERNAL-only — a knob that always fails on this resource), `allow_psc_global_access` (google-beta-only on the released 6.x line), `base_forwarding_rule` (computed, only meaningful with source_ip_ranges), `deletion_policy`, `timeouts`.
- 30-case spec test suite green, including the PSC composite shape and every CEL fence.

### IaC Modules (both engines)

- Both enable `compute.googleapis.com` first; ambient-project contract; the NONE→"" scheme mapping implemented identically (explicit-empty sent for PSC, omit for unset); only-true `no_automate_dns_zone`; zero-means-unset migration percentage.
- Release-equivalent Pulumi build green; `tofu validate` + offline `planton tofu plan` (2 to add; ip/port/scheme inspected) through the real tfvars converter.

### Cross-Engine IaC Parity (Category 5b gate)

Identical naming basis, full spec coverage, identical scheme/null semantics, identical outputs (`ip_address`, `self_link`, `forwarding_rule_name`, `forwarding_rule_id`, `psc_connection_id`, `psc_connection_status`). Conformance case in `pkg/outputs` (6/6 both engines). No PARITY-EXCEPTIONs. **PARITY ✅**

### Secure-by-Default (Category 11 gate)

No secret-bearing fields. `planton secret-coverage --check` green. **COVERAGE ✅**

### Supporting Files, Presets, Docs, E2E

- 3 presets (HTTPS frontend VIP, HTTP redirect shared-IP twin, PSC-to-Google-APIs) — all pass `planton validate`.
- Composed live E2E green on BOTH engines (Pulumi 6m12s, Terraform 6m18s) on the full 7-prerequisite chain (health check → NEG → backend service → URL map → managed cert → HTTPS proxy, plus the reserved global address) — a complete external HTTPS load balancer assembled and destroyed from first-class kinds; verifier asserts the VIP and target wiring; zero orphans after teardown.
