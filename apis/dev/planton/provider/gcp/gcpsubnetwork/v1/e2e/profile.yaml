apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpsubnetwork
spec:
  # Composed scenario: the harness deploys a GcpVpc prerequisite (custom-mode
  # network), resolves the subnet's network reference from its outputs, then
  # runs deploy -> compute subnetworks.get verify (incl. primary-range check)
  # -> destroy -> verify 404, on both provisioners. The scenario carries
  # secondary ranges and flow logs so the deepened surface is exercised live.
  tier: 1
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 15
