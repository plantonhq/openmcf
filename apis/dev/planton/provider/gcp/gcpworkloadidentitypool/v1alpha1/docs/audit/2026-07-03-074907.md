# Audit Report: GcpWorkloadIdentityPool

**Audit Date:** 2026-07-03 07:49:07
**Component Kind:** GcpWorkloadIdentityPool
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpworkloadidentitypool/v1alpha1/`
**Enum Value:** 701
**ID Prefix:** gcpwip

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpWorkloadIdentityPool = 701` exists (GCP band 600–799; first entry of the 701–709 identity overflow block, the 600–622 foundation sub-band being fully allocated)
- `id_prefix` `gcpwip` is unique
- `kind_meta` includes provider, version, id_prefix

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists at `apis/dev/planton/provider/gcp/gcpworkloadidentitypool/v1alpha1/`; name matches the lowercase enum name

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- `spec.proto` (7.9 KB), `api.proto` (866 B), `stack_input.proto` (355 B), `stack_outputs.proto` (1.0 KB) — all present with dense intent-explaining comments
- All four `.pb.go` stubs regenerated via `make protos`
- CEL messages are complete user-facing sentences (`gcp-` prefix reservation, mode guidance including why SYSTEM_TRUST_DOMAIN is rejected, key-algorithm list)
- `spec_test.go` (7.7 KB): 22 ginkgo cases — happy paths (minimal, full federation, ID bounds, all modes, cert issuance, trust bundles) and error paths (prefix, charset, lengths, invalid mode/algorithm/lifetime/rotation, empty CA pools, bundle without anchors)
- `go test ./apis/dev/planton/provider/gcp/gcpworkloadidentitypool/v1alpha1/` passes

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go`, `module/workload_identity_pool.go` present
- Comments explain the immutability triple (id/project/mode), the no-undelete-on-create soft-delete behavior, why `disabled` is always sent, and the ambient-project fallback contract
- Entrypoint at `iac/pulumi/main.go` root (`package main`), `Pulumi.yaml` without `binary:`, `Makefile`
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `variables.tf` (3.9 KB, typed spec object with per-field comments + four validations), `main.tf` (3.0 KB), `locals.tf`, `outputs.tf`, `provider.tf` (`~> 6.0` + google-beta), `backend.tf`
- The pool resource explicitly selects `provider = google-beta` with a comment naming the beta-only surfaces on the 6.x line (`mode`, inline certificate/trust blocks)
- Null-guard comment documents HCL's non-short-circuiting `||` in the certificate-issuance validation
- `tofu init` + `tofu validate` clean; `planton tofu plan --manifest iac/hack/manifest.yaml` clean

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs` (3/3 proto fields populated, zero unmapped) and live dual-engine E2E (create → verify → destroy green on both engines against a real project).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source (project) | `spec.project_id` w/ null fallback | `local.project_id` w/ null fallback | ✅ MATCH |
| Resource naming basis | `spec.workload_identity_pool_id` | `var.spec.workload_identity_pool_id` | ✅ MATCH |
| Labels | n/a (resource carries no labels) | n/a | ✅ MATCH |
| Spec feature coverage | id, project, display, description, disabled, mode, cert issuance, trust config | same | ✅ MATCH |
| Defaults behavior | mode defaults FEDERATION_ONLY; nested optionals left to API defaults | same | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | name, workload_identity_pool_id, state | same | ✅ MATCH |

Note (not an exception — outcomes identical): the Terraform module runs the pool on `google-beta` because `mode` and the inline blocks are beta-only on the 6.x provider line; the Pulumi GCP provider is bridged from google-beta by construction, so both engines exercise the same underlying surface.

**Effect on score:** none.

---

### 6–8. Documentation & Supporting Files

✅ **Passed:**

- `docs/README.md` (6.7 KB): keys-vs-federation problem statement, pool/provider decomposition rationale, lifecycle sharp edges (no undelete-on-create), mode analysis, 90/10 coverage table with recorded skips (`deletion_policy`, `timeouts`, namespace/managed-identity sibling resources), composition walk-through, operational guidance
- `README.md` (6.4 KB) + `catalog-page.md`: quick start, full configuration reference, outputs, related components
- `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all present; hack manifest passes `planton validate`

**Score:** 36.36% / 36.36%

---

### 10. Presets (5.00%)

✅ **Passed:**

- `01-ci-federation-pool` and `02-locked-down-pool` YAML+MD pairs; correct apiVersion/kind; `my-`-style names; `StringValueOrRef` uses `value:`; all fields exist in spec; both pass `planton validate`

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --output json` reports zero `gaps[]` for `GcpWorkloadIdentityPool`. The spec holds no secret material (PEM trust anchors are public certificates and do not trip the heuristic); no annotations or exemptions required. `--check` passes.

---

## Live E2E Evidence

- Pulumi: minimal scenario green (deploy 32s, destroy 18s); Terraform: green (deploy 24s, destroy 18s) — project `planton-e2e`
- Verifier: `workloadIdentityPools.get` must be ACTIVE post-deploy; DELETED-or-404 post-destroy (soft-delete-aware)
- Scenario pool IDs carry the E2E run-id token, so soft-delete ID reservation never collides across runs/engines
- Post-run sweep: zero ACTIVE pools; only expected soft-deleted E2E pools remain

---

**Audit completed at:** 2026-07-03-074907
**Reference:** See `architecture/deployment-component.md` for ideal state definition
