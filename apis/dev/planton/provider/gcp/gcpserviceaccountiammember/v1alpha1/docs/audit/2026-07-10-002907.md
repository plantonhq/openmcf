# Audit Report: GcpServiceAccountIamMember

**Audit Date:** 2026-07-10 00:29:07
**Component Kind:** GcpServiceAccountIamMember
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpserviceaccountiammember/v1alpha1/`
**Enum Value:** 703
**ID Prefix:** gcpsaim

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpServiceAccountIamMember = 703` exists in `cloud_resource_kind.proto`, continuing the registry's declared 701–709 IAM/identity overflow block
- Enum value 703 is in the GCP range (600–799)
- `id_prefix` `gcpsaim` is unique across the registry
- `kind_meta` includes provider, version, id_prefix, and `prerequisites: [GcpServiceAccount]` (exercised live by the E2E chain)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists at `apis/dev/planton/provider/gcp/gcpserviceaccountiammember/v1alpha1/`
- Folder name is the lowercase enum name; `v1/` subfolder present

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- `spec.proto` (4.8 KB): four fields — `service_account_id` (required, FK `GcpServiceAccount.status.outputs.name`), `role` (required, FK `GcpIamCustomRole.status.outputs.name`), `member` (required, FK `GcpServiceAccount.status.outputs.member`), optional `condition` (title required ≤100, expression required, description ≤256) — a 100% model of the released `google_service_account_iam_member` (schema probed against the released v6.50.0 tag, including the shared IAM member base schema). Field comments teach the identity-vs-resource duality, the three canonical usage roles, additive semantics, and immutability.
- `api.proto`, `stack_input.proto`, `stack_outputs.proto` present; outputs echo the resolved grant tuple + policy `etag`.
- All four `.pb.go` stubs generated and current.
- `spec_test.go` (5.6 KB): 14 cases (federated/token-creator/actAs/conditional positives; missing-field, condition-coherence, max-len, kind-literal negatives) — all pass (`go test` green).
- Validation messages: constraints are simple `required`/`max_len` (protovalidate default messages); no custom CEL rules needed for this surface — the deploy-time format checks live in both modules with actionable error strings.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/iam_member.go`, `module/outputs.go` present; comments explain additive-vs-authoritative semantics, immutability (ForceNew), deploy-time validation rationale, and the no-project-argument contract — the spec-comment bar.
- Root `iac/pulumi/main.go` declares `package main`; no `main/`/`entrypoint/` subdir; `Pulumi.yaml` + `Makefile` canonical.
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `variables.tf` (2.9 KB, typed spec object with three validation blocks mirroring the Pulumi module's deploy-time checks), `provider.tf` (`google ~> 6.0` + aliased `google-beta`), `locals.tf`, `main.tf` (richly commented), `outputs.tf` (4 outputs matching `stack_outputs.proto`).
- `tofu init -backend=false && tofu validate` green; real-converter `planton tofu plan` green on the hack manifest.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.service_account_id` | `var.spec.service_account_id` | ✅ MATCH |
| Resource naming basis | n/a (grant creates no named cloud object) | n/a | ✅ MATCH |
| Labels | none (IAM policies carry no labels) | none | ✅ MATCH |
| Selectors | n/a | n/a | ✅ MATCH |
| Spec feature coverage | all 4 fields incl. condition.description | same | ✅ MATCH |
| Deploy-time validation | SA-id regex + member regex + deleted: rejection | identical acceptance in `variables.tf` validations | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 4/4 populated | 4/4 populated | ✅ MATCH |

Output parity verified three ways: the `pkg/outputs/conformance_test.go` case (green), `planton validate-outputs` (generic reflection path, no override), and live E2E VERIFY-OUT reporting 4/4 proto fields populated on BOTH engines.

**Effect on score:** none — engines in parity.

---

### 6–8. Documentation and Supporting Files

✅ **Passed:**

- `docs/README.md` (4.1 KB): identity-vs-resource concept, why account-scoped beats project-scoped, additive-only stopping line, immutability rationale, and the boundary with `GcpGkeWorkloadIdentityBinding` and `GcpProjectIamMember`.
- `README.md` (7.5 KB) + `catalog-page.md`: quick starts (literal + composed), full configuration reference, member-format table, outputs, related components.
- `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` (validates through the CLI).

**Scores:** 13.18% + 13.09% + 10.09% — all full.

---

### 10. Presets (5.00%)

✅ **Passed:**

- 3 preset pairs: `01-github-workload-identity-impersonation`, `02-token-creator-grant`, `03-deployer-act-as` — each YAML has a companion `.md`; correct `apiVersion`/`kind`; `my-` names; `StringValueOrRef` wrapper forms; every field exists in `spec.proto`; no duplicate ranks.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` green; zero `gaps[]` entries for this kind (the spec carries no secret-bearing fields — grants are policy metadata). The GCP baseline remains empty.

---

## Live E2E

Scenario `token-creator-self` (SA prerequisite; `service_account_id` ← its `name` output, `member` ← its `member` output — both FK paths proven with one prerequisite): **live dual-engine green on `planton-e2e`** (Pulumi 1m11s, Terraform comparable), deploy → getIamPolicy verify → destroy → absence verified, zero orphans.

---

**Audit completed at:** 2026-07-10 00:29:07
**Reference:** See `architecture/deployment-component.md` for ideal state definition
