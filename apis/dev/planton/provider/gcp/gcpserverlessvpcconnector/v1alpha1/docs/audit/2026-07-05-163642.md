# Audit Report: GcpServerlessVpcConnector

**Audit Date:** 2026-07-05 16:36:42
**Component Kind:** GcpServerlessVpcConnector
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpserverlessvpcconnector/v1alpha1/`
**Enum Value:** 721
**ID Prefix:** gcpvpcconn

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpServerlessVpcConnector = 721` continues the 720–729 serverless
  overflow block contiguously after GcpCloudRunJob (720)
- `id_prefix: gcpvpcconn` unique; `prerequisites: [GcpVpcNetwork, GcpSubnetwork]`
  drives the E2E chains for both placement arms
- Kind map registered (`pkg/crkreflect/kind_map_gen.go`)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- `apis/dev/planton/provider/gcp/gcpserverlessvpcconnector/v1alpha1/` with the full
  anatomy: 4 protos + BUILD + spec test + README + catalog-page + docs +
  3 preset pairs + `iac/hack` + both engines + `e2e/`

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- Four protos present; `spec.proto` teaches the two placement arms, the
  25-char name cap, the never-scales-in behavior, and the
  grow-in-place / shrink-forces-replace asymmetry from the provider's
  CustomizeDiff
- CEL: exactly-one placement (`network_xor_subnet`), `network_requires_cidr`,
  `cidr_requires_network`, `min_lt_max` — all reference-safe (guard
  `value`/`value_from` arms) and complete sentences
- FK wiring: `project_id` → GcpProject, `network` → GcpVpcNetwork
  (`network_name`), `subnet.name` → GcpSubnetwork (`subnetwork_name`)
- Recorded skip in spec/docs: legacy `min_throughput`/`max_throughput`
  (provider-discouraged, ConflictsWith the instance fields, ForceNew)
- Stubs regenerated; `spec_test.go`: 33 cases, all passing

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- Canonical module shape (`main.go` entrypoint at `iac/pulumi/main.go`,
  `module/{main,connector,locals,outputs}.go`), canonical `Pulumi.yaml`,
  `Makefile`, `README.md`
- `vpcaccess.googleapis.com` enabled before create (disable_on_destroy=false)
- Region output emitted from the plain spec region (bridged-attribute-format
  parity class pre-empted)
- Build green (`make build` in module dir: vet + fmt clean)

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files; converter-contract `variables.tf` (curated `optional()`
  form); `~> 6.0` float (no hard pin, no google-beta)
- `tofu validate` green; offline plan through the real tfvars converter
  clean (placement, scaling band, API enablement inspected)

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs` on BOTH module dirs (full
4-field population, zero unmapped). Live dual-engine E2E green on both
placement arms.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project / scope source | omit arg when spec empty | `null` fallback | ✅ MATCH |
| Resource naming basis | `connector_name` → `metadata.name` fallback | same | ✅ MATCH |
| Labels | n/a — `google_vpc_access_connector` has no labels surface (documented in both modules) | n/a | ✅ MATCH |
| Placement arms | network+cidr XOR subnet(+project) | same | ✅ MATCH |
| Scaling contract | instance band only; throughput fields never set | same | ✅ MATCH |
| API enablement | vpcaccess.googleapis.com, DependsOn | same, depends_on | ✅ MATCH |
| Stack outputs → proto | name/self_link/state from resolved attributes; region from spec | same | ✅ MATCH |

**Effect on score:** none — engines in parity.

---

### 6–10. Documentation, Supporting Files, Presets

✅ **Passed:**

- `docs/README.md`: first-class-node rationale (regional shared
  infrastructure, many consumers per connector), the two placement arms,
  the capacity model with both sharp edges (never-scales-in; shrink
  replaces), recorded skips (`min/max_throughput`, `deletion_policy` —
  absent from released 6.50.0, schema-probe verified; `connected_projects`
  read-side output)
- `README.md` + `catalog-page.md` timeless; `iac/*/README.md` present;
  canonical `iac/hack/manifest.yaml` (validates)
- 3 preset pairs (private-egress-basic / high-throughput /
  shared-vpc-subnet), all pass `planton validate`
- Site catalog page created (`serverless-vpc-access-connector/`)

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

Zero `gaps[]` for `GcpServerlessVpcConnector:` — the spec carries no
secret-bearing fields.

---

## Critical Gaps

None.

## Validation Evidence

- Spec tests 33/33; Pulumi module build (vet+fmt); `tofu validate` + offline
  converter plan; `validate-refs --check`; `secret-coverage --check`;
  `validate-outputs` 4/4 on both module dirs; `pkg/outputs` conformance case
  green; all 8 manifests (hack + 3 presets + prerequisite + 2 consumer-scoped
  prerequisites + 2 scenarios) through `planton validate` on a freshly built CLI
- **Live dual-engine E2E green on `planton-e2e`**: network-arm and subnet-arm
  on BOTH engines (4/4 scenario-runs, ~8m per scenario including the
  VPC + subnetwork prerequisite chain) — zero orphans after teardown
  (connectors, networks, subnets all empty). The subnet-arm run surfaced a
  live-only verifier defect (subnet placement reports no `ip_cidr_range` on
  the connector object — the range lives on the subnet), fixed in the
  posture assertion.

**Audit completed at:** 2026-07-05-163642
**Reference:** `architecture/deployment-component.md`
