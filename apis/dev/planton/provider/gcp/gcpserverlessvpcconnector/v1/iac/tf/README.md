# Terraform module for GcpServerlessVpcConnector

Provisions a `google_vpc_access_connector` from the validated protobuf spec. Enables `vpcaccess.googleapis.com` automatically.

See the component [README](../../README.md) for the full configuration reference.
