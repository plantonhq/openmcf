# Audit Report: GcpCloudRunJob

**Audit Date:** 2026-07-04 23:30:00
**Component Kind:** GcpCloudRunJob
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpcloudrunjob/v1alpha1/`
**Enum Value:** 720
**ID Prefix:** `cldrunjob`

---

## Overall Completion Score

**Score: 99%**

```
██████████ 99% Complete
```

**Status:** Functionally Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 12.00% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Engines are in full parity; no defects found.

---

## Quick Wins

1. **Expand `docs/README.md` (2.8 KB)**
   - **Why:** The spec is one of the largest in the GCP catalog (containers,
     volumes with 5 source types, VPC egress, GPU, binary authorization); a
     moderate research doc undersells the scope/skip decisions.
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudrunjob/v1alpha1/docs/README.md`

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `GcpCloudRunJob = 720` registered (`provider: gcp`, `version: v1`,
`id_prefix: "cldrunjob"` — unique). Enum in the GCP range (600–799).

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Correct hierarchy and lowercase naming; `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Proto files:** `api.proto`, `spec.proto` (19.6 KB), `stack_input.proto`,
`stack_outputs.proto`. Models `google_cloud_run_v2_job` comprehensively: task
template with multi-container support (env value/secret XOR, resources, volume
mounts, depends_on), five volume source types (Cloud SQL, secret, empty_dir, GCS,
NFS) via a validated oneof, VPC access (connector XOR direct egress), GPU node
selector, binary authorization (default XOR policy), task_count/parallelism
(parallelism ≤ task_count CEL), launch stage, and deletion_protection (default
true). Validation messages are complete sentences; regexes explained in prose.

Recorded skip (documented): **no `description`** — same reason as sibling GCP
components (not on the released provider surface being targeted). Confirmed
intentional.

✅ **Stubs:** all `.pb.go` present.

✅ **Tests:** `spec_test.go` (10.8 KB);
`go test ./apis/dev/planton/provider/gcp/gcpcloudrunjob/v1alpha1/` — **ok**.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ `module/main.go`, `module/locals.go`, `module/job.go`, `module/outputs.go`;
root `main.go` is `package main`; `Pulumi.yaml` + `Makefile`; release-equivalent
build passes. `job.go` builds the full nested task template with helper functions
for containers and volumes.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files present; `tofu validate` — **Success**. The nested
`template { template { … } }` shape mirrors the Pulumi task template exactly, with a
dynamic block for every optional sub-message.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project fallback | omit when empty | empty → null | ✅ MATCH |
| API enablement | `run.googleapis.com`, dep=true, destroy=false | same | ✅ MATCH |
| Labels (keys + values) | `gcplabelkeys` (`planton-ai_*`), kind `gcpcloudrunjob`, name=job_name, + spec.labels | identical `planton-ai_*`, spec.labels merged | ✅ MATCH |
| job_name default | spec.job_name → metadata.name | spec.job_name → metadata.name | ✅ MATCH |
| region / launch_stage / annotations | set | set | ✅ MATCH |
| task_count / parallelism | set when non-nil | optional, null when unset | ✅ MATCH |
| deletion_protection | `Bool(true)` default | `optional(bool, true)` | ✅ MATCH |
| binary_authorization (use_default/policy/breakglass) | set | set | ✅ MATCH |
| gpu_zonal_redundancy_disabled | set when true | `? true : null` | ✅ MATCH |
| template: service_account / execution_environment / encryption_key / timeout / max_retries | set | set (timeout `"${n}s"`) | ✅ MATCH |
| containers (image/name/command/args/env/resources/mounts/working_dir/depends_on) | set | set | ✅ MATCH |
| env value XOR secret | secret ref or literal | secret ref or literal | ✅ MATCH |
| volumes (cloud_sql / secret / empty_dir / gcs / nfs) | oneof switch | dynamic blocks | ✅ MATCH |
| vpc_access (connector/egress/network_interfaces) | set | set | ✅ MATCH |
| node_selector.accelerator | set | set | ✅ MATCH |
| Dependency ordering | job→api | job→api | ✅ MATCH |
| Stack outputs → proto | 4/4 (job_name, location, uid, latest_created_execution) | 4/4 | ✅ MATCH |

**Legend:** ✅ MATCH | ⚠️ DIVERGES | 🔵 EXCEPTION

✅ **In parity:** every spec field — including the deep nested task template,
five-way volume oneof, and label precedence (spec.labels merged, platform labels
win) — is honored identically by both engines. Stack outputs flatten onto the same
four proto fields.

**Effect on score:** none.

---

### 6–8. Documentation & Supporting Files

⚠️ `docs/README.md` (2.8 KB) — present, moderate-thin for the spec's size (see
Quick Wins). ✅ `README.md` (5.1 KB), `catalog-page.md`. ✅ `iac/pulumi/README.md`,
`iac/tf/README.md`, `iac/hack/manifest.yaml` present.

**Scores:** 12.00% / 13.18% (research doc), 13.09%, 10.09% — otherwise full credit.

---

### 9. Nice to Have (10%)

✅ `BUILD.bazel` files generated.

---

### 10. Presets (5.00%)

✅ Three ranked pairs (`01-batch-etl-basic`, `02-parallel-vpc-cleanup`,
`03-gpu-batch-inference`), companion `.md` files, no duplicate ranks, `apiVersion`/
`kind` consistent with `api.proto`, `StringValueOrRef` fields use `value:` wrapper.

> Note: live `planton validate` could not be run — the installed CLI predates enum
> 720 ("unsupported cloud resource kind"). Presets verified by inspection.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

| Field path | Classification | Note |
|-----------|----------------|------|
| `spec.template.encryption_key` | exempt | `sensitive_exempt_reason`: KMS crypto key resource ID/reference only |
| `…env.value_from_secret.secret`, `…volume.secret.secret` | exempt | `sensitive_exempt_reason`: Secret Manager secret NAME only — material never in spec |

`planton secret-coverage --output json` reports **zero** `gaps[]` entries prefixed
`GcpCloudRunJob:`. Literal env `value` carries the standard guidance to use
`value_from_secret` for credentials.

---

**Audit completed at:** 2026-07-04 23:30:00
**Reference:** `architecture/deployment-component.md`
