# Terraform module for GcpCloudRunJob

Provisions a `google_cloud_run_v2_job` from the validated protobuf spec. Enables `run.googleapis.com` automatically.

See the component [README](../../README.md) for the full configuration reference.
