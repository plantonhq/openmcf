apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpproject
spec:
  # Live E2E is conditional on org-level Project Creator credentials.
  # When unavailable, scenarios are exercised via offline converter plans only.
  tier: 2
  status: plan-only
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 45
