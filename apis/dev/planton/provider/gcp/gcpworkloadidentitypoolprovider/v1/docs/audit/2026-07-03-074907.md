# Audit Report: GcpWorkloadIdentityPoolProvider

**Audit Date:** 2026-07-03 07:49:07
**Component Kind:** GcpWorkloadIdentityPoolProvider
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpworkloadidentitypoolprovider/v1/`
**Enum Value:** 702
**ID Prefix:** gcpwipp

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpWorkloadIdentityPoolProvider = 702` exists (701–709 identity block)
- `id_prefix` `gcpwipp` is unique
- Registry `prerequisites: [GcpWorkloadIdentityPool]` drives automatic prerequisite deployment in E2E (second GCP kind using the mechanism)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists at `apis/dev/planton/provider/gcp/gcpworkloadidentitypoolprovider/v1/`; name matches the lowercase enum name

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- `spec.proto` (9.5 KB): FK-wired pool reference, oneof issuer (`(buf.validate.oneof).required`), the four arms modeled in full (AWS account, OIDC with audiences + JWKS, SAML metadata, X.509 trust store with intermediates), and a message-level CEL enforcing OIDC's `google.subject` mapping requirement pre-deploy (dereferencing only scalar/map fields — the StringValueOrRef-CEL constraint is respected)
- All four `.pb.go` stubs regenerated; `api.proto`/`stack_input.proto`/`stack_outputs.proto` follow the canonical shape
- `spec_test.go` (8.4 KB): 20 ginkgo cases covering all four issuer arms plus error paths (missing pool ref/issuer, ID rules, subject-mapping rule, URI/audience/account-ID/trust-store validation, condition length)
- `go test ./apis/dev/planton/provider/gcp/gcpworkloadidentitypoolprovider/v1/` passes

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go`, `module/workload_identity_pool_provider.go` present
- Comments explain the immutability set (pool/provider-id/project + issuer TYPE), soft-delete-without-undelete, issuer-default mappings (why an empty map stays unset), and the audience-default trade-off for OIDC
- Entrypoint at `iac/pulumi/main.go` root (`package main`), `Pulumi.yaml` without `binary:`, `Makefile`
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `variables.tf` (4.3 KB, typed spec object incl. all four issuer arms + four validations: ID pattern, `gcp-` prefix, exactly-one-issuer, OIDC subject-mapping), `main.tf` (3.2 KB, dynamic blocks per arm), `locals.tf`, `outputs.tf`, `provider.tf` (`~> 6.0` + google-beta declared), `backend.tf`
- The provider resource runs on the GA `google` provider (the full surface incl. x509 is GA on 6.x)
- `tofu init` + `tofu validate` clean; `planton tofu plan --manifest iac/hack/manifest.yaml` clean

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs` (3/3 proto fields populated, zero unmapped) and live dual-engine E2E with the pool prerequisite chain (green on both engines).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Pool reference source | resolved `spec.workload_identity_pool_id` | `var.spec.workload_identity_pool_id` | ✅ MATCH |
| Scope source (project) | `spec.project_id` w/ null fallback | `local.project_id` w/ null fallback | ✅ MATCH |
| Resource naming basis | `spec.workload_identity_pool_provider_id` | same | ✅ MATCH |
| Labels | n/a (resource carries no labels) | n/a | ✅ MATCH |
| Spec feature coverage | display, description, disabled, mapping, condition, all 4 issuer arms (incl. x509 intermediates) | same | ✅ MATCH |
| Empty-value semantics | empty mapping/audiences/JWKS stay unset | null via locals/guards | ✅ MATCH |
| Dependency ordering | single resource (pool referenced by resolved value) | same | ✅ MATCH |
| Stack outputs → proto | name, workload_identity_pool_provider_id, state | same | ✅ MATCH |

**Effect on score:** none.

---

### 6–8. Documentation & Supporting Files

✅ **Passed:**

- `docs/README.md` (5.7 KB): the token-exchange contract, four issuer types compared, attribute-mapping security rules, the mandatory-condition guidance for multi-tenant issuers, lifecycle sharp edges, 90/10 coverage table with recorded skips (`deletion_policy`, `timeouts`), composition (audience output → web-identity configs)
- `README.md` (7.1 KB) + `catalog-page.md`: GitHub Actions quick start, full reference incl. per-arm table, the lockdown section, outputs
- `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all present; hack manifest passes `planton validate`

**Score:** 36.36% / 36.36%

---

### 10. Presets (5.00%)

✅ **Passed:**

- `01-github-actions-oidc`, `02-aws-account`, `03-gitlab-ci-oidc` YAML+MD pairs; correct apiVersion/kind; refs use `valueFrom`; every preset carries the attribute-condition lockdown its issuer demands; all pass `planton validate`

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --output json` reports zero `gaps[]` for `GcpWorkloadIdentityPoolProvider`. The spec's material (JWKS public keys, SAML IdP metadata, PEM CA certificates) is all public by nature and none of it trips the heuristic; no annotations or exemptions required. `--check` passes.

---

## Live E2E Evidence

- Pulumi: composed scenario green in 75s (pool prerequisite 21s, deploy 19s, destroy 16s, prerequisite teardown 17s); Terraform: green in 76s — project `planton-e2e`
- Verifier: `workloadIdentityPools.providers.get` must be ACTIVE post-deploy; DELETED-or-404 post-destroy (a 404 also covers the parent pool being soft-deleted first)
- Scenario provider IDs (and the prerequisite pool's ID) carry the E2E run-id token, so soft-delete ID reservation never collides across runs/engines
- Post-run sweep: zero ACTIVE pools/providers; only expected soft-deleted E2E resources remain

---

**Audit completed at:** 2026-07-03-074907
**Reference:** See `architecture/deployment-component.md` for ideal state definition
