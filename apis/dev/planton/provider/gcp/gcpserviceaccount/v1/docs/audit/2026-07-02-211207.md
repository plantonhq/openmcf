# Component Audit: GcpServiceAccount (with cross-engine parity)

**Date:** 2026-07-02
**Status: Fully Complete — PARITY ✅**

## Category Scores

| Category | Verdict | Evidence |
|---|---|---|
| 1. Cloud Resource Registry | ✅ | `GcpServiceAccount = 614`, `id_prefix: gsa` (unchanged) |
| 2. Folder Structure | ✅ | Canonical anatomy; hack manifest moved to the canonical `iac/hack/manifest.yaml` |
| 3. Protobuf APIs | ✅ | Spec covers the full provider surface (`display_name`, `description`, `disabled` added; RFC1035 `account_id` pattern; org-roles CEL rule); outputs gained `member`/`unique_id`/`name`; spec test grew from 1 to 14 cases, all green |
| 4. Pulumi Module | ✅ | Implements every spec field; release-equivalent build green; richly commented (immutability, Enable/Disable API behavior, keyless default, project fallback) |
| 5. Terraform Module | ✅ | Implements every spec field; provider pin conformed from hardcoded `6.19.0` to `~> 6.0` + `google-beta` stanza; `tofu validate` + `planton tofu plan` green |
| 5b. Cross-Engine Parity | **✅ GATE PASS** | Identical display-name fallback (metadata name), empty-description omission, disabled default, provider-default project fallback, additive member grants deriving project from the created account; outputs verified with `planton validate-outputs` (5/5) + `pkg/outputs` conformance case (first GCP entries in the conformance table) |
| 6. Research Docs | ✅ | `docs/README.md` keys-to-keyless essay refreshed: stale field list replaced with the full surface, dangling placeholder links removed, example manifest corrected to real schema shape |
| 7. User Docs | ✅ | `v1/README.md` rewritten to the standard reference shape; `catalog-page.md` updated (new fields, outputs, ref-shape examples, grant-kind cross-links) |
| 8. Supporting Files | ✅ | Both engine module docs; `iac/hack/manifest.yaml` (validated) |
| 9. BUILD.bazel | ✅ | Regenerated |
| 10. Presets | ✅ | 3 ranked yaml+md pairs (workload-identity / ci-cd-pipeline / identity-with-first-class-grants), all `planton validate` green |
| 11. Secret Coverage | **✅ GATE PASS** | `planton secret-coverage --check` green; `key_base64` is an output — sensitivity handled by engine-level secret marking (TF `sensitive = true`; Pulumi key resource secret in state) per the repo-wide outputs convention |

## Parity Notes

- The ambient-project defect class (empty `project_id` passed as a literal `""`
  instead of falling back to the provider default, breaking the documented spec
  contract) was fixed in BOTH engines identically.
- Terraform variables take `project_id` as a plain string because the CLI's
  tfvars converter resolves `StringValueOrRef` to its literal before module
  execution.
- No `PARITY-EXCEPTION` markers needed.

## E2E

- `e2e/profile.yaml` tier 1; `scenarios/minimal.yaml` (keyless smoke) +
  `prerequisite.yaml` (used by GcpProjectIamMember's chain); verifier probes
  `iam serviceAccounts.get` by email.
