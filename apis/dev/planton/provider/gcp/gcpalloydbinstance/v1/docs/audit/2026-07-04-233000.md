# Audit Report: GcpAlloydbInstance

**Audit Date:** 2026-07-04 23:30:00
**Component Kind:** GcpAlloydbInstance
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpalloydbinstance/v1/`
**Enum Value:** 639
**ID Prefix:** `gcpadbinst`

---

## Overall Completion Score

**Score: 98%**

```
██████████ 98% Complete
```

**Status:** Functionally Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 11.50% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Engines are in full parity; no defects found.

---

## Quick Wins

1. **Expand `docs/README.md`**
   - **Why:** At 1.4 KB it is a stub relative to the depth of the spec (read pools, PSC, public IP, activation policy). The research doc is the primary source of truth for scope decisions.
   - **How:** Document the READ_POOL-vs-PRIMARY topology, PSC vs public-IP connectivity, and the recorded skips (managed connection pooling).
   - **File:** `apis/dev/planton/provider/gcp/gcpalloydbinstance/v1/docs/README.md`

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `GcpAlloydbInstance = 639` registered (`provider: gcp`, `version: v1`,
`id_prefix: "gcpadbinst"` — unique) with `prerequisites: [GcpAlloydbCluster]` so
E2E installs a parent cluster first. Enum in the GCP range.

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Correct hierarchy and lowercase naming; `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Proto files:** `api.proto`, `spec.proto` (8.2 KB), `stack_input.proto`,
`stack_outputs.proto`. Models a standalone `google_alloydb_instance` on an existing
cluster: instance_type (READ_POOL default), machine config (cpu/machine_type XOR),
read-pool sizing, availability, query insights, client-connection (connectors/SSL),
activation policy, public IP + authorized external networks, and a full PSC
instance config. CEL guards cover machine XOR, READ_POOL node-count, and
authorized-networks-require-public-IP.

Recorded skip (documented inline): managed connection pooling — not exposed by the
released provider for AlloyDB instances.

✅ **Stubs:** all `.pb.go` present.

✅ **Tests:** `spec_test.go` (4.3 KB);
`go test ./apis/dev/planton/provider/gcp/gcpalloydbinstance/v1/` — **ok**.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ `module/main.go`, `module/locals.go`, `module/instance.go`, `module/outputs.go`;
root `main.go` is `package main`; `Pulumi.yaml` + `Makefile`; release-equivalent
build passes.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files present; `tofu validate` — **Success**. Every optional sub-message
maps to a matching dynamic block.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project fallback | omit when empty | empty → null | ✅ MATCH |
| API enablement | `alloydb.googleapis.com`, dep=true, destroy=false | same | ✅ MATCH |
| Labels (keys + values) | `gcplabelkeys` (`planton-ai_*`), kind `gcpalloydbinstance` | identical `planton-ai_*` | ✅ MATCH |
| cluster / instance_id / instance_type | set (default READ_POOL) | set (default READ_POOL) | ✅ MATCH |
| machine_config (cpu/machine_type) | conditional | conditional | ✅ MATCH |
| read_pool_config.node_count | set when >0 | set when >0 | ✅ MATCH |
| availability / database_flags / display_name | set | set | ✅ MATCH |
| query_insights_config | conditional set | always set; provider drops empties | ✅ MATCH |
| client_connection_config (connectors/ssl) | set | set | ✅ MATCH |
| activation_policy | set when non-empty | set when non-empty | ✅ MATCH |
| network_config (public IP / outbound / authorized networks) | set | set | ✅ MATCH |
| psc_instance_config (allowed projects / auto-conn / interfaces) | set | set | ✅ MATCH |
| Dependency ordering | instance→api | instance→api | ✅ MATCH |
| Stack outputs → proto | 3/3 (instance_name, ip_address, state) | 3/3 | ✅ MATCH |

**Legend:** ✅ MATCH | ⚠️ DIVERGES | 🔵 EXCEPTION

✅ **In parity:** every spec field is honored identically by both engines. The
query-insights "conditional (Pulumi) vs always-set (Terraform)" shapes are
behaviorally identical because the released google provider drops empty
ints/bools via `tpgresource.IsEmptyValue` (verified in
`resource_alloydb_instance.go`).

**Effect on score:** none.

---

### 6–8. Documentation & Supporting Files

⚠️ `docs/README.md` (1.4 KB) — present but thin (see Quick Wins). ✅ `README.md`
(3.3 KB), `catalog-page.md`. ✅ `iac/pulumi/README.md`, `iac/tf/README.md`,
`iac/hack/manifest.yaml` present.

**Scores:** 11.50% / 13.18% (research doc), 13.09%, 10.09% — otherwise full credit.

---

### 9. Nice to Have (10%)

✅ `BUILD.bazel` files generated.

---

### 10. Presets (5.00%)

✅ Three ranked pairs (`01-read-pool-basic`, `02-read-pool-ha`,
`03-read-pool-production`), companion `.md` files, no duplicate ranks, `apiVersion`/
`kind` consistent with `api.proto`, `StringValueOrRef` fields use `value:` wrapper.

> Note: live `planton validate` could not be run for this kind — the installed CLI
> binary predates enum 639 and reports "unsupported cloud resource kind". Presets
> verified by inspection; structural correctness confirmed.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

No secret-bearing fields in the spec (auth is IAM/connector-based; no passwords).
`planton secret-coverage --output json` reports **zero** `gaps[]` entries prefixed
`GcpAlloydbInstance:`.

---

**Audit completed at:** 2026-07-04 23:30:00
**Reference:** `architecture/deployment-component.md`
