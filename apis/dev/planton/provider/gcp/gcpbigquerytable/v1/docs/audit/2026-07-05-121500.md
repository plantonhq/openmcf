# Audit Report: GcpBigQueryTable

**Audit Date:** 2026-07-05 12:15:00
**Component Kind:** GcpBigQueryTable
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpbigquerytable/v1/`
**Enum Value:** 653
**ID Prefix:** `gcpbqtbl`

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `GcpBigQueryTable = 653` registered (`provider: gcp`, `version: v1`,
`id_prefix: "gcpbqtbl"` — verified unique against all registry prefixes)
with `prerequisites: [GcpBigQueryDataset]`, which lets the E2E harness
install the dataset chain automatically. 653 is the first free slot in the
analytics sub-band (650–652 used). Kind map regenerated
(`pkg/crkreflect/kind_map_gen.go`).

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Correct hierarchy and lowercase naming; `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Proto files:** `api.proto` (912 B), `spec.proto` (28.3 KB),
`stack_input.proto` (406 B), `stack_outputs.proto` (986 B). One kind covers
all four arms of the single provider resource (`google_bigquery_table`):
native tables, logical views, materialized views, and external/BigLake
tables. Modeled at the released 6.50.0 floor: JSON-string `schema`
(provider-authentic — a structured re-model of recursively nested RECORDs
would be lossy), time XOR range partitioning, clustering (≤4),
`require_partition_filter` (top level only — the nested copy is
provider-deprecated), CMEK ref → GcpKmsKey, the full
`external_data_configuration` (10 source formats, csv/json/sheets/hive/
avro/parquet/bigtable options, object tables, BigLake `connection_id` as a
documented plain string), `table_constraints` (PK + FKs with
`referenced_table.table_id` as a self-kind ref), `table_replication_info`,
`biglake_configuration`, `schema_foreign_type_info`,
`external_catalog_table_options`, and `deletion_protection` defaulting
TRUE. Five cross-field CEL rules: one-partitioning-method, one-table-arm,
logical-view-has-no-native-shape, materialized-view-has-no-schema, plus
nested rules (range coherence, bigtable qualifier XOR, format XOR
object_metadata).

✅ **Stubs:** all `.pb.go` files generated.

✅ **Tests:** `spec_test.go` — 47 cases covering every arm (native,
view, materialized view, external CSV/parquet/hive/object/bigtable),
every CEL rule's accept and reject sides, and constraint/replication/
BigLake shapes; `go test ./apis/dev/planton/provider/gcp/gcpbigquerytable/v1/` — **ok**.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ `module/main.go`, `module/locals.go`, `module/table.go`,
`module/external_data_configuration.go`, `module/outputs.go`; root
`main.go` is `package main`; canonical `Pulumi.yaml`; Makefile present;
release-equivalent build passes. `deletion_protection` sent explicitly
(default TRUE — the destroy-parity contract); `use_legacy_sql` always sent
explicitly on the view arm; API enablement; `planton-ai_*` labels; ambient
project fallback. Every planned surface exists on the pinned
`pulumi-gcp/sdk/v9 v9.29.0` (verified in the module cache).

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files + README present; `tofu validate` — **Success**; provider
`~> 6.0` (GA/beta provider files are byte-identical for this resource);
converter-contract variables; the full external-data nested surface
mirrored with dynamic blocks; presence-tracked CSV `quote` handled
identically to Pulumi (unset → API-default double-quote, explicit "" →
unquoted); offline `planton tofu plan` through the real converter
inspected (labels, partitioning, deletion guard all present).

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified with `planton validate-outputs` on BOTH module dirs (7/7 proto
fields populated in the live E2E VERIFY-OUT phase on both engines).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project fallback | omit arg when empty | empty → null → provider default | ✅ MATCH |
| API enablement | `bigquery.googleapis.com`, disable_on_destroy=false | same | ✅ MATCH |
| Labels | user merged beneath `planton-ai_*` | identical merge order | ✅ MATCH |
| deletion_protection | explicit, default TRUE | explicit, default TRUE | ✅ MATCH |
| view.use_legacy_sql | always sent explicitly | always sent explicitly | ✅ MATCH |
| CSV quote presence semantics | unset → `"`, explicit "" honored | identical | ✅ MATCH |
| Empty-means-unset scalars | omitted (nil ptr) | empty/zero → null | ✅ MATCH |
| Arm coverage | all four arms + constraints/replication/BigLake/catalog | same | ✅ MATCH |
| Stack outputs → proto | 7 keys | same 7 keys | ✅ MATCH |

**Effect on score:** none.

---

### 6–8. Documentation & Supporting Files

✅ `docs/README.md` — research: one-kind-four-arms rationale (mirrors the
single provider resource), 90/10 coverage table, recorded skips with
reasons (`deletion_policy` + `csv_options.source_column_match` absent from
released 6.50.0 — schema-probe verified; `ignore_auto_generated_schema` /
`ignore_schema_changes` / `table_metadata_view` are TF-client drift knobs,
not cloud state — parity-neutral skip).
✅ `README.md` + `catalog-page.md` — user-facing with the dataset
cross-link and configuration reference.
✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all
present.

**Scores:** 13.18%, 13.09%, 10.09% — full credit.

---

### 9. Nice to Have (10%)

✅ `BUILD.bazel` files present (kind package, pulumi lib/binary, module).

---

### 10. Presets (5.00%)

✅ Three ranked pairs (`01-partitioned-analytics`, `02-authorized-view`,
`03-external-gcs-table`), companion `.md` files, `my-` names, all pass
`planton validate`.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

No secret-bearing fields on this spec (connection IDs are resource paths,
not credentials); `secret-coverage --check` passes.

---

## Live E2E Status

Dual-engine live run on `planton-e2e` — **4/4 green** (2026-07-05):
`partitioned-native` (day partitions + clustering + partition-filter guard
on the dataset prerequisite chain) + `literal-view` (self-contained
literal-SELECT view, no base table needed) × pulumi/terraform, all six
phases each. Verifier: `aa_e2e/verify/big_query_table.go` (bigquery v2
tables.get + type/partitioning posture). Recorded live exclusions: CMEK
(needs an org-level KMS grant to the BigQuery service agent) and the
external-GCS arm (needs staged object data) — both proven offline. Zero
orphans (post-run dataset list empty).

---

**Audit completed at:** 2026-07-05 12:15:00
**Reference:** `architecture/deployment-component.md`
