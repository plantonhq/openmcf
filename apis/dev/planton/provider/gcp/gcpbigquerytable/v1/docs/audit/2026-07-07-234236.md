# Touch Audit: GcpBigQueryTable (extend-only output addition)

**Audit Date:** 2026-07-07 23:42:36
**Component Kind:** GcpBigQueryTable
**Scope:** targeted re-verification after the `qualified_name` stack output was added. The full-component verdict of the prior audit (`2026-07-05-121500.md`, Fully Complete PARITY ✅) is unchanged; this touch covers only the delta.

---

## What changed

- `stack_outputs.proto` gained `qualified_name` (field 8, extend-only — no existing output changed): the dotted `{project}.{dataset}.{table}` handle, pre-assembled so consumers that address tables in SQL-style dotted form (Pub/Sub BigQuery delivery, query tooling) never do string assembly. The pre-assembled-handle precedent is `GcpServiceAccount.member`.
- Both engines export it from the created resource's attributes (correct under the ambient-project fallback): Terraform via interpolation in `outputs.tf`, Pulumi via `pulumi.Sprintf` in `module/table.go`.
- `GcpPubSubSubscription.bigquery_config.table` now carries `default_kind = GcpBigQueryTable` with `default_kind_field_path = "status.outputs.qualified_name"` — the first inbound FK consuming the new output.

## Parity verdict for the delta: PARITY ✅

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| qualified_name assembly | `Sprintf("%s.%s.%s", project, dataset_id, table_id)` from resource attributes | `"${project}.${dataset_id}.${table_id}"` from resource attributes | ✅ MATCH |

## Validation evidence

- Both engines build (`go build` + release-equivalent Pulumi entrypoint).
- `validate-outputs` green on BOTH module dirs.
- The `pkg/outputs` conformance case extended with `qualified_name` in `mustPopulate`; suite green.
- `validate-refs --check` green with the new inbound reference.
