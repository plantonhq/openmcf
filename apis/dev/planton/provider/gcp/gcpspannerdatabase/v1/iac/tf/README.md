# Terraform module for GcpSpannerDatabase

Provisions a `google_spanner_database` from the validated protobuf spec. Enables `spanner.googleapis.com` automatically.

See the component [README](../../README.md) for the full configuration reference.
