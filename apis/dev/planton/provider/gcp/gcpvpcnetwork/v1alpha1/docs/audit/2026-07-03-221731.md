# Audit Report: GcpVpcNetwork

**Audit Date:** 2026-07-03 22:17:31  
**Component Kind:** GcpVpcNetwork  
**Provider:** gcp  
**Component Path:** `apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1/`  
**Enum Value:** 610  
**ID Prefix:** gcpvpcnetwork  
**Audit Mode:** `--parity` (parity-led full audit)

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Parity Verdict (lead finding — `--parity` run)

**PARITY ✅** — every dimension MATCHES against the proto contract. One cosmetic
(non-outcome-changing) drift was found and **fixed during this audit**: the Terraform
module's unused `gcp_labels` local used ad-hoc keys (`resource`, `resource-id`,
`resource-org`, `env`) instead of the Pulumi engine's `gcplabelkeys` set
(`planton-ai_*`). Because `google_compute_network` accepts no labels, neither engine
ever applied the map, so deployed outcomes were already identical — but the drift was
aligned anyway so a future labeled sub-resource inherits an identical identity set on
both engines. See §5b for the full dimension table.

---

## Quick Wins

None outstanding. Two housekeeping items were applied during this audit:

1. **Aligned `iac/tf/locals.tf` label keys to the Pulumi `gcplabelkeys` set**
   - **Why:** Removes latent cross-engine label drift (cosmetic today, defect-in-waiting).
   - **How:** Rewrote `local.gcp_labels` as a `merge()` of the `planton-ai_*` keys with the same conditional org/env/id presence as the Pulumi module. `tofu validate` re-run: green.
   - **File:** `apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1/iac/tf/locals.tf`

2. **Removed stale empty `v1/hack/` directory**
   - **Why:** The hack manifest moved to `iac/hack/manifest.yaml` this session; the empty old directory was leftover.
   - **How:** `rmdir` (was empty).
   - **File:** `apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1/hack/` (removed)

---

## Critical Gaps

None. No blocking issues found.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpVpcNetwork = 610` exists in `apis/dev/planton/shared/cloudresourcekind/cloud_resource_kind.proto`
- Enum value 610 is in the correct GCP range (600–799)
- `id_prefix: "gcpvpcnetwork"` is unique (single occurrence in the registry)
- `kind_meta` includes provider (`gcp`), version (`v1`), id_prefix, and `container_kind: true`
- Downstream kinds reference it as a prerequisite (`GcpSubnetwork`, `GcpServiceNetworkingConnection`, `GcpAddress`) — consistent with the composition redesign

❌ **Failed:**

- None

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists: `apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1/`
- Under correct provider hierarchy (`gcp`, non-Kubernetes)
- Folder name is the lowercase enum name
- `v1/` subfolder exists

❌ **Failed:**

- None

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files (13.32%)

✅ **Passed:**

- `api.proto` exists (820 B — small but complete: envelope + status only, correct constants `gcp.planton.dev/v1alpha1` / `GcpVpcNetwork`)
- `spec.proto` exists (4.6 KB) — 12 fields covering the provider-floor depth: `project_id` (FK to GcpProject), `auto_create_subnetworks`, `routing_mode` (optional enum, default REGIONAL), `network_name`, `description`, `mtu` (1300–8896), `enable_ula_internal_ipv6`, `internal_ipv6_range`, `network_firewall_policy_enforcement_order`, `network_profile`, `bgp_best_path_selection` (mode / always_compare_med / inter_region_cost), `delete_default_routes_on_create`
- `stack_input.proto` exists (355 B)
- `stack_outputs.proto` exists (843 B) — 5 fields: `network_self_link`, `network_name`, `network_id`, `gateway_ipv4`, `internal_ipv6_range`
- Private-services-access removal is documented in the spec header (composed from GcpGlobalAddress + GcpServiceNetworkingConnection)
- CEL messages are semantic and enumerate allowed values (`mode must be LEGACY or STANDARD`, `inter_region_cost must be DEFAULT or ADD_COST_TO_MED`, `network_firewall_policy_enforcement_order must be BEFORE_CLASSIC_FIREWALL or AFTER_CLASSIC_FIREWALL`)

⚠️ **Warnings:**

- `network_name` uses a bare `string.pattern` constraint (no CEL wrapper), so a violation surfaces protovalidate's default message rather than a hand-written sentence. The field comment does spell out the naming rule in plain English, and the pattern is the standard RFC1035 rule, so this is noted, not penalized.

**Score:** 13.32% / 13.32%

#### 3.2 Generated Stubs (3.33%)

✅ **Passed:**

- `api.pb.go`, `spec.pb.go`, `stack_input.pb.go`, `stack_outputs.pb.go` all exist and are newer than or contemporaneous with their protos (regenerated 2026-07-03 21:55, after the last spec edit at 21:40)

**Score:** 3.33% / 3.33%

#### 3.3 Unit Tests - Presence (2.77%)

✅ **Passed:**

- `spec_test.go` exists (2.3 KB)
- Contains Ginkgo/Gomega test functions covering: minimal valid spec, missing `network_name`, invalid `network_name` format
- Imports `testing`, `ginkgo/v2`, `gomega`, `protovalidate`

**Score:** 2.77% / 2.77%

#### 3.4 Unit Tests - Execution (2.78%)

✅ **Passed (re-run during this audit):**

```bash
go test ./apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1/
# ok  github.com/plantonhq/planton/apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1  0.620s
```

**Score:** 2.78% / 2.78%

**Total Protobuf API Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

#### 4.1 Module Files (6.66%)

✅ **Passed:**

- `iac/pulumi/module/main.go` exists (1.0 KB) — commented entry point
- `iac/pulumi/module/locals.go` exists (1.7 KB) — labels strategy documented
- `iac/pulumi/module/vpc.go` exists (3.4 KB) — carries why-comments for the omit-when-empty convention and the Compute-API-enablement ordering (comment added during this audit to meet the module-comment bar)
- `iac/pulumi/module/outputs.go` exists — output-name constants matching the proto field names

❌ **Failed:**

- None

**Score:** 6.66% / 6.66%

#### 4.2 Entrypoint Files (6.66%)

✅ **Passed:**

- `iac/pulumi/main.go` exists at the root and declares `package main`
- No `iac/pulumi/main/` and no `iac/pulumi/entrypoint/` subdirectory
- `iac/pulumi/Pulumi.yaml` exists
- `iac/pulumi/Makefile` exists
- Release-equivalent build passes (re-run during this audit, after the comment edit):

```bash
go build -o /dev/null ./apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1/iac/pulumi
# BUILD_OK
```

**Score:** 6.66% / 6.66%

**Total Pulumi Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `iac/tf/variables.tf` exists (2.7 KB, curated `optional()` form, per-field comments, two `validation` blocks mirroring the proto CELs)
- `iac/tf/provider.tf` exists — google provider pinned `~> 6.0`
- `iac/tf/locals.tf` exists — commented (project-id fallback rationale, label-parity rationale)
- `iac/tf/main.tf` exists (1.8 KB) — omit-when-empty conditionals for every optional field
- `iac/tf/outputs.tf` exists — 5 outputs matching the proto field names, with descriptions
- `tofu validate` green (re-run during this audit after the `locals.tf` edit)

❌ **Failed:**

- None

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/`) and Terraform (`iac/tf/`), both judged
against the proto contract (`spec.proto` + `stack_outputs.proto`); neither engine
privileged. Output parity verified with the automated guards, not by eye:
`pkg/outputs/conformance_test.go` `GcpVpcNetwork` case PASS (re-run this audit), and
`planton validate-outputs` dry-run 5/5 fields populated on both engines (verified this
session, zero unmapped).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source (project) | `spec.project_id` value; omitted when empty → provider default | `local.project_id` = `var.spec.project_id` or `null` → provider default | ✅ MATCH |
| Resource naming basis | `spec.network_name` | `var.spec.network_name` | ✅ MATCH |
| Labels | `gcplabelkeys` set (`planton-ai_*`), unused — `google_compute_network` takes no labels | same key set after fix (was ad-hoc keys, also unused) | ✅ MATCH (drift fixed) |
| Pod/service selectors | n/a (GCP network resource, no workloads) | n/a | ✅ N/A |
| Spec feature coverage | all 12 spec fields honored | all 12 spec fields honored | ✅ MATCH |
| API enablement | `projects.Service` compute.googleapis.com, disable_dependent_services=true, disable_on_destroy=false | `google_project_service` identical flags | ✅ MATCH |
| Dependency ordering | `pulumi.DependsOn(computeService)` → network | `depends_on = [google_project_service.compute_api]` | ✅ MATCH |
| Stack outputs → proto | `network_self_link`, `network_name`, `network_id`, `gateway_ipv4`, `internal_ipv6_range` | identical 5 outputs | ✅ MATCH (conformance green) |

**Legend:** ✅ MATCH | ⚠️ DIVERGES (undocumented) | 🔵 EXCEPTION (documented `PARITY-EXCEPTION:`)

Field-by-field spec coverage (both engines, omit-when-empty so the API applies its defaults):

- `project_id` — set only when non-empty (Pulumi `if GetValue() != ""`; TF `!= "" ? ... : null`) ✅
- `auto_create_subnetworks` — always sent (bool, default false = custom mode) ✅
- `routing_mode` — same deployed outcome; Pulumi omits when REGIONAL (API default is REGIONAL), TF sends the resolved value explicitly. Cosmetic wire difference, identical result ✅
- `network_name` — always sent ✅
- `description` — omit when empty ✅
- `mtu` — omit when null (API default 1460) ✅
- `enable_ula_internal_ipv6` — sent only when true ✅
- `internal_ipv6_range` — omit when empty ✅
- `network_firewall_policy_enforcement_order` — omit when empty (API default AFTER_CLASSIC_FIREWALL) ✅
- `network_profile` — omit when empty ✅
- `bgp_best_path_selection.{mode,always_compare_med,inter_region_cost}` — each omitted when empty/false on both engines ✅
- `delete_default_routes_on_create` — sent only when true ✅

Recorded provider-surface skips (intentional, both engines): `params.resource_manager_tags`, `numeric_id` (output), `deletion_policy`.

✅ **In parity:**

- All dimensions above.

⚠️/❌ **Divergences (undocumented):**

- One found and **fixed in this audit**: the TF `gcp_labels` local used ad-hoc keys instead of the Pulumi `gcplabelkeys` set. Impact: cosmetic (map is unused; the network resource accepts no labels). Fix: aligned `iac/tf/locals.tf` to the `planton-ai_*` keys with identical conditional presence; `tofu validate` green.

**Effect on score:** none — the drift was output-neutral and is now fixed; the gate does not cap Categories 4 & 5.

---

### 6. Documentation - Research (13.18%)

✅ **Passed:**

- `docs/README.md` exists (20.7 KB — comprehensive, above the 10 KB bar)
- Landscape analysis present (maturity spectrum Levels 0–5: console, CLI, Deployment Manager, Terraform/OpenTofu, Pulumi, Crossplane)
- Comparative analysis and tool-selection section present
- Planton's approach and 90/10 philosophy explained ("Guard Rails, Not Handcuffs", when-to-use section)
- Architecture patterns documented (single-region, multi-region shared, GKE-ready)

**Score:** 13.18% / 13.18%

---

### 7. Documentation - User-Facing (13.33% band, weighted 13.09%)

✅ **Passed:**

- `README.md` exists at v1/ level (10.7 KB — well above the 2 KB "good" bar, refreshed this session at 22:08)
- `catalog-page.md` also present (7.3 KB, refreshed 22:07)

**Score:** 13.09% / 13.09%

---

### 8. Supporting Files (10.09%)

✅ **Passed:**

- `iac/pulumi/README.md` exists (9.5 KB)
- `iac/tf/README.md` exists (14.2 KB)
- `iac/hack/manifest.yaml` exists (moved from the old `v1/hack/` location this session; the stale empty `v1/hack/` dir was removed during this audit)
- `e2e/` present: `profile.yaml`, `scenarios/minimal.yaml`, `prerequisite.yaml`

❌ **Failed:**

- None

**Score:** 10.09% / 10.09%

---

### 9. Nice to Have (10%)

✅ **Passed:**

- `BUILD.bazel` at v1/, `iac/pulumi/`, and `iac/pulumi/module/` — all present and regenerated after the module changes (21:46)

**Score:** 10.00% / 10.00%

---

### 10. Presets (5.00%)

#### 10.1 Directory and File Checks (2.50%)

✅ **Passed:**

- `presets/` directory exists
- 2 preset pairs: `01-custom-mode-regional.{yaml,md}`, `02-custom-mode-global.{yaml,md}`
- Every `.yaml` has a companion `.md`; no orphans
- Naming follows `{NN}-{kebab-case}.yaml`; no duplicate ranks

**Score:** 2.50% / 2.50%

#### 10.2 Content Correctness (2.50%)

✅ **Passed:**

- `apiVersion: gcp.planton.dev/v1alpha1` and `kind: GcpVpcNetwork` match the `api.proto` constants in both presets
- `metadata.name` present and `my-`-prefixed (`my-production-vpc`, `my-global-vpc`)
- `spec` present and non-empty; no `status` block
- `projectId` (the only `StringValueOrRef` field) uses the `value:` wrapper form in both presets
- All field names exist in the current `spec.proto` (no stale references to the removed `private_services_access`)
- `routingMode: REGIONAL` in preset 01 matches the proto `default` annotation

**Score:** 2.50% / 2.50%

**Total Presets Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

Ran `planton secret-coverage --output json` during this audit and filtered `gaps[]` to
`GcpVpcNetwork:` — result: **zero gaps**. The spec has no secret-bearing fields (network
configuration only: names, ranges, routing/firewall policy strings, booleans), and no
field name trips the analyzer's heuristic, so no annotations or exemptions are needed.

| Field path | Classification | Note |
|-----------|----------------|------|
| (none) | — | no secret-looking fields in `GcpVpcNetworkSpec` |

**Effect on score:** none.

---

## Verification Performed (this audit run)

| Check | Command | Result |
|-------|---------|--------|
| Spec unit tests | `go test ./apis/.../gcpvpcnetwork/v1/` | ✅ ok (0.62s) |
| Pulumi release build | `go build -o /dev/null ./apis/.../iac/pulumi` | ✅ pass (re-run after edits) |
| Terraform validate | `tofu validate` in `iac/tf/` | ✅ valid (re-run after `locals.tf` edit) |
| Outputs conformance | `go test ./pkg/outputs/ -run TestStackOutputsConformance` | ✅ `GcpVpcNetwork` subtest PASS |
| Secret coverage | `planton secret-coverage --output json` | ✅ zero `GcpVpcNetwork:` gaps |

Additionally relied upon from earlier in this session (not re-run): offline
`planton tofu plan` green via the tfvars converter, live dual-engine E2E pass (leaf
scenario `minimal`), `validate-refs` green, and `planton validate-outputs` dry-run
populating 5/5 proto fields on both engines.

---

## Prioritized Recommendations

### High Priority (Do First)

None — no blocking issues.

### Medium Priority (Do Next)

None.

### Low Priority (Polish)

1. **Wrap `network_name`'s pattern constraint in a CEL rule with a hand-written message**
   - **File:** `apis/dev/planton/provider/gcp/gcpvpcnetwork/v1alpha1/spec.proto`
   - **Why:** A bare `string.pattern` violation surfaces protovalidate's default regex-bearing message; a CEL wrapper would show the plain-English sentence already present in the field comment.
   - **How:** Replace `(buf.validate.field).string.pattern` with a `.cel` rule reusing the same regex, message per the `002-spec-validate` standard.

---

## Comparison to Complete Components

**Most Similar Complete Component:** `GcpGlobalAddress` (`apis/dev/planton/provider/gcp/gcpglobaladdress/v1alpha1/`) — same session's rebuild pattern (provider-floor depth, omit-when-empty conventions, 5-field outputs, dual-engine parity, conformance case). GcpVpcNetwork now matches it in structure and rigor; no gaps relative to it.

---

## Next Steps

1. Nothing blocking — component is release-ready and in parity.
2. (Optional polish) CEL-wrap the `network_name` pattern message.
3. Record `GcpVpcNetwork | 2026-07-03 | PARITY ✅ | none open | docs/audit/2026-07-03-221731.md` in the parity ledger if one is maintained.

---

## Appendix: Full Checklist

### Critical Items (48.64%)

#### Cloud Resource Registry (4.44%)

- [x] Enum entry exists (`GcpVpcNetwork = 610`)
- [x] Enum value in correct range (GCP 600–799)
- [x] Unique id_prefix (`gcpvpcnetwork`)
- [x] Complete kind_meta (provider, version, id_prefix, container_kind)
- [x] Kubernetes metadata — n/a (not a Kubernetes component)

#### Folder Structure (4.44%)

- [x] Correct provider hierarchy
- [x] Lowercase enum-name folder
- [x] `v1/` subfolder

#### Protobuf API Definitions (22.20%)

- [x] `api.proto` / `spec.proto` / `stack_input.proto` / `stack_outputs.proto` present and non-trivial
- [x] All four `.pb.go` stubs present and regenerated
- [x] `spec_test.go` present, imports test framework, has validation tests
- [x] Tests compile and pass

#### Validation Message Quality (part of Proto Files score)

- [x] CEL messages are complete, actionable sentences
- [x] No raw regex as primary user-facing text in CEL messages (one bare `string.pattern` noted as polish)
- [x] Conditional rules explain condition + requirement
- [x] No messages shorter than 5 words
- [x] Messages use semantic field names

#### Module Comment Quality (part of Categories 4 & 5)

- [x] Pulumi module explains why / conventions / ordering (omit-when-empty, API enablement)
- [x] Terraform module explains why (project-id fallback, label parity, per-variable intent)
- [x] Not narration-only

#### Cross-Engine IaC Parity (GATE)

- [x] Scope source matches (project_id, empty → provider default)
- [x] Resource naming basis matches (`spec.network_name`)
- [x] Labels identical (aligned during this audit; map unused by design — network takes no labels)
- [x] Selectors — n/a
- [x] Every `spec.proto` field honored by BOTH engines (12/12)
- [x] Dependency ordering matches (compute API → network)
- [x] Stack outputs flatten onto the same proto fields (conformance test + validate-outputs: zero unmapped)
- [x] No undocumented divergences remain

#### Secure-by-Default Sensitive Coverage (GATE)

- [x] `planton secret-coverage --output json`: zero `GcpVpcNetwork:` gaps
- [x] No sensitive/exempt contradiction
- [x] No secret-bearing fields exist in this spec

### Important Items (41.36%)

- [x] `docs/README.md` comprehensive (20.7 KB, landscape + best practices + 90/10)
- [x] `README.md` at v1/ (10.7 KB)
- [x] `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml`
- [x] Presets: 2 valid pairs, correct naming, correct apiVersion/kind, `value:` wrappers, no stale fields

### Nice to Have (10%)

- [x] BUILD.bazel files present and current

---

**Audit completed at:** 2026-07-03 22:17:31  
**Reference:** `architecture/deployment-component.md` (ideal state), `pkg/iac/MODULE_PARITY.md` (parity dimensions)
