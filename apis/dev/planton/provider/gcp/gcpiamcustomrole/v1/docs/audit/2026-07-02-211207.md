# Component Audit: GcpIamCustomRole (with cross-engine parity)

**Date:** 2026-07-02
**Status: Fully Complete — PARITY ✅**

## Category Scores

| Category | Verdict | Evidence |
|---|---|---|
| 1. Cloud Resource Registry | ✅ | `GcpIamCustomRole = 608` in the GCP band, unique `id_prefix: gcprole`, kind map regenerated |
| 2. Folder Structure | ✅ | `provider/gcp/gcpiamcustomrole/v1/` canonical anatomy |
| 3. Protobuf APIs | ✅ | 4 protos + stubs; CEL messages are full sentences; 16-case spec test suite passes (`go test` green) |
| 4. Pulumi Module | ✅ | `module/{main,locals,custom_role,outputs}.go` + root entrypoint; release-equivalent `go build -o /dev/null ./…/iac/pulumi` green; richly commented (immutability, soft-delete, project fallback) |
| 5. Terraform Module | ✅ | 5 files; `tofu validate` + `planton tofu plan` against `iac/hack/manifest.yaml` green; comment bar matched |
| 5b. Cross-Engine Parity | **✅ GATE PASS** | Same resource (`google_project_iam_custom_role` / `projects.IAMCustomRole`), identical field wiring incl. empty-permission filtering, empty-description omission, provider-default project fallback; outputs verified with `planton validate-outputs` (name, role_id populated; `deleted` false = zero value) + `pkg/outputs` conformance case |
| 6. Research Docs | ✅ | `docs/README.md`: role lifecycle, project-vs-org scope, 90/10 coverage table, composition rationale |
| 7. User Docs | ✅ | `v1/README.md` full reference + `catalog-page.md` |
| 8. Supporting Files | ✅ | Both engine READMEs, `iac/hack/manifest.yaml` (validated) |
| 9. BUILD.bazel | ✅ | Generated by gazelle |
| 10. Presets | ✅ | 3 ranked yaml+md pairs (workload-least-privilege / readonly-auditor / ci-cd-deployer), all `planton validate` green |
| 11. Secret Coverage | **✅ GATE PASS** | `planton secret-coverage --check` green; no secret-bearing fields (role definitions are public metadata) |

## Parity Notes

- Both engines omit `project` when the spec leaves it empty, deferring to the
  provider's default project — the documented spec contract.
- Both engines handle GCP's soft-delete/undelete lifecycle through the provider
  natively; destroy → recreate within the 14-day window converges.
- No `PARITY-EXCEPTION` markers needed.

## E2E

- `e2e/profile.yaml` tier 1; `scenarios/minimal.yaml` + `prerequisite.yaml`
  (referenced by GcpProjectIamMember's dependency chain); verifier
  `aa_e2e/verify/iam_custom_role.go` treats soft-deleted as destroyed.
