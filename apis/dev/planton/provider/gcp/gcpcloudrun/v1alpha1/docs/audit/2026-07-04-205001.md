# Audit Report: GcpCloudRun

**Date:** 2026-07-04
**Component:** `GcpCloudRun` (enum 603, provider `gcp`, id_prefix `cldrun`)
**Path:** `apis/dev/planton/provider/gcp/gcpcloudrun/v1alpha1/`
**Audit type:** Full audit with parity focus (`--parity`)

---

## Verdict

**Score: 100% — Fully Complete. PARITY ✅. COVERAGE ✅.**

Both IaC engines implement the same contract at the released google 6.50.0 floor
(verified by schema dump, not the provider clone's main); zero undocumented
divergences; the `pkg/outputs` conformance case and `validate-outputs` dry-runs
pass on BOTH module directories; secret coverage is green with two honest
exemptions (Secret Manager secret names, KMS key ID).

---

## Category Results

| # | Category | Result | Notes |
|---|----------|--------|-------|
| 1 | Cloud Resource Registry | ✅ 4.44% | Enum 603 in the GCP band; unique `cldrun` prefix; kind_meta complete |
| 2 | Folder Structure | ✅ 4.44% | `gcpcloudrun/v1/` under the gcp provider tree |
| 3 | Protobuf API Definitions | ✅ 22.20% | 4 protos + stubs; 27 messages/enums; 12 CEL rules with sentence-quality messages; **81 spec-test cases, all passing** |
| 4 | IaC Modules — Pulumi | ✅ 13.32% | Root `package main` entrypoint; release-equivalent build passes; authoring-comment bar met |
| 5 | IaC Modules — Terraform | ✅ 4.44% | All five files; `tofu validate` clean; offline `planton tofu plan` through the real tfvars converter inspected field-by-field |
| 5b | Cross-Engine Parity | **PARITY ✅** | See dimension table below |
| 6 | Documentation — Research | ✅ 13.34% | `docs/README.md`: landscape levels, coherence-rule table, modeled-surface table, recorded-skips ledger, best practices |
| 7 | Documentation — User-Facing | ✅ 13.33% | `README.md` with overview/features/outputs/skips/related components |
| 8 | Supporting Files | ✅ 13.33% | Both module READMEs; canonical `iac/hack/manifest.yaml` (the stale wrong-kind manifest under `iac/tf/hack/` was removed) |
| 9 | Build Files | ✅ 10% | BUILD.bazel generated |
| 10 | Presets | ✅ 5.00% | 3 yaml+md pairs; all pass `planton validate`; refs use `valueFrom` with realistic replace-me values |
| 11 | Sensitive Coverage | **COVERAGE ✅** | `secret-coverage --check` green; exemptions: `env[].value_from_secret.secret`, `volumes[].secret.secret` (secret NAMES), `encryption_key` (KMS key ID) |

---

## Category 5b: Cross-Engine Parity (dimension by dimension)

| Dimension | Verdict | Evidence |
|---|---|---|
| Scope/project source | MATCH | Both read `spec.project_id` with the ambient fallback (TF `local.project_id` null-fallback; Pulumi omits `Project` when empty) |
| Resource naming basis | MATCH | Both name the service `spec.service_name` else `metadata.name` (TF `local.service_name`; Pulumi `locals.ServiceName`) |
| Labels | MATCH | Identical `planton-ai_*` keys/values, kind = lowercase enum string, user labels merged beneath attribution on both sides |
| Spec feature coverage | MATCH | Every spec field wired on both engines: containers (env/secret refs, ports+h2c, resources+cpu_idle+boost, mounts, both probes, depends_on), all five volume sources, both scaling layers, traffic, vpc_access (connector XOR interfaces), GPU, CMEK, binary auth, launch stage, custom audiences, session affinity, invoker toggles |
| Auth mechanism | MATCH | `allow_unauthenticated` → `roles/run.invoker` to `allUsers` as an IAM member on BOTH engines; `invoker_iam_disabled` → the API field on both (the prior Pulumi module conflated the two — fixed) |
| Enum translation | MATCH | Spec enums use provider value names; both engines pass them through untranslated, UNSPECIFIED → omitted (the prior Pulumi ingress map emitted an invalid LB value — eliminated by construction) |
| Defaults honesty | MATCH | `deletion_protection` unset → true on both; unset timeout/concurrency omitted (GCP defaults) on both; `timeout_seconds` → `"Ns"` identically |
| Dependency ordering | MATCH | run.googleapis.com enablement (`disable_on_destroy=false`) → service → invoker grant on both |
| Stack outputs | MATCH | `validate-outputs` dry-run 6/6 on BOTH module dirs; `pkg/outputs` conformance case passes; outputs read the same provider attributes (`uri`, `name`, `latest_ready_revision`, `location`, `uid`, `urls`) |

**Zero PARITY-EXCEPTIONs needed.**

---

## Validation Evidence

- `go test ./apis/dev/planton/provider/gcp/gcpcloudrun/v1alpha1/` — 81/81 pass
- `go build -o /dev/null ./apis/.../v1/iac/pulumi` — release-equivalent build passes
- `tofu validate` + offline `planton tofu plan` (hack manifest through the real tfvars converter; plan inspected) — clean
- `planton secret-coverage --check` — green (the kind's former baseline gap entry removed)
- `planton validate-refs --check` — all foreign keys resolve
- `planton validate-outputs --kind GcpCloudRun` — 6/6 both engines
- Every preset/hack/scenario manifest — `planton validate` green
- Live E2E: `minimal` scenario create → verify (Run Admin API: Ready condition, URL, latest-ready revision, 100% traffic) → destroy on BOTH engines, zero orphans
- Live E2E recorded exclusion: direct VPC egress (GCP holds a serverless address reservation in the subnetwork for 1-2 hours after service deletion — incompatible with ephemeral teardown; surface offline-proven)

## Recorded Skips (README ledger)

`readiness_probe`, `iap_enabled`, `default_uri_disabled`, `deletion_policy`,
`multi_region_settings`, `health_check_disabled`, `gcs.mount_options` (all
main-only, absent from released 6.50.0); `mesh` (beta-only); `build_config` /
`base_image_uri` (source-deploy surface); domain mapping (separate-lifecycle
resource — the composed LB family is the custom-domain path); `client` /
`client_version`, `annotations`; per-identity invoker IAM.
