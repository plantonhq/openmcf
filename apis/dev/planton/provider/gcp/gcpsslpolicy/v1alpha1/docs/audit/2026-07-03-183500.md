# Audit Report: GcpSslPolicy

**Audit Date:** 2026-07-03 18:35:00
**Component Kind:** GcpSslPolicy
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpsslpolicy/v1alpha1/`
**Enum Value:** 711
**ID Prefix:** gcpsslpol

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpSslPolicy = 711` in the 710–719 networking/LB overflow block; `id_prefix` `gcpsslpol` unique (full-registry scan); no prerequisites (true leaf). Kind map regenerated.

### Protobuf API Definitions

- Full released 6.50.0 GA surface on plain `google ~> 6.0` (GA and beta released schemas verified identical for this resource): `project_id` ref → GcpProject, RFC1035 `ssl_policy_name`, dual-scope `region` (empty = global, set = regional — schemas field-for-field identical, the fold-when-identical test), immutable `description` (ForceNew on this resource, a GCP quirk documented in the field comment), `profile` (COMPATIBLE/MODERN/RESTRICTED/CUSTOM), `min_tls_version` (TLS_1_0/1_1/1_2), `custom_features` (uppercase suite-name pattern).
- Message-level CEL enforces the provider's CustomizeDiff pairing both ways pre-deploy: CUSTOM requires `custom_features`; `custom_features` requires CUSTOM.
- **Deliberately unmodeled:** `post_quantum_key_exchange`, `FIPS_202205`, `TLS_1_3` minimum (all unreleased-main only), `deletion_policy` (catalog-wide client-side-lever skip). Recorded in `docs/README.md`.
- 21-case spec test suite green (positive per profile/version, CUSTOM pairing both directions, unreleased-value rejections, name/region/description bounds, kind/api_version constants).

### IaC Modules — Pulumi

- `compute.SSLPolicy` / `compute.RegionSslPolicy` branch on `spec.region`, mirroring the Terraform count guards; Compute API enablement with `disable_on_destroy=false`; ambient-project contract honored (empty project_id → provider default); unset enum-likes fall through to API defaults (COMPATIBLE / TLS_1_0); `custom_features` only sent when non-empty; region output exports the plain spec region name.
- Release-equivalent build green.

### IaC Modules — Terraform

- Same two resources under count guards on plain `google ~> 6.0`; variable validations mirror the CEL (profile/min-TLS enums, CUSTOM pairing); `tofu validate` + offline `planton tofu plan` through the real tfvars converter green (plan inspected: policy + API service, outputs wired).

### Cross-Engine IaC Parity — PARITY ✅

- Field-by-field: name, project, description, profile, min_tls_version, custom_features, region — identical send/omit semantics ("" → null/unset) in both engines. Outputs identical keys and semantics (`self_link`, `ssl_policy_name`, `enabled_features`, plain-name `region`). API enablement identical. Zero PARITY-EXCEPTIONs.
- `validate-outputs` dry-runs fully populated against BOTH module dirs; `pkg/outputs` conformance case covers the repeated `enabled_features`.

### Secure-by-Default Coverage — COVERAGE ✅

- No secret-bearing fields (TLS version/cipher configuration is public posture); `secret-coverage --check` green.

### E2E

- Two scenarios cover both module branches: global (MODERN/TLS_1_2) and regional (RESTRICTED/TLS_1_2, us-central1). Live dual-engine: Pulumi 64.7s, Terraform 87.4s — both green, zero orphans. Verifier asserts existence AND a non-empty enabled-features set (the hardening actually landed). `e2e/prerequisite.yaml` published for composed proxy scenarios; consumed live by the target HTTPS proxy hardened-frontend scenario via the field's `default_kind`.
