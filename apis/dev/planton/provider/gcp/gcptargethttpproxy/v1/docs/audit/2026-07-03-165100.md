# Audit Report: GcpTargetHttpProxy

**Audit Date:** 2026-07-03 16:51:00
**Component Kind:** GcpTargetHttpProxy
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcptargethttpproxy/v1/`
**Enum Value:** 629
**ID Prefix:** gcpthp

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpTargetHttpProxy = 629` (fills the 623–629 LB sub-band); `id_prefix` `gcpthp` unique; `prerequisites: [GcpUrlMap]` drives the composed E2E chain transitively through the backend service and health check.

### Protobuf API Definitions

- Full released 6.50.0 GA surface on plain `google ~> 6.0`: `url_map` (required ref, the resource's only mutable field), `http_keep_alive_timeout_sec` (5-1200 CEL, EXTERNAL_MANAGED-only documented), `proxy_bind` (Traffic Director), RFC1035 `proxy_name` defaulting to `metadata.name`.
- **Deliberately unmodeled:** `deletion_policy` (absent from the released 6.x line; Planton owns destroy semantics), `timeouts` (operation plumbing). Recorded in `docs/README.md`.
- 21-case spec test suite green (happy paths + every validation error path).

### IaC Modules (both engines)

- Both enable `compute.googleapis.com` first (`disable_on_destroy=false`); ambient-project contract (empty → provider default); zero-means-unset keep-alive; only-true `proxy_bind`.
- Release-equivalent Pulumi build green; `tofu validate` + offline `planton tofu plan` (2 to add) through the real tfvars converter.

### Cross-Engine IaC Parity (Category 5b gate)

Identical naming basis (`proxy_name` → `metadata.name` fallback), full spec coverage, identical null/omit semantics per field, identical outputs (`self_link`, `proxy_name`, `proxy_id`, `fingerprint`; `proxy_id` stringified in both). Conformance case in `pkg/outputs` (4/4 both engines). No PARITY-EXCEPTIONs. **PARITY ✅**

### Secure-by-Default (Category 11 gate)

No secret-bearing fields; `planton secret-coverage --check` green. **COVERAGE ✅**

### Supporting Files, Presets, Docs, E2E

- 3 presets (https-redirect frontend, plain-http frontend, Traffic Director mesh) — all pass `planton validate`.
- Composed live E2E green on BOTH engines (Pulumi 3m50s, Terraform 4m20s) on the 4-prerequisite chain (health check → NEG → backend service → URL map); verifier asserts URL-map wiring; zero orphans after teardown.
