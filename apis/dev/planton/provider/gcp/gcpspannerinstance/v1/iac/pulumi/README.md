# Pulumi module for GcpSpannerInstance

Provisions a `spanner.Instance` from the validated protobuf spec. Enables `spanner.googleapis.com` automatically.

See the component [README](../../README.md) for the full configuration reference.
