# Terraform module for GcpSpannerBackupSchedule

Provisions a `google_spanner_backup_schedule` from the validated protobuf spec. Enables `spanner.googleapis.com` automatically.

See the component [README](../../README.md) for the full configuration reference.
