# Pulumi module for GcpSpannerBackupSchedule

Provisions a `spanner.BackupSchedule` from the validated protobuf spec. Enables `spanner.googleapis.com` automatically.

See the component [README](../../README.md) for the full configuration reference.
