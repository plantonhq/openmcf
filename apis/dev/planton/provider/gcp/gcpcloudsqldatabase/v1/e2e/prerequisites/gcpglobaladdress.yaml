# Consumer-scoped GcpGlobalAddress install profile for the database chain.
# The transitive prerequisite graph (database → instance → service networking
# connection) needs an INTERNAL VPC_PEERING range, not the address kind's
# published EXTERNAL VIP profile.
apiVersion: gcp.planton.dev/v1
kind: GcpGlobalAddress
metadata:
  name: planton-oss-e2e-gcpgip-psa-prereq
  id: planton-oss-e2e-gcpgip-psa-prereq
  org: planton-oss
  env: e2e
  labels:
    managed-by: planton-e2e
    e2e-component: gcpcloudsqldatabase
  annotations:
    planton.dev/e2e: "true"
  tags:
    - planton-e2e
spec:
  description: Ephemeral VPC_PEERING range for the Cloud SQL database E2E chain; safe to delete
  addressName: planton-oss-e2e-gcpgip-${E2E_RUN_ID}
  addressType: INTERNAL
  purpose: VPC_PEERING
  prefixLength: 16
  network:
    valueFrom:
      kind: GcpVpc
      name: planton-oss-e2e-gcpvpc-prereq
      fieldPath: status.outputs.network_self_link
