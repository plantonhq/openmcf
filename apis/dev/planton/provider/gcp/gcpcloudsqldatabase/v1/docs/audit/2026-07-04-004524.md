# Audit Report: GcpCloudSqlDatabase

**Audit Date:** 2026-07-04 00:45:24
**Component Kind:** GcpCloudSqlDatabase
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/`
**Enum Value:** 637
**ID Prefix:** gcpsqldb
**Run Mode:** `--parity` (parity-focused audit; Category 5b leads)

---

## Overall Completion Score

**Score: 93.41%**

```
█████████░ 93% Complete
```

**Status:** Functionally Complete (80-99% band) — minor improvements needed

**Gate verdicts (recorded explicitly):**

- **Cross-Engine IaC Parity (Category 5b): PARITY ✅** — every dimension MATCHES; zero undocumented divergences.
- **Secure-by-Default Sensitive Coverage (Category 11): COVERAGE ✅** — `planton secret-coverage --check` passes repo-wide; this kind has no secret-bearing fields and no `gaps[]` entry.

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 6.59% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

**Totals:** Critical 48.64/48.64 + Important 34.77/41.36 + Nice to Have 10.00/10.00 = **93.41%**

---

### 5b. Cross-Engine IaC Parity (Critical GATE) — LEAD FINDING

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/`) and Terraform (`iac/tf/`), both judged
against the proto contract (`spec.proto` + `stack_outputs.proto`) and intended behavior —
neither engine privileged. Stack-output parity is verified by the automated guard, not by
eye: `pkg/outputs/conformance_test.go` carries a `GcpCloudSqlDatabase` case
(`mustPopulate: ["database_name", "self_link"]`) that passes (verified this session).

| Dimension | Pulumi (`module/database.go`) | Terraform (`main.tf` + `locals.tf`) | Verdict |
|-----------|-------------------------------|-------------------------------------|---------|
| Project scope source | `spec.ProjectId.GetValue()`; when empty, `Project` arg omitted → provider default project | `local.project_id = var.spec.project_id != "" ? var.spec.project_id : null` → provider default project | ✅ MATCH |
| Instance reference | `spec.Instance.GetValue()` (plain string after CLI ref resolution) | `var.spec.instance` (plain string after CLI ref resolution) | ✅ MATCH |
| Resource naming basis | `Name: spec.DatabaseName` | `name = var.spec.database_name` | ✅ MATCH |
| Charset empty handling | `""` → arg omitted (nil pointer) → engine default | `""` → `null` → engine default | ✅ MATCH |
| Collation empty handling | `""` → arg omitted (nil pointer) → engine default | `""` → `null` → engine default | ✅ MATCH |
| Labels | none — `google_sql_database` has no labels surface (documented in `locals.go`) | none — same API surface | ✅ MATCH (N/A by API) |
| Pod/service selectors | N/A (not a Kubernetes workload) | N/A | ✅ MATCH (N/A) |
| Spec feature coverage | all 5 spec fields honored (`project_id`, `instance`, `database_name`, `charset`, `collation`) | all 5 spec fields honored | ✅ MATCH |
| API enablement | none — deliberate; documented in `database.go` ("the instance … cannot exist without sqladmin.googleapis.com already enabled") | none — deliberate; same rationale documented in `main.tf` header comment | ✅ MATCH (documented on BOTH sides) |
| Dependency ordering | single resource; no intra-module edges | single resource; no intra-module edges | ✅ MATCH |
| Stack outputs → proto | exports `database_name`, `self_link` (`outputs.go` constants) | outputs `database_name`, `self_link` (`outputs.tf`) | ✅ MATCH |

**Legend:** ✅ MATCH | ⚠️ DIVERGES (undocumented) | 🔵 EXCEPTION (documented `PARITY-EXCEPTION:`)

✅ **In parity:**

- All eleven dimensions above. The empty→omitted/null convention for `project_id`,
  `charset`, and `collation` is behaviorally identical on both engines: the GCP API never
  receives an empty string on either side, and the ambient-project fallback resolves the
  same way (provider configuration / `GOOGLE_PROJECT` chain).
- The one deliberate design decision that could look like a gap — no
  `gcp.projects.Service` / `google_project_service` enablement of
  `sqladmin.googleapis.com` — is present on NEITHER side and documented with the same
  rationale in BOTH modules, so it is a shared design decision, not a divergence.

⚠️/❌ **Divergences (undocumented):**

- **None.**

**Effect on score:** none — the gate passes; Categories 4 and 5 keep their full scores.

**Automated guard evidence (verified this session):**

- `pkg/outputs/conformance_test.go` `GcpCloudSqlDatabase` case passes (`database_name` + `self_link` fully populate the proto, zero unmapped).
- `tofu validate` passes; offline `planton tofu plan` through the real tfvars converter passes (1 to add).
- Release-equivalent Pulumi build passes: `go build -o /dev/null ./apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/iac/pulumi`.

---

## Quick Wins

Items that are easy to fix and would improve the score:

1. **Expand `docs/README.md` toward the 5-10KB (moderate) or >10KB (comprehensive) band**
   - **Why:** This is the single largest score lever (+~6.6 points available). The current doc is dense and high-quality but only 2.4KB, and it lacks the landscape-analysis and deployment-methods-comparison sections the rule checks for.
   - **How:** Add a short landscape section (gcloud / console / Terraform / Config Connector approaches to database creation), a deployment-methods comparison, and expand the per-engine charset/collation research (known-good pairs per engine version).
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/docs/README.md`

2. **(Optional) Add a third preset for SQL Server collation**
   - **Why:** `spec.proto` documents SQL Server collation semantics but no preset demonstrates them; a rank-03 preset would complete the engine coverage story.
   - **How:** `03-sqlserver-collation.yaml` + `.md` with a known-good SQL Server collation name.
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/presets/`

3. **(Cosmetic) Note the no-labels rationale in the Terraform module too**
   - **Why:** The "google_sql_database has no labels surface" note lives only in the Pulumi `locals.go`. Behavior is identical (the API has no labels field), so this is NOT a parity defect — but mirroring the one-line comment in `locals.tf` keeps the two modules' documentation symmetric.
   - **How:** Add the one-line comment to `iac/tf/locals.tf`.
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/iac/tf/locals.tf`

---

## Critical Gaps

Blocking issues that prevent production readiness:

- **None.** Both gates pass, all critical categories score full, tests pass, and both engines build/validate.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry exists in `cloud_resource_kind.proto`: `GcpCloudSqlDatabase = 637`
- Enum value 637 is in the correct GCP range (600-799)
- `id_prefix` `gcpsqldb` is unique (exactly one occurrence in the registry)
- `kind_meta` complete: `provider: gcp`, `version: v1`, `id_prefix: "gcpsqldb"`, `prerequisites: [GcpCloudSql]`
- Kind map regenerated (`pkg/crkreflect/kind_map_gen.go`) — verified this session

❌ **Failed:**

- None (kubernetes_meta not applicable — not a Kubernetes component)

**Score: 4.44% / 4.44%**

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists: `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/`
- Correct provider hierarchy (`provider/gcp/`)
- Folder name `gcpcloudsqldatabase` is the lowercase enum name
- `v1/` subfolder exists

❌ **Failed:**

- None

**Score: 4.44% / 4.44%**

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files (13.32%)

✅ **Passed:**

- `api.proto` exists (947 bytes, >500) — apiVersion const `gcp.planton.dev/v1`, kind const `GcpCloudSqlDatabase`
- `spec.proto` exists (2,526 bytes, >500) — 5 fields, all with substantive teaching comments (engine-specific charset/collation semantics, ambient-project contract, immutability)
- `stack_input.proto` exists (422 bytes, >300)
- `stack_outputs.proto` exists (390 bytes, >300)

**Validation message quality:** `spec.proto` uses only standard `buf.validate` constraints
(`required`, `string.min_len/max_len`) — there are no custom CEL rules, so there are no
terse or regex-bearing custom messages to flag. The constraint surface matches the spec's
simplicity (two required fields, one length bound); no missing validations identified.

**Score: 13.32% / 13.32%**

#### 3.2 Generated Stubs (3.33%)

✅ **Passed:**

- `api.pb.go`, `spec.pb.go`, `stack_input.pb.go`, `stack_outputs.pb.go` all exist and are newer than their `.proto` sources (regenerated 2026-07-04)

**Score: 3.33% / 3.33%**

#### 3.3 Unit Tests - Presence (2.77%)

✅ **Passed:**

- `spec_test.go` exists (2,852 bytes, >500)
- Contains a Ginkgo suite with 8 cases: minimal accept, missing/ref instance, missing/overlong database_name, project_id ref, MySQL charset+collation, wrong kind constant
- Imports `testing`, `ginkgo/v2`, `gomega`, `protovalidate`

**Score: 2.77% / 2.77%**

#### 3.4 Unit Tests - Execution (2.78%)

✅ **Passed:**

- `go test ./apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/` passes (verified this session)

**Score: 2.78% / 2.78%**

**Total Protobuf API Score: 22.20% / 22.20%**

---

### 4. IaC Modules - Pulumi (13.32%)

#### 4.1 Module Files (6.66%)

✅ **Passed:**

- `iac/pulumi/module/main.go` exists (841 bytes) — thin coordinator
- `iac/pulumi/module/locals.go` exists (848 bytes)
- `iac/pulumi/module/outputs.go` exists (223 bytes)
- `iac/pulumi/module/database.go` exists (1,879 bytes) — the resource logic

**Comment quality:** meets the bar — comments explain *why*, not narration: the no-API-enablement
trade-off, the per-engine charset/collation semantics, the ambient-project contract, and the
no-labels rationale (`google_sql_database` has no labels surface) in `locals.go`.

**Score: 6.66% / 6.66%**

#### 4.2 Entrypoint Files (6.66%)

✅ **Passed:**

- `iac/pulumi/main.go` exists at the ROOT and declares `package main`
- No `iac/pulumi/main/` and no `iac/pulumi/entrypoint/` subdir
- `iac/pulumi/Pulumi.yaml` exists
- `iac/pulumi/Makefile` exists
- Release-equivalent build passes: `go build -o /dev/null ./apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/iac/pulumi` (verified this session)

**Score: 6.66% / 6.66%**

**Total Pulumi Score: 13.32% / 13.32%**

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `iac/tf/variables.tf` exists (1,250 bytes, >1KB) with typed `metadata` + `spec` objects and per-field comments (including the ref-resolution contract for `StringValueOrRef` fields)
- `iac/tf/provider.tf` exists — pins `hashicorp/google ~> 6.0` with the catalog-wide version-float rationale
- `iac/tf/locals.tf` exists — empty→null normalization with the why-comment (empty string would be sent verbatim and rejected)
- `iac/tf/main.tf` exists (856 bytes) — single `google_sql_database` resource with the shared design-decision comments
- `iac/tf/outputs.tf` exists — `database_name`, `self_link`

⚠️ **Warnings:**

- `main.tf` is 856 bytes, under the rule's >1KB "substantial" heuristic. That heuristic is a
  stub detector; this module is NOT a stub — it implements 100% of the spec surface (all 5
  fields), `tofu validate` passes, and an offline `planton tofu plan` through the real tfvars
  converter plans cleanly (1 to add). Full credit awarded with this note on the record.

**Comment quality:** meets the bar — the same why-comments as the Pulumi side (no API
enablement rationale, engine-specific charset/collation, ambient-project fallback).

**Score: 4.24% / 4.24%**

---

### 5b. Cross-Engine IaC Parity

See the **LEAD FINDING** section at the top of this report. **PARITY ✅** — zero divergences; gate passes; no cap applied to Categories 4 and 5.

---

### 6. Documentation - Research (13.18%)

#### 6.1 Research Doc (9.88% scaled)

⚠️ **Partial:**

- `docs/README.md` exists but is 2,488 bytes — in the "<5KB = minimal" band
- Quality-per-byte is high: split-test justification, design notes (instance-by-name, engine-specific charset/collation with the reason cross-validation is deliberately NOT done), deliberately-unmodeled list with reasons, composition map, operational guidance
- Size nonetheless keeps it in the minimal band → 50% credit

**Score: 4.94% / 9.88% (scaled from 5.00/10.00)**

#### 6.2 Content Quality (3.30% scaled)

✅ **Passed:**

- Best practices / operational guidance section present
- 90/10 coverage decisions explained ("Deliberately unmodeled (with reasons)": deletion_policy, grants/privileges)

❌ **Failed:**

- No landscape analysis section (no "Level" / "Approach" / "Method" / "Comparison" style survey)
- No deployment methods comparison

**Score: 1.65% / 3.30% (2 of 4 checks)**

**Total Research Documentation Score: 6.59% / 13.18%**

---

### 7. Documentation - User-Facing (13.09%)

✅ **Passed:**

- `README.md` exists at v1/ level (3,331 bytes, >2KB = good)
- Covers: what gets created, prerequisites, quick start, full configuration reference (required/optional tables), stack outputs, deployment methods, important notes (data-loss on drop, engine-specific validation), deliberately-not-modeled table, related components
- `catalog-page.md` also present (2,283 bytes) as an additional user-facing surface

❌ **Failed:**

- None

**Score: 13.09% / 13.09%**

---

### 8. Supporting Files (10.09%)

✅ **Passed:**

- `iac/pulumi/README.md` exists (1,842 bytes) — prerequisites, directory structure, quick start, outputs
- `iac/tf/README.md` exists (1,717 bytes) — CLI + direct usage, variables, outputs, parity statement
- `iac/hack/manifest.yaml` exists (385 bytes) — validates with `planton validate` (verified this session); exercises literal instance + explicit MySQL charset/collation with project_id omitted (ambient fallback path)

❌ **Failed:**

- None

**Score: 10.09% / 10.09%**

---

### 9. Nice to Have (10.00%)

✅ **Passed:**

- Build files (5.00%): `BUILD.bazel` present at `v1/`, `iac/pulumi/`, and `iac/pulumi/module/`
- Discretionary bonus (5.00%): complete e2e suite present — `e2e/profile.yaml` (tier 1, both provisioners), `e2e/scenarios/minimal.yaml`, and `e2e/prerequisites/` (gcpcloudsql + gcpglobaladdress chain); all manifests validate with `planton validate` (verified this session)

**Score: 10.00% / 10.00%**

---

### 10. Presets (5.00%)

#### 10.1 Directory and File Checks (2.50%)

✅ **Passed:**

- `presets/` directory exists under `v1/`
- 2 preset YAML files: `01-postgres-app-database.yaml`, `02-mysql-utf8mb4-database.yaml`
- Both have companion `.md` files with matching base names; no orphan `.md` files
- Naming follows `{NN}-{kebab-case}.yaml` convention; no duplicate ranks

**Score: 2.50% / 2.50%**

#### 10.2 Content Correctness (2.50%)

✅ **Passed:**

- `apiVersion: gcp.planton.dev/v1` and `kind: GcpCloudSqlDatabase` match the `api.proto` constants in both presets
- `metadata.name` prefixed with `my-` in both (`my-orders-database`, `my-app-database`)
- `spec` present and non-empty; no `status` block
- `StringValueOrRef` fields use wrapper forms: `projectId` uses `value:`, `instance` uses `valueFrom:` with kind/name/fieldPath
- All field names exist in current `spec.proto`; no stale references
- Both presets validate with `planton validate` (verified this session)
- The MySQL preset teaches the utf8mb4-vs-legacy-utf8 trap inline — matches the research doc

❌ **Failed:**

- None

**Score: 2.50% / 2.50%**

**Total Presets Score: 5.00% / 5.00%**

> **Tip:** For a deeper preset audit, run `@audit-planton-presets GcpCloudSqlDatabase`.

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` passes repo-wide (verified this session). This kind's spec
fields are `project_id`, `instance`, `database_name`, `charset`, `collation` — none is
secret-bearing, none looks secret-bearing to the analyzer, and there is no
`GcpCloudSqlDatabase:` entry in `gaps[]`.

| Field path | Classification | Note |
|-----------|----------------|------|
| `spec.project_id` | not secret-bearing | project identifier |
| `spec.instance` | not secret-bearing | instance name reference |
| `spec.database_name` | not secret-bearing | database name |
| `spec.charset` | not secret-bearing | character set name |
| `spec.collation` | not secret-bearing | collation name |

❌ **Gaps (unannotated secret-bearing fields):**

- None.

**Effect on score:** none — the gate passes.

---

## Prioritized Recommendations

### High Priority (Do First)

1. **Expand `docs/README.md` with landscape analysis and deployment-methods comparison**
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/docs/README.md`
   - **Why:** The only category below full marks; +~6.6 points available. The rule expects a landscape survey and methods comparison in addition to the (already good) design notes.
   - **How:** Run forge Rule 007 in enrichment mode, keeping the existing design-notes content.

### Medium Priority (Do Next)

1. **Add a SQL Server collation preset (rank 03)**
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/presets/`
   - **Why:** Completes per-engine coverage that the spec comments promise.
   - **How:** Forge Rule 022 with a known-good SQL Server collation.

### Low Priority (Polish)

1. **Mirror the no-labels rationale comment into `iac/tf/locals.tf`**
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/iac/tf/locals.tf`
   - **Why:** Documentation symmetry between engines (behavior already identical; not a parity defect).

---

## Reference to Forge Rules

- **Rule 007** — docs/README.md enrichment (landscape + methods comparison) — the one open item
- **Rule 022** — additional presets (optional SQL Server preset)

No parity remediation needed — Category 5b passed with zero divergences.

---

## Comparison to Complete Components

**Most Similar Complete Component:** GcpCloudSqlUser (`apis/dev/planton/provider/gcp/gcpcloudsqluser/v1/`)

Same split-node family (instance / database / user), same composition pattern (plain-string
instance ref resolving `GcpCloudSql.status.outputs.instance_name`), same no-API-enablement
design decision. The database component matches it structurally; the main difference is that
the user kind carries a sensitive `password` field (annotated), while this kind has no secret
surface at all.

---

## Next Steps

1. Enrich `docs/README.md` (landscape + deployment-methods sections) — the only sub-full category.
2. Optionally add the SQL Server preset.
3. Re-run this audit to confirm the score moves into the high-90s.

---

## Appendix: Full Checklist

### Critical Items (48.64%)

#### Cloud Resource Registry (4.44%)

- [x] Enum entry exists (`GcpCloudSqlDatabase = 637`)
- [x] Enum value in correct range (GCP 600-799)
- [x] Unique id_prefix (`gcpsqldb`)
- [x] Complete kind_meta (provider, version, id_prefix, prerequisites)
- [x] Kubernetes metadata — N/A (not a Kubernetes component)

#### Folder Structure (4.44%)

- [x] Correct provider hierarchy
- [x] Lowercase folder name matches enum
- [x] `v1/` subfolder exists

#### Protobuf API Definitions (22.20%)

- [x] `api.proto` / `spec.proto` / `stack_input.proto` / `stack_outputs.proto` present and non-empty
- [x] All four `.pb.go` stubs present and regenerated
- [x] `spec_test.go` present with real validation cases
- [x] `go test ./...v1/` passes

#### Validation Message Quality (part of Proto Files score)

- [x] No custom CEL rules present; standard constraints only — no terse or regex-bearing messages exist
- [x] Field comments teach semantics (engine-specific charset/collation, ambient project, immutability)

#### Module Comment Quality (part of Categories 4 & 5 module score)

- [x] Pulumi module explains why/trade-offs (API enablement, charset semantics, ambient project, no-labels)
- [x] Terraform module carries the same why-comments (main.tf header, locals.tf null-normalization, provider.tf version pin)
- [x] No narration-only or uncommented module

#### Cross-Engine IaC Parity (GATE on Categories 4 & 5)

- [x] Project scope source matches (empty → provider default on both)
- [x] Resource naming basis matches (`spec.database_name` on both)
- [x] Labels — N/A by API (`google_sql_database` has no labels surface); identical on both
- [x] Pod/service selectors — N/A (not Kubernetes)
- [x] Every `spec.proto` field honored by BOTH engines (5/5)
- [x] Dependency ordering matches (single resource, no edges, both sides)
- [x] Stack outputs flatten onto the same proto fields (`pkg/outputs/conformance_test.go` case passes)
- [x] No divergences requiring `PARITY-EXCEPTION:` (none exist)

#### Secure-by-Default Sensitive Coverage (GATE on the whole component)

- [x] `planton secret-coverage --check` passes; zero `gaps[]` for this kind
- [x] No `sensitive`/`sensitive_exempt_reason` contradiction
- [x] No secret-bearing fields exist in this spec (nothing to annotate or exempt)

### Important Items (41.36%)

#### Documentation - Research (13.18%)

- [x] `docs/README.md` exists
- [ ] Size ≥5KB (currently 2.4KB — minimal band)
- [ ] Landscape analysis section
- [x] Best practices / operational guidance
- [x] 90/10 coverage decisions with reasons
- [ ] Deployment methods comparison

#### Documentation - User-Facing (13.09%)

- [x] `README.md` at v1/ (3.3KB, >2KB = good)

#### Supporting Files (10.09%)

- [x] `iac/pulumi/README.md`
- [x] `iac/tf/README.md`
- [x] `iac/hack/manifest.yaml` (validates)

#### Presets (5.00%)

- [x] `presets/` directory exists
- [x] 2 preset YAML + MD pairs
- [x] Naming convention correct; no duplicate ranks
- [x] `apiVersion` and `kind` match `api.proto`
- [x] `StringValueOrRef` fields use wrapper forms
- [x] All field names exist in current `spec.proto`
- [x] Companion `.md` files carry When-to-Use / Key-Choices / Placeholders sections

### Nice to Have (10%)

- [x] BUILD.bazel files exist (v1/, iac/pulumi/, iac/pulumi/module/)
- [x] Bonus: full e2e suite (profile, scenario, prerequisites) present and validating

---

**Audit completed at:** 2026-07-04 00:45:24
**Reference:** See `architecture/deployment-component.md` for ideal state definition
