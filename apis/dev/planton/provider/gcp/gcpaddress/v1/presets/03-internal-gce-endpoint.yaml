apiVersion: gcp.planton.dev/v1
kind: GcpAddress
metadata:
  name: my-internal-gce-endpoint
spec:
  projectId:
    value: my-gcp-project-123
  addressName: vm-static-internal-ip
  region: us-central1
  addressType: INTERNAL
  purpose: GCE_ENDPOINT
  ipVersion: IPV4
  subnetwork:
    value: app-subnet
  description: Static internal IP for a VM instance in the subnetwork
