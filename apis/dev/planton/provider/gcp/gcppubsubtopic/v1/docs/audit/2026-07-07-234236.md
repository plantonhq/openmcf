# Audit Report: GcpPubSubTopic

**Audit Date:** 2026-07-07 23:42:36
**Component Kind:** GcpPubSubTopic
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcppubsubtopic/v1/`
**Enum Value:** 660
**ID Prefix:** gcppst

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Two defect classes were closed in this rebuild:

1. **Live label-parity break** — the Terraform module previously stamped NO labels while Pulumi stamped the full platform set; identically deployed topics differed by engine. Both engines now merge user labels beneath the identical `planton-ai_*` platform set, and the E2E verifier asserts the attribution label landed (the parity canary runs live on every scenario).
2. **Composition defect** — `schema_settings.schema` was a plain string; it is now a `StringValueOrRef` with `default_kind = GcpPubSubSchema`, proven live by the schema-validated scenario's prerequisite chain. The five ingestion `gcp_service_account` fields likewise became `GcpServiceAccount` references.

---

### 3. Protobuf API Definitions — validation quality note

Spec at the released 6.50.0 floor: user `labels`, `message_transforms[]` (JavaScript UDF arm), the full five-source ingestion surface, storage policy, CMEK, retention, and schema settings. New CEL: the reserved-`goog` topic-name prefix rejected pre-deploy, and Cloud Storage ingestion requires exactly one input format (`avro_format` XOR `pubsub_avro_format` XOR `text_format` — the provider enforces this only at the API). `project_id` is ambient-optional (the repo-wide contract). 48-case spec test.

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/topic.go` + `locals.go`) and Terraform (`iac/tf/main.tf` + `locals.tf`), judged against the proto contract. Live-proven 4/4 on `planton-e2e` (minimal + schema-validated × both engines).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| API enablement | `pubsub.googleapis.com`, disable_on_destroy false | same | ✅ MATCH |
| Resource naming basis | `spec.topic_name` | `local.topic_name` | ✅ MATCH |
| Ambient project | empty omitted → provider default | empty → `null` | ✅ MATCH |
| Labels | user-first merge beneath `planton-ai_*` set | identical merge order | ✅ MATCH |
| CMEK / retention | omit-when-empty | `null`-when-empty | ✅ MATCH |
| Storage policy | presence-gated; enforce_in_transit true-only vs explicit false | provider default false — behaviorally identical | ✅ MATCH |
| Schema settings | resolved ref value; encoding omit-when-empty | same | ✅ MATCH |
| Ingestion arms (×5 + platform logs) | field-for-field, resolved SA refs | same | ✅ MATCH |
| Message transforms | ordered list; disabled true-only vs explicit false (provider default false) | behaviorally identical | ✅ MATCH |
| Dependency ordering | `DependsOn(api)` | `depends_on = [api]` | ✅ MATCH |
| Stack outputs → proto | topic_id (resource ID), topic_name | same | ✅ MATCH |

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅** — no secret-bearing fields (ingestion authenticates by federated identity, never by embedded credential); `secret-coverage --check` green.

---

## Recorded skips (with reasons; schema-probe verified against released 6.50.0)

- `tags` (resource-manager tags) — absent from the released `google ~> 6.x` line.
- `message_transforms.ai_inference` — the Vertex AI arm is absent from the released line; only the JavaScript UDF arm is released.
- `schema_settings.first_revision_id`/`last_revision_id` — revision pinning absent from the released line.
- `deletion_policy` — client-side lever conflicting with Planton-managed destroy (catalog-wide skip).
- Per-topic IAM trio — resource-scoped IAM deferred pending concrete pull.
- Live-E2E exclusions: the cross-cloud ingestion arms (AWS Kinesis/MSK, Azure Event Hubs, Confluent Cloud — need live external-cloud resources and cross-cloud trust the test project does not carry) and CMEK (org-level KMS grant to the Pub/Sub service agent) are proven offline.

## Validation evidence

- Spec test: 48/48 green. Both engines build (`go build` + release-equivalent Pulumi entrypoint).
- `validate-outputs` green on BOTH module dirs; `pkg/outputs` conformance case present.
- Offline `planton tofu plan` via the real converter: 2 to add, clean. 6 presets + hack manifest validate.
- Live dual-engine E2E on `planton-e2e`: minimal + schema-validated 4/4 green (67s–88s per scenario incl. the schema prerequisite chain); zero orphans.
