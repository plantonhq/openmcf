# Audit Report: GcpCloudSqlUser

**Audit Date:** 2026-07-04 00:45:32  
**Component Kind:** GcpCloudSqlUser  
**Provider:** gcp  
**Component Path:** `apis/dev/planton/provider/gcp/gcpcloudsqluser/v1/`  
**Enum Value:** 638  
**ID Prefix:** gcpsqlusr  
**Audit Mode:** `--parity` (Category 5b foregrounded)

---

## Overall Completion Score

**Score: 94.2%**

```
█████████▍ 94.2% Complete
```

**Status:** Functionally Complete

> **Gate verdicts:** Cross-Engine IaC Parity — **PARITY ✅** (all dimensions MATCH, zero divergences).
> Secure-by-Default Sensitive Coverage — **COVERAGE ✅** (zero gaps, no contradictions).

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 7.42% | ⚠️ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

**Total:** 48.64% (critical) + 35.60% (important) + 10.00% (nice to have) = **94.24%**

---

## Quick Wins

1. **Add `instance_name` to the Stack Outputs table in `README.md`**
   - **Why:** `stack_outputs.proto` was extended this session with `instance_name` (field 2) and both engines export it, but the user-facing README's Stack Outputs table still lists only `user_name`. Docs must track the proto contract.
   - **How:** Add a row: `instance_name | string | Name of the Cloud SQL instance this user belongs to`.
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqluser/v1/README.md`

2. **Add `instance_name` to the Stack Outputs table in `catalog-page.md`**
   - **Why:** Same staleness as the README — the catalog page renders publicly and omits the new output.
   - **How:** Add the same `instance_name` row to the outputs table.
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqluser/v1/catalog-page.md`

3. **Expand `docs/README.md` toward the 5–10KB "moderate" band**
   - **Why:** At 3.1KB it sits in the "minimal" size band. The content is dense and honest (auth-family landscape, deliberately-unmodeled features with reasons, composition map), but a short deployment-methods comparison (Pulumi vs Terraform vs gcloud/console) and an expanded IAM-auth operational section would lift both size and content-quality checks.
   - **How:** Add a "Deployment methods" comparison section and deepen the IAM authentication guidance (Auth Proxy / connector requirements, `roles/cloudsql.instanceUser`).
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsqluser/v1/docs/README.md`

---

## Critical Gaps

None. Both gates pass, all critical-path categories are at full score, and the release-equivalent Pulumi build, spec tests, `tofu validate`, offline `planton tofu plan`, `planton secret-coverage --check`, `planton validate-refs --check`, and the `pkg/outputs` conformance case all pass (verified this session).

---

## Detailed Findings

### 5b. Cross-Engine IaC Parity (Critical GATE) — audited FIRST (`--parity` run)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/user.go`, `locals.go`, `main.go`, `outputs.go`) and Terraform (`iac/tf/main.tf`, `locals.tf`, `variables.tf`, `outputs.tf`), both judged against `spec.proto` + `stack_outputs.proto` (neither engine privileged). Output parity verified by the automated guard: the `GcpCloudSqlUser` case in `pkg/outputs/conformance_test.go` (lines 438–448) passes with `mustPopulate: ["user_name", "instance_name"]` — full proto population, zero unmapped.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| `project_id` ambient fallback | `args.Project` set only when `spec.ProjectId.GetValue() != ""` (user.go:34–36); omitted → provider default project | `local.project_id = var.spec.project_id != "" ? ... : null` (locals.tf:7); null → provider default project | ✅ MATCH |
| `instance` (plain string after ref resolution) | `pulumi.String(spec.Instance.GetValue())` (user.go:28) | `var.spec.instance` declared `string`; tfvars converter resolves the ref pre-module (variables.tf:23–25) | ✅ MATCH |
| `user_name` | `pulumi.String(spec.UserName)` (user.go:27) | `name = var.spec.user_name` (main.tf:15) | ✅ MATCH |
| `password` (sensitive) | Set only when non-empty, wrapped `pulumi.ToSecret(...)` → encrypted in state (user.go:40–42); never exported | `local.password = "" → null` so IAM users carry no password (locals.tf:11); `google_sql_user.password` is sensitive in the provider schema; never output | ✅ MATCH |
| `type` (middleware default `BUILT_IN`) | `pulumi.StringPtr(spec.GetType())` — receives `BUILT_IN` from the proto `(default)` middleware (user.go:29) | `type = local.user_type` with variable-level fallback `optional(string, "BUILT_IN")` mirroring the same middleware default (variables.tf:36, locals.tf:16) | ✅ MATCH |
| `host` (MySQL-only, empty → omitted) | Set only when `spec.Host != ""` (user.go:45–47) | `local.host = "" → null` → omitted from API payload (locals.tf:12) | ✅ MATCH |
| `password_policy` block condition | Emitted iff `spec.PasswordPolicy != nil` (user.go:49) | Dynamic block iff `local.password_policy != null`; variable defaults to `null` (main.tf:28–29, variables.tf:43–48) | ✅ MATCH |
| `password_policy.allowed_failed_attempts` | Set only when pointer non-nil (user.go:54–56) | Passed through; `optional(number, null)` when unset (main.tf:31) | ✅ MATCH |
| `password_policy.password_expiration_duration` | Set only when non-empty (user.go:57–59) | `"" → null` inline (main.tf:32) | ✅ MATCH |
| `password_policy.enable_failed_attempts_check` / `enable_password_verification` | Always set (`BoolPtr`, proto zero-value `false`) (user.go:51–52) | Always set (`optional(bool, false)`) (main.tf:33–34) | ✅ MATCH |
| Labels | None — `google_sql_user` has no labels surface; documented in `locals.go` ("attribution lives on the instance node") | None — same API constraint | ✅ MATCH (N/A by API) |
| Dependency ordering | Single resource; the instance edge is carried by upstream ref resolution — no intra-module edges | Same | ✅ MATCH |
| Stack outputs → proto | Exports `user_name` (`createdUser.Name`) + `instance_name` (`createdUser.Instance`) (user.go:68–69, outputs.go) | Outputs `user_name` (`google_sql_user.this.name`) + `instance_name` (`google_sql_user.this.instance`) (outputs.tf) | ✅ MATCH |

**Legend:** ✅ MATCH | ⚠️ DIVERGES (undocumented) | 🔵 EXCEPTION (documented `PARITY-EXCEPTION:`)

✅ **In parity:**

- All 13 dimensions above MATCH. No `PARITY-EXCEPTION:` comments exist and none are needed.
- The `instance_name` output added to `stack_outputs.proto` this session (field 2) is exported by BOTH engines from the same source attribute (the resource's `instance`), and the conformance case guards it.

⚠️/❌ **Divergences (undocumented):**

- None.

**Effect on score:** none — Categories 4 and 5 keep full credit.

---

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpCloudSqlUser = 638` exists in `cloud_resource_kind.proto` (verified this session; kind map regenerated)
- Enum value 638 is within the GCP range (600–799)
- `id_prefix: "gcpsqlusr"` is unique (single occurrence in the registry)
- `kind_meta` complete: provider `gcp`, version `v1`, id_prefix, and `prerequisites: [GcpCloudSql]`
- `kubernetes_meta` not applicable (non-Kubernetes component)

❌ **Failed:**

- None

**Score: 4.44% / 4.44%**

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists under the correct provider hierarchy: `apis/dev/planton/provider/gcp/gcpcloudsqluser/`
- Folder name is the lowercase enum name
- `v1/` subfolder exists

❌ **Failed:**

- None

**Score: 4.44% / 4.44%**

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files (13.32%)

✅ **Passed:**

- `api.proto` exists (903 bytes) — apiVersion const `gcp.planton.dev/v1`, kind const `GcpCloudSqlUser`
- `spec.proto` exists (5,274 bytes) — 7 fields + `GcpCloudSqlUserPasswordPolicy` sub-message
- `stack_input.proto` exists (402 bytes)
- `stack_outputs.proto` exists (510 bytes) — `user_name` + `instance_name`
- Validation message quality: all CEL messages are complete user-facing sentences (e.g. "IAM-authenticated users … must not set a password — authentication goes through IAM"); the duration regex is explained in plain English ("must be a seconds duration string such as 2592000s") with the raw regex only in the expression; conditional rules state both condition and requirement; no message under 5 words; semantic field names used throughout

❌ **Failed:**

- None

**Score: 13.32% / 13.32%**

#### 3.2 Generated Stubs (3.33%)

✅ **Passed:**

- `api.pb.go`, `spec.pb.go`, `stack_input.pb.go`, `stack_outputs.pb.go` all exist and are newer than or contemporaneous with their protos (regenerated after the `instance_name` addition)

**Score: 3.33% / 3.33%**

#### 3.3 Unit Tests - Presence (2.77%)

✅ **Passed:**

- `spec_test.go` exists (4,721 bytes)
- 14 ginkgo specs covering: minimal built-in user, passwordless built-in, missing instance, instance-by-ref, missing user_name, unknown type, IAM-user-with-password rejection, IAM SA and group acceptance, password-policy-on-IAM rejection, MySQL host scoping, full password policy, malformed expiration duration, zero allowed_failed_attempts, wrong kind constant
- Imports `testing`, ginkgo/gomega, and `protovalidate`

**Score: 2.77% / 2.77%**

#### 3.4 Unit Tests - Execution (2.78%)

✅ **Passed:**

- `go test ./apis/dev/planton/provider/gcp/gcpcloudsqluser/v1/` passes (verified this session)
- Tests exercise the buf.validate CEL rules directly (both accept and reject paths)

**Score: 2.78% / 2.78%**

**Total Protobuf API Score: 22.20% / 22.20%**

---

### 4. IaC Modules - Pulumi (13.32%)

#### 4.1 Module Files (6.66%)

✅ **Passed:**

- `iac/pulumi/module/main.go` exists (813 bytes) — entry point wiring provider + user
- `iac/pulumi/module/locals.go` exists (812 bytes)
- `iac/pulumi/module/user.go` exists (2,534 bytes) — the resource logic
- `iac/pulumi/module/outputs.go` exists (226 bytes) — `user_name`, `instance_name` keys
- Comment quality meets the bar: comments explain *why*, not narration — e.g. why no API enablement is done here (the hosting instance already required `sqladmin.googleapis.com`), why the password is `ToSecret` (state encryption, never exported), why there is no label map (`google_sql_user` has no labels surface — attribution lives on the instance node), the ambient-project contract, and MySQL-only host semantics

❌ **Failed:**

- None

**Score: 6.66% / 6.66%**

#### 4.2 Entrypoint Files (6.66%)

✅ **Passed:**

- `iac/pulumi/main.go` exists at the ROOT and declares `package main`
- No `iac/pulumi/main/` and no `iac/pulumi/entrypoint/` subdirectories
- `iac/pulumi/Pulumi.yaml` exists
- `iac/pulumi/Makefile` exists
- Release-equivalent build passes: `go build -o /dev/null ./apis/dev/planton/provider/gcp/gcpcloudsqluser/v1/iac/pulumi` (verified this session)

❌ **Failed:**

- None

**Score: 6.66% / 6.66%**

**Total Pulumi Score: 13.32% / 13.32%**

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `iac/tf/variables.tf` exists (1,830 bytes, >1KB) — fully typed `spec` object mirroring `spec.proto`, with per-field comments explaining ref resolution and defaults
- `iac/tf/provider.tf` exists — pins `hashicorp/google ~> 6.0` with a comment explaining the catalog-wide version-float policy
- `iac/tf/locals.tf` exists (900 bytes) — empty-string → null normalization with *why* comments (provider project resolution chain; IAM users carry no password; non-MySQL engines have no host)
- `iac/tf/main.tf` exists (1,560 bytes, >1KB) — single `google_sql_user` with the same header rationale as the Pulumi module
- `iac/tf/outputs.tf` exists — `user_name` + `instance_name` with descriptions
- `tofu validate` passes; offline `planton tofu plan` through the real tfvars converter passes (1 to add) — verified this session
- Comment quality meets the module-comment bar (why/trade-off comments, not narration)

❌ **Failed:**

- None

**Score: 4.24% / 4.24%**

---

### 6. Documentation - Research (13.18%)

#### 6.1 Research Doc (9.88% of this category)

⚠️ **Partial:**

- `docs/README.md` exists but is 3,164 bytes — the "minimal" size band (<5KB) per the rubric. The content is dense and well-reasoned, but short of the moderate/comprehensive bands.

**Score: 4.94% / 9.88%** (50% credit — present and substantive, but minimal by size)

#### 6.2 Content Quality (3.30% of this category)

✅ **Passed:**

- Landscape analysis: "The two auth families" section (BUILT_IN vs IAM types) with per-engine caveats (PostgreSQL flag, MySQL truncation)
- Best practices / recommendations: "Operational guidance" (one user per app, prefer IAM types, ownership-reassignment before drop)
- 90/10 coverage decisions: "Deliberately unmodeled (with reasons)" covers `password_wo`/`password_wo_version`, `database_roles`/`iam_email`, `deletion_policy`, and grants

⚠️ **Warnings:**

- No explicit deployment-methods comparison section (Pulumi vs Terraform vs gcloud/console)

**Score: 2.48% / 3.30%** (3 of 4 checks)

**Total Research Score: 7.42% / 13.18%**

---

### 7. Documentation - User-Facing (13.09%)

✅ **Passed:**

- `README.md` exists at v1/ level (4,599 bytes, >2KB = good)
- Covers what gets created, prerequisites, quick start, full configuration reference (required/optional/validation tables), stack outputs, deployment methods, important notes, deliberately-not-modeled table, related components

⚠️ **Warnings (do not affect the size-based score; listed as Quick Wins):**

- The Stack Outputs table lists only `user_name`; `instance_name` (added to `stack_outputs.proto` this session) is missing
- `catalog-page.md` has the same stale outputs table

**Score: 13.09% / 13.09%**

---

### 8. Supporting Files (10.09%)

✅ **Passed:**

- `iac/pulumi/README.md` exists (2,037 bytes) — **5.05%**
- `iac/tf/README.md` exists (1,801 bytes), explicitly documents feature parity with the Pulumi module — **2.52%**
- `iac/hack/manifest.yaml` exists (526 bytes), validates with `planton validate` (verified this session) — **2.52%**

❌ **Failed:**

- None

**Score: 10.09% / 10.09%**

---

### 9. Nice to Have (10.00%)

✅ **Passed:**

- `BUILD.bazel` present at `v1/`, `iac/pulumi/`, and `iac/pulumi/module/`
- Bonus assets beyond the checklist: `e2e/profile.yaml` (tier 1, both provisioners), `e2e/scenarios/minimal.yaml`, `e2e/prerequisites/` (GcpCloudSql + GcpGlobalAddress chain), and a `GcpCloudSqlUser` case in `pkg/outputs/conformance_test.go`

**Score: 10.00% / 10.00%**

---

### 10. Presets (5.00%)

#### 10.1 Directory and File Checks (2.50%)

✅ **Passed:**

- `presets/` directory exists with 2 preset pairs: `01-application-user` and `02-iam-service-account-user`
- Every `.yaml` has a companion `.md`; no orphan `.md` files
- Naming follows `{NN}-{kebab-case}.yaml` + `.md`; no duplicate ranks

**Score: 2.50% / 2.50%**

#### 10.2 Content Correctness (2.50%)

✅ **Passed:**

- `apiVersion: gcp.planton.dev/v1` and `kind: GcpCloudSqlUser` match the `api.proto` constants in both presets
- `metadata.name` prefixed with `my-` in both (`my-orders-app-user`, `my-ci-runner-user`)
- `spec` present and non-empty; no `status` block
- `StringValueOrRef` fields use wrapper form: `projectId.value`, `instance.valueFrom`
- All field names exist in the current `spec.proto`; the IAM preset correctly omits `password` and sets `type: CLOUD_IAM_SERVICE_ACCOUNT`
- Both presets validate with `planton validate` (verified this session)

**Score: 2.50% / 2.50%**

**Total Presets Score: 5.00% / 5.00%**

> **Tip:** For a deeper preset audit, run `@audit-planton-presets GcpCloudSqlUser`.

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` passes repo-wide (verified this session). Per-field result for this kind:

| Field path | Classification | Note |
|-----------|----------------|------|
| `spec.password` | covered | annotated `(dev.planton.shared.options.sensitive) = true`; Pulumi wraps in `pulumi.ToSecret`, Terraform never outputs it |
| `spec.password_policy.password_expiration_duration` | exempt | `sensitive_exempt_reason = "an expiry duration, not credential material"` — truthful: a lifetime string like `2592000s`, not a secret |

❌ **Gaps (unannotated secret-bearing fields):**

- None — zero `gaps[]` entries for `GcpCloudSqlUser:` and no `sensitive`/`sensitive_exempt_reason` contradiction.

**Effect on score:** none.

---

## Prioritized Recommendations

### High Priority (Do First)

1. **Sync the Stack Outputs tables with `stack_outputs.proto`**
   - **Files:** `README.md`, `catalog-page.md`
   - **Why:** `instance_name` was added to the proto this session and both engines export it; the public docs must not lag the contract.
   - **How:** Add the `instance_name` row to both outputs tables (copy the description from `stack_outputs.proto`).

### Medium Priority (Do Next)

1. **Grow `docs/README.md` past 5KB with a deployment-methods comparison**
   - **File:** `docs/README.md`
   - **Why:** Lifts the Research category from 7.42% toward full credit; the deployment-methods comparison is the one missing content check.
   - **How:** Add a short Pulumi vs Terraform vs gcloud/console comparison and expand IAM-auth operational guidance (Auth Proxy, `roles/cloudsql.instanceUser`).

### Low Priority (Polish)

1. **Consider a MySQL host-scoped preset**
   - **File:** `presets/`
   - **Why:** The `host` field is the one spec surface with no preset exemplar; a rank-03 MySQL preset would round out coverage.
   - **How:** Follow `architecture/presets.md`; pair YAML + MD, rank 03.

---

## Reference to Forge Rules

No forge rules are required for critical gaps (there are none). For the documentation quick wins, direct edits suffice; for the optional preset, use **Rule 022** (Presets).

---

## Comparison to Complete Components

**Most Similar Complete Component:** GcpCloudSqlDatabase (`apis/dev/planton/provider/gcp/gcpcloudsqldatabase/v1/`)

Both are child kinds of GcpCloudSql (prerequisite chain, instance-by-ref, single-resource modules, e2e composed scenarios, conformance cases). GcpCloudSqlUser matches it structurally; neither has an outstanding parity divergence.

---

## Next Steps

1. Add `instance_name` to the Stack Outputs tables in `README.md` and `catalog-page.md` (only real staleness found).
2. Expand `docs/README.md` toward the 5–10KB band with a deployment-methods comparison.
3. Optionally add a rank-03 MySQL host-scoped preset.
4. Re-run this audit to confirm the score moves into the high-90s.

---

## Appendix: Full Checklist

### Critical Items (48.64%)

#### Cloud Resource Registry (4.44%)

- [x] Enum entry exists (`GcpCloudSqlUser = 638`)
- [x] Enum value in correct range (GCP 600–799)
- [x] Unique id_prefix (`gcpsqlusr`)
- [x] Complete metadata (provider, version, id_prefix, prerequisites)
- [x] Kubernetes metadata — N/A (non-Kubernetes)

#### Folder Structure (4.44%)

- [x] Correct provider hierarchy
- [x] Lowercase enum name
- [x] `v1/` subfolder

#### Protobuf API Definitions (22.20%)

- [x] `api.proto` (903 B), `spec.proto` (5,274 B), `stack_input.proto` (402 B), `stack_outputs.proto` (510 B)
- [x] All four `.pb.go` stubs regenerated
- [x] `spec_test.go` present (4,721 B), 14 specs, passes

#### Validation Message Quality (part of Proto Files score)

- [x] Every CEL `message` is a complete, actionable sentence
- [x] No raw regex in user-facing messages (duration format explained in plain English)
- [x] Conditional rules explain both condition and requirement
- [x] No messages shorter than 5 words
- [x] Messages use semantic field names, not proto path names

#### Module Comment Quality (part of Categories 4 & 5 module score)

- [x] Pulumi and Terraform modules explain *why* / trade-offs / quirks (no-API-enablement rationale, secret handling, no-labels-surface, ambient project, MySQL host semantics)
- [x] Comment density matches the `spec.proto` field-comment standard
- [x] No narration-only modules

#### Cross-Engine IaC Parity (GATE on Categories 4 & 5)

- [x] Namespace / scope source matches (project: empty → provider default on both)
- [x] Resource naming basis matches (`spec.user_name` on both)
- [x] Labels identical — N/A (`google_sql_user` has no labels surface; documented)
- [x] Pod/service selectors — N/A (no workload)
- [x] Every `spec.proto` field honored by BOTH engines (all 7 fields + 4 password-policy subfields)
- [x] Dependency ordering matches (single resource; instance edge via ref resolution)
- [x] Stack outputs flatten onto the same proto fields (`user_name`, `instance_name`; conformance case passes)
- [x] No divergences, so no `PARITY-EXCEPTION:` comments needed

#### Secure-by-Default Sensitive Coverage (GATE on the whole component)

- [x] `planton secret-coverage` reports zero `gaps[]` for this kind
- [x] No `sensitive`/`sensitive_exempt_reason` contradiction
- [x] `spec.password` annotated `(sensitive) = true`
- [x] `password_expiration_duration` exemption is truthful (expiry duration, not credential material)

### Important Items (41.36%)

#### Documentation - Research (13.18%)

- [x] `docs/README.md` exists
- [ ] Size ≥5KB (3.1KB — minimal band)
- [x] Landscape analysis (auth families)
- [x] Best practices / operational guidance
- [x] 90/10 decisions recorded with reasons
- [ ] Deployment-methods comparison

#### Documentation - User-Facing (13.09%)

- [x] `README.md` exists, >2KB (4.6KB)
- [ ] Stack Outputs table current (missing `instance_name` — quick win)

#### Supporting Files (10.09%)

- [x] `iac/pulumi/README.md`
- [x] `iac/tf/README.md`
- [x] `iac/hack/manifest.yaml` (validates)

#### Presets (5.00%)

- [x] `presets/` directory exists
- [x] 2 preset YAML + MD pairs
- [x] Naming convention correct, no duplicate ranks
- [x] `apiVersion` and `kind` match `api.proto`
- [x] `StringValueOrRef` fields use wrapper form
- [x] All field names exist in current `spec.proto`
- [x] Companion `.md` files present

### Nice to Have (10%)

- [x] BUILD.bazel files present (v1, pulumi, module)
- [x] Bonus: e2e profile/scenario/prerequisites; conformance case in `pkg/outputs`

---

**Audit completed at:** 2026-07-04-004532  
**Reference:** See `architecture/deployment-component.md` for ideal state definition
