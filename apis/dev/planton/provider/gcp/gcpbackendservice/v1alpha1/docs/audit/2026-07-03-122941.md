# Audit Report: GcpBackendService

**Audit Date:** 2026-07-03 12:29:41
**Component Kind:** GcpBackendService
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpbackendservice/v1alpha1/`
**Enum Value:** 625
**ID Prefix:** gcpbs

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpBackendService = 625` in the networking/LB sub-band (623–629); `id_prefix` `gcpbs` unique across the whole registry; `prerequisites: [GcpHealthCheck]` drives the E2E dependency chain. Kind map regenerated.

### Protobuf API Definitions

- All four protos with dense field comments. FK wiring: `health_check` → `GcpHealthCheck(status.outputs.self_link)` (SINGULAR — the provider caps `health_checks` at exactly one), `security_policy` and `edge_security_policy` → `GcpCloudArmorPolicy(status.outputs.policy_self_link)` (post-cache vs pre-cache filtering), `project_id` → `GcpProject`, and `backends[].group` as an intentionally un-defaulted `StringValueOrRef` (instance-group/NEG kinds attach their `default_kind` as they land in the catalog; `ValueFromRef.kind` composes explicitly today).
- Spec models the full released 6.x surface (verified against a released-provider schema dump, not the clone's main): protocol incl. H2C, all four schemes, all 8 affinity modes incl. STRONG_COOKIE_AFFINITY + its cookie, backends with all balancing modes and capacity dials + per-backend ORCA metrics, the backend-service-flavor cdn_policy with the rich cache key, IAP, log config, custom headers, compression, circuit breakers, outlier detection, consistent hash, max stream duration, security settings (mTLS + AWS SigV4), TLS settings with the SAN oneof, IP selection policy, the EXTERNAL→EXTERNAL_MANAGED canary controls, service-level custom metrics, service LB policy, and folded signed-URL keys.
- 14 message-level CEL rules enforce pre-deploy what GCP would reject or silently strip: CDN↔external-scheme, scheme applicability of circuit breakers / max_stream_duration / outlier detection / consistent hash, affinity-mode↔cookie coherence, UDP↔affinity, TLS-settings↔protocol, per-backend balancing-mode↔dial coherence, cache-mode/TTL coherence, query white/blacklist exclusivity, IAP client pairing, migration-state coherence, and backend-preference↔scheme. All messages user-facing quality.
- 49-case spec test suite green (`go test ./apis/dev/planton/provider/gcp/gcpbackendservice/v1alpha1/`).

### IaC Modules (both engines)

- Both create the backend service plus one signed-URL-key resource per configured key (folded per the granularity rule: never FK-referenced, max 3, lifecycle owned by the service), enable the Compute Engine API first (`disable_on_destroy=false`), apply the ambient-project contract, and treat `""`/0 as unset so GCP API defaults apply — with `capacity_scaler` passing through whenever present because an explicit 0 is the drain-this-backend semantics (it is `optional` in the proto, so unset is distinguishable).
- Documented provider subtleties in both modules: `security_policy`/`edge_security_policy` apply via dedicated setSecurityPolicy sub-calls, `max_utilization` is stripped from NEG backends by the provider, and `max_stream_duration.seconds` is a string (int64 format) unlike every other Duration in the resource.
- TF runs on plain `google ~> 6.0` (every modeled field is GA on the released line — no beta dependency; google-beta declared for catalog-wide consistency only). Pulumi on pinned pulumi-gcp v9.29.0, whose surface was verified field-by-field before authoring.
- Release-equivalent Pulumi build, `tofu validate`, and offline `planton tofu plan` through the real tfvars converter green (plan inspected — all fields flow into the resource).

### Cross-Engine Parity (Category 5b gate)

Walked all parity dimensions: naming basis (`backend_service_name` → `metadata.name` fallback), full spec coverage both sides (every field in variables.tf has its `if`-guarded Pulumi counterpart), identical `""`→absent and 0→unset normalization incl. the capacity-scaler pass-through, identical API-enablement dependency edge, identical outputs (`self_link`, `backend_service_name`, `generated_id` string-ified on both sides, `fingerprint`). `planton validate-outputs` dry-run: 4/4 populated against BOTH module dirs. Conformance case added to `pkg/outputs` and green. **No PARITY-EXCEPTION needed.** **PARITY ✅**

### Secure-by-Default (Category 11 gate)

Three secret-bearing fields annotated `(sensitive) = true`: `iap.oauth2_client_secret` (an OAuth2 credential), `security_settings.aws_v4_authentication.access_key` (an AWS secret access key), and `signed_url_keys[].key_value` (signing key material). All three are `pulumi.ToSecret` in state and never surface in outputs. `planton secret-coverage --check` green; zero `gaps[]` entries for this kind. **COVERAGE ✅**

### Supporting Files, Presets, Docs

- `iac/hack/manifest.yaml` validates; 3 presets (external web backend, CDN-cached API, IAP-protected internal tool) with companion docs, all validating; README + catalog page + research doc complete, incl. recorded skip reasons (beta-only `dynamic_forwarding` / `network_pass_through_lb_traffic_policy` / in-flight dials / backend-service IAM trio; `deletion_policy` and `params` absent from the released 6.x provider entirely).

### Live E2E

- Dual-engine composed scenario with a GcpHealthCheck prerequisite (the health check's first outing as a prerequisite): Pulumi 110s, Terraform 121s — prerequisite deploy → FK resolution → create → compute API verify (incl. the exactly-one-health-check wiring) → destroy → 404 verify → prerequisite teardown. The scenario exercises CDN + cache-key policy, generated-cookie affinity, compression, custom headers, and sampled logging live; it deliberately carries no `backends[]` (valid — probe first, attach capacity later; no instance-group/NEG kind exists yet to compose against — recorded in the profile). IAP is offline-only (needs an OAuth client, an org-level artifact). Zero orphans confirmed by post-run `gcloud compute backend-services list` and `gcloud compute health-checks list`.
