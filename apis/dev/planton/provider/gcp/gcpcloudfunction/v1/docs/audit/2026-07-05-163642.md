# Audit Report: GcpCloudFunction

**Audit Date:** 2026-07-05 16:36:42
**Component Kind:** GcpCloudFunction
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpcloudfunction/v1/`
**Enum Value:** 602
**ID Prefix:** cldfunc

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpCloudFunction = 602`, `id_prefix: cldfunc` (unchanged —
  depth rebuild in place, no rename, no renumber)
- `prerequisites: [GcpServerlessVpcConnector]` — the commonly-composed
  private-egress reference, driving the composed E2E chain. GcpGcsBucket is
  deliberately NOT a prerequisite: the source object must be test-staged
  (object bytes are not IaC), so a prerequisite bucket would deploy unused
  every run

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Full anatomy; legacy defects closed: mislabeled `iac/tf/hack/manifest.yaml`
  (declared GcpArtifactRegistryRepo) replaced by canonical
  `iac/hack/manifest.yaml`; stray `module/planton-with-providers.code-workspace`
  removed; Pulumi entrypoint conformed from `module/resources.go` to
  `module/main.go`

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- Spec rebuilt to the released Gen 2 floor, fields renumbered contiguously;
  the "80/20" header language is gone
- New surfaces: `description`, user `labels`, CMEK (`kms_key_name` →
  GcpKmsKey), build `service_account` (→ GcpServiceAccount, fully-qualified
  name), `worker_pool`, `docker_repository` (ref-shaped), AUTOMATIC XOR
  ON_DEPLOY update policy, `repo_source` arm (branch/tag/commit XOR),
  `available_memory` in provider quantity-string form + `available_cpu`,
  `secret_volumes`, `all_traffic_on_latest_revision`,
  `binary_authorization_policy`
- FK wiring: `source.bucket` → GcpGcsBucket; three SA fields →
  GcpServiceAccount; `vpc_connector` → GcpServerlessVpcConnector
  (`self_link`); `pubsub_topic` → GcpPubSubTopic (`topic_id` — the full
  `projects/*/topics/*` path Eventarc expects)
- **Secret-gap closure:** `secret_environment_variables` remodeled from
  `map<string,string>` to provider-authentic `{key, secret, version,
  project_id}` reference messages with `sensitive_exempt_reason` on the
  secret NAME (the Gen 2 API accepts no literal secret material);
  `GcpCloudFunction` line removed from `pkg/secretcoverage/baseline.yaml` —
  the GCP surface's last baseline entry
- `runtime` is a free validated string (the provider models it as one; a
  hardcoded runtime allowlist is a staleness trap)
- CEL: source exactly-one, repo-source exactly-one-revision, scaling
  min≤max, event-trigger-required — complete sentences
- Outputs extend-only: `name` (the serverless-NEG composition key), `uri`,
  `environment`, `update_time` added; all six prior outputs preserved
- 51-case spec test, all passing

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- Full new-surface implementation; five APIs enabled (cloudfunctions,
  cloudbuild, run, artifactregistry, eventarc); stale commented `binary:`
  option dropped from `Pulumi.yaml`
- Ambient project materialized only where the API demands it (secret
  entries require an explicit per-entry project), mirroring the TF module's
  `data.google_project` lookup
- Optional-block outputs resolved through ApplyT so a function without
  service_config still exports empty strings

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- Hard pin `6.19.0` → `~> 6.0`; API enablement added; converter-contract
  variables; `tofu validate` green; offline plan through the real converter
  clean (labels, secret env + volumes with resolved project, connector
  egress, update policy inspected)

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs` on BOTH module dirs (9/10
fields populated; `eventarc_trigger_id` legitimately empty for HTTP
functions). Live dual-engine E2E green on both scenarios.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| **Labels** | `planton-ai_*` platform set, user labels merged beneath | same (was: unprefixed keys — **the live label-parity break is closed**) | ✅ MATCH |
| Project / scope source | omit arg when spec empty | `null` fallback | ✅ MATCH |
| Resource naming basis | `function_name` → `metadata.name` fallback | same | ✅ MATCH |
| Secret entry project default | ambient project via GetClientConfig | `data.google_project` | ✅ MATCH |
| Update policy | ON_DEPLOY sends empty block; AUTOMATIC sends nothing (API default) | same | ✅ MATCH |
| Egress settings | only sent with a connector attached | same | ✅ MATCH |
| Public invoker | run.invoker/allUsers on the underlying Cloud Run service, HTTP + opt-in only | same | ✅ MATCH |
| API enablement | 5 services, disable_on_destroy=false | same | ✅ MATCH |
| Stack outputs → proto | resolved attributes; optional blocks null-safe | same via `try()` | ✅ MATCH |

**Effect on score:** none — engines in parity.

---

### 6–10. Documentation, Supporting Files, Presets

✅ **Passed:**

- `docs/README.md`: Gen 2 = Cloud Run + Eventarc composition boundary, the
  two-config split, trigger model, and **recorded skips verified against the
  released schema**: direct VPC egress (`direct_vpc_network_interface`/
  `direct_vpc_egress`) and `deletion_policy` are absent from released 6.50.0
  for this resource (one-engine-field class — revisit when the released line
  carries them); function IAM trio unmodeled (authoritative-clobber class)
- `README.md` + `catalog-page.md` rewritten timeless (no history narration)
- Presets 2 → 3: http-api / pubsub-event / private-vpc-egress (teaching the
  connector composition); all validate
- Site catalog refreshed (function page + presets; stale http-trigger preset
  removed)

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

Zero `gaps[]` for `GcpCloudFunction:` — the baseline entry is REMOVED (not
merely accepted): secret env vars and secret volumes are Secret Manager
references whose names carry `sensitive_exempt_reason`; no secret material
can enter the spec.

---

## Critical Gaps

None.

## Validation Evidence

- Spec tests 51/51; Pulumi module build (vet+fmt); `tofu validate` + offline
  converter plan; `validate-refs --check`; `secret-coverage --check` (gate
  green with the baseline entry removed); `validate-outputs` on both module
  dirs; `pkg/outputs` conformance case green; all manifests (hack + 3 presets
  + 3 consumer-scoped prerequisites + 2 scenarios) through `planton validate`
  on a freshly built CLI
- **Live dual-engine E2E green on `planton-e2e`** (4/4 scenario-runs):
  http-minimal 11m11s (Pulumi) / 9m18s (Terraform) and vpc-connector-egress
  10m49s (Pulumi) / 10m05s (Terraform) — each deploy running a real Cloud
  Build from test-staged GCS source, the composed scenario resolving the
  connector prerequisite's `self_link` live through the FK default. Zero
  orphans after teardown (functions, connectors, networks, staging buckets
  all cleaned; only GCP's own `gcf-v2-sources-*` system bucket remains).
- Recorded live-E2E exclusions: event-trigger arm (Eventarc SA propagation
  wait is flaky on fresh projects — proven offline via converter plan), CMEK
  (org-level KMS grants to the Cloud Functions + Artifact Registry service
  agents — proven offline), `repo_source` (Cloud Source Repositories is
  deprecated for new customers — no test repo exists).

**Audit completed at:** 2026-07-05-163642
**Reference:** `architecture/deployment-component.md`
