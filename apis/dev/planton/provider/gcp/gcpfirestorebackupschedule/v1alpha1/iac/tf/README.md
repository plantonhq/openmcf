# Terraform module for GcpFirestoreBackupSchedule

Provisions a `google_firestore_backup_schedule` from the validated protobuf spec. Enables `firestore.googleapis.com` automatically.

See the component [README](../../README.md) for the full configuration reference.
