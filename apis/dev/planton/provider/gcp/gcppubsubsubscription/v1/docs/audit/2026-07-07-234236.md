# Audit Report: GcpPubSubSubscription

**Audit Date:** 2026-07-07 23:42:36
**Component Kind:** GcpPubSubSubscription
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcppubsubsubscription/v1/`
**Enum Value:** 661
**ID Prefix:** gcppss

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Three defect classes were closed in this rebuild:

1. **Deploy-blocking Pulumi scaffolding** — the module had NO `Pulumi.yaml` (and no `Makefile`): the Pulumi engine could never have deployed this kind at all. Both authored to the canonical shape.
2. **Live label-parity break** — the Terraform module stamped no labels while Pulumi stamped the full platform set. Both engines now merge user labels beneath the identical `planton-ai_*` set; the E2E verifier asserts the attribution label live.
3. **Composition defects** — `bigquery_config.table` was a plain dotted string (now a `GcpBigQueryTable` reference via its new `qualified_name` output) and the three delivery `service_account_email` fields were plain strings (now `GcpServiceAccount` references). The registry gained `prerequisites: [GcpPubSubTopic]` for the required topic FK.

---

### 3. Protobuf API Definitions — validation quality note

Spec at the released 6.50.0 floor: all four delivery modes under the message-level exclusivity CEL, dead-lettering, retry backoff, expiration (with the empty-ttl never-expire sentinel), exactly-once, ordering, filtering, user `labels`, and `message_transforms[]` (JavaScript UDF arm). New CEL: the reserved-`goog` name prefix rejected pre-deploy. `project_id` is ambient-optional. ForceNew semantics (`topic`, `filter`, `enable_message_ordering`) taught in field comments. 56-case spec test.

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/subscription.go` + `locals.go`) and Terraform (`iac/tf/main.tf` + `locals.tf`), judged against the proto contract. Live-proven 4/4 on `planton-e2e` (basic-pull + reliable-pull × both engines, each on the schema → topic prerequisite chain).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| API enablement | `pubsub.googleapis.com`, disable_on_destroy false | same | ✅ MATCH |
| Resource naming basis | `spec.subscription_name` | `local.subscription_name` | ✅ MATCH |
| Topic attachment | resolved ref value (ForceNew) | same | ✅ MATCH |
| Ambient project | empty omitted → provider default | empty → `null` | ✅ MATCH |
| Labels | user-first merge beneath `planton-ai_*` set | identical merge order | ✅ MATCH |
| Ack/retention/filter | omit-when-zero/empty | `null`-when-zero/empty | ✅ MATCH |
| Booleans (retain/ordering/exactly-once) | true-only vs explicit false | provider defaults false — behaviorally identical | ✅ MATCH |
| Expiration ttl | sent as-is ("" = never) | same | ✅ MATCH |
| Dead-letter / retry | same omit rules | same | ✅ MATCH |
| Push (attributes/OIDC/no-wrapper) | resolved SA ref; audience omit-when-empty | same | ✅ MATCH |
| BigQuery config | resolved table + SA refs; bool flags true-only | provider defaults false — identical | ✅ MATCH |
| Cloud Storage config (+avro) | field-for-field | same | ✅ MATCH |
| Message transforms | ordered list; disabled true-only | provider default false — identical | ✅ MATCH |
| Dependency ordering | `DependsOn(api)` | `depends_on = [api]` | ✅ MATCH |
| Stack outputs → proto | subscription_id (resource ID), subscription_name | same | ✅ MATCH |

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅** — no secret-bearing fields (push auth mints OIDC tokens from a referenced identity, never an embedded credential); `secret-coverage --check` green.

---

## Recorded skips (with reasons; schema-probe verified against released 6.50.0)

- `tags` (resource-manager tags) — absent from the released `google ~> 6.x` line.
- `message_transforms.ai_inference` — the Vertex AI arm is absent from the released line.
- `deletion_policy` — client-side lever conflicting with Planton-managed destroy (catalog-wide skip).
- Per-subscription IAM trio — resource-scoped IAM deferred pending concrete pull.
- Live-E2E exclusions: the dead-letter arm needs a SECOND topic instance, which the one-manifest-per-prerequisite-kind install model cannot express — its `dead_letter_topic` reference is the identical `StringValueOrRef` resolution mechanism as the live-proven `topic` ref, and the arm is proven by the offline converter plan on the dead-letter preset shape. Push/BigQuery/Cloud-Storage delivery need a live receiving endpoint / IAM-granted table / bucket write grant — proven offline.

## Validation evidence

- Spec test: 56/56 green. Both engines build (`go build` + release-equivalent Pulumi entrypoint).
- `validate-outputs` green on BOTH module dirs; `pkg/outputs` conformance case present.
- Offline `planton tofu plan` via the real converter: 2 to add, clean. 4 presets + hack manifest validate.
- Live dual-engine E2E on `planton-e2e`: basic-pull + reliable-pull 4/4 green (~110s–121s per scenario incl. the schema → topic prerequisite chain); zero orphans.
