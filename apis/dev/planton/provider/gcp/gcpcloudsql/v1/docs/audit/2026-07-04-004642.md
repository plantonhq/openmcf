# Audit Report: GcpCloudSql

**Audit Date:** 2026-07-04 00:46:42  
**Component Kind:** GcpCloudSql  
**Provider:** gcp  
**Component Path:** `apis/dev/planton/provider/gcp/gcpcloudsql/v1/`  
**Enum Value:** 604  
**ID Prefix:** gcpsql  
**Audit Mode:** `--parity` (parity-led full audit)

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Parity Verdict (lead finding — `--parity` run)

**PARITY ✅** — every dimension MATCHES against the proto contract
(`spec.proto` + `stack_outputs.proto`); neither engine privileged. All 35
spec fields are honored by BOTH engines with identical omitted-block
defaults, identical `planton-ai_*` labels, identical API enablement, the
same dependency edge, and the same 8 stack-output names. Output parity is
verified by the automated guard: the `GcpCloudSql` case in
`pkg/outputs/conformance_test.go` passes with full proto population from
both engines' output names (verified this session). No undocumented
divergences were found. See §5b for the full dimension table.

---

## Quick Wins

None outstanding. Two non-blocking housekeeping observations:

1. **Local run artifacts under `iac/`**
   - **Why:** `iac/pulumi/stack-input.yaml`, `iac/pulumi/Pulumi.e2e-*.yaml`, and `iac/tf/{.terraform/,backend.tf,.terraform.lock.hcl}` are leftovers from this session's offline/E2E runs. They are gitignored-or-untracked local state, not component content — just confirm they never get committed.
   - **How:** Nothing to do if `.gitignore` already covers them (it appears to).
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsql/v1/iac/`

2. **Bare `string.pattern` constraints without CEL wrappers**
   - **Why:** `instance_name`, `region`, and `authorized_networks[].value` use bare `string.pattern` rules, so a violation surfaces protovalidate's default regex-bearing message instead of a hand-written sentence. The field comments do explain the rules in plain English with examples, so this is polish, not a defect (same note as the GcpVpcNetwork audit precedent).
   - **How:** Wrap each pattern in a `.cel` rule with a plain-English message per the `002-spec-validate` standard.
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsql/v1/spec.proto`

---

## Critical Gaps

None. No blocking issues found.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpCloudSql = 604` exists in `apis/dev/planton/shared/cloudresourcekind/cloud_resource_kind.proto` (verified this session)
- Enum value 604 is in the correct GCP range (600–799)
- `id_prefix: "gcpsql"` is unique — single occurrence in the registry (the sibling kinds use distinct `gcpsqldb` / `gcpsqlusr` prefixes)
- `kind_meta` includes provider (`gcp`), version, id_prefix, and `prerequisites: [GcpServiceNetworkingConnection]` (verified this session)

❌ **Failed:**

- None

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists: `apis/dev/planton/provider/gcp/gcpcloudsql/v1/`
- Under the correct provider hierarchy (`gcp`, non-Kubernetes)
- Folder name is the lowercase enum name
- `v1/` subfolder exists

❌ **Failed:**

- None

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files (13.32%)

✅ **Passed:**

- `api.proto` exists (857 B — envelope + status, correct constants `gcp.planton.dev/v1` / `GcpCloudSql`)
- `spec.proto` exists (37.5 KB) — 35 top-level fields plus 12 nested messages covering the full instance surface: engine/version/tier/edition, HA, disk, three-path connectivity (public IPv4 + authorized networks, private VPC IP, PSC with auto-connections), backups/PITR, maintenance scheduling, Query Insights, password policy, connection pooling, database flags, the SQL Server-only surface, CMEK, both delete guards, and read-replica configuration
- `stack_input.proto` exists (381 B)
- `stack_outputs.proto` exists (1.3 KB) — 8 fields, each with a purpose-explaining comment
- 15 message-level CEL rules encode cross-field engine coherence (version-prefix ⇔ engine, PITR vs binary-log per engine, REGIONAL ⇒ backups, SQL Server fencing, Enterprise Plus data cache, connectivity coherence, replica pairing)

⚠️ **Warnings:**

- Three bare `string.pattern` constraints (`instance_name`, `region`, `authorized_networks[].value`) surface protovalidate default messages on violation; field comments explain the rules in plain English with examples. Noted as low-priority polish, not penalized (consistent with the GcpVpcNetwork audit precedent).

**Validation message quality:** every CEL `message` is a complete, user-facing sentence naming the semantic field and explaining both condition and requirement (e.g. "availability_type REGIONAL (high availability) requires automated backups — set backup.enabled to true"). No terse messages, no raw regex as primary text (duration/format rules state the expected shape with an example, e.g. "must be a seconds duration string such as 3600s").

**Score:** 13.32% / 13.32%

#### 3.2 Generated Stubs (3.33%)

✅ **Passed:**

- `api.pb.go` (9.3 KB), `spec.pb.go` (92.3 KB), `stack_input.pb.go` (6.5 KB), `stack_outputs.pb.go` (8.6 KB) all exist and were regenerated 2026-07-04 00:09, after the last proto edits (23:53)

**Score:** 3.33% / 3.33%

#### 3.3 Unit Tests - Presence (2.77%)

✅ **Passed:**

- `spec_test.go` exists (21.3 KB)
- Ginkgo/Gomega suite with 75 `It(...)` cases exercising the required fields, every enum-valued string, and each of the 15 cross-field CEL rules (both accepting and rejecting sides)
- Imports `testing`, `ginkgo/v2`, `gomega`, `protovalidate`

**Score:** 2.77% / 2.77%

#### 3.4 Unit Tests - Execution (2.78%)

✅ **Passed (verified this session):**

```bash
go test ./apis/dev/planton/provider/gcp/gcpcloudsql/v1/
# ok — all tests pass
```

**Score:** 2.78% / 2.78%

**Total Protobuf API Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

#### 4.1 Module Files (6.66%)

✅ **Passed:**

- `iac/pulumi/module/main.go` exists — commented entry point
- `iac/pulumi/module/locals.go` exists — documents why the resource-name label keys off `spec.instance_name` (matches what is visible in the GCP console) and that Terraform applies the identical set
- `iac/pulumi/module/instance.go` exists (14.5 KB) — carries the full why-comment bar: lifecycle semantics the API enforces (name reservation window, in-place vs ForceNew, disks never shrink, private network never removable), why `disable_on_destroy` stays false, the engine-side vs API-side delete-guard distinction, why the data-cache stanza is emitted only when enabled, and which fields are secret material encrypted in state
- `iac/pulumi/module/outputs.go` exists — output-name constants matching the proto field names, each annotated

❌ **Failed:**

- None

**Score:** 6.66% / 6.66%

#### 4.2 Entrypoint Files (6.66%)

✅ **Passed:**

- `iac/pulumi/main.go` exists at the root and declares `package main`
- No `iac/pulumi/main/` and no `iac/pulumi/entrypoint/` subdirectory
- `iac/pulumi/Pulumi.yaml` exists
- `iac/pulumi/Makefile` exists
- Release-equivalent build passes (verified this session):

```bash
go build -o /dev/null ./apis/dev/planton/provider/gcp/gcpcloudsql/v1/iac/pulumi
```

**Score:** 6.66% / 6.66%

**Total Pulumi Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `iac/tf/variables.tf` exists (8.2 KB) — curated `optional()` form with per-field comments, including where the tfvars converter resolves `StringValueOrRef` fields to plain strings
- `iac/tf/provider.tf` exists — google provider pinned `~> 6.0`, with the catalog-wide pinning rationale in a comment
- `iac/tf/locals.tf` exists (4.8 KB) — commented: project-id null-fallback rationale, empty-string→null conversion rationale, the omitted-block disk/network defaults, the HCL `&&` non-short-circuit / `try()` quirk, and the label-parity rationale
- `iac/tf/main.tf` exists (12.2 KB) — mirrors the Pulumi module's lifecycle comments and dynamic-block conditions
- `iac/tf/outputs.tf` exists — 8 outputs matching the proto field names, with descriptions
- `tofu validate` passes; offline `planton tofu plan` through the real tfvars converter passes with 2 to add (API enablement + instance) — verified this session

❌ **Failed:**

- None

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/`) and Terraform (`iac/tf/`), both judged
against the proto contract (`spec.proto` + `stack_outputs.proto`); neither engine
privileged. Output parity verified with the automated guard, not by eye: the
`GcpCloudSql` case in `pkg/outputs/conformance_test.go` passes with FULL proto
population from both engines' output names (verified this session, zero unmapped).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source (project) | `spec.project_id` value; omitted when empty → provider default (on API-enable + instance) | `local.project_id` = value or `null` → provider default (on both resources) | ✅ MATCH |
| Resource naming basis | `spec.instance_name` | `var.spec.instance_name` | ✅ MATCH |
| Labels (keys + values) | `gcplabelkeys` set: `planton-ai_resource/name/kind` always, `planton-ai_id/organization/environment` conditional; kind = lowercased `CloudResourceKind_GcpCloudSql.String()` = `gcpcloudsql`; name = `spec.instance_name` | identical `planton-ai_*` keys and values, same conditional presence; kind literal `gcpcloudsql` (same value — HCL cannot reference the Go enum) | ✅ MATCH |
| Pod/service selectors | n/a (managed GCP database, no workloads) | n/a | ✅ N/A |
| Spec feature coverage | all 35 fields honored (see field walk below) | all 35 fields honored | ✅ MATCH |
| Omitted-block defaults | disk → 10 GB PD_SSD, auto-resize on, limit 0; network → public IPv4, no authorized networks | identical (`locals.tf` disk/network fallbacks) | ✅ MATCH |
| API enablement | `projects.Service` `sqladmin.googleapis.com`, `disable_dependent_services=true`, `disable_on_destroy=false` | `google_project_service` identical flags | ✅ MATCH |
| Dependency ordering | `pulumi.DependsOn(createdProjectService)` → instance | `depends_on = [google_project_service.sqladmin_api]` | ✅ MATCH |
| Secret handling | `root_password`, `replica_configuration.password/client_key` wrapped `pulumi.ToSecret` (encrypted in state) | same fields flow through the sensitive-annotated tfvars path; empty → `null` so nothing is sent | ✅ MATCH |
| Stack outputs → proto | `instance_name`, `connection_name`, `private_ip`, `public_ip`, `self_link`, `service_account_email`, `dns_name`, `psc_service_attachment_link` | identical 8 outputs | ✅ MATCH (conformance green) |

**Legend:** ✅ MATCH | ⚠️ DIVERGES (undocumented) | 🔵 EXCEPTION (documented `PARITY-EXCEPTION:`)

Field-by-field spec coverage (both engines; omit-when-empty so the API applies its defaults):

- `project_id` — set only when non-empty; empty falls back to the provider's default project on BOTH the API-enable resource and the instance ✅
- `instance_name`, `region`, `database_version`, `tier` — always sent ✅
- `database_engine` — validation-only on both engines by design (the API derives the engine from `database_version`); documented in `variables.tf` ✅
- `edition` / `availability_type` / `activation_policy` — middleware-defaulted (`ENTERPRISE` / `ZONAL` / `ALWAYS`) and passed through on both ✅
- `disk.{type,size_gb,auto_resize,auto_resize_limit}` — identical omitted-block default (10 GB PD_SSD, auto-resize, no limit) and identical passthrough when present ✅
- `network` omitted — both resolve to `ipv4_enabled=true`, no authorized networks (Auth Proxy / connector access only). Pulumi omits `enable_private_path_for_google_cloud_services` while TF sends `false` explicitly — cosmetic wire difference, `false` is the API default; identical deployed outcome ✅
- `network.{private_network,ipv4_enabled,authorized_networks(value,name,expiration_time),allocated_ip_range,enable_private_path_for_google_cloud_services,ssl_mode,server_ca_mode,server_ca_pool,custom_subject_alternative_names}` — all honored, empty → omitted/null on both ✅
- `network.psc.{enabled,allowed_consumer_projects,network_attachment_uri,auto_connections(consumer_network,consumer_service_project_id)}` — block emitted only when `psc.enabled` on both ✅
- `location_preference.{zone,secondary_zone}` — block emitted only when a zone is set, on both ✅
- `backup.{enabled,start_time,location,binary_log_enabled,point_in_time_recovery_enabled,transaction_log_retention_days,retained_backups}` — block emitted when present; `retained_backups` maps to `backup_retention_settings` with `retention_unit COUNT` on both ✅
- `maintenance_window.{day,hour,update_track}` / `deny_maintenance_period.{start_date,end_date,time}` — identical block conditions ✅
- `insights_config.*` — block emitted only when `query_insights_enabled`, on both ✅
- `password_validation_policy.*` — all 6 fields, identical empty→omit handling ✅
- `data_cache_enabled` — stanza emitted only when true on both (ENTERPRISE instances carry no cache stanza; the API rejects it) ✅
- `connection_pooling.{enabled,flags}` — block emitted only when enabled; flags map → provider list shape on both ✅
- `database_flags` — map → list on both ✅
- `threads_per_core` (advanced_machine_features), `time_zone`, `collation`, `sql_server_audit_config.*`, `active_directory_domain` (active_directory_config) — SQL Server surface identical ✅
- `connector_enforcement`, `enable_google_ml_integration`, `enable_dataplex_integration` — identical ✅
- `encryption_key_name` (CMEK), `master_instance_name` — set only when non-empty, on both ✅
- `deletion_protection` (engine-side guard) and `deletion_protection_enabled` (API-side guard) — both wired distinctly on both engines, with the distinction documented in comments on both sides ✅
- `retain_backups_on_delete` — identical ✅
- `replica_configuration.*` — all 12 fields, block emitted only when present, empty→omit per field, secrets protected on both ✅
- `root_password` — `pulumi.ToSecret` on Pulumi; `null`-when-empty on TF with the sensitive tfvars path ✅

✅ **In parity:**

- All dimensions above.

⚠️/❌ **Divergences (undocumented):**

- None found.

**Effect on score:** none — the gate passes; Categories 4 & 5 keep their full scores.

---

### 6. Documentation - Research (13.18%)

✅ **Passed:**

- `docs/README.md` exists (7.1 KB) — dense design-and-research doc in the current sweep's style (precedent: GcpWorkloadIdentityPool audit, 6.7 KB, full credit)
- Decomposition rationale with the split test applied per candidate (databases, users, replicas-as-arm-not-kind, deferred SSL client certs)
- Connectivity-model analysis ("the part people get wrong"): all three paths with their prerequisites and the safe default
- Engine-coherence validation rationale (why each CEL rule exists, tied to the cost of mid-deploy API failures)
- Lifecycle sharp edges (name reservation, in-place vs ForceNew, never-shrink disks, two delete guards)
- Explicit 90/10 coverage section ("Deliberately unmodeled (with reasons)") including provider-version-gated fields recorded for the next depth pass
- Composition map (all FK edges in and out) and operational guidance

⚠️ **Warnings:**

- The doc is in the 5–10 KB band and carries no deployment-methods maturity comparison (console/CLI/DM/TF/Pulumi levels) like the older 20 KB-style research docs. The sweep's convention credits content density over raw size, and every content check that matters for maintainability (purpose, scope decisions, gotchas, composition) is covered — full credit, noted for a future depth pass.

**Score:** 13.18% / 13.18%

---

### 7. Documentation - User-Facing (13.09%)

✅ **Passed:**

- `README.md` exists at v1/ level (11.1 KB — well above the 2 KB "good" bar, refreshed this session)
- `catalog-page.md` also present (6.3 KB, refreshed this session)

**Score:** 13.09% / 13.09%

---

### 8. Supporting Files (10.09%)

✅ **Passed:**

- `iac/pulumi/README.md` exists (3.0 KB)
- `iac/tf/README.md` exists (2.5 KB)
- `iac/hack/manifest.yaml` exists (1.2 KB) — exercises the deep instance surface offline (HA + backups + PITR, authorized network, maintenance, insights, flags, both guards); passes `planton validate` (verified this session)
- `e2e/` present: `profile.yaml`, `scenarios/{public-postgres,private-postgres}.yaml`, `prerequisites/gcpglobaladdress.yaml` (scenario `${E2E_RUN_ID}` tokens fail raw-regex validation by established precedent — not a defect)

❌ **Failed:**

- None

**Score:** 10.09% / 10.09%

---

### 9. Nice to Have (10%)

✅ **Passed:**

- `BUILD.bazel` at `v1/`, `iac/pulumi/`, and `iac/pulumi/module/` — all present and regenerated after this session's changes

**Score:** 10.00% / 10.00%

---

### 10. Presets (5.00%)

#### 10.1 Directory and File Checks (2.50%)

✅ **Passed:**

- `presets/` directory exists
- 3 preset pairs: `01-postgres-production-private.{yaml,md}`, `02-mysql-high-availability.{yaml,md}`, `03-postgres-read-replica.{yaml,md}`
- Every `.yaml` has a companion `.md`; no orphans
- Naming follows `{NN}-{kebab-case}.yaml`; no duplicate ranks

**Score:** 2.50% / 2.50%

#### 10.2 Content Correctness (2.50%)

✅ **Passed:**

- `apiVersion: gcp.planton.dev/v1` and `kind: GcpCloudSql` match the `api.proto` constants in all three presets
- `metadata.name` present and `my-`-prefixed (`my-postgres-prod`, `my-mysql-ha`, `my-postgres-replica`)
- `spec` present and non-empty; no `status` block in any preset
- All `StringValueOrRef` fields use wrapper form: `projectId` uses `value:`; `privateNetwork` and `masterInstanceName` use `valueFrom:` with kind/name/fieldPath — demonstrating the composition edges (GcpVpcNetwork network, GcpCloudSql primary)
- All field names exist in the current `spec.proto`; engine-coherent combinations (PITR on Postgres, binary log on MySQL HA, replica without backup block)
- Annotated defaults respected (`edition: ENTERPRISE` explicit in 01; middleware defaults relied on elsewhere)
- All presets pass `planton validate` (verified this session)

**Score:** 2.50% / 2.50%

**Total Presets Score:** 5.00% / 5.00%

> **Tip:** For a deeper preset audit (gap analysis, missing patterns), run `@audit-planton-presets GcpCloudSql`.

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` passes repo-wide with zero `GcpCloudSql:` gaps and no
sensitive/exempt contradiction (verified this session). Per-field result for this kind:

| Field path | Classification | Note |
|-----------|----------------|------|
| `spec.root_password` | covered | annotated `(sensitive) = true`; write-only in GCP, `ToSecret` in Pulumi state |
| `spec.replica_configuration.password` | covered | annotated `(sensitive) = true`; replication-channel secret |
| `spec.replica_configuration.client_key` | covered | annotated `(sensitive) = true`; private-key material |
| `spec.password_validation_policy.password_change_interval` | exempt | `sensitive_exempt_reason: "a rotation-interval duration, not credential material"` — truthful look-alike exemption |
| `spec.replica_configuration.ca_certificate` / `client_certificate` | not flagged | public trust/handshake material, documented as such in field comments |

❌ **Gaps (unannotated secret-bearing fields):**

- None.

**Effect on score:** none.

---

## Verification Performed (facts from this session, relied upon per audit instructions)

| Check | Command | Result |
|-------|---------|--------|
| Spec unit tests | `go test ./apis/.../gcpcloudsql/v1/` | ✅ pass |
| Pulumi release build | `go build -o /dev/null ./apis/.../v1/iac/pulumi` | ✅ pass |
| Terraform validate | `tofu validate` in `iac/tf/` | ✅ valid |
| Offline TF plan | `planton tofu plan` via real tfvars converter | ✅ 2 to add (API enablement + instance) |
| Outputs conformance | `pkg/outputs/conformance_test.go` `GcpCloudSql` case | ✅ PASS, full population from both engines |
| Secret coverage | `planton secret-coverage --check` | ✅ zero `GcpCloudSql:` gaps |
| Reference validation | `planton validate-refs --check` | ✅ pass |
| Manifest validation | `planton validate` on presets + hack manifest | ✅ pass (E2E `${E2E_RUN_ID}` tokens exempt by precedent) |

The parity comparison itself (§5b) was performed in this audit by reading both modules
field-by-field against `spec.proto`.

---

## Prioritized Recommendations

### High Priority (Do First)

None — no blocking issues.

### Medium Priority (Do Next)

None.

### Low Priority (Polish)

1. **CEL-wrap the bare `string.pattern` constraints**
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsql/v1/spec.proto`
   - **Why:** `instance_name`, `region`, and `authorized_networks[].value` violations currently surface protovalidate's default regex-bearing message; a CEL wrapper would show a plain-English sentence.
   - **How:** Replace each `(buf.validate.field).string.pattern` with a `.cel` rule reusing the regex, message per the `002-spec-validate` standard.

2. **Future depth pass on provider-gated fields**
   - **File:** `apis/dev/planton/provider/gcp/gcpcloudsql/v1/docs/README.md`
   - **Why:** Read pools, `entraid_config`, write-only root password, and Hyperdisk IOPS dials are recorded as not-on-6.x skips; re-evaluate when the catalog moves past the `~> 6.0` provider line.
   - **How:** Already recorded in the research doc's "Deliberately unmodeled" section; revisit on the provider major bump.

---

## Comparison to Complete Components

**Most Similar Complete Component:** `GcpVpcNetwork` (`apis/dev/planton/provider/gcp/gcpvpcnetwork/v1/`) — same sweep's rebuild pattern (omit-when-empty conventions, API-enablement dependency edge, `planton-ai_*` label parity, conformance case, presets with FK `valueFrom` composition). GcpCloudSql matches it in structure and rigor and exceeds it in spec depth (35 fields, 15 cross-field CELs) and preset count (3 vs 2). No gaps relative to it.

---

## Next Steps

1. Nothing blocking — component is release-ready and in parity.
2. (Optional polish) CEL-wrap the three bare pattern constraints.
3. Record `GcpCloudSql | 2026-07-04 | PARITY ✅ | none open | docs/audit/2026-07-04-004642.md` in the parity ledger if one is maintained.

---

## Appendix: Full Checklist

### Critical Items (48.64%)

#### Cloud Resource Registry (4.44%)

- [x] Enum entry exists (`GcpCloudSql = 604`)
- [x] Enum value in correct range (GCP 600–799)
- [x] Unique id_prefix (`gcpsql`)
- [x] Complete kind_meta (provider, version, id_prefix, prerequisites)
- [x] Kubernetes metadata — n/a (not a Kubernetes component)

#### Folder Structure (4.44%)

- [x] Correct provider hierarchy
- [x] Lowercase enum-name folder
- [x] `v1/` subfolder

#### Protobuf API Definitions (22.20%)

- [x] `api.proto` / `spec.proto` / `stack_input.proto` / `stack_outputs.proto` present and non-trivial
- [x] All four `.pb.go` stubs present and regenerated after the last proto edit
- [x] `spec_test.go` present (21.3 KB, 75 cases), imports test framework
- [x] Tests compile and pass

#### Validation Message Quality (part of Proto Files score)

- [x] Every CEL `message` is a complete, actionable sentence
- [x] No raw regex as primary user-facing text in CEL messages (three bare `string.pattern` constraints noted as polish)
- [x] Conditional rules explain condition + requirement
- [x] No messages shorter than 5 words
- [x] Messages use semantic field names

#### Module Comment Quality (part of Categories 4 & 5)

- [x] Pulumi module explains why / trade-offs / provider quirks / ordering (lifecycle semantics, delete-guard distinction, API-enablement rationale, secret handling)
- [x] Terraform module explains why (null-fallback rationale, HCL `try()` quirk, omitted-block defaults, label parity)
- [x] Not narration-only

#### Cross-Engine IaC Parity (GATE on Categories 4 & 5)

- [x] Scope source matches (project_id, empty → provider default, on both resources)
- [x] Resource naming basis matches (`spec.instance_name`)
- [x] Labels identical (`planton-ai_*` keys + values; kind = CloudResourceKind enum string `gcpcloudsql`; conditional labels under the same conditions)
- [x] Selectors — n/a (no workloads)
- [x] Every `spec.proto` field honored by BOTH engines (35/35)
- [x] Omitted-block defaults identical (disk 10 GB PD_SSD auto-resize; network public IPv4 no authorized networks)
- [x] Dependency ordering matches (sqladmin API → instance)
- [x] Stack outputs flatten onto the same proto fields (conformance case: full population, zero unmapped)
- [x] No undocumented divergences

#### Secure-by-Default Sensitive Coverage (GATE)

- [x] `planton secret-coverage --check`: zero `GcpCloudSql:` gaps
- [x] No sensitive/exempt contradiction
- [x] Secret-bearing fields annotated (`root_password`, `replica_configuration.password`, `replica_configuration.client_key`)
- [x] Look-alike exempted truthfully (`password_change_interval` — rotation interval, not credential material)

### Important Items (41.36%)

- [x] `docs/README.md` (7.1 KB dense design doc: decomposition, connectivity model, lifecycle, explicit 90/10 skips with reasons, composition map, ops guidance)
- [x] `README.md` at v1/ (11.1 KB) + `catalog-page.md` (6.3 KB)
- [x] `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml`
- [x] Presets: 3 valid pairs, correct naming, correct apiVersion/kind, `value:`/`valueFrom:` wrappers, no stale fields, no duplicate ranks

### Nice to Have (10%)

- [x] BUILD.bazel files present and current (v1/, iac/pulumi/, iac/pulumi/module/)

---

**Audit completed at:** 2026-07-04 00:46:42  
**Reference:** `architecture/deployment-component.md` (ideal state), `pkg/iac/MODULE_PARITY.md` (parity dimensions)
