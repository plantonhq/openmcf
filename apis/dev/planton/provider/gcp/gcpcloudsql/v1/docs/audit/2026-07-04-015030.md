# Audit Report: GcpCloudSql

**Audit Date:** 2026-07-04 01:50:30
**Component Kind:** GcpCloudSql
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcpcloudsql/v1/`
**Enum Value:** 604
**ID Prefix:** `gcpsql`

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None. Both gates (parity, sensitive coverage) pass and every category scores full credit.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpCloudSql = 604` exists in `cloud_resource_kind.proto` with
  `provider: gcp`, `version: v1`, `id_prefix: "gcpsql"`.
- 604 is inside the GCP band (600–799).
- `id_prefix` `gcpsql` is unique (no duplicate id_prefix values registry-wide).
- `prerequisites: [GcpServiceNetworkingConnection]` declares the private-IP
  composition chain, which the E2E harness resolves transitively
  (VPC → VPC_PEERING global address → service networking connection).

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- `apis/dev/planton/provider/gcp/gcpcloudsql/v1/` exists; folder name is the
  lowercase enum name; `v1/` layering correct.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files (13.32%)

✅ **Passed:**

- `api.proto` (857 B), `spec.proto` (37.5 KB), `stack_input.proto` (381 B),
  `stack_outputs.proto` (1.3 KB) — all present and substantive.
- The spec covers the released google provider ~> 6.0 floor (v6.50.0):
  engine/version/edition, disk (incl. HYPERDISK_BALANCED), full
  `ip_configuration` (private network ref, allocated range, PSC, ssl_mode,
  server CA modes, authorized networks), full backup + PITR, maintenance +
  deny-maintenance windows, insights, password validation policy, data
  cache, database flags, SQL Server time_zone/collation/audit, CMEK ref,
  deletion protection (both layers), replica arm (`master_instance_name` ref
  + `replica_configuration` + failover target).
- CEL validation messages are complete sentences, explain condition +
  requirement, no raw regex in user-facing text (regex kept in comments).
  Examples verified: engine↔version coherence, SQL Server ⇒ root_password,
  PITR ⇒ Postgres/SQL Server, binary logs ⇒ MySQL.

#### 3.2 Generated Stubs (3.33%)

✅ All four `.pb.go` stubs exist and are regenerated (verified
`instance_name` and new outputs are present in `stack_outputs.pb.go`).

#### 3.3 Unit Tests — Presence (2.77%)

✅ `spec_test.go` (21.3 KB) exists with per-rule validation cases.

#### 3.4 Unit Tests — Execution (2.78%)

✅ `go test ./apis/dev/planton/provider/gcp/gcpcloudsql/v1/` — **ok** (all pass).

**Total Protobuf API Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

#### 4.1 Module Files (6.66%)

✅ `module/main.go`, `module/locals.go`, `module/outputs.go`,
`module/instance.go` present. Comments meet the authoring bar: they explain
provider quirks (root_password secrecy in state, disable_on_destroy=false
rationale, engine-side destroy guard at preview time, PSC/data-cache
edition coupling), not line-by-line narration.

#### 4.2 Entrypoint Files (6.66%)

✅ Root `iac/pulumi/main.go` declares `package main`; no `main/` or
`entrypoint/` subdir; `Pulumi.yaml` + `Makefile` present.
Release-equivalent build passes:
`go build -o /dev/null ./apis/dev/planton/provider/gcp/gcpcloudsql/v1/iac/pulumi` — OK.

**Total Pulumi Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files present and substantive (`variables.tf`, `provider.tf`
pinned `~> 6.0`, `locals.tf`, `main.tf`, `outputs.tf`). `tofu init
-backend=false && tofu validate` — **Success**. HCL comments explain the
same quirks as the Go module (null-vs-empty-string API behavior, HCL `&&`
non-short-circuit requiring `try()`, disk defaults for omitted blocks).

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/`) and Terraform (`iac/tf/`),
both judged against the proto contract. Verified outputs with
`planton validate-outputs` (8/8 proto fields populated, zero unmapped).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project / scope source | `spec.project_id` with ambient fallback | `local.project_id` (empty → null → provider default) | ✅ MATCH |
| Resource naming basis | `spec.instance_name` | `var.spec.instance_name` | ✅ MATCH |
| Labels (keys + values) | `gcplabelkeys` set (`planton-ai_*`, kind = `gcpcloudsql`, name = instance_name) | identical hardcoded set, same conditionals | ✅ MATCH |
| Spec feature coverage | all 97 spec fields honored | all 97 spec fields honored (scripted field-name sweep: zero missing) | ✅ MATCH |
| Secrets in state | `root_password`, replica `password`/`client_key` → `pulumi.ToSecret` | flows through sensitive var; never exported | ✅ MATCH |
| API enablement | `sqladmin.googleapis.com`, `DisableOnDestroy: false`, `DependsOn` edge | `google_project_service.sqladmin_api`, `disable_on_destroy = false`, `depends_on` edge | ✅ MATCH |
| Deletion protection | both layers (engine-side + API-side `deletion_protection_enabled`) | both layers | ✅ MATCH |
| Stack outputs → proto | 8 keys | same 8 keys, `validate-outputs` 8/8 | ✅ MATCH |

**Note (not a divergence):** `spec.database_engine` maps to no provider
argument in either engine — it is the validation selector (CEL derives
engine coherence from it); the provider derives the engine from
`database_version`. Identical treatment on both sides.

**Effect on score:** none.

---

### 6. Documentation - Research (13.18%)

✅ `docs/README.md` (7.1 KB): landscape analysis, released-floor scoping,
recorded skips with reasons (v7-era fields, read pools, clone/restore
workflows, replication_cluster, ssl_cert kind).

**Score:** 13.18% / 13.18%

---

### 7. Documentation - User-Facing (13.09%)

✅ `README.md` (11.1 KB) — substantial, current field names, replica and
private-IP composition documented.

**Score:** 13.09% / 13.09%

---

### 8. Supporting Files (10.09%)

✅ `iac/pulumi/README.md` (3.0 KB), `iac/tf/README.md` (2.5 KB),
`iac/hack/manifest.yaml` (canonical location).

**Score:** 10.09% / 10.09%

---

### 9. Nice to Have (10%)

✅ `BUILD.bazel` files present and regenerated (gazelle).

**Score:** 10.00% / 10.00%

---

### 10. Presets (5.00%)

✅ Three ranked pairs, no orphans, no duplicate ranks:

- `01-postgres-production-private` (private-IP + PSA composition)
- `02-mysql-high-availability` (REGIONAL HA)
- `03-postgres-read-replica` (replica arm)

All three YAMLs pass `planton validate`; `apiVersion`/`kind` match
`api.proto` constants; names carry the `my-` prefix; `StringValueOrRef`
fields use `value:`/`valueFrom:` forms.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --output json`: zero `gaps[]` entries prefixed
`GcpCloudSql:`; `--check` passes with no contradiction.

| Field path | Classification | Note |
|-----------|----------------|------|
| `spec.root_password` | covered | `(sensitive) = true`, ToSecret in Pulumi state |
| `spec.replica_configuration.password` | covered | sensitive + ToSecret |
| `spec.replica_configuration.client_key` | covered | sensitive + ToSecret |
| `spec.encryption_key_name` | exempt | KMS key *path*, not credential material |

**Effect on score:** none.

---

## Live E2E Status

Scenario manifests: `e2e/scenarios/public-postgres.yaml` (leaf) and
`e2e/scenarios/private-postgres.yaml` (full PSA chain). Verifier:
`aa_e2e/verify/cloud_sql_instance.go` via the `sqladmin/v1` API (settings
posture + IP-mode assertion). The dual-engine live run is tracked in the
session wrap-up; the Pulumi private-postgres flagship scenario (VPC →
VPC_PEERING address → service networking connection → private-IP instance)
has passed live.

---

**Audit completed at:** 2026-07-04 01:50:30
**Reference:** `architecture/deployment-component.md`
