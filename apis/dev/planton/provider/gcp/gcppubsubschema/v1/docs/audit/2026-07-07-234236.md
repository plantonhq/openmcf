# Audit Report: GcpPubSubSchema

**Audit Date:** 2026-07-07 23:42:36
**Component Kind:** GcpPubSubSchema
**Provider:** gcp
**Component Path:** `apis/dev/planton/provider/gcp/gcppubsubschema/v1/`
**Enum Value:** 664
**ID Prefix:** gcppsch

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

### 3. Protobuf API Definitions — validation quality note

`type` is a required string constrained by CEL to `AVRO | PROTOCOL_BUFFER` (the `TYPE_UNSPECIFIED` sentinel is rejected — a schema without a declared language is not a usable contract). `schema_name` carries the shared Pub/Sub name pattern plus the reserved-`goog`-prefix CEL, moving the API's create-time rejection to pre-deploy validation. `definition` is required. The revision lifecycle (in-place commits, the 20-revision cap, the `_deleted-schema_` sentinel) is taught in the spec comments. All rules are exercised by the 22-case spec test.

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/schema.go`) and Terraform (`iac/tf/main.tf`), judged against the proto contract. Live-proven 2/2 on `planton-e2e` (leaf minimal, both engines) and as the topic's schema-validated prerequisite chain (2/2).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| API enablement | `pubsub.googleapis.com`, disable_on_destroy false | same | ✅ MATCH |
| Resource naming basis | `spec.schema_name` | `local.schema_name` | ✅ MATCH |
| Ambient project | empty `project_id` omitted → provider default | empty → `null` → provider default | ✅ MATCH |
| type + definition | always sent (required in spec) | always sent | ✅ MATCH |
| Labels | none — the schema API has no labels surface (noted identically in both modules) | same | ✅ MATCH |
| Dependency ordering | `DependsOn(api)` | `depends_on = [api]` | ✅ MATCH |
| Stack outputs → proto | schema_id (resource ID), schema_name | same | ✅ MATCH |

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅** — no secret-bearing fields (a schema definition is a public contract, not a credential); `secret-coverage --check` green.

---

## Recorded skips (with reasons)

- `deletion_policy` — client-side virtual lever conflicting with Planton-managed destroy (catalog-wide skip; absent from the released 6.x line, schema-probe verified).
- `revision_id` output — not exposed as a resource attribute on the released `google ~> 6.x` line; exporting from Pulumi only would break output parity.
- Per-schema IAM trio — resource-scoped IAM deferred pending concrete pull.

## Validation evidence

- Spec test: 22/22 green. Both engines build (`go build` + release-equivalent Pulumi entrypoint).
- `validate-outputs` green on BOTH module dirs; `pkg/outputs` conformance case present.
- Offline `planton tofu plan` via the real converter: 2 to add, clean.
- Live dual-engine E2E on `planton-e2e`: minimal 2/2 (45s/45s) + the topic's schema-validated chain 2/2; zero orphans.
