apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpvertexaiindexendpoint
spec:
  # Index endpoints create in ~1-5 minutes per engine (provider timeout
  # 20m). The public scenario proves ambient project, API enablement, the
  # GCP-assigned numeric ID contract (index_endpoint_name output vs live),
  # and the public_endpoint_domain_name output. The peered and PSC arms
  # are recorded live exclusions: both need Private Services Access /
  # consumer wiring the test project does not carry — proven offline via
  # converter plans (the GcpVertexAiEndpoint precedent).
  tier: 1
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 15
