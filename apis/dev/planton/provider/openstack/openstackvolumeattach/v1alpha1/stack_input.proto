syntax = "proto3";

package dev.planton.provider.openstack.openstackvolumeattach.v1alpha1;

import "dev/planton/provider/openstack/openstackvolumeattach/v1alpha1/api.proto";
import "dev/planton/provider/openstack/provider.proto";

// OpenStackVolumeAttachStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackVolumeAttachStackInput {
  // target cloud-resource to be deployed
  OpenStackVolumeAttach target = 1;
  // provider-config for OpenStack authentication
  dev.planton.provider.openstack.OpenStackProviderConfig provider_config = 2;
}
