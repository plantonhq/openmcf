apiVersion: aws.planton.dev/v1
kind: AwsCloudwatchLogGroup
metadata:
  name: my-app-logs
spec:
  region: <aws-region>
  retentionInDays: 30  # recommended default; log events older than 30 days are deleted
