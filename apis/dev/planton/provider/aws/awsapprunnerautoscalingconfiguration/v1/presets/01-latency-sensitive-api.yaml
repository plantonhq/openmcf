apiVersion: aws.planton.dev/v1
kind: AwsAppRunnerAutoScalingConfiguration
metadata:
  org: example-org
  env: prod
  name: latency-sensitive-api
  id: latency-sensitive-api-id
spec:
  region: <aws-region>
  maxConcurrency: 50
  maxSize: 15
  minSize: 3
