syntax = "proto3";

package org.openmcf.provider.kubernetes.kubernetesrookcephoperator.v1;

import "org/openmcf/provider/kubernetes/kubernetesrookcephoperator/v1/api.proto";
import "org/openmcf/provider/kubernetes/provider.proto";

// KubernetesRookCephOperatorStackInput defines the inputs for the IaC modules that deploy the Rook Ceph Operator.
message KubernetesRookCephOperatorStackInput {
  // The target resource to deploy.
  KubernetesRookCephOperator target = 1;
  // Kubernetes provider configuration including credentials.
  org.openmcf.provider.kubernetes.KubernetesProviderConfig provider_config = 2;
}
