# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureakscluster
spec:
  presets:
    - preset: 01-standard
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: node pool VM hours
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: default pool at the autoscaling floor (min_count 3) x 730 hours, Standard_D4s_v5 Linux pay-as-you-go
          list_unit_price: "0.192"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'eastus' and armSkuName eq 'Standard_D4s_v5' and priceType eq 'Consumption' and serviceName eq 'Virtual Machines'"
          retrieved_on: "2026-08-14"
          list_cost: "420.4800"
        - service_name: Azure Kubernetes Service
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: sku_tier STANDARD bills the uptime-SLA control plane every hour of the month
          list_unit_price: "0.10"
          price_source: https://azure.microsoft.com/en-us/pricing/details/kubernetes-service/
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
        - service_name: Storage
          sku_meter: node OS disk storage
          pricing_unit: disk-months
          pricing_quantity: "3"
          quantity_basis: one P10 (128 GiB) Premium SSD managed OS disk per node at the autoscaling floor -- the AKS default size when os_disk_size_gb is unset
          list_unit_price: "19.71"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'eastus' and meterName eq 'P10 LRS Disk' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "59.1300"
        - service_name: Load Balancer
          sku_meter: included load balancer rules
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the default outbound_type (LOAD_BALANCER) provisions a Standard Load Balancer whose included-rules bundle bills every hour
          list_unit_price: "0.025"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=serviceName eq 'Load Balancer' and contains(meterName, 'Standard Included')"
          retrieved_on: "2026-08-14"
          list_cost: "18.2500"
        - service_name: Virtual Network
          sku_meter: standard static public IP
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: one managed outbound public IP (the default managed_outbound_ip_count) x 730 hours
          list_unit_price: "0.005"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'Global' and contains(meterName, 'Static Public IP') and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "3.6500"
      total_list_cost: "574.5100"
      exclusions:
        - Log Analytics ingestion for Container Insights (oms_agent) depends on workload log volume
        - autoscaled nodes above the floor (max_count 5 raises the VM ceiling to about $700.80)
        - load balancer data processed and network egress are traffic-dependent
      notes: Estimated at the autoscaling floor -- the committed always-on spend.
    - preset: 02-private
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: node pool VM hours
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: default pool at the autoscaling floor (min_count 3) x 730 hours, Standard_D4s_v5 Linux pay-as-you-go
          list_unit_price: "0.192"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'eastus' and armSkuName eq 'Standard_D4s_v5' and priceType eq 'Consumption' and serviceName eq 'Virtual Machines'"
          retrieved_on: "2026-08-14"
          list_cost: "420.4800"
        - service_name: Azure Kubernetes Service
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: sku_tier STANDARD bills the uptime-SLA control plane every hour of the month
          list_unit_price: "0.10"
          price_source: https://azure.microsoft.com/en-us/pricing/details/kubernetes-service/
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
        - service_name: Storage
          sku_meter: node OS disk storage
          pricing_unit: disk-months
          pricing_quantity: "3"
          quantity_basis: one P10 (128 GiB) Premium SSD managed OS disk per node at the autoscaling floor -- the AKS default size when os_disk_size_gb is unset
          list_unit_price: "19.71"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'eastus' and meterName eq 'P10 LRS Disk' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "59.1300"
        - service_name: Load Balancer
          sku_meter: included load balancer rules
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the default outbound_type (LOAD_BALANCER) provisions a Standard Load Balancer whose included-rules bundle bills every hour
          list_unit_price: "0.025"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=serviceName eq 'Load Balancer' and contains(meterName, 'Standard Included')"
          retrieved_on: "2026-08-14"
          list_cost: "18.2500"
        - service_name: Virtual Network
          sku_meter: standard static public IP
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: one managed outbound public IP (the default managed_outbound_ip_count) x 730 hours
          list_unit_price: "0.005"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'Global' and contains(meterName, 'Static Public IP') and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "3.6500"
      total_list_cost: "574.5100"
      exclusions:
        - private endpoint and private DNS zone charges for the private API server
        - autoscaled nodes above the floor (max_count 5 raises the VM ceiling to about $700.80)
        - load balancer data processed and network egress are traffic-dependent
      notes: Estimated at the autoscaling floor -- the committed always-on spend.
    - preset: 03-hardened-enterprise
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: node pool VM hours
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: default pool at the autoscaling floor (min_count 3) x 730 hours, Standard_D4s_v5 Linux pay-as-you-go
          list_unit_price: "0.192"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'eastus' and armSkuName eq 'Standard_D4s_v5' and priceType eq 'Consumption' and serviceName eq 'Virtual Machines'"
          retrieved_on: "2026-08-14"
          list_cost: "420.4800"
        - service_name: Azure Kubernetes Service
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: sku_tier STANDARD bills the uptime-SLA control plane every hour of the month
          list_unit_price: "0.10"
          price_source: https://azure.microsoft.com/en-us/pricing/details/kubernetes-service/
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
        - service_name: Storage
          sku_meter: node OS disk storage
          pricing_unit: disk-months
          pricing_quantity: "3"
          quantity_basis: one P10 (128 GiB) Premium SSD managed OS disk per node at the autoscaling floor -- the AKS default size when os_disk_size_gb is unset
          list_unit_price: "19.71"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'eastus' and meterName eq 'P10 LRS Disk' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "59.1300"
        - service_name: Load Balancer
          sku_meter: included load balancer rules
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the default outbound_type (LOAD_BALANCER) provisions a Standard Load Balancer whose included-rules bundle bills every hour
          list_unit_price: "0.025"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=serviceName eq 'Load Balancer' and contains(meterName, 'Standard Included')"
          retrieved_on: "2026-08-14"
          list_cost: "18.2500"
        - service_name: Virtual Network
          sku_meter: standard static public IP
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: one managed outbound public IP (the default managed_outbound_ip_count) x 730 hours
          list_unit_price: "0.005"
          price_source: "https://prices.azure.com/api/retail/prices?$filter=armRegionName eq 'Global' and contains(meterName, 'Static Public IP') and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "3.6500"
      total_list_cost: "574.5100"
      exclusions:
        - Microsoft Defender for Containers plan charges (billed per protected vCPU per month)
        - Log Analytics ingestion for Container Insights and Defender depends on workload volume
        - autoscaled nodes above the floor (max_count 5 raises the VM ceiling to about $700.80)
        - load balancer data processed and network egress are traffic-dependent
      notes: Estimated at the autoscaling floor -- the committed always-on spend. Host encryption on the default pool adds no charge of its own.
