# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kuberneteskeycloakoperator
spec:
  presets:
    - preset: 01-operator
      capacity_footprint:
        cpu_requests: 300m
        memory_requests: 450Mi
        cpu_limits: 700m
        memory_limits: 450Mi
        basis: 1 operator pod x (300m CPU / 450Mi memory requests, 700m CPU / 450Mi limits) from the spec-declared defaults (upstream's own values, applied when the manifest omits resources) -- no persistent volumes
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the Keycloak servers this operator reconciles reserve their own capacity -- each rides its KubernetesKeycloak resource
