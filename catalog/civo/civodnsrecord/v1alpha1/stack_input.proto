syntax = "proto3";

package dev.planton.civo.civodnsrecord.v1alpha1;

import "catalog/civo/civodnsrecord/v1alpha1/api.proto";
import "catalog/civo/provider.proto";

// CivoDnsRecordStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message CivoDnsRecordStackInput {
  // target cloud-resource to be deployed
  CivoDnsRecord target = 1;
  // provider-config for Civo authentication
  dev.planton.civo.CivoProviderConfig provider_config = 2;
}
