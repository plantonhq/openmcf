# CivoFirewall

> Generated from the protobuf schema by `make generate-reference` -- do not
> edit by hand. To change a fact on this page, change the proto field comment
> or validation rule it is derived from, then regenerate.

**apiVersion**: `civo.planton.dev/v1alpha1`

CivoFirewallSpec defines the user configuration for a Civo firewall.

## Spec Fields

| Path | Type | Required | Default | References |
|---|---|---|---|---|
| `spec.name` | `string` | yes |  |  |
| `spec.networkId` | `string \| valueFrom` | yes |  | CivoVpc (`status.outputs.network_id`) |
| `spec.inboundRules` | `[]CivoFirewallInboundRule` |  |  |  |
| `spec.inboundRules[].protocol` | `string` |  |  |  |
| `spec.inboundRules[].portRange` | `string` |  |  |  |
| `spec.inboundRules[].cidrs` | `[]string` |  |  |  |
| `spec.inboundRules[].action` | `string` |  |  |  |
| `spec.inboundRules[].label` | `string` |  |  |  |
| `spec.outboundRules` | `[]CivoFirewallOutboundRule` |  |  |  |
| `spec.outboundRules[].protocol` | `string` |  |  |  |
| `spec.outboundRules[].portRange` | `string` |  |  |  |
| `spec.outboundRules[].cidrs` | `[]string` |  |  |  |
| `spec.outboundRules[].action` | `string` |  |  |  |
| `spec.outboundRules[].label` | `string` |  |  |  |
| `spec.tags` | `[]string` |  |  |  |

## Field Details

### spec.name

`string` · required

Name of the firewall (must be unique per Civo account/project).

- rule: {"required":true}

### spec.networkId

`string | valueFrom` · required

The network (VPC) in which to create this firewall. Must refer to an existing CivoNetwork.

- references: CivoVpc (`status.outputs.network_id`)
- rule: {"required":true}
- rule: write as {value: <literal>} or {valueFrom: {kind: CivoVpc, name: <that resource's name>, fieldPath: status.outputs.network_id}} -- a bare string does not parse

### spec.inboundRules

`[]CivoFirewallInboundRule`

Inbound (ingress) rules: traffic allowed **to** instances.
Any traffic not matching an inbound rule is denied (stateful firewall).

### spec.inboundRules[].protocol

`string`

Protocol to allow ("tcp", "udp", or "icmp").

- rule: {"string":{"pattern":"^(tcp|udp|icmp)$"}}

### spec.inboundRules[].portRange

`string`

Port or port range to allow (e.g., "80", "443", "22", "8000-9000"; for "tcp"/"udp"
leave empty or use "1-65535" to allow all ports).

### spec.inboundRules[].cidrs

`[]string`

CIDR blocks from which traffic is permitted (defaults to "0.0.0.0/0" if not specified).

### spec.inboundRules[].action

`string`

Action for this rule: "allow" (default) or "deny". Default is "allow" if unspecified.

### spec.inboundRules[].label

`string`

Optional label for the rule (for user reference).

### spec.outboundRules

`[]CivoFirewallOutboundRule`

Outbound (egress) rules: traffic allowed **from** instances.
If no egress rules are specified, all outbound traffic is allowed by default.

### spec.outboundRules[].protocol

`string`

Protocol to allow or deny ("tcp", "udp", or "icmp").

- rule: {"string":{"pattern":"^(tcp|udp|icmp)$"}}

### spec.outboundRules[].portRange

`string`

Port or port range to allow/deny (format as in inbound rules).

### spec.outboundRules[].cidrs

`[]string`

CIDR blocks to which traffic is permitted (defaults to "0.0.0.0/0" if not specified).

### spec.outboundRules[].action

`string`

Action for this rule: "allow" (default) or "deny". Default is "allow" if unspecified.

### spec.outboundRules[].label

`string`

Optional label for the rule.

### spec.tags

`[]string`

Instance tag selectors: names of instance tags to auto-apply this firewall to.
Any instance in the same network with any of these tags will use this firewall.

## Outputs

Reference an output from another manifest as `valueFrom: {kind: CivoFirewall, name: <resource-name>, fieldPath: status.outputs.<output>}`.

| Output | Type | Description |
|---|---|---|
| `status.outputs.firewall_id` | `string` | The unique ID of the created firewall (UUID generated by Civo). |
| `status.outputs.created_at_rfc3339` | `string` | Timestamp when the firewall was created, in RFC3339 format. |

## References

Fields that can point at another resource's outputs:

| Field | Kind | Output |
|---|---|---|
| `spec.networkId` | CivoVpc | `status.outputs.network_id` |

## Referenced By

Fields on other kinds that can point at this resource:

| Kind | Field | Reads |
|---|---|---|
| CivoComputeInstance | `spec.firewallIds` | `status.outputs.firewall_id` |
| CivoDatabase | `spec.firewallIds` | `status.outputs.firewall_id` |

## See Also

- [Overview](../README.md)
