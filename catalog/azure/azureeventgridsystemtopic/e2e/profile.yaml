apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureeventgridsystemtopic
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (the fixture
  # storage account's event stream -- free at rest, creates in
  # seconds). Azure allows ONE system topic per source per topic type,
  # so this lane must never run concurrently with the event
  # subscription kind's system-topic lane (both stand up a system
  # topic on the fixture account).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
