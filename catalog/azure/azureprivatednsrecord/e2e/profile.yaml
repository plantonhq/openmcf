apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureprivatednsrecord
spec:
  # PROVEN live on both engines (2026-08-13): `minimal` (an A record in
  # the fixture private zone, addressed by reference) -- all phases
  # green, 2/2 outputs on both engines, the zone-output wiring edge and
  # the Pulumi id-parse seam confirmed, the fqdn's trailing-dot DNS form
  # verified. Blind import round-trip: zero-change (the type-carrying
  # ARM id addressed the A variant). Measured: Pulumi lane 6m19s
  # (deploy 31s, destroy 36s), Terraform lane 4m36s incl. 21s import
  # round-trip; fixture chain ~2-3.5m of each lane. OFFLINE-ONLY with
  # reasons: the six other payload types -- aaaa, cname, mx, ptr, srv,
  # txt -- are pure ARM property writes on the exact create path the A
  # lane proved (each is a RecordSet PUT differing only in the typed
  # value array; plan-verified with rendered-value inspection), and the
  # seven variants share one verifier keyed on the type-carrying ARM id.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
