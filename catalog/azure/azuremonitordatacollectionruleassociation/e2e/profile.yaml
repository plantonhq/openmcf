apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremonitordatacollectionruleassociation
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (the fixture VM
  # attached to the rule kind's smoke instance -- the chain deploys the
  # VM's NIC/subnet/VNet fixtures and the rule's workspace fixture; the
  # association itself is free and creates in seconds; the lane's cost
  # is the fixture VM's hour at ~Standard_B2s). OFFLINE-ONLY with
  # reasons: the ENDPOINT binding arm cannot run live -- a Data
  # Collection Endpoint is not a catalog kind yet, and the arm needs a
  # live DCE to bind (unblock: the DCE kind entering the catalog; the
  # arm is a two-field ARM write on the same association resource the
  # rule arm proves). The timeout budgets the VM chain, not the
  # association.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
