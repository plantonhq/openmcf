syntax = "proto3";

package dev.planton.azure.azurerediscache.v1alpha1;

import "catalog/azure/azurerediscache/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureRedisCacheStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureRedisCacheStackInput {
  // target cloud-resource to be deployed
  AzureRedisCache target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
