# Audit Report: AzureStorageEncryptionScope

**Audit Date:** 2026-07-05 14:47:13
**Component Kind:** AzureStorageEncryptionScope
**Provider:** azure
**Component Path:** `catalog/azure/azurestorageencryptionscope/v1alpha1/`
**Enum Value:** 494
**ID Prefix:** azses

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `AzureStorageEncryptionScope = 494`, unique `id_prefix: "azses"`, storage-data
grouping; covered by the shared no-account-prerequisite rationale.

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Standard anatomy at `catalog/azure/azurestorageencryptionscope/v1alpha1/`.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- Four protos present; the scope-name CEL mirrors azurerm's exact validator (4-63
  alphanumerics, NO hyphens) with a plain-English message; `source` is a required closed
  enum (`defined_only` + `not_in: [0]`)
- `key_vault_key_id` is a `StringValueOrRef` → `AzureKeyVaultKey.versionless_id`
  (rotation propagates — the account-CMK precedent); the required-when-KeyVault pairing
  is a message-level CEL PRESENCE check (`has(this.key_vault_key_id)`) — the
  protovalidate-java-safe pattern, no ref sub-field dereference
- Generated stubs present; 13 spec-test cases (happy + every error path incl. the
  KeyVault-without-key rejection and both invalid-enum classes);
  `go test` — ALL PASS

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ Module trio + root entrypoint + Pulumi.yaml + Makefile; release-equivalent build
passes; shared provider builder; comments explain the soft-disable delete semantics, the
versionless-key rationale, and the send-only-when-true flag convention.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files; `azurerm ~> 4.0` + canonical empty provider block; `tofu validate`
clean; full `planton tofu plan` on the hack manifest renders the DEEPER source path
(`source = "Microsoft.KeyVault"` + key URI + `infrastructure_encryption_required = true`),
proving the dotted-wire-value enum map and the key pairing.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `StorageAccountId` (ARM id) | `storage_account_id` (ARM id) | ✅ MATCH |
| Resource naming basis | ARM name = `spec.scope_name` | ARM name = `var.spec.scope_name` | ✅ MATCH |
| Source enum | `sourceStrings` map → dotted wire values | `source_map` → dotted wire values | ✅ MATCH |
| Key pairing | sent only when set (resolved versionless URI) | null-guarded local | ✅ MATCH |
| Infrastructure encryption | sent only when true | direct pass-through; the tfvars wire format drops the zero-valued plain bool, so unset/false arrives null → azurerm's `GetOk` treats it as not-set — behaviorally identical to only-when-true | ✅ MATCH |
| Stack outputs → proto | encryption_scope_id, encryption_scope_name, storage_account_name | identical | ✅ MATCH |
| Azure tags | none (ARM: unsupported on encryptionScopes) | none | ✅ MATCH |

Verified with `planton validate-outputs` (schema check) and the
`pkg/outputs/conformance_test.go` case for AzureStorageEncryptionScope (PASS, 3/3 fields).

**Effect on score:** none.

---

### 6-9. Documentation, Supporting Files, Nice to Have

✅ All present: research doc (the soft-disable delete semantics get their own section;
the account-identity plumbing for the KeyVault source; the deliberate non-invention of
the reverse source/key restriction), README, catalog page, both module READMEs, hack
manifest (exercising the KeyVault source path), BUILD files, E2E profile + scenario +
scenario-local account fixture. The E2E verifier is STATE-AWARE by design (a 404 probe
would report a destroyed scope as still existing); the MICROSOFT_KEY_VAULT source's live
exclusion is recorded in the profile with the specific reason (account-identity + vault +
key + grant fixture chain with a ~10-minute purge, for a pairing the offline gate fully
proves — the account-CMK treatment).

---

### 10. Presets (5.00%)

✅ Three pairs — `01-platform-managed-scope`, `02-customer-managed-key-scope`,
`03-double-encryption-scope` — one per real key-governance posture. All validate.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`key_vault_key_id` is a key REFERENCE (a URI), not key material — consistent with the
established treatment of every CMK reference in the catalog (account, ACR, databases);
the analyzer reports no gap for this kind and the Azure slice stays 100%.

---

**Audit completed at:** 2026-07-05-144713
**Reference:** `architecture/deployment-component.md`
