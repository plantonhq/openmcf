apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuretrafficmanagerprofile
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green for the full-monitor
  # canonical shape and the MultiValue + fast-interval shapes with
  # rendered-value inspection); live dual-engine proof has not yet run.
  # ONE scenario runs: `minimal` (a Performance profile with an HTTPS
  # probe; the run-id token keeps the globally-namespaced DNS relative
  # name collision-free across runs). Profiles are cheap at rest.
  # OFFLINE-ONLY with reasons: the five other routing methods and the
  # monitor dials (ranges, headers, fast interval, MultiValue's
  # max_return) are pure ARM property writes on the exact create path
  # the minimal lane proves (one CreateOrUpdate body differing only in
  # property values -- plan-verified with rendered-value inspection);
  # Traffic View is a billing switch on the same body (verified
  # rendering "Enabled" offline).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
