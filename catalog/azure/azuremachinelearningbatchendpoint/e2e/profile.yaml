apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningbatchendpoint
spec:
  # Proven live on both engines: an Entra-auth (AADToken default)
  # system-identity endpoint on the fixture workspace -- a pure
  # routing object, free at rest, minutes-class create; scoring/
  # swagger URI read-back 4/5 (the swagger URI reads back empty on a
  # deployment-less endpoint -- populated only once a deployment
  # serves). The Terraform blind import round-trip is clean under the
  # shared azapi catalog tolerances. The default-deployment pointer
  # stays offline-proven (it names a deployment -- the deployment
  # kind's territory).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 45
