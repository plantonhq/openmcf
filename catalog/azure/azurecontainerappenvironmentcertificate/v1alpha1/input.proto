syntax = "proto3";

package dev.planton.azure.azurecontainerappenvironmentcertificate.v1alpha1;

import "catalog/azure/azurecontainerappenvironmentcertificate/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureContainerAppEnvironmentCertificateStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureContainerAppEnvironmentCertificateStackInput {
  // target cloud-resource to be deployed
  AzureContainerAppEnvironmentCertificate target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
