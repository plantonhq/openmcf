syntax = "proto3";

package dev.planton.azure.azureprivatednszone.v1alpha1;

import "catalog/azure/azureprivatednszone/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzurePrivateDnsZoneStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzurePrivateDnsZoneStackInput {
  // target cloud-resource to be deployed
  AzurePrivateDnsZone target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
