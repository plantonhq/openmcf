syntax = "proto3";

package dev.planton.azure.azureresourcegroup.v1alpha1;

import "catalog/azure/azureresourcegroup/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureResourceGroupStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureResourceGroupStackInput {
  // target cloud-resource to be deployed
  AzureResourceGroup target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
