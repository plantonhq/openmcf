syntax = "proto3";

package dev.planton.azure.azuremachinelearningonlinedeployment.v1alpha1;

import "catalog/azure/azuremachinelearningonlinedeployment/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureMachineLearningOnlineDeploymentStackInput is the input to the
// IaC modules (Pulumi/Terraform). It contains the target resource
// definition and Azure provider credentials.
message AzureMachineLearningOnlineDeploymentStackInput {
  // The online deployment resource to deploy.
  AzureMachineLearningOnlineDeployment target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
