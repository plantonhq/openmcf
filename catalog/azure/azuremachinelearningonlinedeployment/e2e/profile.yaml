apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningonlinedeployment
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The deployment provisions a REAL VM (10-20 minutes,
  # bills until destroy -- no scale-to-zero); the smoke scenario is
  # deliberately model-less, and whether the service accepts a bare
  # managed deployment is itself the lane's proof point (the ARM
  # contract allows it; the fallback -- a minimal registered model as a
  # scenario-declared fixture -- rides the queue's watch-list). Probes,
  # data collection, and secure egress are offline-proven only this
  # wave. Managed-endpoint VM quota (separate from compute quota) is
  # the likely first blocker on a fresh subscription.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
