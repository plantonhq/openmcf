apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureexpressrouteport
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The port bills its FULL MONTHLY RATE from creation
  # (pro-rated cents-to-dollars for a lane-length life at 10 Gbps) and
  # the create may reject on subscriptions not enrolled for ExpressRoute
  # Direct -- that outcome is the lane's proof point (accept-and-assert
  # or reject-and-defer), never a module defect. The physical
  # cross-connects are out-of-band; verifiers assert ARM object state,
  # the link pair, and the composed authorization, never link light.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
