# Audit Report: AzureServicePlan

**Audit Date:** 2026-07-09 17:50:00
**Component Kind:** AzureServicePlan
**Provider:** azure
**Component Path:** `catalog/azure/azureserviceplan/v1alpha1/`
**Enum Value:** 442
**ID Prefix:** azsp

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `AzureServicePlan = 442` with `kind_meta` (provider azure, `id_prefix: azsp`, app-hosting sub-band 440-449); `prerequisites: [AzureResourceGroup]` — honest: the plan is created inside a referenced resource group and needs nothing else.

### 2. Folder Structure (4.44%)

✅ All required paths present: 4 protos + BUILD + spec test + `iac/tf` + `iac/pulumi/module` + `iac/hack/manifest.yaml` + docs + presets + README + catalog-page.

### 3. Protobuf API Definitions (22.20%)

✅ The spec covers the complete `azurerm_service_plan` argument surface (azurerm v4.80, `internal/services/appservice/service_plan_resource.go`):

- `region`, `resource_group` (FK → AzureResourceGroup), `service_plan_name` (the provider's exact 1-60 `^[0-9a-zA-Z-_]+$` contract)
- `os_type` enum (LINUX default / WINDOWS / WINDOWS_CONTAINER — all three provider values)
- `sku_name` as the closed 52-value `AzureServicePlanSku` enum mirroring `AllKnownServicePlanSkus()` row for row (Free/Shared/Basic/Standard/Premium v2-v4 incl. memory-optimized/Consumption/Elastic Premium/Flex Consumption/Isolated v1-v2 incl. memory-optimized/Workflow), family-contiguous numbering so the SKU-gated rules are range checks
- `app_service_environment_id` with ARM-ID format CEL, `worker_count`, `premium_plan_auto_scale_enabled`, `maximum_elastic_worker_count`, `zone_balancing_enabled`, `per_site_scaling_enabled`, user `tags`
- Message CELs front-load ALL FOUR provider validators (see the source-diff section below)
- Outputs: `service_plan_id` (the FK target), `service_plan_name`, `os_type`, `sku_name`, plus the computed `kind` and `reserved`
- Spec test: 40 Ginkgo cases covering every CEL happy/error path (verified `go test` green)

### 4/5. IaC Modules (17.56%)

✅ **Pulumi** (`iac/pulumi/module/`): shared `pulumiazureprovider.Get` builder (keyless-ready); enum wire maps spelled row by row; presence-guarded proto defaults; exports all six outputs. Release-equivalent build green.
✅ **Terraform** (`iac/tf/`): `azurerm ~> 4.0`, empty keyless provider block with the standard comment; `locals.tf` carries the identical enum wire maps; full `tofu plan` against the hack manifest renders every field (verified green).

### 5b. Cross-Engine IaC Parity — PARITY ✅

Field-by-field walk: both engines send identical arguments for every spec field; unset optional ints are omitted on both engines (Azure applies SKU defaults); the documented boolean defaults are materialized identically. Outputs are byte-identical in name and shape (`pkg/outputs` conformance case + `validate-outputs` green).

**PARITY-EXCEPTION (recorded in both modules):** the metadata-derived tag shape (`resource_kind` casing + `resource_id` fallback) — the family-wide, output-neutral exception.

**Apply-time validator source-diff** (`service_plan_resource.go` `CustomizeDiff()` + `Create()` vs the spec):

| Provider validator | Spec front-load |
|---|---|
| `premium_plan_auto_scale_enabled` premium-only | `service_plan_auto_scale_premium_only` CEL (premium range 10-30) |
| `maximum_elastic_worker_count` > 1 requires EP/WS or premium+auto-scale | `service_plan_elastic_worker_count_sku_gate` CEL |
| `zone_balancing_enabled` only on supporting tiers | `service_plan_zone_balancing_sku_gate` CEL (>= 10) |
| ASE plans require Isolated SKUs (create-time `strings.HasPrefix(sku, "I")`) | `service_plan_ase_requires_isolated_sku` CEL (range 36-49) |
| zone-balancing false→true with `worker_count < 2` ForceNew | Not statically checkable (state transition) — documented on both fields |

### 6/7. Documentation (26.27%)

✅ `docs/README.md` rewritten timeless (SKU landscape, six design decisions incl. the closed-enum rationale and the ASE raw-ARM-ID boundary); README + catalog-page carry the full field/output tables in the current shape with enum spellings.

### 8. Supporting Files (10.09%)

✅ Hack manifest exercises the premium range gates (auto-scale + elastic ceiling + zone balancing + tags); `validate-manifest` green.

### 9. Presets (5.00%)

✅ 3 presets (linux-standard, linux-premium, consumption-serverless), all validating, with enum spellings and guidance comments.

### 10. Nice to Have (10.00%)

✅ Registry prerequisites wired; site catalog regenerated; conformance case added.

### Secure-by-Default Coverage — COVERAGE ✅

No secret-bearing fields on this kind. `planton secret-coverage --check` green.
