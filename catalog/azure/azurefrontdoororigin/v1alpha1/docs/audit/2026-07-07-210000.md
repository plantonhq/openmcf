# Audit Report: AzureFrontDoorOrigin

**Audit Date:** 2026-07-07 21:00:00
**Component Kind:** AzureFrontDoorOrigin
**Provider:** azure
**Component Path:** `catalog/azure/azurefrontdoororigin/v1alpha1/`
**Enum Value:** 483
**ID Prefix:** azfdo

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ Enum `AzureFrontDoorOrigin = 483`;
`prerequisites: [AzureFrontDoorOriginGroup]` with rationale (profile +
RG chain transitively); `id_prefix: "azfdo"` unique.

### 2. Folder Structure (4.44%)

✅ Correct hierarchy.

### 3. Protobuf API Definitions (22.20%)

✅ Full azurerm v4.80 surface: parent FK (ForceNew), name (2-90 regex),
`host_name` required, `certificate_name_check_enabled` optional default
true, `origin_host_header` non-empty-when-set, ports 1-65535 with 80/443
defaults, priority 1-5, weight 1-1000, `enabled`, and the complete
`private_link` block (location, ARM-id-validated target, closed
target-type enum with Azure's exact seven group ids, request message
≤140 with Azure's default). Message CELs: private-link-requires-
certificate-check; target-type-XOR-Private-Link-Service (keyed on the
`/privateLinkServices/` id segment). `spec_test.go`: 26 cases (8 valid
incl. every target type + the PLS-without-type path, 18 error paths) --
all PASS.

### 4. IaC Modules - Pulumi (13.32%)

✅ Single `cdn.FrontdoorOrigin`; shared builder; comments explain the
always-send certificate check, the apply-time Premium gate (SKU lives on
a different resource), and the engine-dialect note on secondary target
types. Release-equivalent build passes.

### 5. IaC Modules - Terraform (4.24%)

✅ Canonical provider block; `planton tofu plan` renders the full
private-link seam (sites target, request message) + host-header +
priority/weight.

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅** (`pkg/outputs`: 2/2 populate, zero
unmapped)

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Parent addressing | `spec.origin_group_id.GetValue()` | `var.spec.origin_group_id` | ✅ MATCH |
| Naming basis | `spec.origin_name` | `var.spec.origin_name` | ✅ MATCH |
| Certificate check | always sent, default-true materialized | same (`coalesce(..., true)`) | ✅ MATCH |
| Optional dials | sent only when set | null passthrough | ✅ MATCH |
| Private link | when set; camelCase secondaries (`blobSecondary`) | when set; snake_case secondaries (`blob_secondary`) | ✅ MATCH -- same ARM group id; each engine speaks its provider's dialect, documented in BOTH modules |
| Tags | none (unsupported on origins) | none | ✅ MATCH |
| Stack outputs → proto | 2 exports | 2 outputs, same names | ✅ MATCH |

### 6-10. Documentation, Supporting Files, Presets

✅ `docs/README.md` (field map, the three private-link contracts split
into statically-checkable CELs vs the apply-time Premium gate, the
engine-dialect note, zero recorded skips); user docs + module READMEs;
hack manifest exercises private link; presets ×3 (App Service origin,
weighted canary, private-link origin) -- validate green; E2E
`prerequisite.yaml` (the route chain's fixture), `scenarios/minimal.yaml`,
`profile.yaml` (records the private-link live-E2E exclusion: manual
connection approval breaks create-and-destroy automation), verifier +
test entrypoints.

### 11. Secure-by-Default Coverage (Critical GATE)

**COVERAGE ✅** -- no secret-bearing fields.

---

## Apply-Time Validator Source-Diff

`cdn_frontdoor_origin_resource.go` `expandPrivateLinkSettings`:

- **Premium-SKU gate** -- cross-resource (profile fetched at apply);
  cannot be a CEL; documented on the field, in docs/README.md, and in
  the private-link preset.
- **certificate_name_check required with private link** -- modeled as a
  message CEL (front-loaded).
- **PLS-vs-target_type contract** -- modeled as a message CEL keyed on
  the id segment (front-loaded).
- The always-resend of `origin_host_header` on update is
  provider-internal PATCH hygiene both engines inherit.

No unmodeled contracts remain.

## Critical Gaps

None.

---

**Audit completed at:** 2026-07-07-210000
**Reference:** `architecture/deployment-component.md`
