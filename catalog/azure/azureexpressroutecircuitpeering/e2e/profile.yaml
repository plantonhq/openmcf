apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureexpressroutecircuitpeering
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. KNOWN LIVE BOUNDARY: ARM accepts peering configuration
  # only on a provider-PROVISIONED circuit, and no carrier exists behind
  # the test subscription -- the fixture circuit stays NotProvisioned. If
  # the live create is rejected on that state, the lane records a
  # deferral with the unblock condition (a provisioned circuit), not a
  # module defect.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
