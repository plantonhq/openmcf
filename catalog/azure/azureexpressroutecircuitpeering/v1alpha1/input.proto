syntax = "proto3";

package dev.planton.azure.azureexpressroutecircuitpeering.v1alpha1;

import "catalog/azure/azureexpressroutecircuitpeering/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureExpressRouteCircuitPeeringStackInput is the input to the IaC
// modules (Pulumi/Terraform). It contains the target resource definition
// and Azure provider credentials.
message AzureExpressRouteCircuitPeeringStackInput {
  // The ExpressRoute circuit peering resource to deploy.
  AzureExpressRouteCircuitPeering target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
