apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureavailabilityset
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (the
  # provider-default shape -- free at rest, near-instant). The
  # proximity-placement arm is offline-proven with reason: proximity
  # placement groups are a P2 kind the catalog does not carry yet, and
  # the field is a pass-through ARM ID exercised by the canonical
  # manifest's offline plan.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
