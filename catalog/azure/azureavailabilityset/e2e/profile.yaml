apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureavailabilityset
spec:
  # PROVEN live on both engines: the `minimal` scenario (the
  # provider-default shape: 5 update / 3 fault domains, managed
  # alignment -- all OMITTED in the manifest) through the full
  # lifecycle in ~2.5-3 min/engine, with the apply-idempotency re-plan
  # clean (the omitted defaults produced no echo drift) and the
  # terraform lane's blind import round-trip zero-change (IMPORT-RT
  # ~14s, the `managed`/Aligned-SKU read-back included). The
  # proximity-placement arm is offline-proven with reason: proximity
  # placement groups are a P2 kind the catalog does not carry yet, and
  # the field is a pass-through ARM ID exercised by the canonical
  # manifest's offline plan. Free at rest -- measured lane spend = $0.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
