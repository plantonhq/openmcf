apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurecognitiveaccount
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The S0 OpenAI account carries no idle cost and
  # provisions in one to three minutes -- only the fixture resource
  # group rides its chain. This kind is also a fixture of the
  # deployment and project lanes (both chain through this kind's
  # install profile). The scenario proves the composed responsible-AI
  # children (blocklist + policy) surfacing in the name-keyed outputs;
  # the CMK / network-injection / user-owned-storage arms are
  # offline-proven only (they need a Key Vault key, a delegated subnet,
  # and a storage account the minimal lane deliberately does not
  # deploy). Deletion is a SOFT delete -- the module purges the ghost
  # on destroy, and the orphan sweep adds
  # `az cognitiveservices account list-deleted`.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
