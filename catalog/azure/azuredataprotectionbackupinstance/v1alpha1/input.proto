syntax = "proto3";

package dev.planton.azure.azuredataprotectionbackupinstance.v1alpha1;

import "catalog/azure/azuredataprotectionbackupinstance/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureDataProtectionBackupInstanceStackInput is the input to the IaC
// modules (Pulumi/Terraform). It contains the target resource
// definition and Azure provider credentials.
message AzureDataProtectionBackupInstanceStackInput {
  // The Data Protection backup instance resource to deploy.
  AzureDataProtectionBackupInstance target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
