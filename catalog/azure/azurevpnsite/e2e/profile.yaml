apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurevpnsite
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The site is a free, seconds-fast description object --
  # the timeout covers the fixture WAN chain, not the site itself. This
  # kind is also a fixture of the VPN family: the connection lane chains
  # through its install profile.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
