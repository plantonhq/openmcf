apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremonitordatacollectionrule
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green for the deep canonical
  # shape -- Linux syslog + two perf sources, two destination arms, a
  # KQL-transformed flow -- with rendered-value inspection); live
  # dual-engine proof has not yet run. ONE scenario runs: `minimal`
  # (filtered syslog + perf to the fixture workspace, InsightsMetrics
  # to Azure Monitor metrics). The rule object is free at rest and
  # creates in seconds. OFFLINE-ONLY with reasons: the Event Hub /
  # storage / monitor-account destination arms and the remaining seven
  # data-source arms are pure ARM property writes on the exact
  # CreateOrUpdate body the minimal lane proves (plan-verified with
  # rendered-value inspection); custom streams additionally need a
  # Data Collection Endpoint, which is not a catalog kind yet (unblock:
  # the DCE kind entering the catalog).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
