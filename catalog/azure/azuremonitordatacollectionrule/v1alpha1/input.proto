syntax = "proto3";

package dev.planton.azure.azuremonitordatacollectionrule.v1alpha1;

import "catalog/azure/azuremonitordatacollectionrule/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureMonitorDataCollectionRuleStackInput is the input to the IaC
// modules (Pulumi/Terraform). It contains the target resource
// definition and Azure provider credentials.
message AzureMonitorDataCollectionRuleStackInput {
  // The data collection rule resource to deploy.
  AzureMonitorDataCollectionRule target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
