apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureeventgridnamespace
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (a 1-TU
  # namespace with the MQTT broker on at defaults -- the create-only
  # MQTT decision proven live; the route-topic arm stays
  # offline-proven, reason in the scenario). NOT free at rest (~1
  # TU-hour per lane); creates in a few minutes.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
