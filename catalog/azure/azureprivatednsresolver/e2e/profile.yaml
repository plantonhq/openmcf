apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureprivatednsresolver
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green for the static-inbound
  # and dynamic-hybrid shapes); live dual-engine proof has not yet run.
  # ONE scenario runs: `minimal` (one dynamic inbound + one outbound
  # endpoint on the two delegated fixture subnets -- the fixtures'
  # FIRST live exercise of the subnet delegation surface). Endpoints
  # bill hourly (cents for a lane-length life); the resolver object is
  # free. Azure allows ONE resolver per virtual network, and endpoints
  # are one-per-subnet: this kind's lane never runs in parallel with
  # the forwarding-ruleset or link lanes (their fixture resolver
  # anchors to the same network and subnets). OFFLINE-ONLY with
  # reasons: the STATIC inbound arm (pins an address inside the
  # fixture subnet's range -- the same create path the dynamic lane
  # proves, with one property pinned; plan-verified with
  # rendered-value inspection); multi-endpoint shapes (pure repetition
  # of the proven per-endpoint path; Azure caps 5 each way).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
