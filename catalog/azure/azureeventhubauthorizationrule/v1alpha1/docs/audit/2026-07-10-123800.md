# Audit Report: AzureEventHubAuthorizationRule

**Date:** 2026-07-10-123800
**Component:** AzureEventHubAuthorizationRule (enum 479, provider azure, id_prefix `azehauth`)
**Mode:** full audit with `--parity` focus (new kind)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged as ONE polymorphic kind over Azure's two byte-identical SAS-rule
ARM types: exactly-one-of `namespace_id` XOR `event_hub_id` dispatches to
the matching provider resource on both engines, the shared rights
contract (at least one; manage requires listen+send) is front-loaded as
CELs, `RootManageSharedAccessKey` is CEL-reserved, and the six credential
faces (plus the geo-DR alias pair) surface as sensitive outputs with
identical shapes regardless of scope.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 479 (fills the messaging sub-band), id_prefix `azehauth` unique (checked against the full prefix list; two edits from `azsbauth`); no registry prerequisite (scenario-local parents) |
| 2 | Folder Structure | ✅ `provider/azure/azureeventhubauthorizationrule/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 14 spec tests pass (both scopes, XOR both directions, rights contracts, the reserved name, the 60-char boundary -- happy + error paths); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; dispatch via typed switch with parsed parent names; release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; two count-gated resources with PER-ATTRIBUTE output coalescing; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ the one-kind-over-two-ARM-types verdict, name contract, rotation guidance |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders the hub dispatch path with the manage trio) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 2 pairs (hub-producer / namespace-operator), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED -- all six key/connection-string outputs annotated sensitive; `secret-coverage --check` green |

## Apply-Time Validator Source-Diff

Read `eventhub_authorization_rule_resource.go` +
`eventhub_namespace_authorization_rule_resource.go` + the shared
`eventHubAuthorizationRuleCustomizeDiff` in `helpers.go`:

- **At least one right** (shared CustomizeDiff): front-loaded as the
  `event_hub_auth_rule_at_least_one_right` message CEL.
- **manage requires listen AND send** (shared CustomizeDiff):
  front-loaded as the `event_hub_auth_rule_manage_requires_listen_send`
  message CEL.
- **Name contract** (ValidateEventHubAuthorizationRuleName, ≤60 with the
  start/end alnum rule): front-loaded as a field CEL.
- The two resources share one schema builder
  (`eventHubAuthorizationRuleSchemaFrom`) -- the byte-identical shape is
  the provider's own evidence for the one-kind dispatch verdict.
- No spec CEL invents a rule the provider does not enforce
  (RootManageSharedAccessKey reservation mirrors the service's built-in
  rule, whose keys the namespace kind already surfaces).

## Parity Gate Walk (5b)

- Scope source: MATCH -- both engines parse RG/namespace[/hub] names from
  the SAME resolved parent ARM id with identical anchored patterns
  (TF regex with named groups; Go regexp), both failing loudly on
  malformed ids
- Naming basis: MATCH (`rule_name` both engines, both resources)
- Labels/tags: N/A (ARM does not support tags on Event Hubs entities)
- Feature coverage: MATCH -- rights trio presence-guarded to false on
  both engines (azurerm's defaults); both dispatch paths implemented on
  both engines
- Dependency ordering: MATCH (single resource per scope)
- Stack outputs: MATCH -- the TF module coalesces PER ATTRIBUTE across
  the count-gated variants (whole-object coalescing would
  sensitivity-taint the id/name outputs); Pulumi's switch assigns the
  same faces; alias faces fall back to "" identically. `pkg/outputs`
  conformance case added

**PARITY ✅** (no exceptions needed).

## Recorded Skips

None -- the provider surface is fully modeled.

## Critical Gaps

None.
