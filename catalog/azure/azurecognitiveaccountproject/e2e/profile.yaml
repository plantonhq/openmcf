apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurecognitiveaccountproject
spec:
  # LIVE-PROVEN 2026-08-12 on BOTH engines: full six-phase lifecycle
  # (~5 min/engine; the chain pays the fixture account's cycle) plus the
  # blind single-id import round-trip, zero-change, zero tolerances.
  # The parent-shape contract held live (AIServices + project management
  # + identity), is_default read TRUE as the account's first project,
  # the endpoints map populated (the AI Foundry API endpoint), and the
  # project's system-assigned identity principal surfaced -- all 5/5
  # outputs on both engines. The user-assigned identity arm is
  # offline-proven only (the minimal lane deliberately brings no
  # pre-created identity). No agents or data-plane assets are ever
  # created -- the verifier asserts the ARM project object. One
  # non-reproducible teardown race observed and swept: the fixture RG
  # once survived EMPTY after a lane whose destroys all reported
  # success (recorded in the provider harness README's soft-delete
  # sweep note).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
