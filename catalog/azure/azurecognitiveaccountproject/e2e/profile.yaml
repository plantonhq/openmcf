apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurecognitiveaccountproject
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The project is a free workspace object provisioning in
  # seconds to a couple of minutes; the chain pays the fixture account's
  # cycle (the fixture is the AIServices + project-management + identity
  # shape ARM requires of a project's parent). The user-assigned
  # identity arm is offline-proven only (the minimal lane deliberately
  # brings no pre-created identity). No agents or data-plane assets are
  # ever created -- the verifier asserts the ARM project object.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
