syntax = "proto3";

package dev.planton.azure.azurecontainerapp.v1alpha1;

import "catalog/azure/azurecontainerapp/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureContainerAppStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureContainerAppStackInput {
  // target cloud-resource to be deployed
  AzureContainerApp target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
