apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurebackupprotectedfileshare
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. The lane rides the group's deepest fixture
  # chain -- resource group, vault, storage account, file share, the
  # container REGISTRATION, and the file-share policy (its minimal
  # smoke doubling as install manifest) -- and the protect/unprotect
  # operations sit in the provider's 80-minute timeout class (normal
  # runs are minutes; the budget covers the class). Destroy leaves the
  # backup data to vault soft delete (always on) -- the queue
  # watch-list leads with whether the held item delays the fixture
  # registration's unregister at teardown.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 120
