apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureeventgridnamespacetopic
spec:
  # PROVEN live on both engines (dual-engine six-phase runner + blind
  # import round-trip). ONE scenario runs: `minimal` (one CloudEvents
  # stream inside the namespace kind's smoke instance -- the typed
  # namespace reference proven resolving through the registry chain,
  # plus the retention dial). Measured: topic deploy 20-35s and
  # destroy 20-35s; the ~2.5-minute namespace fixture chain dominates
  # the ~5-minute lane. The blind import by the full namespace-scoped
  # id ({namespace_id}/topics/{name}) round-tripped zero-change with
  # zero tolerances. The topic is free; the timeout budgets the
  # namespace chain, not the topic.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
