apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureeventgridnamespacetopic
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (one stream
  # declared inside the namespace kind's smoke instance -- the
  # registry prerequisite deploys the namespace first, and the lane
  # proves the typed namespace reference resolving through the chain
  # plus the retention update surface). The topic is free; the
  # timeout budgets the namespace chain, not the topic.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
