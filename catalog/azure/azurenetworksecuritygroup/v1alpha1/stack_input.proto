syntax = "proto3";

package dev.planton.azure.azurenetworksecuritygroup.v1alpha1;

import "catalog/azure/azurenetworksecuritygroup/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureNetworkSecurityGroupStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureNetworkSecurityGroupStackInput {
  // target cloud-resource to be deployed
  AzureNetworkSecurityGroup target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
