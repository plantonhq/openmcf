apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredatafactorylinkedservice
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green across seven variant
  # shapes); live dual-engine proof has not yet run. THREE scenarios
  # run -- the representative variants provable without external
  # systems: web (anonymous, zero dependencies), key-vault (the
  # fixture vault by reference -- the cross-kind chart edge), and
  # blob storage (the fixture storage account's endpoint + managed
  # identity -- the heaviest auth matrix's no-secret arm). All three
  # carry ZERO secret material. The other 20 variants are
  # OFFLINE-PROVEN by design: each needs a live external system (a
  # Snowflake account, a Databricks workspace, an SFTP server, an
  # on-premises DSN, ...) that E2E cannot honestly stand up; their
  # tofu plans are render-inspected instead, and Azure saves linked
  # service definitions without dialing the target, so a live create
  # would prove no more than the management-plane contract the three
  # live lanes already prove.
  # Linked services are authoring metadata -- free at rest; the lanes
  # pay only the factory chain.
  # WATCH: whether ARM save-time-validates any connection shape
  # (none expected -- saving is not connecting); masked secret
  # read-back on the import round-trip (the three live shapes are
  # secret-free by design, so the round-trip must be zero-change).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
