apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredatafactorydataflow
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (a mapping data
  # flow whose source embeds a flowlet created as a prerequisite doc --
  # proves BOTH provider forms and the self-kind flowlet reference live
  # in one lane). Data flows are authoring metadata -- free at rest,
  # created in seconds; the lane pays the fixture factory's chain.
  # WATCH: whether ARM's save-time validation accepts name-only sinks
  # without a linked service (the provider's own tests always attach
  # one; the linked-service kind does not exist yet) -- if ARM rejects,
  # the proof lane's recorded fallback is flowlet-only smoke plus a
  # frozen mapping-arm note until the linked-service kind ships.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
