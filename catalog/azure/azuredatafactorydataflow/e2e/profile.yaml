apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredatafactorydataflow
spec:
  # PROVEN live on both engines (proof session, 2026-08-13): `minimal`
  # (a mapping data flow whose source embeds a flowlet created as a
  # prerequisite doc -- BOTH provider forms and the self-kind flowlet
  # reference proven in one lane). Pulumi 10m03s, Terraform 12m33s
  # incl. a 20s blind import round-trip with a no-differences re-plan.
  # LIVE-RESOLVED: ARM accepts a mapping flow whose sink names no
  # linked service -- save-time validation checks the script's stream
  # names against sources/sinks, not their bindings (the authored
  # fallback never activated). Data flows are authoring metadata --
  # free at rest, created in seconds; the lane pays the fixture
  # factory's chain.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 45
