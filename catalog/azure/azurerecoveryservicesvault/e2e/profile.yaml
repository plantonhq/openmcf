apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurerecoveryservicesvault
spec:
  # PROVEN live on both engines (proof session, 2026-08-16): `minimal`
  # (LocallyRedundant vault + the monitoring block with one switch
  # explicitly false) -- Pulumi 5m6s (deploy 2m13s, destroy 32s),
  # Terraform 4m25s incl. a 15s blind import round-trip with a
  # no-differences re-plan. Outputs read 2/4 populated as designed
  # (the guard-association and identity-principal outputs are empty on
  # a guard-less, identity-less smoke). Immutability, CMK encryption,
  # and the Resource Guard arm stay offline-proven: immutability
  # transitions and the CMK ratchet are ONE-WAY doors (a smoke lane
  # that walks through them cannot walk back), and the guard needs a
  # Data Protection resource the catalog has not modeled yet. FREE at
  # rest; creates in one to three minutes.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
