apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurerecoveryservicesvault
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. The vault is FREE at rest and creates in
  # one to three minutes -- the smoke proves the vault object with a
  # non-default redundancy and the monitoring block. Immutability, CMK
  # encryption, and the Resource Guard arm are offline-proven only
  # this wave: immutability transitions and the CMK ratchet are
  # ONE-WAY doors (a smoke lane that walks through them cannot walk
  # back), and the guard needs a Data Protection resource the catalog
  # has not modeled yet.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
