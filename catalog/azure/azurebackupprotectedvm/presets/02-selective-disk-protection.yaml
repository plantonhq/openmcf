apiVersion: azure.planton.dev/v1alpha1
kind: AzureBackupProtectedVm
metadata:
  name: protect-db-vm-os-only
spec:
  resourceGroup:
    valueFrom:
      kind: AzureResourceGroup
      name: "<your-resource-group>"
      fieldPath: status.outputs.resource_group_name
  recoveryVaultName:
    valueFrom:
      kind: AzureRecoveryServicesVault
      name: "<your-recovery-services-vault>"
      fieldPath: status.outputs.recovery_services_vault_name
  sourceVmId:
    valueFrom:
      kind: AzureVirtualMachine
      name: "<your-virtual-machine>"
      fieldPath: status.outputs.vm_id
  backupPolicyId:
    valueFrom:
      kind: AzureBackupPolicyVm
      name: "<your-backup-policy>"
      fieldPath: status.outputs.backup_policy_id
  # Skip the data disks at LUNs 0 and 1 -- the database on them has its
  # own native backups; the OS disk is always backed up regardless.
  excludeDiskLuns: [0, 1]
