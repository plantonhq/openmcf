apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureaifoundry
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. The hub is free at rest and provisions in a
  # few minutes on the fixture companion chain (resource group + key
  # vault + HNS-off storage account). This kind is also a fixture of
  # the AzureAiFoundryProject lane. The CMK / user-assigned /
  # isolation arms are offline-proven only (they need pre-granted key
  # access the minimal lane deliberately does not compose; the CMK key
  # must be a VERSIONED URL -- the hub's own contract). Deletion is a
  # SOFT delete -- the hub ghost holds its name until purged; both
  # modules purge on destroy (proven live: dual-engine recreate of the
  # same fixed name). No CLI or REST API lists ML ghosts -- the
  # portal's "Recently deleted" view is the one listing surface
  # (shared with the ML workspace class; see e2e/README.md).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 40
