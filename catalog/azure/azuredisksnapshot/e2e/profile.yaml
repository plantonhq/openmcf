apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredisksnapshot
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green across BOTH source
  # arms); live dual-engine proof has not yet run. ONE scenario runs:
  # `minimal` (an incremental Copy snapshot of the fixture disk --
  # pennies for the lane's minutes). Offline-proven with reasons: the
  # Import arm (needs an uploaded page-blob VHD the lane cannot
  # honestly stand up -- covered by its own offline plan with rendered
  # -value inspection); the ADE encryption-settings block (records
  # legacy in-guest encryption secrets a fresh fixture disk does not
  # have -- exercised by the canonical manifest's offline plan); the
  # AllowPrivate/disk-access posture (disk-access resources are a P2
  # kind the catalog does not carry yet).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
