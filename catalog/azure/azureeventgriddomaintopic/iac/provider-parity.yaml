# Provider-parity manifest: this kind's recorded judgment against the
# pinned azurerm provider (checked by `planton provider-parity --check`;
# format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  azurerm_eventgrid_domain_topic:
    mappings:
      # The provider addresses domain topics by (resource group, domain
      # name); the spec takes the parent domain's ARM ID -- the
      # composable reference shape -- and both modules derive the two
      # addressing arguments from its segments.
      - spec: spec.domain_id
        arg: domain_name
      - spec: spec.domain_id
        arg: resource_group_name
