apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureeventgriddomaintopic
spec:
  # PROVEN live on both engines (2026-08-13): `minimal` (one tenant
  # stream declared inside the domain kind's smoke instance -- the
  # registry prerequisite deployed the domain first, and the typed
  # domain reference resolved through the chain on both engines,
  # proving the pinned-topics posture end to end). Blind import
  # round-trip: zero-change on the domain-scoped id
  # ({domain_id}/topics/{name} composed under the run-id domain name).
  # Measured: Pulumi lane 4m35s, Terraform lane 4m00s incl. 11s import
  # round-trip; the domain chain is ~1.5m of each lane.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
