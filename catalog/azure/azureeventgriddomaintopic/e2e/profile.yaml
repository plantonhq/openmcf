apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureeventgriddomaintopic
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (one tenant
  # stream declared inside the domain kind's smoke instance -- the
  # registry prerequisite deploys the domain first, and the lane proves
  # the pinned-topics posture plus the typed domain reference resolving
  # through the chain). Free; the timeout budgets the domain chain,
  # not the topic.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
