apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredatafactorytrigger
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green); live dual-engine
  # proof has not yet run. FOUR scenarios run, one per variant --
  # schedule, tumbling-window, blob-event (fixture storage account),
  # custom-event (the Event Grid topic kind's smoke instance). All
  # four deploy activated with NEVER-FIRING conditions (far-future
  # start times; a blob path that never matches; an event type never
  # published), so the Start/Stop lifecycle is exercised live at zero
  # pipeline-run cost. Triggers are authoring metadata -- free at
  # rest; the lanes pay the factory+pipeline chain.
  # WATCH: the blob-event trigger auto-creates Event Grid wiring on
  # the TARGET storage account -- the zero-orphan sweep must check the
  # target side after destroy; also tumbling-window acceptance of a
  # far-future start_time.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
