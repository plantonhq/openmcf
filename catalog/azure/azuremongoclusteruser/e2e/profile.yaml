apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremongoclusteruser
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (the fixture
  # identity's service principal granted root on the cluster kind's
  # smoke instance -- the three-kind reference chain live: registry
  # chains the cluster, the scenario annotation chains the identity).
  # The grant is free; the timeout budgets the cluster chain, not the
  # grant. Watch-list: the deploy-time Entra-auth contract (the
  # cluster smoke shape enables MicrosoftEntraID for this lane).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 90
