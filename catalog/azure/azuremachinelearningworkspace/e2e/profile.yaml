apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningworkspace
spec:
  # PROVEN both engines. The smoke is a Basic system-identity workspace
  # on the fixture companion chain in DEFAULT managed-network mode
  # (create ~1 min, destroy ~2 min, purge-on-destroy proven: the
  # Terraform lane recreated the same fixed name minutes after Pulumi
  # destroy). Two live attempts at the approved-outbound +
  # provision-on-creation flagship failed in different ways (PE-rule
  # destroy 409-loop; managed-VNet create rolled the workspace back
  # with "workspace identity has been deleted") -- that arm and the
  # three composed outbound-rule children stay offline-proven, findings
  # on the spec fields. timeout_minutes stays 90 so a future
  # managed-network scenario has budget; default-network lanes finish
  # in ~15 min including the companion chain.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 90
