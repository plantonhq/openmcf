apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningworkspace
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The Basic workspace is free at rest and provisions in
  # two to five minutes; the scenario also provisions the managed VNet
  # at deploy time (approved-outbound isolation with one rule of each
  # type -- the composed-children flagship), which adds several minutes
  # including the private endpoint to the fixture vault. The companion
  # chain (storage account + key vault + application insights on the
  # log-analytics fixture) rides the registry prerequisites. This kind
  # is also a fixture of the datastore lane (and the compute lanes to
  # come). The CMK / feature-store / serverless-subnet arms are
  # offline-proven only (they need a Key Vault key grant, a feature
  # store, and a delegated subnet the minimal lane deliberately does
  # not deploy). storage_account_access_type is the kind's one
  # PARITY-EXCEPTION (Terraform-only; the Pulumi module fails loudly).
  # Deletion is a SOFT delete -- the workspace ghost holds its name
  # until purged; the orphan sweep adds `az ml workspace list
  # --archived`.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
