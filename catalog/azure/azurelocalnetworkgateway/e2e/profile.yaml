apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurelocalnetworkgateway
spec:
  # Live-proven 2026-08-11 on both engines (full six-phase lifecycle;
  # deploy ~40-45s, destroy ~20-35s) plus the blind import round-trip on
  # the terraform lane (re-imported state planned zero changes and owned
  # the destroy). The fastest lane of the site-to-site trio: the object
  # is pure ARM metadata (fixture resource group -> the site
  # description), provisioning in seconds with nothing on-premises
  # contacted.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 20
