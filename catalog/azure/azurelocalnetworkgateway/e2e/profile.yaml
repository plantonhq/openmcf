apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurelocalnetworkgateway
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The fastest lane of the site-to-site trio: the object is
  # pure ARM metadata (fixture resource group -> the site description),
  # provisioning in seconds with nothing on-premises contacted.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
