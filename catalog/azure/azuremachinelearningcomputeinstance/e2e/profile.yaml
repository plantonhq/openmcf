apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningcomputeinstance
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The instance is a REAL VM for the minutes it lives
  # (create ~5-10 min; small workstation size keeps the lane's VM cost
  # in cents); the rest of the time lives in its chain (the fixture
  # workspace and its companion services). The scenario's cloud-side
  # name carries ${E2E_RUN_ID}: instance names are reserved REGION-WIDE
  # per subscription (and briefly after deletion), exactly the collision
  # class the token exists for. The personal/assign-to-user arm is
  # offline-proven only (honest values embed a real user's tenant and
  # object IDs -- the same tenant-specificity that keeps AAD arms out of
  # committed fixtures); SSH and VNet placement are offline-proven only
  # this wave.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
