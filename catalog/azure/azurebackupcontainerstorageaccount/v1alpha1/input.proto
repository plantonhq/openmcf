syntax = "proto3";

package dev.planton.azure.azurebackupcontainerstorageaccount.v1alpha1;

import "catalog/azure/azurebackupcontainerstorageaccount/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureBackupContainerStorageAccountStackInput is the input to the
// IaC modules (Pulumi/Terraform). It contains the target resource
// definition and Azure provider credentials.
message AzureBackupContainerStorageAccountStackInput {
  // The backup container registration resource to deploy.
  AzureBackupContainerStorageAccount target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
