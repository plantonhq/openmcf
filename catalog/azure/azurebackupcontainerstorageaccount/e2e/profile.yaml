apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurebackupcontainerstorageaccount
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. Registration is FREE and data-free; the
  # lane's time is the fixture chain (resource group + vault + storage
  # account) plus the Register/Unregister LROs (the provider allows 30
  # minutes each; normal runs are minutes). NOTE: this kind's smoke
  # and the protected-share lane's fixture registration target the
  # SAME ARM identity (fixture account x fixture vault) -- safe under
  # the runner's sequential execution; never run the two lanes
  # concurrently.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
