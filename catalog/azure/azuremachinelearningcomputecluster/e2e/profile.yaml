apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningcomputecluster
spec:
  # PROVEN both engines 2026-08-14. Scale-to-zero dedicated cluster
  # (STANDARD_DS2_V2, min 0 / max 1, system identity) on the fixture
  # workspace: the compute OBJECT creates in ~1-2 min and holds zero
  # nodes for its whole life. Outputs 3/3 including the identity
  # principal. Terraform import RT zero-change
  # (.../workspaces/{ws}/computes/{name}). SSH, VNet, and low-priority
  # stay offline-proven (grammars spec/CEL-tested; no job runs so a
  # node-level lane would prove billing, not behavior). Chain dominates
  # wall-clock (~16 min Pulumi / ~15 min Terraform including import).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 45
