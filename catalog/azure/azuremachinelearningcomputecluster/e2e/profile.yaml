apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningcomputecluster
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The cluster OBJECT creates in a few minutes and, at
  # min_node_count 0, holds zero nodes -- the smoke lane never pays for
  # a running VM; the cost and most of the time live in its chain (the
  # fixture workspace and its companion services). The scenario proves
  # the scale-to-zero shape with a system identity; SSH, VNet placement,
  # and low-priority nodes are offline-proven only this wave (their
  # grammars are spec/CEL-tested; no job ever runs in the smoke lane, so
  # a node-level lane would prove billing, not behavior).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
