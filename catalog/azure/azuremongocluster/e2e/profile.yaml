apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremongocluster
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (the Free-tier
  # sandbox with both auth methods and one composed firewall rule --
  # the keyed-child machinery and the sensitive connection-string
  # outputs proven live). The GeoReplica and PointInTimeRestore arms
  # stay offline-proven (reasons: a replica needs a second live
  # cluster with the GeoReplicas preview feature -- double the lane's
  # cost and time for a mode Azure documents as source-inherited; a
  # restore needs backup history that exists only minutes after
  # create -- both offline plans render the arms; unblock: a proven
  # long-lived source cluster). Watch-list: Free-tier regional
  # availability (fallback M10 under proof design authority) and the
  # cluster create's duration (the provider budgets 60m).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 90
