apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureeventgridtopic
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (a CloudEvents
  # topic with an IP rule in the fixture resource group -- free at
  # rest, creates in seconds; the cloud-side name carries the run id
  # because topic names are region-wide public DNS labels). The lane
  # also proves the sensitive access-key outputs round-trip unmasked.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
