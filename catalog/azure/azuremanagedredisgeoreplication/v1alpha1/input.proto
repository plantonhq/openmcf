syntax = "proto3";

package dev.planton.azure.azuremanagedredisgeoreplication.v1alpha1;

import "catalog/azure/azuremanagedredisgeoreplication/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureManagedRedisGeoReplicationStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureManagedRedisGeoReplicationStackInput {
  // target cloud-resource to be deployed
  AzureManagedRedisGeoReplication target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
