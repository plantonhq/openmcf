apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureexpressroutecircuit
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The circuit object provisions in minutes and never
  # reaches provider-Provisioned in the lane (no carrier behind the test
  # subscription) -- verifiers assert ARM object state, SKU, and the
  # composed authorization, never the cross-connect. Billing runs from
  # creation (~cents for a lane-length life at 50 Mbps metered).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
