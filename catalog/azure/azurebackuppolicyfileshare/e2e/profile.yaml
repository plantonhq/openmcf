apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurebackuppolicyfileshare
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. The policy is a FREE configuration object
  # on the fixture vault -- the lane's time is the fixture chain
  # (resource group + vault, both fast). TWO scenarios run: the daily
  # snapshot smoke (which doubles as the protected-share lane's
  # install manifest -- deliberately the SAFE shape) and the
  # hourly-vault-standard scenario proving the window dials and the
  # vaulted tier live, isolated so a tier-side failure never couples
  # into the dependent lane.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
