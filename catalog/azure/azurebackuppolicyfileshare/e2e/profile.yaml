apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurebackuppolicyfileshare
spec:
  # PROVEN live on both engines (proof session, 2026-08-18), BOTH
  # scenarios: the daily snapshot smoke (which doubles as the
  # protected-share lane's install manifest) and hourly-vault-standard
  # (window dials + the vaulted tier + month-days retention). Pulumi
  # 5m25s / 5m44s; Terraform 6m14s / 5m55s each incl. a blind import
  # round-trip (11-13s) with a no-differences re-plan. A FREE
  # configuration object; the lane's time is the fixture chain
  # (resource group + vault, both fast).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 45
