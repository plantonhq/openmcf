syntax = "proto3";

package dev.planton.azure.azurebackuppolicyfileshare.v1alpha1;

import "catalog/azure/azurebackuppolicyfileshare/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureBackupPolicyFileShareStackInput is the input to the IaC modules
// (Pulumi/Terraform). It contains the target resource definition and
// Azure provider credentials.
message AzureBackupPolicyFileShareStackInput {
  // The backup policy resource to deploy.
  AzureBackupPolicyFileShare target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
