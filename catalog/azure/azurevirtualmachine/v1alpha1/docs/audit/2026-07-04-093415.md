# Audit Report: AzureVirtualMachine

**Audit Date:** 2026-07-04 09:34:15  
**Component Kind:** AzureVirtualMachine  
**Provider:** azure  
**Component Path:** `catalog/azure/azurevirtualmachine/v1alpha1/`  
**Enum Value:** 408  
**ID Prefix:** azvm  
**Audit Mode:** `--parity` (parity-focused full audit; post-remediation re-verification)

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

Both gates hold: **PARITY ✅** (one documented, output-neutral tag-shape exception) and **COVERAGE ✅** (zero secret-coverage gaps). All automated guards pass: spec tests, the release-equivalent Pulumi build, the `pkg/outputs` conformance case, `planton validate-outputs` (7/7 proto fields populated, zero unmapped), the secret-coverage gate, and a full `tofu plan` against a patch-seam-bearing fixture.

> **Remediation note:** an earlier audit in this session found an undocumented, misdeploy-class Terraform divergence — the patch-orchestration locals in `iac/tf/locals.tf` compared enum values against prefix-less strings (`"AUTOMATIC_BY_PLATFORM"`) while the tfvars wire format carries the full proto enum names (`LINUX_AUTOMATIC_BY_PLATFORM`, `WINDOWS_AUTOMATIC_BY_PLATFORM`, `ASSESSMENT_*`), so platform patching was silently dropped on Terraform. The fix has been applied and independently re-verified in this audit (see Category 5b), and the offline fixture now permanently exercises the seam.

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Quick Wins

None required for the score. One zero-cost informational item:

1. **Local Terraform/Pulumi dev artifacts present but ignored**
   - **Why:** `iac/tf/` contains `.terraform/`, `terraform.tfplan`, `backend.tf`, `.terraform.lock.hcl`, and `iac/pulumi/` contains `stack-input.yaml` from today's live E2E and plan runs. All are git-ignored (`git check-ignore` confirms), so nothing will be committed — informational only.
   - **How:** Nothing to do; optionally clean the workdir before release.
   - **File:** `iac/tf/`, `iac/pulumi/`

---

## Critical Gaps

None. No blocking issues found. The patch-orchestration divergence found earlier in this session has been remediated and re-verified.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureVirtualMachine = 408` exists in `cloud_resource_kind.proto`
- Enum value 408 is within the Azure range (400-599)
- `id_prefix` `azvm` is unique (single occurrence in the registry)
- `kind_meta` includes provider (`azure`), version (`v1`), and `id_prefix`
- `prerequisites: [AzureNetworkInterface]` is declared with an explanatory comment (a VM attaches at least one NIC; the subnet/network/resource-group chain arrives transitively)

❌ **Failed:**

- None

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists at `catalog/azure/azurevirtualmachine/v1alpha1/`
- Folder name is the lowercase enum name under the correct provider hierarchy
- `v1/` subfolder exists

❌ **Failed:**

- None

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files (13.32%)

✅ **Passed:**

- `api.proto` exists (997 bytes) with the `azure.planton.dev/v1alpha1` apiVersion and `AzureVirtualMachine` kind constants
- `spec.proto` exists (44.7 KB) — 31 top-level fields, 18 messages, 14 enums, and 13 CEL message-level rules mirroring ARM's contract (exactly-one image source, per-OS auth requirements, boot-from-disk-forbids-auth, patch-mode prerequisites for reboot control/hotpatching/bypass, confidential-VM secure-boot/vTPM pairing, placement mutual exclusions)
- `stack_input.proto` exists (431 bytes) with target + `AzureProviderConfig`
- `stack_outputs.proto` exists (1.4 KB) with 7 fields (`vm_id`, `vm_name`, `virtual_machine_guid`, `private_ip_address`, `public_ip_address`, `computer_name`, `system_assigned_identity_principal_id`)

**Validation Message Quality:**

- Every CEL `message` is a complete, user-facing sentence (e.g. "a Linux VM booting from an image requires admin_username and at least one credential -- SSH keys when password authentication is disabled (the default), admin_password when it is enabled")
- No raw regex appears in any user-facing message
- Conditional rules state both condition and requirement (e.g. "patching.reboot_setting requires the OS profile's patch_mode to be AUTOMATIC_BY_PLATFORM")
- No terse or proto-path-named messages found

**Score:** 13.32% / 13.32%

#### 3.2 Generated Stubs (3.33%)

✅ **Passed:**

- `api.pb.go`, `spec.pb.go` (162.7 KB), `stack_input.pb.go`, `stack_outputs.pb.go` all exist and are newer than their protos

**Score:** 3.33% / 3.33%

#### 3.3 Unit Tests - Presence (2.77%)

✅ **Passed:**

- `spec_test.go` exists (15.2 KB)
- Ginkgo/Gomega suite with mutate-one-field-at-a-time structure over `validLinuxSpec()` / `validWindowsSpec()`
- Imports `buf.build/go/protovalidate` and exercises the CEL rules (image-source arity, per-OS auth, boot-from-disk, patch prerequisites, placement conflicts, identity/type pairing)

**Score:** 2.77% / 2.77%

#### 3.4 Unit Tests - Execution (2.78%)

```bash
go test ./catalog/azure/azurevirtualmachine/v1alpha1/ -count=1
```

✅ **Passed:**

- Tests compile and pass fresh (`ok ... 0.577s`, `-count=1`)

**Score:** 2.78% / 2.78%

**Total Protobuf API Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

#### 4.1 Module Files (6.66%)

✅ **Passed:**

- `iac/pulumi/module/main.go` exists (5.2 KB) — engine selection (linux XOR windows), attachment loop, output exports
- `iac/pulumi/module/locals.go` exists (8.7 KB) — all enum→ARM mappings, tag merge with documented `PARITY-EXCEPTION:`
- `iac/pulumi/module/vm_args.go` exists (22.8 KB) — the full per-OS argument surface
- `iac/pulumi/module/outputs.go` exists (output-name constants matching `stack_outputs.proto`)

**Module Comment Quality:**

- Comments explain lifecycle trade-offs (which fields replace the VM, what survives), ARM's two-surface Linux/Windows model, the presence-guarded true-default optional-bool pattern ("an absent spec value explicitly falls back to Azure's default"), custom_data-vs-user_data secrecy, and why attachments are separate ARM operations. Meets the `spec.proto` field-comment bar; not narration.

**Score:** 6.66% / 6.66%

#### 4.2 Entrypoint Files (6.66%)

✅ **Passed:**

- `iac/pulumi/main.go` exists at the root and declares `package main`
- No `main/` or `entrypoint/` subdirectory under `iac/pulumi/`
- `iac/pulumi/Pulumi.yaml` and `iac/pulumi/Makefile` exist
- Uses the shared `pulumiazureprovider.Get` builder (static client secret, keyless web identity, or ambient chain)
- Release-equivalent build passes:

```bash
go build -o /dev/null ./catalog/azure/azurevirtualmachine/v1alpha1/iac/pulumi
```

**Score:** 6.66% / 6.66%

**Total Pulumi Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `iac/tf/variables.tf` exists (7.7 KB), curated `optional()` style mirroring the full spec with per-field comments
- `iac/tf/provider.tf` exists — the CANONICAL EMPTY provider block (azurerm `~> 4.0`, `features {}` only; **no client_secret/credential injection remains** — verified)
- `iac/tf/locals.tf` exists (5.6 KB) — all enum→ARM mappings now compare against the full proto enum names, with comments explaining the per-OS prefix disambiguation
- `iac/tf/main.tf` exists (17.0 KB) — linux XOR windows via `count`, attachment resources, full argument surface
- `iac/tf/outputs.tf` exists (7 outputs matching the proto, `try()` fallback for the principal id)

**Module Comment Quality:**

- The HCL carries the same lifecycle/why commentary as the Pulumi module (compute-shell model, replacement identity fields, per-OS patch vocabularies, why attachments are separate resources), plus the new wire-format comments on the patch-mode locals. Meets the bar.

❌ **Failed:**

- None (the patch-orchestration enum comparisons found earlier in this session are fixed and re-verified)

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Compared engines: Pulumi (`iac/pulumi/module/`, including `vm_args.go`) and Terraform (`iac/tf/`), both judged against `spec.proto` + `stack_outputs.proto` (neither privileged). Output parity verified with the automated guards, not by eye: `go run . validate-outputs --kind AzureVirtualMachine --module-dir .../iac/tf --sample-outputs <sample>.json` → **7/7 proto fields populated, zero unmapped**, and the per-kind `pkg/outputs/conformance_test.go` case (`AzureVirtualMachine`) passes fresh.

**Patch-seam re-verification (this session's remediation):**

- Confirmed the three ternary chains in `iac/tf/locals.tf` now compare against the exact enum names declared in `spec.proto`: `LINUX_IMAGE_DEFAULT` / `LINUX_AUTOMATIC_BY_PLATFORM` (Linux), `MANUAL` / `AUTOMATIC_BY_OS` / `WINDOWS_AUTOMATIC_BY_PLATFORM` (Windows), `ASSESSMENT_IMAGE_DEFAULT` / `ASSESSMENT_AUTOMATIC_BY_PLATFORM` (assessment) — cross-checked against the enum declarations in `spec.proto` and the generated `spec.pb.go`.
- Re-ran the full pipeline myself:

```bash
go run . tofu plan --manifest apis/.../v1/iac/hack/manifest.yaml --module-dir apis/.../v1/iac/tf
```

  The plan (2 to add) emits `patch_mode = "AutomaticByPlatform"`, `patch_assessment_mode = "AutomaticByPlatform"`, `reboot_setting = "IfRequired"` — byte-identical to the ARM strings the Pulumi module sends for the same spec. The seam is proven end-to-end on the Terraform side (real serializer → real tfvars → real plan), not just by reading code.
- The offline fixture `iac/hack/manifest.yaml` now permanently exercises the seam (linux `patchMode: LINUX_AUTOMATIC_BY_PLATFORM` + `patching.assessmentMode` / `rebootSetting`), closing the regression hole the live E2E's minimal scenario could not cover.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source (resource group) | `spec.resource_group.GetValue()` | `var.spec.resource_group` (resolved literal) | ✅ MATCH |
| Resource naming basis | `spec.name` (VM name); attachments `{name}-lun-{n}` | `var.spec.name`; attachments `for_each` by LUN | ✅ MATCH |
| Linux XOR Windows selection | `locals.IsLinux` from `os_profile.linux` presence | `local.is_linux` from same field | ✅ MATCH |
| NIC references (repeated FK) | resolved ARM IDs, order preserved (first = primary) | same | ✅ MATCH |
| OS disk (caching, storage, size, name, diff_disk `Local`+placement, encryption sets, security encryption type, write accelerator) | full | full, identical ARM strings | ✅ MATCH |
| Image source (reference / id / os_managed_disk_id, exactly one) | full | full | ✅ MATCH |
| Data disk attachments as attachment resources | `compute.NewDataDiskAttachment` per entry (LUN, caching, write accel) | `azurerm_virtual_machine_data_disk_attachment` per entry | ✅ MATCH |
| Identity (type mapping incl. `"SystemAssigned, UserAssigned"`, identity_ids FKs) | full | full | ✅ MATCH |
| Spot (priority=Spot on presence, eviction policy, max_bid_price default −1) | full | full (explicit `Regular`/`−1` ≡ provider defaults) | ✅ MATCH |
| Availability (zone, availability set, PPG, capacity reservation, dedicated host/group, scale set, fault domain) | full | full | ✅ MATCH |
| Security (secure_boot, vtpm, encryption_at_host) | full | full | ✅ MATCH |
| Patch orchestration (per-OS patch_mode, assessment_mode, reboot_setting, bypass) | maps `LINUX_*`/`WINDOWS_*`/`ASSESSMENT_*` enums → ARM strings | same full-name comparisons → same ARM strings (verified via live `tofu plan`) | ✅ MATCH (remediated this session) |
| Windows surface (automatic updates, hotpatching, timezone, WinRM, unattend, Hybrid Benefit license) | full | full | ✅ MATCH |
| Linux license (BYOS enums = ARM literals) | `.String()` passthrough | name passthrough | ✅ MATCH |
| Gallery applications (order, tag, blob, upgrade, failure mode) | full | full | ✅ MATCH |
| Termination / OS-image notifications | presence-gated, `enabled=true`, timeout | same | ✅ MATCH |
| Plan / custom_data / user_data / extensions budget / VM-agent gates / disk controller / additional capabilities / Key Vault secrets / edge zone | full | full | ✅ MATCH |
| Tags (keys + values, user-wins merge) | metadata-derived + user tags | same merge order | 🔵 EXCEPTION (documented in BOTH modules) |
| Dependency ordering (VM → attachments) | implicit via `vmId` output | implicit via resource reference | ✅ MATCH |
| Stack outputs → proto | 7 exports, names = proto fields | 7 outputs, same names | ✅ MATCH (validate-outputs: 7/7) |

**Legend:** ✅ MATCH | ⚠️ DIVERGES (undocumented) | 🔵 EXCEPTION (documented `PARITY-EXCEPTION:`)

✅ **In parity:**

- Every spec dimension is honored identically on both engines, including the subtle ones: presence-guarded true-default optional bools (`disable_password_authentication`, `provision_vm_agent`, `allow_extension_operations`, `automatic_updates_enabled`) resolve identically (explicit true vs `optional(bool, true)`); zero-default bools resolve to the provider's `false` on both sides; unset optional enums send nothing on both engines so Azure's defaults apply.
- Both output sets flatten onto the same 7 `AzureVirtualMachineStackOutputs` fields; the system-assigned principal id degrades to `""` identically (`ApplyT` nil-guard ↔ `try(..., "")`).
- Live dual-engine E2E passed today (`TestAzureVirtualMachine_Pulumi` 418.57s, `_Terraform` 551.19s; composed NIC chain + managed-disk attachment, all 8 phases), and the previously uncovered patch seam is now proven offline via the fixture plan.

🔵 **Documented exception:**

- **Tag shape (family-wide).** Terraform emits the snake-case `resource_kind = "azure_virtual_machine"` literal and falls back `resource_id → metadata.name`; Pulumi emits the lowered `CloudResourceKind` enum string and omits `resource_id` when `metadata.id` is empty. The `PARITY-EXCEPTION:` comment is present in **both** modules (`iac/tf/locals.tf` line 8, `iac/pulumi/module/locals.go` line 195), output-neutral, flagged as a family-wide convention change. NOT a defect.

⚠️/❌ **Divergences (undocumented):**

- None. (The patch-orchestration enum-mapping divergence found earlier in this session has been fixed and re-verified.)

**Effect on score:** none — Categories 4 and 5 keep full credit.

---

### 6. Documentation - Research (13.18%)

✅ **Passed:**

- `docs/README.md` exists (21.1 KB — comprehensive)
- Landscape analysis present: the compute-shell model (why NICs/disks/identities are referenced, never created), Linux-vs-Windows surface comparison, image-source trichotomy, spot contract, placement-vs-failure analysis, trusted-launch vs confidential-VM
- Best practices woven throughout; explicit `custom_data` vs `user_data` leak-prevention section
- 90/10 decisions documented (the one deliberate inline exception — the OS disk — and the plain-ARM-ID stances for availability sets, PPGs, dedicated hosts, and scale sets)
- "The Planton Approach" section covers the deployment model and defaults

**Score:** 13.18% / 13.18%

---

### 7. Documentation - User-Facing (13.09%)

✅ **Passed:**

- `README.md` exists at v1/ level (4.6 KB — good; overview, key features, prerequisites, stack outputs, related resources)
- `catalog-page.md` exists (17.6 KB)

**Score:** 13.09% / 13.09%

---

### 8. Supporting Files (10.09%)

✅ **Passed:**

- `iac/pulumi/README.md` exists (5.1 KB)
- `iac/tf/README.md` exists (7.5 KB)
- `iac/hack/manifest.yaml` exists and is valid (correct apiVersion/kind, literal `resourceGroup.value`, SSH-key Linux VM with data-disk attachment, trusted launch, boot diagnostics — and now the platform-patching seam: `patchMode: LINUX_AUTOMATIC_BY_PLATFORM` + `assessmentMode`/`rebootSetting`, keeping the spec's CEL pairing rule satisfied)

**Score:** 10.09% / 10.09%

---

### 9. Nice to Have (10.00%)

✅ **Passed:**

- `BUILD.bazel` at v1/, `iac/pulumi/`, and `iac/pulumi/module/` levels
- e2e harness present: `e2e/profile.yaml` (tier 1, green, both provisioners validated, 30-minute budget, documented Windows-offline rationale) and `e2e/scenarios/minimal.yaml` (composed NIC-chain + extra-fixture managed disk at LUN 0)

**Score:** 10.00% / 10.00%

---

### 10. Presets (5.00%)

#### 10.1 Directory and File Checks (2.50%)

✅ **Passed:**

- `presets/` directory exists with 3 preset pairs: `01-ubuntu-ssh`, `02-windows-server`, `03-spot-data-disk`
- Every `.yaml` has a companion `.md`; no orphans
- Naming follows `{NN}-{kebab-case}.yaml`; no duplicate ranks

**Score:** 2.50% / 2.50%

#### 10.2 Content Correctness (2.50%)

✅ **Passed:**

- `apiVersion: azure.planton.dev/v1alpha1` and `kind: AzureVirtualMachine` match the `api.proto` constants in all three presets
- All `metadata.name` values are `my-` prefixed (`my-ubuntu-vm`, `my-windows-vm`, `my-spot-vm`)
- `spec` present and non-empty; no `status` blocks
- `StringValueOrRef` fields use wrapper forms throughout: `resourceGroup` uses `value:`, and the FK seams (`networkInterfaceIds`, `managedDiskId`, the Windows `adminPassword`) deliberately use `valueFrom:` — the reference form is the intended pattern for first-class-resource seams, and the password-as-secret-reference is exactly the secure-by-default posture ("never a literal password in a manifest")
- All field names exist in the current `spec.proto`; enum values (`READ_WRITE`, `PREMIUM_LRS`, `DEALLOCATE`, `SYSTEM_ASSIGNED`, `WINDOWS_SERVER`) are valid
- Preset 02 respects the Windows 15-character computer-name limit (`win-vm`); preset 03 pairs spot with `DEALLOCATE` + max-drain termination notification

❌ **Failed:**

- None

**Score:** 2.50% / 2.50%

**Total Presets Score:** 5.00% / 5.00%

> **Tip:** For a deeper preset audit, run `@audit-planton-presets AzureVirtualMachine`.

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

Ran `go run . secret-coverage --output json` from the repo root and filtered `gaps[]` to `AzureVirtualMachine:` — **zero gaps** (11 gaps exist repo-wide; none for this kind). `secret-coverage --check` passes with no `sensitive`/`sensitive_exempt_reason` contradiction.

| Field path | Classification | Note |
|-----------|----------------|------|
| `spec.custom_data` | covered | `(sensitive) = true` — may embed bootstrap secrets, delivered once at first boot |
| `spec.os_profile.linux.admin_password` | covered | `(sensitive) = true` on the `StringValueOrRef` — literal or secret reference |
| `spec.os_profile.windows.admin_password` | covered | `(sensitive) = true` |
| `spec.os_profile.windows.additional_unattend_contents[].content` | covered | `(sensitive) = true` — AutoLogon fragments carry the admin password |
| `spec.user_data` | correctly NOT sensitive | IMDS-readable and updatable; the proto comment explicitly warns "never put secrets here" |
| `spec.os_profile.linux.ssh_public_keys[].public_key` | correctly NOT sensitive | public material by definition |

**Effect on score:** none.

---

## Prioritized Recommendations

### High Priority (Do First)

- None — the component is production-ready.

### Medium Priority (Do Next)

- None.

### Low Priority (Polish)

- Optionally clean the git-ignored local dev artifacts (`iac/tf/.terraform/`, `terraform.tfplan`, `backend.tf`, `.terraform.lock.hcl`, `iac/pulumi/stack-input.yaml`) left by today's E2E and plan runs.

---

## Reference to Forge Rules

No forge rules need to be run — all artifacts exist and pass their guards. For any future parity work on this component, `@fix-planton-component` remains the remediation path, re-verified with `planton validate-outputs`, the `AzureVirtualMachine` case in `pkg/outputs/conformance_test.go`, and a `tofu plan` against the patch-seam-bearing `iac/hack/manifest.yaml`.

---

## Comparison to Complete Components

**Most Similar Complete Component:** `AzureManagedDisk` (same family sweep, same structure: prerequisite-declaring registry entry, curated `optional()` variables.tf, documented family-wide tag-shape PARITY-EXCEPTION, conformance case, e2e harness, 3 preset pairs — audited today at 100%).

**What it has that this component lacks:** Nothing structural — the two are at the same completeness bar.

**Path to reference:** `catalog/azure/azuremanageddisk/v1alpha1/`

---

## Next Steps

1. Record PARITY ✅ for `AzureVirtualMachine` in the parity ledger (documented tag-shape exception noted; patch-seam divergence remediated in-session).
2. Proceed to the next unaudited component in the Azure family sweep.

---

## Appendix: Full Checklist

### Critical Items (48.64%)

#### Cloud Resource Registry (4.44%)

- [x] Enum entry exists (`AzureVirtualMachine = 408`)
- [x] Enum value in correct range (Azure: 400-599)
- [x] Unique id_prefix (`azvm`)
- [x] Complete kind_meta (provider, version, id_prefix, prerequisites)
- [x] Kubernetes metadata — n/a (non-Kubernetes component)

#### Folder Structure (4.44%)

- [x] Correct provider hierarchy
- [x] Lowercase enum name
- [x] `v1/` subfolder

#### Protobuf API Definitions (22.20%)

- [x] `api.proto` (997 B), `spec.proto` (44.7 KB), `stack_input.proto` (431 B), `stack_outputs.proto` (1.4 KB)
- [x] All four `.pb.go` stubs present
- [x] `spec_test.go` present (15.2 KB), Ginkgo/Gomega, protovalidate
- [x] Tests pass fresh (`go test -count=1`)

#### Validation Message Quality (part of Proto Files score)

- [x] Every CEL `message` is a complete, actionable sentence
- [x] No raw regex in user-facing messages
- [x] Conditional rules explain both condition and requirement
- [x] No messages shorter than 5 words
- [x] Messages use semantic field names

#### Module Comment Quality (part of Categories 4 & 5 module score)

- [x] Both modules explain why / trade-offs / quirks / lifecycle, not narration
- [x] Comment density matches the `spec.proto` field-comment standard
- [x] No uncommented or narration-only module

#### Cross-Engine IaC Parity (GATE on Categories 4 & 5)

- [x] Scope source matches (resolved `resource_group` literal on both engines)
- [x] Resource naming basis matches (`spec.name`; attachments keyed by LUN)
- [x] Labels/tags: documented `PARITY-EXCEPTION:` in BOTH modules, output-neutral
- [x] No pod/service selectors — n/a (no Kubernetes workload)
- [x] Every `spec.proto` field honored by BOTH engines (incl. the remediated patch-orchestration seam, proven via live `tofu plan`)
- [x] Dependency ordering matches (VM → attachment on both)
- [x] Stack outputs flatten onto the same proto fields (`validate-outputs`: 7/7, zero unmapped; conformance case passes)
- [x] Divergence carries `PARITY-EXCEPTION:` in BOTH modules (tag shape only)

#### Secure-by-Default Sensitive Coverage (GATE on the whole component)

- [x] `secret-coverage --output json`: zero `gaps[]` for AzureVirtualMachine
- [x] `secret-coverage --check`: no contradiction
- [x] Every secret-bearing field annotated `(sensitive) = true` (custom_data, both admin_passwords, unattend content)
- [x] Look-alikes correctly unannotated without needing exemptions (public SSH keys, user_data, encryption-set ARM IDs)

#### IaC Modules - Pulumi (13.32%)

- [x] `module/main.go`, `module/locals.go`, `module/outputs.go`, `module/vm_args.go`
- [x] Root `main.go` with `package main`; no `main/`/`entrypoint/` subdir
- [x] `Pulumi.yaml`, `Makefile`
- [x] Release-equivalent build passes

#### IaC Modules - Terraform (4.24%)

- [x] `variables.tf` (7.7 KB), `provider.tf` (canonical empty block), `locals.tf` (5.6 KB, full-proto-enum-name comparisons), `main.tf` (17.0 KB), `outputs.tf` (1.8 KB)

### Important Items (41.36%)

#### Documentation - Research (13.18%)

- [x] `docs/README.md` (21.1 KB, comprehensive)
- [x] Landscape analysis, best practices, 90/10 decisions, deployment model

#### Documentation - User-Facing (13.09%)

- [x] `README.md` (4.6 KB), `catalog-page.md` (17.6 KB)

#### Supporting Files (10.09%)

- [x] `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` (now exercises the patch seam)

#### Presets (5.00%)

- [x] `presets/` directory exists
- [x] 3 preset YAML + MD pairs
- [x] Naming convention correct; no duplicate ranks
- [x] `apiVersion` and `kind` match `api.proto`
- [x] `StringValueOrRef` fields use wrapper forms (`value:` / `valueFrom:`)
- [x] All field names exist in current `spec.proto`
- [x] Companion `.md` files present

### Nice to Have (10%)

- [x] BUILD.bazel files present (v1/, iac/pulumi/, iac/pulumi/module/)
- [x] e2e harness (`profile.yaml`, `scenarios/minimal.yaml`; live dual-engine run green today)

---

**Audit completed at:** 2026-07-04 09:34:15  
**Reference:** See `architecture/deployment-component.md` for ideal state definition
