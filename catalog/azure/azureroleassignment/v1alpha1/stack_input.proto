syntax = "proto3";

package dev.planton.azure.azureroleassignment.v1alpha1;

import "catalog/azure/azureroleassignment/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureRoleAssignmentStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureRoleAssignmentStackInput {
  // target cloud-resource to be deployed
  AzureRoleAssignment target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
