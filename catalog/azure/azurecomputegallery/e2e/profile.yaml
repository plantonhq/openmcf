apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurecomputegallery
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (a private
  # gallery -- free at rest; the cloud name carries the underscore
  # run-id token because gallery names forbid dashes). The
  # Community-sharing arm is offline-proven with reason: it publishes
  # a PUBLIC community gallery under a generated name, which a test
  # lane cannot honestly stand up and tear down as a private artifact;
  # the canonical manifest exercises the whole sharing tree through
  # the offline plan and the CELs carry the provider's expand-time
  # contract. The kind also publishes e2e/prerequisite.yaml (a
  # stable-named fixture gallery) for the image kind's chain.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
