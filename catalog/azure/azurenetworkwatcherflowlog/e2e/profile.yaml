apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurenetworkwatcherflowlog
spec:
  # PROVEN live on both engines (proof session, 2026-08-20/22): Pulumi
  # 8m37s (create 36s incl. live Traffic Analytics); Terraform 7m56s
  # (create 32s) incl. a blind import round-trip (15s) with a
  # no-differences re-plan -- the flow log imported by its
  # watcher-child ID (.../networkWatchers/NetworkWatcher_eastus/
  # flowLogs/{name}), zero tolerances consumed, proving the
  # auto-created-watcher addressing end to end (the module locals
  # resolved the regional singleton the spec never names). Both
  # idempotency re-plans clean. ONE transient caught and recorded: the
  # first Pulumi create polled 12m into a bare ARM `ServiceUnavailable`
  # ("retry the request later", empty error code) and Azure rolled the
  # half-created flow log back cleanly; the identical body created in
  # 36s on retry -- the transient-service-fault class, see the flowLogs
  # entry in e2e/README.md. OFFLINE-ONLY with reasons: subnet and NIC
  # targets (the same flowLogs PUT with a different target ID --
  # plan-verified; unblock: none needed unless a live seam appears);
  # the self-managed-watcher addressing (needs a second watcher, and
  # Azure allows ONE per region per subscription -- structurally
  # unprovable beside the singleton); retention-disabled and paused
  # shapes (property writes on the proven path).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
