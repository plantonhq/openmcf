syntax = "proto3";

package dev.planton.azure.azurenetworkwatcherflowlog.v1alpha1;

import "catalog/azure/azurenetworkwatcherflowlog/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureNetworkWatcherFlowLogStackInput is the input to the IaC modules
// (Pulumi/Terraform). It contains the target resource definition and
// Azure provider credentials.
message AzureNetworkWatcherFlowLogStackInput {
  // The Network Watcher flow log resource to deploy.
  AzureNetworkWatcherFlowLog target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
