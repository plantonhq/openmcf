apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningonlineendpoint
spec:
  # Proven live on both engines: a key-auth system-identity endpoint
  # on the fixture workspace (create a few minutes, free at rest;
  # scoring/swagger URI read-back 5/5). FIRST azapi / azure-native
  # kind: both engines' raw-ARM credential paths held. The Terraform
  # blind import round-trip is clean after the shared azapi catalog
  # tolerances (body, identity computed members, response_export_values).
  # Traffic maps, BYO keys, and private access stay offline-proven
  # (traffic needs deployments -- the deployment kind's territory).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 45
