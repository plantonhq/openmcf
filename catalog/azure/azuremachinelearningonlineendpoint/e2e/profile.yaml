apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremachinelearningonlineendpoint
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The endpoint OBJECT creates in a few minutes and holds
  # no instances -- the smoke lane never pays for a VM; the cost and
  # most of the time live in its chain (the fixture workspace and its
  # companions). The scenario proves the key-auth shape with a system
  # identity and the scoring/swagger URI read-back; the traffic maps,
  # BYO keys, and private access are offline-proven only this wave
  # (traffic needs deployments to route to -- the deployment lane's
  # territory). FIRST raw-API (azapi / azure-native) kind to reach the
  # proof lane: the engines' credential paths on a raw ARM client are
  # themselves a proof point.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
