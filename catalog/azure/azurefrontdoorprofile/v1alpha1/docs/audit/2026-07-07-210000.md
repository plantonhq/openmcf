# Audit Report: AzureFrontDoorProfile

**Audit Date:** 2026-07-07 21:00:00
**Component Kind:** AzureFrontDoorProfile
**Provider:** azure
**Component Path:** `catalog/azure/azurefrontdoorprofile/v1alpha1/`
**Enum Value:** 480
**ID Prefix:** azfd

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureFrontDoorProfile = 480` opens the CDN/Front Door
  family; `container_kind: true` (the delivery family nests under it)
- `prerequisites: [AzureResourceGroup]` with a rationale comment
- `id_prefix: "azfd"` unique across the registry; crkreflect kind map
  regenerated

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:** correct provider hierarchy, lowercase folder, `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- All four protos present; generated stubs current
- Full azurerm v4.80 surface: name/sku (closed enum, unspecified
  deploys STANDARD), response timeout, identity (system/user-assigned
  with UAI FKs and the ids-match-type CEL), log scrubbing (repeated
  enum, max 3, unique -- the block's only configurable field), user tags
- The bundled endpoints/origin_groups/routes lists are DISSOLVED into
  first-class kinds (481-484); fields renumbered contiguously
- `spec_test.go`: 29 cases (10 valid-path, 19 error-path) -- all PASS

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- Single `cdn.FrontdoorProfile` resource; authoring comments explain the
  container decomposition, presence-guards, scrubbing presence
  semantics, and the constant-shape principal-id export
- Migrated off inline `NewProvider` to the shared
  `pulumiazureprovider.Get` builder (keyless-auth compliant)
- Release-equivalent build passes: `go build -o /dev/null .../iac/pulumi`

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All files present; canonical empty `provider "azurerm" { features {} }`
- `planton tofu plan` on the hack manifest renders Premium sku +
  system identity + two scrubbing rules + merged tags

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with the `pkg/outputs` conformance case (4/4 fields
populate, zero unmapped).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.resource_group.GetValue()` | `var.spec.resource_group` | ✅ MATCH |
| Resource naming basis | `spec.profile_name` | `var.spec.profile_name` | ✅ MATCH |
| Sku default materialization | map + STANDARD fallback | `coalesce(sku, "STANDARD")` map | ✅ MATCH |
| Response timeout | sent only when set | null passthrough | ✅ MATCH |
| Identity block | when set; ids from FKs | dynamic block; same CEL contract | ✅ MATCH |
| Log scrubbing | presence-enabled, enum map | presence-enabled, same map | ✅ MATCH |
| Tags | merged, user wins (documented PARITY-EXCEPTION on derived-tag shape, both modules) | same | ✅ MATCH |
| Stack outputs → proto | 4 exports | 4 outputs, same names | ✅ MATCH |

**Effect on score:** none — the tag-shape divergence carries the
family-wide `PARITY-EXCEPTION` comment in BOTH modules and never feeds
outputs.

---

### 6-10. Documentation, Supporting Files, Presets

✅ **Passed:**

- `docs/README.md`: field map, outputs table, behavior notes (global
  resource, one-way sku, slow deletion), recorded skips with reasons
  (decomposition + the deferred custom-domain/WAF kinds)
- `README.md`, `catalog-page.md`, both module READMEs rewritten to the
  container design; hack manifest exercises Premium/identity/scrubbing
- Presets: 3 pairs (standard delivery, premium private origins,
  compliance log scrubbing); all pass `planton validate-manifest`
- E2E: `e2e/prerequisite.yaml` (the family's shared fixture),
  `scenarios/minimal.yaml`, `profile.yaml`; `frontDoorProfileVerifier`
  registered; `TestAzureFrontDoorProfile_*` entrypoints added

**Score:** full marks in each category

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`go run . secret-coverage --check` green; no secret-bearing spec fields
on this kind.

---

## Apply-Time Validator Source-Diff (beyond the schema walk)

Checked `cdn_frontdoor_profile_resource.go` Create/Update/CustomizeDiff:

- **Premium → Standard downgrade rejected** (CustomizeDiff) --
  cross-update contract, not expressible statically; documented on the
  sku field and in docs/README.md.
- **Log scrubbing expansion** hardcodes `EqualsAny` + empty selector --
  matches the repeated-enum modeling exactly (nothing lost).

No unmodeled apply-time contracts found.

## Critical Gaps

None.

## Quick Wins

None outstanding.

---

**Audit completed at:** 2026-07-07-210000
**Reference:** `architecture/deployment-component.md`
