# Audit Report: AzureMonitorMetricAlert

**Date:** 2026-07-10-034017
**Component:** AzureMonitorMetricAlert (enum 454, provider azure, id_prefix `azmalert`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_monitor_metric_alert` surface:
all three condition families (static multi-criteria, singular dynamic,
web-test availability) behind the provider's exactly-one-of contract, the
shared six-value operator vocabulary with per-family direction CELs,
dimension filters, the closed frequency/window duration vocabularies, and
action-group FKs.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 454, unique prefix `azmalert`, `prerequisites: [AzureMonitorActionGroup]` (scopes are polymorphic -- no scope prerequisite) |
| 2 | Folder Structure | ✅ `provider/azure/azuremonitormetricalert/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 21 spec tests pass (every family, both operator-direction error paths, vocabularies); messages user-facing |
| 4 | IaC Pulumi | ✅ module + root entrypoint; release-equivalent build passes; richly commented; shared builder |
| 5 | IaC Terraform | ✅ one dynamic block per condition family; richly commented; canonical empty provider block |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ field map, the shared-operator-enum design, front-loaded vs documented contracts, legacy wire-type skip |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders the DYNAMIC family -- the only place the sensitivity map renders -- plus StartsWith dimensions and webhook properties; the static family is E2E-exercised) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 3 pairs (static-threshold / dynamic-anomaly / webtest-availability), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (no secret-bearing fields; no gaps) |

## Apply-Time Validator Source-Diff

Read `monitor_metric_alert_resource.go` Create/Update and the criteria
expand logic:

- **ExactlyOneOf across the three criteria families** (schema) --
  front-loaded as the exactly-one message CEL.
- **Per-family operator vocabularies** (schema StringInSlice sets) --
  front-loaded as the two direction CELs on the shared enum.
- **Multi-scope requires target_resource_type + target_resource_location**
  -- Azure's apply-time contract keyed on the RESOLVED scope shapes (a
  reference's shape is unknowable statically); documented on the fields
  and in the research doc, not CEL'd.
- **Legacy single-resource criteria wire type** -- the provider keeps
  `MetricAlertSingleResourceMultipleMetricCriteria` for pre-existing rules
  only; new creates always use the multi-resource type on both engines
  (recorded skip -- no spec surface).
- **Dynamic failing-count <= total-count** -- ARM's contract; the provider
  does not validate it statically -- documented on the fields, not
  invented as CEL.
- No spec CEL invents a rule the provider does not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `scopes[]` values both engines)
- Naming basis: MATCH (`spec.alert_name`)
- Labels/tags: EXCEPTION (family-wide tag-shape note, documented in BOTH modules)
- Feature coverage: MATCH -- all three families, dimensions, actions, and
  the target type/location pair wired identically; presence-guarded
  defaults (severity 3, PT1M/PT5M, auto-mitigate true, 4/4 dynamic counts)
  sent explicitly on both engines
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- `pkg/outputs` conformance case added (2 fields)

**PARITY ✅** (one documented exception: the family-wide tag shape).

## Live Evidence

Live dual-engine E2E GREEN: Pulumi 401s / Terraform 408s (all 8 phases;
zero orphans) -- the composed chain RG -> fixture action group +
scenario-local storage account -> static-threshold rule, proving the
polymorphic scope seam, the static wire maps (Total/GreaterThan), and the
action-group FK.

## Critical Gaps

None.
