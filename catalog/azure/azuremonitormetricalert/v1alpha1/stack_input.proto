syntax = "proto3";

package dev.planton.azure.azuremonitormetricalert.v1alpha1;

import "catalog/azure/azuremonitormetricalert/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureMonitorMetricAlertStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureMonitorMetricAlertStackInput {
  // target cloud-resource to be deployed
  AzureMonitorMetricAlert target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
