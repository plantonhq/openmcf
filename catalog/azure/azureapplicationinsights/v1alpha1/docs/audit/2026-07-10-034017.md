# Audit Report: AzureApplicationInsights

**Date:** 2026-07-10-034017
**Component:** AzureApplicationInsights (enum 451, provider azure, id_prefix `azai`)
**Mode:** full audit with `--parity` focus

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

The kind was reworked breaking from an 8-field spec to the full azurerm v4.80
`azurerm_application_insights` surface: the complete 8-value application-type
vocabulary as a closed enum with case-exact wire maps, the privacy/auth/
network posture (IP masking, Entra-only ingestion, AMPLS-only paths), the
daily-cap notification toggle, profiler BYO storage, and user tags. The
workspace binding stays required (classic mode retired). Both engines
rewritten; the Pulumi module migrated to the shared provider builder.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 451, azure band, unique prefix `azai`, `prerequisites: [AzureLogAnalyticsWorkspace]` added |
| 2 | Folder Structure | ✅ `provider/azure/azureapplicationinsights/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 17 spec tests pass; CEL messages user-facing |
| 4 | IaC Pulumi | ✅ module + root entrypoint; release-equivalent build passes; richly commented; shared builder |
| 5 | IaC Terraform | ✅ full variable surface; richly commented; canonical empty provider block |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below; one documented bridge-lag exception) |
| 6 | Research Docs | ✅ field map, deprecated-alias skips, bridge-lag exception, provider behaviors |
| 7 | User README | ✅ rewritten to the reworked surface |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (plan renders the NODE_JS irregular-casing seam + posture flags) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 2 pairs (standard / production-sampled), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (no `AzureApplicationInsights:` gaps; instrumentation key + connection string are OUTPUTS, prose-documented) |

## Apply-Time Validator Source-Diff

Read `application_insights_resource.go` Create/Update and referenced
validators:

- **Positive/negative boolean inversion** -- the API stores DisableIPMasking /
  DisableLocalAuth; the modules map the spec's positive booleans; wire-verified.
- **Billing API split** -- the daily cap + notification toggle apply through a
  separate `ComponentCurrentBillingFeaturesUpdate` call after create/update;
  provider behavior, documented in both modules.
- **Generated smart-detector rule** -- Azure auto-creates "Failure Anomalies";
  the provider disables it by default (deletion resurrects); documented.
- **`workspace_id` immutability-once-set** -- an update-behavior contract,
  documented on the spec field; the spec makes the binding REQUIRED (classic
  retirement), a deliberate strictening recorded in `docs/README.md`.
- No spec CEL invents a rule the provider does not enforce (retention set,
  sampling range, and cap floor are the provider's own validators).

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `resource_group` value both engines)
- Naming basis: MATCH (`spec.application_insights_name` both engines)
- Labels/tags: EXCEPTION (family-wide tag-shape note, documented in BOTH modules)
- Feature coverage: MATCH -- every spec field wired on both engines;
  unspecified application type deploys "web" identically
- **Boolean-form EXCEPTION (documented in BOTH modules)**: pulumi-azure v6.38
  bridges only the deprecated negative-form toggles; the Pulumi module inverts
  the spec's positive booleans. Wire-identical per pair; outputs unaffected.
  Re-alignment trigger recorded (bridge shipping the positive forms).
- Stack outputs: MATCH -- `pkg/outputs` conformance case added (5 fields);
  both engines export identical keys

**PARITY ✅** (two documented exceptions: tag shape, boolean bridge lag).

## Live Evidence

Live dual-engine E2E GREEN through the composed RG -> workspace fixture
chain: Pulumi 239s / Terraform 254s (all 8 phases; zero orphans). Run 1
live-caught the unspecified-enum wire-map defect (an unmapped enum sent the
empty string, which the provider rejects); fixed by mapping the unspecified
row to "web", re-run green.

## Critical Gaps

None.
