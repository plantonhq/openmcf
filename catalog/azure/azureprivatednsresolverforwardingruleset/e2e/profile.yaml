apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureprivatednsresolverforwardingruleset
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green for the two-rule
  # canonical shape); live dual-engine proof has not yet run. ONE
  # scenario runs: `minimal` (one rule to an RFC-5737 documentation
  # target, bound to the fixture resolver's outbound endpoint by
  # reference -- which live-proves the resolver-output wiring edge).
  # The lane's chain deploys the fixture resolver (outbound endpoint
  # hours are the only billing meter -- cents); rulesets and rules are
  # free at rest. This kind's lane never runs in parallel with the
  # resolver or link lanes (the fixture resolver anchors the shared
  # fixture network -- Azure allows one resolver per network).
  # OFFLINE-ONLY with reasons: multi-rule shapes, the disabled-rule
  # arm, per-rule metadata, and non-standard ports (pure ARM property
  # writes on the exact create path the one-rule lane proves --
  # plan-verified with rendered-value inspection); the two-endpoint
  # binding (Azure caps a ruleset at 2 endpoints from the SAME
  # resolver; the fixture carries one -- unblock: a second fixture
  # outbound endpoint, worth it only when a consumer needs the shape).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
