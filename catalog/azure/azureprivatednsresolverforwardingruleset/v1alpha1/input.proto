syntax = "proto3";

package dev.planton.azure.azureprivatednsresolverforwardingruleset.v1alpha1;

import "catalog/azure/azureprivatednsresolverforwardingruleset/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzurePrivateDnsResolverForwardingRulesetStackInput is the input to
// the IaC modules (Pulumi/Terraform). It contains the target resource
// definition and Azure provider credentials.
message AzurePrivateDnsResolverForwardingRulesetStackInput {
  // The DNS forwarding ruleset resource to deploy.
  AzurePrivateDnsResolverForwardingRuleset target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
