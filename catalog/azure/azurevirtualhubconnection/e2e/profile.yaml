apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurevirtualhubconnection
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The connection itself provisions in minutes and is
  # free -- the lane's cost and duration are dominated by its
  # prerequisite chain: the fixture hub's 15-30 minute create (plus
  # 10-15 minute delete) runs INSIDE this lane, alongside the fixture
  # VNet. The timeout budgets the full chain.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 120
