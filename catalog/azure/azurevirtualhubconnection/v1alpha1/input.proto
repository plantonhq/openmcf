syntax = "proto3";

package dev.planton.azure.azurevirtualhubconnection.v1alpha1;

import "catalog/azure/azurevirtualhubconnection/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureVirtualHubConnectionStackInput is the input to the IaC modules
// (Pulumi/Terraform). It contains the target resource definition and
// Azure provider credentials.
message AzureVirtualHubConnectionStackInput {
  // The Virtual Hub Connection resource to deploy.
  AzureVirtualHubConnection target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
