apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureaifoundryproject
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. The project itself is seconds-to-minutes
  # and free; the lane's cost is the fixture-hub chain it deploys
  # (resource group + key vault + storage account + hub -- all free at
  # rest, minutes each). The proof is the cross-kind wiring: the hub
  # reference resolves and ARM returns kind "Project" in the HUB's
  # resource group. The user-assigned identity arm is offline-proven
  # only (tenant-scoped values).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
