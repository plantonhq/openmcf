# Audit Report: AzureServiceBusQueue

**Date:** 2026-07-10-085450
**Component:** AzureServiceBusQueue (enum 472, provider azure, id_prefix `azsbque`)
**Mode:** full audit with `--parity` focus (initial forge audit)

## Summary

**Score: 100% -- Fully Complete. PARITY ✅ COVERAGE ✅**

Forged at the full azurerm v4.80 `azurerm_servicebus_queue` surface:
parent-by-ARM-id addressing, the fixed size ladder as a closed int set,
Premium large messages, the ForceNew trio (partitioning/dedup/sessions),
every lifecycle dial with Azure's defaults preserved, express with its
dedup conflict as CEL, name-referenced forwarding upgraded to
StringValueOrRef seams, and the four-state administrative gate enum.

## Category Results

| # | Category | Result |
|---|----------|--------|
| 1 | Cloud Resource Registry | ✅ enum 472, prefix `azsbque` (deliberately not `azsbq` -- one edit from the storage queue's `azsq`); NO registry prerequisite (globally-unique parent names -- scenario-local fixtures) |
| 2 | Folder Structure | ✅ `provider/azure/azureservicebusqueue/v1/` |
| 3 | Protobuf API Definitions | ✅ 4 protos + stubs; 24 spec tests pass (name regex paths, size ladder, dedup-window pairing, express conflict, gate states); messages user-facing |
| 4 | IaC Pulumi | ✅ shared builder; unspecified-enum row in the status wire map; release-equivalent build passes; richly commented |
| 5 | IaC Terraform | ✅ canonical empty provider block; anchored namespace-name parse regex; richly commented |
| 5b | Cross-Engine Parity | ✅ GATE PASSED (walk below) |
| 6 | Research Docs | ✅ field mapping, the apply-time Premium contracts (cross-resource, documented not invented), recorded skips |
| 7 | User README | ✅ |
| 8 | Supporting Files | ✅ pulumi README, tf README, hack manifest (fully-dialed queue incl. dedup+window, sessions, SEND_DISABLED gate, forwarding) |
| 9 | Build Files | ✅ BUILD.bazel via gazelle |
| 10 | Presets | ✅ 2 pairs (work-queue / session-fifo-queue), all validate |
| 11 | Sensitive Coverage | ✅ GATE PASSED (no secret-bearing fields -- credentials live on the authorization-rule kind by design) |

## Apply-Time Validator Source-Diff

Read `servicebus_queue_resource.go` Create/Update (no CustomizeDiff):

- **Express-on-Premium rejection** -- cross-resource (the tier lives on
  the referenced namespace); documented on the field. The IN-SHAPE
  conflict the provider also implies (express holds messages in memory,
  dedup requires durable MessageId tracking) is front-loaded as the
  express-vs-dedup CEL.
- **Premium partition-layout pairing** (queue partitioning must match the
  namespace's) -- cross-resource; documented on the field.
- **max_message_size Premium-only** -- cross-resource; documented.
- **The 16x partitioned-size read normalization** -- provider Read-side;
  documented in the research doc (plans stay clean).
- **Update convergence polling** -- provider-internal; no module impact.
- No spec CEL invents a rule the provider does not enforce.

## Parity Gate Walk (5b)

- Scope source: MATCH (resolved `namespace_id` both engines)
- Naming basis: MATCH (`queue_name` for the ARM name both engines)
- Labels/tags: N/A -- ARM does not support tags on Service Bus entities
  (noted in both modules)
- Feature coverage: MATCH -- all 18 spec fields wired on both engines;
  `batched_operations_enabled` presence-guarded to true on both; unset
  dials leave Azure defaults on both
- Dependency ordering: MATCH (single resource)
- Stack outputs: MATCH -- 3 outputs; the namespace-name parse uses the
  same anchored contract on both engines (regex vs split-with-guards);
  `pkg/outputs` conformance case added

**PARITY ✅** (zero exceptions).

## Recorded Skips

- Transitional `status` states (Creating/Deleting/Renaming/Unknown) --
  server states, not knobs; the enum models the four settable gates.

## Critical Gaps

None.
