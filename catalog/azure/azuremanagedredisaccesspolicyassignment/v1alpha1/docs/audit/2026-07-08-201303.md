# Audit Report: AzureManagedRedisAccessPolicyAssignment

**Audit Date:** 2026-07-08 20:13:03
**Component Kind:** AzureManagedRedisAccessPolicyAssignment
**Provider:** azure
**Component Path:** `catalog/azure/azuremanagedredisaccesspolicyassignment/v1alpha1/`
**Enum Value:** 512
**ID Prefix:** azmredisgrant

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `AzureManagedRedisAccessPolicyAssignment = 512` exists
  with `kind_meta` in the Managed Redis sub-band (510-519)
- `id_prefix: azmredisgrant` is unique across the registry
- NO registry prerequisite on `AzureManagedRedis` -- deliberate and
  documented in the registry comment (expensive scenario-local
  fixtures; deploy ordering flows from the reference)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- `catalog/azure/azuremanagedredisaccesspolicyassignment/v1alpha1/`
  exists; folder name is the lowercase enum name

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

#### 3.1 Proto Files

✅ **Passed:**

- `spec.proto`: the full
  `azurerm_managed_redis_access_policy_assignment` surface -- the
  instance FK and the principal FK; the header teaches the
  keyless-by-default story, the built-in-"default"-policy constant, the
  assignment-named-after-the-object-ID contract, and the
  principal-vs-client-id trap
- `api.proto`, `stack_input.proto`, `stack_outputs.proto` complete

#### 3.2 Generated Stubs

✅ All four `.pb.go` files generated and current.

#### 3.3 / 3.4 Unit Tests

✅ `spec_test.go` -- 5 cases (minimal grant, identity-principal
reference, cluster kind-output reference; missing-cluster,
missing-object-id error paths);
`go test ./.../azuremanagedredisaccesspolicyassignment/v1/` ALL PASS.

#### Apply-Time Validator Source-Diff (mandatory)

Read in full:
`managed_redis_access_policy_assignment_resource.go` (schema,
Create/Read/Delete -- no Update: every argument ForceNew).

Contracts found OUTSIDE the schema and their disposition:

- **Create body (policy constant):** the provider hardcodes
  `AccessPolicyName: "default"` -- Managed Redis exposes exactly one
  policy today -> a one-value knob is a constant, not configuration;
  recorded with its re-enable trigger (Azure shipping custom policies
  for Managed Redis).
- **Create body (name = object_id):** the provider uses the object ID
  as the assignment name (one grant per identity per database) -> a
  name field would be contradictable redundant state; not modeled,
  documented.
- **`validation.IsUUID` on object_id:** the value may be a reference
  (the UAI principal-id output), so a literal-only UUID CEL on a
  `StringValueOrRef` is not expressible (the protovalidate-java
  constraint) -> documented on the field; the provider validates the
  resolved literal at plan/apply.
- **No invented rules:** none added beyond the provider's contract.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/outputs.go` present;
  comments explain the grant class, the ForceNew-revoke-regrant
  semantics, the principal-vs-client-id trap, and the no-tags rationale
  -- the module-comment bar
- Entrypoint at `iac/pulumi/main.go` root (`package main`),
  `Pulumi.yaml` + `Makefile` present, no subdir
- Release-equivalent build passes:
  `go build -o /dev/null ./.../iac/pulumi`
- Shared provider builder (`pulumiazureprovider.Get`)

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files present; proportionally sized and richly commented for
  a two-field kind; canonical empty provider block with `~> 4.0`
- Full `planton tofu plan` against `iac/hack/manifest.yaml` -- 1 to
  add, both outputs wired

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with the `pkg/outputs` conformance case.

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Instance reference | `spec.managed_redis_id` resolved value | `var.spec.managed_redis_id` | ✅ MATCH |
| Principal | `spec.object_id` resolved value | `var.spec.object_id` | ✅ MATCH |
| Tags | none (ARM does not support tags on assignments) -- documented in both | none | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs | `access_policy_assignment_id`, `access_policy_assignment_name` (= object id) | same | ✅ MATCH |

**Effect on score:** none.

---

### 6-9. Documentation and Supporting Files

✅ `docs/README.md`: field mapping, the grant-class split verdict, the
recorded constants (built-in "default" policy, name = object ID, no
alias -- each with its rationale), and the operational notes (token
connection recipe, the last-grant-lockout warning). ✅ `README.md`,
`catalog-page.md`, both module READMEs, hack manifest. ✅ BUILD.bazel
trees build via Bazel.

---

### 10. Presets (5.00%)

✅ Two ranked pairs (`01-identity-grant`, `02-human-operator`) covering
the kind's decision space (workload identity by reference vs
user/group by literal); both validate; `my-` naming; `valueFrom` forms
correct.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` passes; the kind carries no
secret-bearing fields (an object ID is a principal identifier, not a
credential) and no name-heuristic look-alikes.

---

## Recorded Constants and Skips (with reasons)

- **Access policy = "default"** -- the only policy Managed Redis
  exposes; the provider hardcodes it. Re-enable trigger: Azure shipping
  custom policies for Managed Redis.
- **No assignment name** -- Azure names the assignment after the object
  ID; a field would be contradictable redundant state.
- **No `object_id_alias`** -- a classic-Redis-only convenience absent
  from the Managed Redis assignment API.

---

**Audit completed at:** 2026-07-08 20:13:03
**Reference:** `architecture/deployment-component.md`
