apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredataprotectionresourceguard
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. The guard is a FREE governance object that
  # creates in seconds -- the fastest lane in the family. The smoke
  # carries one exclusion entry to prove the list's wire shape; no
  # vault references the guard in the lane, so create and delete are
  # unconstrained (a guard deletes cleanly even while referenced --
  # the approval requirement simply goes with it).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
