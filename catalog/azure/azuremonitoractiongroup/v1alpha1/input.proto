syntax = "proto3";

package dev.planton.azure.azuremonitoractiongroup.v1alpha1;

import "catalog/azure/azuremonitoractiongroup/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureMonitorActionGroupStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureMonitorActionGroupStackInput {
  // target cloud-resource to be deployed
  AzureMonitorActionGroup target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
