apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredatafactorypipeline
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (a
  # Wait-activity pipeline chained to the factory kind's own smoke
  # instance -- proves the opaque activities_json path and the typed
  # factory reference end to end). The pipeline itself is free and
  # creates in seconds; the lane pays the fixture factory's chain.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
