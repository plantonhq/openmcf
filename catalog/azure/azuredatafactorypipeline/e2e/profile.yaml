apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredatafactorypipeline
spec:
  # PROVEN live on both engines (proof session, 2026-08-13): `minimal`
  # (a Wait-activity pipeline chained to the factory kind's own smoke
  # instance -- the opaque activities_json path and the typed factory
  # reference proven end to end through the registry chain). Pulumi
  # 9m59s (chain 6m51s, deploy 29s), Terraform 9m59s incl. a 21s blind
  # import round-trip with a no-differences re-plan. The pipeline
  # itself is free and creates in seconds; the lane pays the fixture
  # factory's chain.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 45
