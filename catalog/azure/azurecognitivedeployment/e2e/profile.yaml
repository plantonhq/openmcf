apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurecognitivedeployment
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The deployment itself provisions in seconds to minutes
  # and bills per token (no idle cost at GlobalStandard); the chain pays
  # the fixture account's one-to-three-minute cycle. The lane's proof
  # point is the QUOTA BOUNDARY: gpt-4o-mini availability and
  # tokens-per-minute quota on the shared test subscription -- an
  # insufficient-quota rejection is a recorded deferral (unblock: quota
  # request), never a module defect. The ProvisionedManaged arm is
  # offline-proven only (reserved PTU capacity bills continuously --
  # deliberately never deployed by a lane).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
