apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurefabriccapacity
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (an F2 with
  # the shared test account as administrator). NOT free at rest --
  # the capacity bills PER HOUR from create (~$0.36/h at F2
  # pay-as-you-go), so the lane creates-verifies-destroys tightly.
  # Capacity names forbid hyphens AND underscores, so the run-id
  # token cannot apply -- the scenario name is stable; a crashed
  # run's leftover holds the name and the sweep is the remedy (the
  # system-topic class). Watch-list: sponsored-offer/region
  # restrictions on Microsoft.Fabric registration.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
