apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureprivatelinkservice
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The lane composes the fixture load balancer (declared
  # via the scenario's e2e-prerequisites annotation) and the
  # policies-disabled subnet fixture; the service itself provisions in
  # ~1-2 minutes.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
