apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurevpnserverconfiguration
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The configuration is free and provisions in seconds --
  # only the fixture resource group rides its chain. This kind is also a
  # fixture of the point-to-site pair: the gateway lane chains through
  # this kind's install profile. The scenario proves the certificate arm
  # and the composed policy-group child; the AAD and RADIUS arms are
  # offline-proven only (no tenant-independent AAD values exist, and no
  # RADIUS server exists behind the test subscription).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
