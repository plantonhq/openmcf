syntax = "proto3";

package dev.planton.azure.azuredataprotectionbackuppolicy.v1alpha1;

import "catalog/azure/azuredataprotectionbackuppolicy/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureDataProtectionBackupPolicyStackInput is the input to the IaC
// modules (Pulumi/Terraform). It contains the target resource
// definition and Azure provider credentials.
message AzureDataProtectionBackupPolicyStackInput {
  // The Data Protection backup policy resource to deploy.
  AzureDataProtectionBackupPolicy target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
