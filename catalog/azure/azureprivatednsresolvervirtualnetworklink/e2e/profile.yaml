apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azureprivatednsresolvervirtualnetworklink
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. ONE scenario runs: `minimal` (links the
  # fixture ruleset to the fixture network -- the hub-link pattern;
  # the lane's chain deploys fixture resolver -> fixture ruleset, so
  # this lane live-proves the family's full three-level reference
  # chain). Links are free at rest; the chained fixture resolver's
  # outbound endpoint hours are the only billing meter (cents). This
  # kind's lane never runs in parallel with the resolver or ruleset
  # lanes (the chained fixture resolver anchors the shared fixture
  # network -- Azure allows one resolver per network). OFFLINE-ONLY
  # with reasons: the metadata-update arm (the link's one in-place
  # surface -- a property write on the object the lane creates);
  # cross-subscription links (the shared test subscription is single
  # -- unblock: a second subscription in the suite).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 60
