syntax = "proto3";

package dev.planton.azure.azuredataprotectionbackupvault.v1alpha1;

import "catalog/azure/azuredataprotectionbackupvault/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureDataProtectionBackupVaultStackInput is the input to the IaC
// modules (Pulumi/Terraform). It contains the target resource
// definition and Azure provider credentials.
message AzureDataProtectionBackupVaultStackInput {
  // The Data Protection backup vault resource to deploy.
  AzureDataProtectionBackupVault target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
