apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuredataprotectionbackupvault
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plan green); live dual-engine
  # proof has not yet run. The vault is FREE at rest and creates in
  # one to three minutes; deletion runs past the API's early answer
  # (the provider polls until the name frees). The smoke proves the
  # storage settings, soft-delete defaults, and identity live; the
  # one-way doors (Locked immutability, AlwaysOn soft delete, CMK)
  # stay offline-proven BY DESIGN -- each is permanent on a live
  # vault, so their wire shapes are proven at plan time instead of
  # welding a test vault shut.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
