apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azuremonitorautoscalesetting
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, offline tofu plans green for the deep canonical
  # shape -- three profiles, dimensions, predictive, both notification
  # channels -- with rendered-value inspection); live dual-engine proof
  # has not yet run. ONE scenario runs: `minimal` (default profile +
  # CPU rule + weekend recurrence + email notification on a
  # scenario-local Standard S1 plan -- the shared fixture plan is
  # Basic, which autoscale rejects). The setting object is free; the
  # S1 target bills pro-rated cents for the lane's minutes.
  # OFFLINE-ONLY with reasons: fixed-date profiles, predictive
  # autoscale (VM Scale Set targets only -- no VMSS in this lane), the
  # remaining statistic/aggregation/operator/action-type vocabulary
  # values, dimensions, and webhook notifications are pure ARM property
  # writes on the exact CreateOrUpdate body the minimal lane proves
  # (plan-verified with rendered-value inspection).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
