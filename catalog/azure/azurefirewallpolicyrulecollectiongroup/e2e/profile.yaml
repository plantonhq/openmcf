apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurefirewallpolicyrulecollectiongroup
spec:
  # Composed chain: fixture resource group -> fixture STANDARD firewall
  # policy (registry prerequisite) -> the group under test carrying all
  # THREE collection types (application, network, DNAT) with an
  # AzureIpGroup extra fixture proving the source_ip_groups FK seam live.
  # Verified via the ARM API (generic GetByID on the group's nested ARM
  # id), destroyed, verified gone -- on both provisioners. Control-plane
  # documents throughout; the lane runs in minutes.
  tier: 1
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 25
