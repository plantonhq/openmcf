apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurebastionhost
spec:
  # PROVEN live on both engines (proof session, 2026-08-20): Pulumi
  # 41m26s (create 13m33s, destroy 6m16s); Terraform 33m13s (create
  # 9m59s, destroy 8m41s) incl. a blind import round-trip (12s) with a
  # no-differences re-plan -- the host imported by its ARM ID, zero
  # tolerances consumed. Both idempotency re-plans clean. The `minimal`
  # scenario proves the BASIC dedicated-infrastructure mainline on the
  # fixture AzureBastionSubnet + the lane-owned exclusive public IP
  # (both fixture docs' first live exercise); VERIFY-OUT reads 3/4
  # proto fields -- private_only_enabled is legitimately FALSE on the
  # public BASIC shape (raw outputs 4/4). Measured timing brackets the
  # ~10-minute class: budget creates 10-14m and deletes 6-9m.
  # OFFLINE-ONLY with reasons: the DEVELOPER shape (shared
  # infrastructure in a LIMITED region set -- a live lane would pin the
  # suite to Developer-enabled regions; unblock: a region-matrix lane
  # class); the Standard/Premium feature knobs and zones (pure ARM
  # property writes on the exact code path the Basic lane proves --
  # plan-verified with rendered-value inspection); Premium private-only
  # (omitting the public IP flips one ARM property; same create path).
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 60
