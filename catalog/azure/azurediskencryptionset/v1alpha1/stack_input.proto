syntax = "proto3";

package dev.planton.azure.azurediskencryptionset.v1alpha1;

import "catalog/azure/azurediskencryptionset/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureDiskEncryptionSetStackInput is the input to the IaC modules
// (Pulumi/Terraform). It contains the target resource definition and Azure
// provider credentials.
message AzureDiskEncryptionSetStackInput {
  // The disk encryption set resource to deploy.
  AzureDiskEncryptionSet target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
