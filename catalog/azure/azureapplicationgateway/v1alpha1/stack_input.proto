syntax = "proto3";

package dev.planton.azure.azureapplicationgateway.v1alpha1;

import "catalog/azure/azureapplicationgateway/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureApplicationGatewayStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message AzureApplicationGatewayStackInput {
  // target cloud-resource to be deployed
  AzureApplicationGateway target = 1;
  // provider-config for Azure authentication
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
