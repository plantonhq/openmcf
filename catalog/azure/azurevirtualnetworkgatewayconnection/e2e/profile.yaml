apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurevirtualnetworkgatewayconnection
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The connection object itself provisions in seconds, but
  # its prerequisite chain deploys (and tears down) the fixture VPN
  # gateway -- a 25-45 minute create plus 10-20 minute delete INSIDE this
  # lane -- so the timeout budgets the full chain. The fixture site's
  # endpoint has no real device: the tunnel provisions but never reaches
  # Connected, and the verifier asserts ARM state, never tunnel-up.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 90
