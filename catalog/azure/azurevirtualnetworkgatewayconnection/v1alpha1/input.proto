syntax = "proto3";

package dev.planton.azure.azurevirtualnetworkgatewayconnection.v1alpha1;

import "catalog/azure/azurevirtualnetworkgatewayconnection/v1alpha1/api.proto";
import "catalog/azure/provider.proto";

// AzureVirtualNetworkGatewayConnectionStackInput is the input to the IaC
// modules (Pulumi/Terraform). It contains the target resource definition
// and Azure provider credentials.
message AzureVirtualNetworkGatewayConnectionStackInput {
  // The gateway connection resource to deploy.
  AzureVirtualNetworkGatewayConnection target = 1;

  // Azure provider configuration (credentials).
  dev.planton.azure.AzureProviderConfig provider_config = 2;
}
