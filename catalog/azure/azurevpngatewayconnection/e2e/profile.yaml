apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurevpngatewayconnection
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. The connection itself is free and provisions in
  # minutes, but its prerequisite chain pays the WHOLE VPN family:
  # fixture WAN -> fixture hub (15-30 min create) -> fixture gateway
  # (30-45 min create, bills while alive) plus the fixture site -- the
  # family's longest lane. The tunnel never reaches Connected (no real
  # branch device exists behind the fixture site) -- the verifier
  # asserts ARM state, never tunnel-up.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 180
