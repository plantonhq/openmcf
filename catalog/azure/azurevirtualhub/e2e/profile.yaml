apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: azurevirtualhub
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, offline tofu plan green); live dual-engine proof has
  # not yet run. A NEW slow class for the suite: ARM takes 15-30 minutes
  # to bring the hub's managed router to a Provisioned routing state,
  # and the hub bills (~$0.25/hr class) from creation -- the timeout
  # leaves headroom for a slow region day plus the composed children
  # (route table, route map) and teardown. The hub is also the fixture
  # pivot of the Virtual WAN family: connection and gateway lanes chain
  # through this kind's install profile.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 90
