syntax = "proto3";

package dev.planton.openstack.openstacknetworkport.v1alpha1;

import "catalog/openstack/openstacknetworkport/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackNetworkPortStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackNetworkPortStackInput {
  // target cloud-resource to be deployed
  OpenStackNetworkPort target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
