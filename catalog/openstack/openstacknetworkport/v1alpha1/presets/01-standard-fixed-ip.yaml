apiVersion: openstack.planton.dev/v1alpha1
kind: OpenStackNetworkPort
metadata:
  name: my-network-port
spec:
  networkId:
    value: <network-id>
  fixedIps:
    - subnetId:
        value: <subnet-id>
