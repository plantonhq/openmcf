syntax = "proto3";

package dev.planton.openstack.openstackapplicationcredential.v1alpha1;

import "catalog/openstack/openstackapplicationcredential/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackApplicationCredentialStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackApplicationCredentialStackInput {
  // target cloud-resource to be deployed
  OpenStackApplicationCredential target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
