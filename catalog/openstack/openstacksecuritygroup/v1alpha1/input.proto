syntax = "proto3";

package dev.planton.openstack.openstacksecuritygroup.v1alpha1;

import "catalog/openstack/openstacksecuritygroup/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackSecurityGroupStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackSecurityGroupStackInput {
  // target cloud-resource to be deployed
  OpenStackSecurityGroup target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
