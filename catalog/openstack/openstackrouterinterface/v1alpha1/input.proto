syntax = "proto3";

package dev.planton.openstack.openstackrouterinterface.v1alpha1;

import "catalog/openstack/openstackrouterinterface/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackRouterInterfaceStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackRouterInterfaceStackInput {
  // target cloud-resource to be deployed
  OpenStackRouterInterface target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
