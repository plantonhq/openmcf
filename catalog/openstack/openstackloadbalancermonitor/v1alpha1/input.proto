syntax = "proto3";

package dev.planton.openstack.openstackloadbalancermonitor.v1alpha1;

import "catalog/openstack/openstackloadbalancermonitor/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackLoadBalancerMonitorStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackLoadBalancerMonitorStackInput {
  // target cloud-resource to be deployed
  OpenStackLoadBalancerMonitor target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
