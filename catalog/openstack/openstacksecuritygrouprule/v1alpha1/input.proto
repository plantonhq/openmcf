syntax = "proto3";

package dev.planton.openstack.openstacksecuritygrouprule.v1alpha1;

import "catalog/openstack/openstacksecuritygrouprule/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackSecurityGroupRuleStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackSecurityGroupRuleStackInput {
  // target cloud-resource to be deployed
  OpenStackSecurityGroupRule target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
