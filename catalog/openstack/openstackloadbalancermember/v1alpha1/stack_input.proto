syntax = "proto3";

package dev.planton.openstack.openstackloadbalancermember.v1alpha1;

import "catalog/openstack/openstackloadbalancermember/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackLoadBalancerMemberStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackLoadBalancerMemberStackInput {
  // target cloud-resource to be deployed
  OpenStackLoadBalancerMember target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
