syntax = "proto3";

package dev.planton.openstack.openstackservergroup.v1alpha1;

import "catalog/openstack/openstackservergroup/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackServerGroupStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackServerGroupStackInput {
  // target cloud-resource to be deployed
  OpenStackServerGroup target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
