syntax = "proto3";

package dev.planton.openstack.openstackroleassignment.v1alpha1;

import "catalog/openstack/openstackroleassignment/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackRoleAssignmentStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackRoleAssignmentStackInput {
  // target cloud-resource to be deployed
  OpenStackRoleAssignment target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
