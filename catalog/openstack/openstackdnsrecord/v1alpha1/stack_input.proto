syntax = "proto3";

package dev.planton.openstack.openstackdnsrecord.v1alpha1;

import "catalog/openstack/openstackdnsrecord/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackDnsRecordStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackDnsRecordStackInput {
  // target cloud-resource to be deployed
  OpenStackDnsRecord target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
