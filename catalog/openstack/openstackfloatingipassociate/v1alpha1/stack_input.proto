syntax = "proto3";

package dev.planton.openstack.openstackfloatingipassociate.v1alpha1;

import "catalog/openstack/openstackfloatingipassociate/v1alpha1/api.proto";
import "catalog/openstack/provider.proto";

// OpenStackFloatingIpAssociateStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackFloatingIpAssociateStackInput {
  // target cloud-resource to be deployed
  OpenStackFloatingIpAssociate target = 1;
  // provider-config for OpenStack authentication
  dev.planton.openstack.OpenStackProviderConfig provider_config = 2;
}
