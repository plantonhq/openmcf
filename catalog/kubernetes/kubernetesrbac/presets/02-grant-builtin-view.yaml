apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesRbac
metadata:
  name: team-view
spec:
  namespaceScope:
    namespace:
      value: "<your-namespace>"
  existingRole:
    name: view
    isClusterRole: true
  subjects:
    - group: "<your-group>"
