syntax = "proto3";

package dev.planton.kubernetes.kubernetesclusterissuer.v1alpha1;

import "catalog/kubernetes/kubernetesclusterissuer/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

// Input for the KubernetesClusterIssuer IaC stack.
message KubernetesClusterIssuerStackInput {
  // Target cloud-resource.
  KubernetesClusterIssuer target = 1;
  // Kubernetes provider configuration (cluster credentials).
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
