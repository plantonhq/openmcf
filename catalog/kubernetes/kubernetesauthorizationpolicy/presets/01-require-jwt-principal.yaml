apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesAuthorizationPolicy
metadata:
  name: require-jwt
spec:
  namespace:
    value: <namespace>
  action: ALLOW
  rules:
    - from:
        - source:
            request_principals:
              - "*"
