apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesServiceAccount
metadata:
  name: private-registry-identity
spec:
  name: private-registry-identity
  namespace:
    value: "<your-namespace>"
  imagePullSecrets:
    - value: my-registry-cred
  automountServiceAccountToken: false
