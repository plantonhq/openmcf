apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesServiceAccount
metadata:
  name: gcp-app-identity
spec:
  name: gcp-app-identity
  namespace:
    value: "<your-namespace>"
  workloadIdentity:
    gke:
      serviceAccountEmail:
        value: "<your-gcp-service-account-email>"
