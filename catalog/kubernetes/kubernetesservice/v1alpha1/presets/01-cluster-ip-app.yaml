apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesService
metadata:
  name: my-app
spec:
  name: my-app
  namespace:
    value: "<your-namespace>"
  type: cluster_ip
  selector:
    app: "<your-app-name>"
  ports:
    - name: http
      port: 80
      target_port: "8080"
