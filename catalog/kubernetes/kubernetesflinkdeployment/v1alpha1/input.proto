syntax = "proto3";

package dev.planton.kubernetes.kubernetesflinkdeployment.v1alpha1;

import "catalog/kubernetes/kubernetesflinkdeployment/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

//flink-deployment-kubernetes stack-input
message KubernetesFlinkDeploymentStackInput {
  //target cloud-resource
  KubernetesFlinkDeployment target = 1;
  //provider-config
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
