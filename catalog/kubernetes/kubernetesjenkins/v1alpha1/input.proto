syntax = "proto3";

package dev.planton.kubernetes.kubernetesjenkins.v1alpha1;

import "catalog/kubernetes/kubernetesjenkins/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

//jenkins-kubernetes stack-input
message KubernetesJenkinsStackInput {
  //target cloud-resource
  KubernetesJenkins target = 1;
  //provider-config
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
