apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesOtelCollector
metadata:
  name: cluster-logs
spec:
  namespace:
    value: observability
  mode: daemonset
  configYaml: |
    receivers:
      filelog:
        include:
          - /var/log/pods/*/*/*.log
        operators:
          - type: container
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318
    processors:
      k8sattributes: {}
      memory_limiter:
        check_interval: 1s
        limit_mib: 400
        spike_limit_mib: 100
      batch: {}
    exporters:
      otlphttp:
        # Replace the host with your KubernetesLoki gateway service —
        # Loki ingests OTLP at the gateway's /otlp route.
        endpoint: http://loki-gateway.observability.svc.cluster.local:80/otlp
    service:
      pipelines:
        logs:
          receivers: [filelog, otlp]
          processors: [memory_limiter, k8sattributes, batch]
          exporters: [otlphttp]
  serviceAccount: otel-logs-collector
  volumes:
    - name: varlogpods
      mountPath: /var/log/pods
      readOnly: true
      hostPath:
        path: /var/log/pods
        type: Directory
    - name: checkpoints
      mountPath: /var/lib/otelcol-checkpoints
      hostPath:
        path: /var/lib/otelcol-checkpoints
        type: DirectoryOrCreate
  scheduling:
    tolerations:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
  # Container runtimes write pod log files readable only by root — the
  # default collector image runs non-root and the filelog receiver
  # would report permission errors instead of shipping logs.
  podSecurityContext:
    runAsUser: 0
