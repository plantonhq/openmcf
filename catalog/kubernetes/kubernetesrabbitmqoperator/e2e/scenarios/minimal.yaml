# The release manifest's own defaults: the operator watching ALL
# namespaces, pinned images, manifest-default sizing. Doubles as the
# kind's prerequisite install profile for the KubernetesRabbitMq lanes.
# cert-manager auto-installs first (a registry prerequisite of this kind
# — the admission webhooks' serving certificate is a cert-manager
# Certificate).
apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesRabbitMqOperator
metadata:
  name: e2e-rmqop-min
spec: {}
