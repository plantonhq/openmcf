syntax = "proto3";

package dev.planton.kubernetes.kubernetesargocd.v1alpha1;

import "catalog/kubernetes/kubernetesargocd/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

//input for argocd-kubernetes stack
message KubernetesArgocdStackInput {
  //target cloud-resource
  KubernetesArgocd target = 1;
  //provider-config
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
