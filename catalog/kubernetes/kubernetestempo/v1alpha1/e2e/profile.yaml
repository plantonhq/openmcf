apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetestempo
spec:
  tier: 2
  # Forged onto the official monolithic tempo chart 2.2.3. The lanes prove
  # the OTLP-ingest→trace-by-ID round-trip and the trace-survives-pod-loss
  # durability proof.
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 25
