syntax = "proto3";

package dev.planton.kubernetes.kubernetestempo.v1alpha1;

import "buf/validate/validate.proto";
import "catalog/kubernetes/kubernetestempo/v1alpha1/spec.proto";
import "catalog/kubernetes/kubernetestempo/v1alpha1/stack_outputs.proto";
import "shared/metadata.proto";

//tempo-kubernetes
message KubernetesTempo {
  //api-version
  string api_version = 1 [(buf.validate.field).string.const = 'kubernetes.planton.dev/v1alpha1'];

  //resource-kind
  string kind = 2 [(buf.validate.field).string.const = 'KubernetesTempo'];

  //metadata
  dev.planton.shared.CloudResourceMetadata metadata = 3 [(buf.validate.field).required = true];

  //spec
  KubernetesTempoSpec spec = 4 [(buf.validate.field).required = true];

  //status
  KubernetesTempoStatus status = 5;
}

//tempo-kubernetes status.
message KubernetesTempoStatus {
  //stack-outputs
  KubernetesTempoStackOutputs outputs = 1;
}
