syntax = "proto3";

package dev.planton.kubernetes.kubernetesistiobasecrds.v1alpha1;

import "catalog/kubernetes/kubernetesistiobasecrds/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

// KubernetesIstioBaseCrdsStackInput provides the inputs for installing the Istio CRDs
// on a Kubernetes cluster.
message KubernetesIstioBaseCrdsStackInput {
  // target cloud-resource
  KubernetesIstioBaseCrds target = 1;
  // provider-config
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
