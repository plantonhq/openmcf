syntax = "proto3";

package dev.planton.kubernetes.kubernetesgatewayclass.v1alpha1;

import "catalog/kubernetes/kubernetesgatewayclass/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

// Input for the KubernetesGatewayClass IaC stack.
message KubernetesGatewayClassStackInput {
  // Target cloud-resource.
  KubernetesGatewayClass target = 1;
  // Kubernetes provider configuration (cluster credentials).
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
