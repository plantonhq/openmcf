syntax = "proto3";

package dev.planton.kubernetes.kubernetessuperset.v1alpha1;

import "catalog/kubernetes/kubernetessuperset/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

//superset-kubernetes stack-input
message KubernetesSupersetStackInput {
  //target cloud-resource
  KubernetesSuperset target = 1;
  //provider-config
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
