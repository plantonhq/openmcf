# Kubernetes Kafka User

Declares one Kafka client identity on the Strimzi KafkaUser custom resource. The target cluster's user operator reconciles it: it generates credentials into a Secret named after the user (SCRAM password + ready `sasl.jaas.config`, or a TLS client certificate) and applies the declared ACLs when the cluster runs simple authorization. Credentials are operator-born — no manifest ever carries secret material.

## What Gets Created

- **KafkaUser** — named after the resource, in the Kafka cluster's own namespace, bound to the cluster through the `strimzi.io/cluster` label
- **Credentials Secret** — generated by the cluster's user operator (not by the IaC modules), named after the user: `password` + `sasl.jaas.config` for `scram-sha-512`, `user.crt` / `user.key` (plus keystore forms) for `tls`. NO Secret for `tls-external` — those certificates are issued outside the cluster

## Prerequisites

- A Kafka cluster (**KubernetesKafka**) with its user operator enabled (the default), a listener whose authentication type matches this user's, and `simple` authorization when ACLs should be enforced
- The Strimzi operator watching the cluster's namespace (**KubernetesStrimziKafkaOperator**)

## Quick Start

```yaml
apiVersion: kubernetes.planton.dev/v1alpha1
kind: KubernetesKafkaUser
metadata:
  name: order-service
spec:
  namespace:
    value: kafka # MUST be the Kafka cluster's own namespace
  kafkaCluster:
    value: my-kafka
  authentication:
    type: scram-sha-512
  authorization:
    acls:
      - resource:
          type: topic
          name: order-events
        operations:
          - Write
          - Describe
```

## Stack Outputs

| Output | Description |
|---|---|
| `namespace` | Where the KafkaUser resource lives (the Kafka cluster's namespace) |
| `username` | Kafka principal name (`metadata.name`) |
| `secret_name` | The operator-generated credentials Secret (empty for tls-external users) |

## Next Steps

Mount or env-reference this resource's `secret_name` output in workloads, combined with the KubernetesKafka cluster's `internal_bootstrap_endpoint` output; declare the topics with **KubernetesKafkaTopic**.
