syntax = "proto3";

package dev.planton.kubernetes.kubernetessecretstore.v1alpha1;

import "catalog/kubernetes/kubernetessecretstore/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

//input for kubernetes-secret-store stack
message KubernetesSecretStoreStackInput {
  //target cloud-resource
  KubernetesSecretStore target = 1;
  //provider-config
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
