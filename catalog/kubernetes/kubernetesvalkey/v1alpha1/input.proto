syntax = "proto3";

package dev.planton.kubernetes.kubernetesvalkey.v1alpha1;

import "catalog/kubernetes/kubernetesvalkey/v1alpha1/api.proto";
import "catalog/kubernetes/provider.proto";

//KubernetesValkey stack-input
message KubernetesValkeyStackInput {
  //target cloud-resource
  KubernetesValkey target = 1;
  //provider-config
  dev.planton.kubernetes.KubernetesProviderConfig provider_config = 2;
}
