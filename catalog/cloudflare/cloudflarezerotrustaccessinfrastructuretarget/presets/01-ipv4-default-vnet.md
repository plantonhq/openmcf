---
display_name: IPv4 Default VNet
---

# IPv4 target in the default virtual network

The everyday shape: one server, one private IPv4 address, placed in the account's default virtual network by omission. The hostname is the selection surface infrastructure applications match on -- name it by scheme (environment-role-index), not by whim.
