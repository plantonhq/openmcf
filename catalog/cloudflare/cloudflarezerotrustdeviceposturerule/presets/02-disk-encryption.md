# Disk encryption required

The data-protection baseline: every disk on every desktop platform must be encrypted (`require_all` checks all volumes; use `check_disks` to name specific volumes instead). The single most common posture gate in front of internal applications.
