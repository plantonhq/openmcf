apiVersion: cloudflare.planton.dev/v1alpha1
kind: CloudflareZeroTrustDevicePostureRule
metadata:
  name: disks-encrypted
spec:
  account_id: "0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d"
  name: disks-encrypted
  type: disk_encryption
  description: every disk must be encrypted
  schedule: 10m
  match:
    - platform: windows
    - platform: mac
    - platform: linux
  input:
    require_all: true
