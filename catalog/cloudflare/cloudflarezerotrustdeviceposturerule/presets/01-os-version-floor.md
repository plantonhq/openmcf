# OS version floor

The patch-hygiene shape: macOS devices must run at least the named version, re-checked every five minutes with results trusted for an hour. Clone per platform (the os_version type also reads os_distro_name/os_distro_revision on linux) and reference the rule from Access or Gateway policies.
