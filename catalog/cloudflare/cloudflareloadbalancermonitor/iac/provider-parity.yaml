# Provider-parity manifest: this kind's recorded judgment against the pinned
# cloudflare provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  cloudflare_load_balancer_monitor:
    mappings:
      # The provider's header is one map-of-lists attribute; proto3 maps
      # cannot hold repeated values, so the spec models it as a repeated
      # {name, values} message. Fan-in: one argument realizes both leaves.
      - spec: spec.headers.name
        arg: header
      - spec: spec.headers.values
        arg: header
