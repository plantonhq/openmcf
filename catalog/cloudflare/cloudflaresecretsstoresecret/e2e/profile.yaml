apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: cloudflaresecretsstoresecret
spec:
  # Store secret under live E2E: the secret deploys into the run-scoped
  # store fixture (first live use 2026-08-27, worked as designed; the
  # ONE-store-per-account limit applies to the fixture install too -- see
  # the store profile for the measured slot discipline). The scenario's
  # value is a throwaway run-scoped literal, not a real credential. Live
  # answers to the authoring watch items: (a) the write-only value fired
  # exactly as declared -- the blind round-trip consumed the catalog row's
  # value tolerance (the re-assert is idempotent); (b) the canonical-order
  # scopes CEL held -- the idempotency re-plan measured clean, the
  # provider's documented ordered-list drift never fired.
  tier: 1
  status: green
  # LIVE-PROVEN 2026-08-27: minimal on both engines, full lifecycle, all
  # first-run; the blind import round-trip passed consuming exactly the
  # declared write-only value tolerance.
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 10
