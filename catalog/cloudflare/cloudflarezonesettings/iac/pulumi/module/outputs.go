package module

const (
	// OpZoneId is the exported stack output containing the zone ID the settings
	// belong to (zone settings are a zone singleton -- the zone IS the identity).
	OpZoneId = "zone_id"
)
