syntax = "proto3";

package dev.planton.cloudflare.cloudflarednsrecord.v1alpha1;

import "catalog/cloudflare/cloudflarednsrecord/v1alpha1/api.proto";
import "catalog/cloudflare/provider.proto";

// CloudflareDnsRecordStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message CloudflareDnsRecordStackInput {
  // target cloud-resource to be deployed
  CloudflareDnsRecord target = 1;
  // provider-config for Cloudflare authentication
  dev.planton.cloudflare.CloudflareProviderConfig provider_config = 2;
}
