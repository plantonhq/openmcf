# Office network location

The office shape: the site's egress CIDR allowlisted on the plain-IPv4 endpoint, the other endpoint types declared off (the endpoints tree always carries all four). Point the office resolver/DHCP at the assigned destination IP (the `ip` stack output) and match Gateway policies on this location.
