# Curated audience portal

The audience shape: the same registered servers, curated for one team -- destructive ticketing tools disabled FOR THIS PORTAL (the registration keeps them), a friendlier alias on the resolve tool, and code mode off for a non-engineering audience. Portal overrides only subtract from what the server registration allows.
