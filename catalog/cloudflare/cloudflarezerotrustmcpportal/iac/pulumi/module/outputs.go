package module

const (
	// OpPortalId is the exported stack output containing the portal's
	// identifier (the user-chosen slug).
	OpPortalId = "portal_id"

	// OpHostname is the exported stack output containing the hostname the
	// portal is served on.
	OpHostname = "hostname"
)
