# Logging and branded block page

The everyday shape: blocks logged (not all traffic), PII redacted, and a branded block page users can report false positives from. Everything undeclared (TLS inspection, antivirus, isolation) stays exactly as the dashboard has it -- unset means unmanaged on the settings singleton.
