# OFFLINE-PLAN ARM (never a live scenario from the authoring lane): the two
# PAID toggles. cache_reserve bills storage while on; argo_smart_routing
# bills monthly plus per-GB and KEEPS BILLING after destroy (its provider
# Delete is a no-op). Both arms are plan-proven offline and deferred to a
# budget-approved proof lane -- the deferral reason lives in the profile.
apiVersion: cloudflare.planton.dev/v1alpha1
kind: CloudflareCacheSettings
metadata:
  name: test-cache-paid-arms
spec:
  zone_id:
    value: "0a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d"
  cache_reserve: true
  argo_smart_routing: true
