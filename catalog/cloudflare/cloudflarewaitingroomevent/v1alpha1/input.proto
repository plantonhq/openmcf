syntax = "proto3";

package dev.planton.cloudflare.cloudflarewaitingroomevent.v1alpha1;

import "catalog/cloudflare/cloudflarewaitingroomevent/v1alpha1/api.proto";
import "catalog/cloudflare/provider.proto";

// CloudflareWaitingRoomEventStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message CloudflareWaitingRoomEventStackInput {
  // target cloud-resource to be deployed
  CloudflareWaitingRoomEvent target = 1;
  // provider-config for Cloudflare authentication
  dev.planton.cloudflare.CloudflareProviderConfig provider_config = 2;
}
