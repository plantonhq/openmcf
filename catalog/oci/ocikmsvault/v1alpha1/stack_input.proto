syntax = "proto3";

package dev.planton.oci.ocikmsvault.v1alpha1;

import "catalog/oci/ocikmsvault/v1alpha1/api.proto";
import "catalog/oci/provider.proto";

// OciKmsVaultStackInput is the input to the IaC modules
// (Pulumi / Terraform) that deploy an OciKmsVault resource.
message OciKmsVaultStackInput {
  // target cloud-resource
  OciKmsVault target = 1;
  // provider-config
  dev.planton.oci.OciProviderConfig provider_config = 2;
}
