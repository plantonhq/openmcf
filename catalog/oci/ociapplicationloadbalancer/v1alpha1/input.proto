syntax = "proto3";

package dev.planton.oci.ociapplicationloadbalancer.v1alpha1;

import "catalog/oci/ociapplicationloadbalancer/v1alpha1/api.proto";
import "catalog/oci/provider.proto";

// OciApplicationLoadBalancerStackInput is the input to the IaC modules
// (Pulumi / Terraform) that deploy an OciApplicationLoadBalancer resource.
message OciApplicationLoadBalancerStackInput {
  // target cloud-resource
  OciApplicationLoadBalancer target = 1;
  // provider-config
  dev.planton.oci.OciProviderConfig provider_config = 2;
}
