syntax = "proto3";

package dev.planton.scaleway.scalewaydnsrecord.v1alpha1;

import "catalog/scaleway/provider.proto";
import "catalog/scaleway/scalewaydnsrecord/v1alpha1/api.proto";

// ScalewayDnsRecordStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message ScalewayDnsRecordStackInput {
  // target cloud-resource to be deployed
  ScalewayDnsRecord target = 1;
  // provider-config for Scaleway authentication
  dev.planton.scaleway.ScalewayProviderConfig provider_config = 2;
}
