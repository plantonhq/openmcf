syntax = "proto3";

package dev.planton.scaleway.scalewayserverlessfunction.v1alpha1;

import "catalog/scaleway/provider.proto";
import "catalog/scaleway/scalewayserverlessfunction/v1alpha1/api.proto";

// ScalewayServerlessFunctionStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message ScalewayServerlessFunctionStackInput {
  // target cloud-resource to be deployed
  ScalewayServerlessFunction target = 1;
  // provider-config for Scaleway authentication
  dev.planton.scaleway.ScalewayProviderConfig provider_config = 2;
}
