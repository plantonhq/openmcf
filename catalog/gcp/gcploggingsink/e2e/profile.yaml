apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcploggingsink
spec:
  # Composed scenarios: minimal exports errors to the GCS bucket
  # prerequisite fixture (registry prerequisite; proves URI rendering,
  # writer-identity mint, and the exclusions surface); pubsub-stream
  # proves the second destination arm and a disabled sink via the
  # scenario-level topic prerequisite. Folder/organization scopes stay
  # OFFLINE-ONLY (org credentials — the recorded GcpProject-grain
  # deferral); the billing scope has no test billing account. deploy ->
  # logging sinks.get verify -> destroy -> verify gone, on both
  # provisioners. Sinks are free and create in seconds.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 15
