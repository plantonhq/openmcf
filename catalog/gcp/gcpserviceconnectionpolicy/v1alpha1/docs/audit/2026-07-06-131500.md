# Audit Report: GcpServiceConnectionPolicy

**Audit Date:** 2026-07-06 13:15:00
**Component Kind:** GcpServiceConnectionPolicy
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpserviceconnectionpolicy/v1alpha1/`
**Enum Value:** 715
**ID Prefix:** gcpscp

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.44% | 4.44% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.34% | 13.34% | ✅ |
| Documentation - User-Facing | 13.33% | 13.33% | ✅ |
| Supporting Files | 13.33% | 13.33% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Detailed Findings

### 1. Cloud Resource Registry

✅ Enum `GcpServiceConnectionPolicy = 715` continues the 710–719
networking/LB overflow block after GcpAddress (714); `id_prefix: gcpscp`
unique (full-registry check); `prerequisites: [GcpVpcNetwork, GcpSubnetwork]`
drive the E2E chain; kind map regenerated.

### 2. Folder Structure

✅ Full anatomy: 4 protos + BUILD + spec test + README + catalog-page +
docs + 3 preset pairs + `iac/hack` + both engines + `e2e/` (profile,
scenario, consumer-scoped VPC/subnetwork prerequisites, published
`prerequisite.yaml` for the Memorystore chain).

### 3. Protobuf API Definitions

✅ `spec.proto` models the released 6.50.0 floor: location/network/
service_class triple (all ForceNew, cardinality documented), psc_config
(subnetworks min 1, string-typed limit modeled as int32 with 0=unset,
producer-location allowlist), labels, description, `policy_name` →
`metadata.name` fallback. CEL: producer-allowlist coherence both ways,
policy-name format, multi-digit region pattern. FK wiring: `network` →
GcpVpcNetwork `network_id` (relative-path format the Service Connectivity
API requires — verified live), `subnetworks` → GcpSubnetwork
`subnetwork_self_link` with both engines normalizing self-link URLs to
relative paths. 28-case spec test green. Recorded skip: `deletion_policy`
(client-side lever; catalog-wide decision).

### 4–5. IaC Modules

✅ Both engines: Network Connectivity + Compute API enablement
(disable_on_destroy=false), identical self-link normalization, identical
`planton-ai_*` label merge beneath user labels, string-encoded limit,
canonical Pulumi.yaml (no `binary:`), `~> 6.0` float. Release-equivalent
Pulumi build green; `tofu validate` + offline converter plan green (fields
inspected: relative-path network/subnets, labels, limit "10").

### 5b. Cross-Engine IaC Parity (GATE)

**PARITY ✅** — walked every dimension:

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project source | omit arg when spec empty | `null` fallback | ✅ |
| Naming basis | `policy_name` → `metadata.name` fallback | same | ✅ |
| Labels | user labels beneath `planton-ai_*` set | same merge order | ✅ |
| Address normalization | strip compute self-link prefix | identical `replace()` | ✅ |
| psc_config coverage | subnetworks/limit/producer allowlist | same | ✅ |
| Dependency ordering | DependsOn both APIs | depends_on both APIs | ✅ |
| Outputs | policy_id/name/infrastructure/etag | same keys | ✅ |

`validate-outputs` green on BOTH module dirs; `pkg/outputs` conformance
case (4/4 fields) green.

### 6–10. Documentation, Supporting Files, Presets

✅ `docs/README.md` teaches the PSC-first producer model, the
one-per-(network, service class, region) cardinality, the connection
limit as a governance lever, the producer allowlist, the address-format
trap, and teardown ordering. README + catalog-page timeless; both engine
READMEs present; canonical hack manifest validates. 3 preset pairs
(memorystore-valkey / shared-vpc-guarded / producer-allowlist) all pass
`planton validate`. Site catalog page created
(`service-connection-policy/`).

### 11. Secure-by-Default Coverage (GATE)

**COVERAGE ✅** — no secret-bearing fields; `secret-coverage --check` green.

---

## Critical Gaps

None.

## Validation Evidence

- Spec tests 28/28; release-equivalent Pulumi build; `tofu validate` +
  offline converter plan (inspected); `secret-coverage --check`;
  `validate-refs --check`; `validate-outputs` on both module dirs;
  conformance case green; all manifests through `planton validate`
  (freshly built CLI; run-id tokens substituted).
- **Live dual-engine E2E green on `planton-e2e`**: memorystore-policy
  scenario on the VPC → subnetwork chain — Pulumi 2m48s, Terraform 4m21s
  (create → policy posture verify → destroy → verify gone). Zero orphans.

**Audit completed at:** 2026-07-06-131500
**Reference:** `architecture/deployment-component.md`
