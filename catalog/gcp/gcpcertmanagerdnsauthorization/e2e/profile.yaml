apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpcertmanagerdnsauthorization
spec:
  # DNS authorization leaf (~30–60s).
  tier: 1
  # The 6.x-era proof predates this spec surface (deletion_policy) — the
  # kind awaits live re-proof at the current shape.
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
