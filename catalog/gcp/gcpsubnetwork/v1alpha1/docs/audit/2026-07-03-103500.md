# Audit Report: GcpSubnetwork

**Audit Date:** 2026-07-03 10:35:00
**Component Kind:** GcpSubnetwork
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpsubnetwork/v1alpha1/`
**Enum Value:** 611
**ID Prefix:** gcpsnw

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None. (This audit supersedes the prior report, which reflected a 7-field spec and pre-dated the current module implementations.)

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpSubnetwork = 611`; `prerequisites: [GcpVpcNetwork]` added (the network is a hard deploy-time dependency). Kind map regenerated.

### Protobuf API Definitions

- Spec deepened from 7 to 17 fields at the provider floor: `description`, `purpose` (all six values incl. proxy-only and PSC subnets), `role`, `private_ipv6_google_access`, `stack_type` + `ipv6_access_type` + `external_ipv6_prefix` (dual-stack), `allow_subnet_cidr_routes_overlap`, `send_secondary_ip_range_if_empty` (the secondary-range removal safety latch), and the full `log_config` flow-logs block (5 fields). Field numbering contiguous; comments timeless and intent-dense; stray research artifacts removed.
- `project_id` conformed to the ambient-project contract (optional; empty → provider default) — matching every kind touched since the contract landed. Inbound-FK safety held: all seven inbound references (5× `subnetwork_self_link`, 2× `secondary_ranges.[*].range_name`, 1× `subnetwork_name`) land on preserved output paths; `validate-refs --check` green.
- CEL enforces: IPv4 range required except IPV6_ONLY (and forbidden there), role gated to REGIONAL_MANAGED_PROXY, ipv6_access_type ↔ stack_type pairing, external prefix ↔ EXTERNAL access, metadata_fields ↔ CUSTOM_METADATA.
- Spec test expanded 3 → 23 cases, green.

### IaC Modules (both engines)

- Both wire every spec field identically, enable the Compute Engine API first, apply the ambient-project contract, normalize `""`→absent, filter blank secondary ranges, and mirror the API's flow-log defaults so an empty `log_config` behaves sanely. The TF subnetwork resource runs on `google-beta`: `allow_subnet_cidr_routes_overlap` is beta-only on the released 6.x line (schema-probe verified); Pulumi is beta-bridged. Fixed defects: hardcoded `google 6.19.0` pin → `~> 6.0` + google-beta (DD-conformant), hack manifest moved to canonical `iac/hack/`, TF output drift (`subnetwork_name` missing) closed, stale Pulumi.yaml placeholder replaced, dead label plumbing removed from both engines (subnetworks accept no labels).
- Outputs extended: `gateway_address`, `subnetwork_id` (stringified identically in both engines), `internal_ipv6_prefix`, `external_ipv6_prefix`.
- Release-equivalent Pulumi build, `tofu validate`, offline `planton tofu plan` green.

### Cross-Engine Parity (Category 5b gate)

Walked all parity dimensions: naming basis (`subnetwork_name`), full 17-field coverage both sides, identical defaults (PRIVATE/IPV4_ONLY via proto defaults + safety-net coalesces), identical secondary-range filtering, identical API-enablement edge, identical 9-output surface. `planton validate-outputs` dry-run: full population incl. the repeated secondary ranges. Conformance case added and green. **No PARITY-EXCEPTION needed.** **PARITY ✅**

### Secure-by-Default (Category 11 gate)

No secret-bearing fields (address plans and flow-log filters are not secrets). `planton secret-coverage --check` green. **COVERAGE ✅**

### Supporting Files, Presets, Docs

- Canonical `iac/hack/manifest.yaml` validates (legacy root `hack/` removed); presets 2 → 3 (GKE-ready, general-purpose, proxy-only — the regional-ALB prerequisite) all validating; README/catalog/research docs rewritten to timeless 90/10 framing (legacy coverage-percentage language and broken links removed), incl. recorded skip reasons (`reserved_internal_range`, `ip_collection` BYOIP, `resolve_subnet_mask`, `params.resource_manager_tags`, deprecated `enable_flow_logs`).

### Live E2E

- Dual-engine composed scenario with a GcpVpcNetwork prerequisite (custom-mode network; secondary ranges + flow logs exercised live): Pulumi 135s, Terraform 148s — prerequisite deploy → FK resolution → create → compute API verify (incl. primary-range check) → destroy → 404 verify → prerequisite teardown. Zero orphans confirmed by post-run `gcloud compute networks subnets list` / `networks list`.
