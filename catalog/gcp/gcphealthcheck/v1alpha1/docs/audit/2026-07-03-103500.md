# Audit Report: GcpHealthCheck

**Audit Date:** 2026-07-03 10:35:00
**Component Kind:** GcpHealthCheck
**Provider:** gcp
**Component Path:** `catalog/gcp/gcphealthcheck/v1alpha1/`
**Enum Value:** 623
**ID Prefix:** gcphc

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpHealthCheck = 623` in the networking/LB sub-band (623–629); `id_prefix` `gcphc` unique (full-registry uniqueness verified); kind map regenerated.

### Protobuf API Definitions

- All four protos present with dense intent-explaining comments. The probe protocol is a required `oneof` across 7 arms (`http`/`https`/`http2`/`tcp`/`ssl`/`grpc`/`grpc_tls`); one kind covers both API scopes via the optional `region` field (schemas verified field-for-field identical between the global and regional provider resources).
- CEL enforces pre-deploy what GCP would reject at apply: timeout ≤ interval, port-spec coherence per protocol arm (USE_NAMED_PORT/USE_SERVING_PORT rules, incl. gRPC-TLS's no-named-port), and all five `source_regions` constraints (exactly 3, global-only, HTTP/HTTPS/TCP only, interval ≥ 30, no proxy header / TCP request). All CEL messages are user-facing quality.
- 24-case spec test green (`go test ./apis/.../gcphealthcheck/v1/`).

### IaC Modules (both engines)

- Each engine creates exactly one of the global/regional resource from the same `region` switch, enables the Compute Engine API first (never disabled on destroy), applies the ambient-project contract (empty project → provider default), normalizes unset optionals to absent (never empty strings), and leaves ports to the API's protocol defaults. Both run on `google-beta`: the `grpc_tls_health_check` block is beta-only on the released 6.x line (verified with a `tofu providers schema -json` probe; the clone's main is ahead of the release). Both modules are richly commented to the module-comment bar.
- Release-equivalent build green: `go build -o /dev/null ./apis/.../v1/iac/pulumi`. `tofu validate` + `planton tofu plan` (offline) green.

### Cross-Engine Parity (Category 5b gate)

Walked all `pkg/iac/MODULE_PARITY.md` dimensions: scope source (spec.region), naming basis (`health_check_name` → `metadata.name` fallback in both), no labels on either side (the resource accepts none), full spec coverage in both engines, identical API-enablement dependency edge, identical outputs (`self_link`, `health_check_name`, `type`, `region` — empty marking global scope in both). `planton validate-outputs` dry-run: full population. `pkg/outputs/conformance_test.go` case added and green. **No PARITY-EXCEPTION needed.** **PARITY ✅**

### Secure-by-Default (Category 11 gate)

No secret-bearing fields (probe configuration is public material). `planton secret-coverage --check` green. **COVERAGE ✅**

### Supporting Files, Presets, Docs

- `iac/hack/manifest.yaml` (canonical location) validates; 3 presets (HTTP serving-port, regional TCP, gRPC) with companion docs, all validating; README + catalog page + research doc (`docs/README.md`) complete, incl. the recorded skip reasons (`deletion_policy`, `params.resource_manager_tags`, regional `health_check_id`).

### Live E2E

- Dual-engine, both scenarios (global HTTP + regional TCP): Pulumi 80.6s, Terraform 98.7s — create → compute API verify → destroy → 404 verify. Zero orphans confirmed by post-run `gcloud compute health-checks list`.
