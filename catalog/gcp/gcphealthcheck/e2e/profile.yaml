apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcphealthcheck
spec:
  # Three leaf scenarios exercising BOTH provider resources behind the one
  # kind: minimal (global HTTP), regional (region TCP), and grpc-tls (global
  # gRPC-with-TLS — the formerly-beta protocol arm, GA on the 7.x line).
  # Health checks are free, instant to create/destroy, and have no
  # dependencies — deploy -> compute healthChecks/regionHealthChecks get
  # verify -> destroy -> verify 404, on both provisioners.
  tier: 1
  # pending_proof: the spec grew deletion_policy fanning to BOTH provider
  # resources — the prior green described code that no longer exists. The
  # minimal/grpc-tls scenarios prove the global resource's arm and the
  # regional scenario proves the regional one.
  status: pending_proof
  timeout_minutes: 10
