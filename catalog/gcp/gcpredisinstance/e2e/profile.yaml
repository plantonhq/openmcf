apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpredisinstance
spec:
  # Composed scenarios: the registry installs the full service networking
  # chain before every instance create. Redis instances take ~5-10 min each;
  # run the package with a generous go test -timeout.
  tier: 1
  # The prior green predates the current spec shape (user labels,
  # deletion_protection defaulting TRUE — both engines now send it
  # explicitly — maintenance description, deletion_policy) — awaits
  # live re-proof.
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
