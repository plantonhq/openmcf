# Provider-parity manifest: this kind's recorded judgment against the pinned
# google provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  google_bigquery_dataset:
    mappings:
      # Renames: the spec says what each field IS, where the provider
      # inherits terser API names.
      - spec: spec.project_id
        arg: project

      # CMEK: the provider wraps the key in a one-field block; the spec
      # carries the key reference directly.
      - spec: spec.kms_key_name
        arg: default_encryption_configuration.kms_key_name

      # Hive-catalog metadata: the provider's block name carries a
      # redundant "dataset"; children match by name below the mapping.
      - spec: spec.external_catalog_options
        arg: external_catalog_dataset_options

      # Dataset-to-dataset authorization: the provider nests a container
      # block confusingly also named "dataset" around the target's
      # identity pair; the spec flattens it into the access entry.
      - spec: spec.access.dataset.dataset_id
        arg: access.dataset.dataset.dataset_id
      - spec: spec.access.dataset.project_id
        arg: access.dataset.dataset.project_id

  google_project_service:
    internal: >-
      API enablement plumbing -- the module enables
      bigquery.googleapis.com with fixed arguments (disable_on_destroy
      false so destroying one dataset never disables the API
      project-wide); not spec surface.
