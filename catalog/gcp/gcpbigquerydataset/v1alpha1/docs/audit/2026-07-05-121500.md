# Audit Report: GcpBigQueryDataset

**Audit Date:** 2026-07-05 12:15:00
**Component Kind:** GcpBigQueryDataset
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpbigquerydataset/v1alpha1/`
**Enum Value:** 650
**ID Prefix:** `gcpbqds`

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `GcpBigQueryDataset = 650` registered (`provider: gcp`, `version: v1`,
`id_prefix: "gcpbqds"` — unique registry-wide). 650 opens the analytics
sub-band. The kind is now the published E2E prerequisite for
GcpBigQueryTable (653).

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Correct hierarchy and lowercase naming; `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Proto files:** `api.proto` (815 B), `spec.proto` (15.9 KB),
`stack_input.proto` (335 B), `stack_outputs.proto` (1.3 KB). The depth
rebuild brought the spec to the released 6.50.0 floor: user `labels`
(merged beneath platform labels — the Spanner-instance precedent),
`resource_tags`, the full access surface (5 principal identities + view /
routine / authorized-dataset authorizations + IAM `condition`),
`external_dataset_reference` (BigQuery Omni), `external_catalog_options`
(Hive Metastore interop), and the ambient-project `project_id` relaxation
(required → optional ref → GcpProject). Two arm-aware CEL rules the prior
spec lacked: `access_exactly_one_identity` (exactly one of the 8
identity/resource arms) and `access_role_coherence` (role required for
principal grants, forbidden for resource authorizations). Field numbering
contiguous 1–18.

✅ **Stubs:** all `.pb.go` files regenerated; CLI kind registry includes
the extended spec (verified via a fresh `make build-cli` + `planton
validate` on all manifests).

✅ **Tests:** `spec_test.go` — 49 cases covering every new field, both CEL
rules (accept + reject arms), boundary values (expiration minimum,
time-travel 48/96/168), and identity-shape rejections;
`go test ./catalog/gcp/gcpbigquerydataset/v1alpha1/` — **ok**.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ `module/main.go`, `module/locals.go`, `module/dataset.go`,
`module/outputs.go`; root `main.go` is `package main`; canonical
`Pulumi.yaml` (no stale `binary:`); Makefile present; release-equivalent
build passes (`make build` + `go build -o pulumi .`). Adds
`bigquery.googleapis.com` enablement (`disable_on_destroy=false`),
`planton-ai_*` labels (user labels merged beneath), ambient-project
fallback (empty `project_id` omits the arg), full access mapping including
the provider's nested `dataset { dataset { ... } target_types }` wrapper
(flattened in the spec, documented in a comment), CMEK, external dataset
reference, and catalog options.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files present; `tofu validate` — **Success**; provider floated
`~> 6.0` (was `~> 5.0`); converter-contract variables (plain-string refs,
never `object({value})`); label merge identical to Pulumi (this rebuild
fixed the live label-parity break — the old TF module set zero labels);
API enablement added; offline `planton tofu plan` through the real tfvars
converter inspected (labels, access blocks, CMEK, outputs all present).

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs` on BOTH module dirs
(generic reflection transformer; 6/6 proto fields populated in the live
E2E VERIFY-OUT phase on both engines).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project fallback | omit arg when `project_id` empty | empty → null → provider default | ✅ MATCH |
| API enablement | `bigquery.googleapis.com`, disable_on_destroy=false | same | ✅ MATCH |
| Labels | user merged beneath `planton-ai_*` | identical merge order | ✅ MATCH |
| Access mapping | all 8 arms + condition | all 8 arms + condition | ✅ MATCH |
| Empty-means-unset scalars | omitted (nil ptr) | empty/zero → null | ✅ MATCH |
| max_time_travel_hours typing | int32 → string for SDK | int32 → tostring() | ✅ MATCH |
| Destroy guard | `delete_contents_on_destroy` honored | same | ✅ MATCH |
| Stack outputs → proto | 6 keys | same 6 keys | ✅ MATCH |

**Effect on score:** none.

---

### 6–8. Documentation & Supporting Files

✅ `docs/README.md` — timeless research: dataset/table design boundary
(stated as design, not history), 90/10 coverage table, recorded skips with
reasons (`deletion_policy` absent from released 6.50.0 — verified with a
fresh `tofu providers schema -json` probe; the additive
`google_bigquery_dataset_access` kind deliberately deferred — mixing it
with the authoritative inline ACL causes permanent diffs).
✅ `README.md` + `catalog-page.md` — rewritten with the `planton-ai_*`
labels section (legacy `planton-resource*` keys gone) and the
GcpBigQueryTable cross-link.
✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all
present and current.

**Scores:** 13.18%, 13.09%, 10.09% — full credit.

---

### 9. Nice to Have (10%)

✅ `BUILD.bazel` files present and consistent.

---

### 10. Presets (5.00%)

✅ Three ranked pairs (`01-basic-analytics`, `02-cmek-encrypted`,
`03-team-shared` — the last exercising the new access arms: condition-gated
grant + authorized view), companion `.md` files, `my-` names, all pass
`planton validate`.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

No secret-bearing fields on this spec; `secret-coverage --check` passes.

---

## Live E2E Status

Dual-engine live run on `planton-e2e` — **4/4 green** (2026-07-05):
`minimal` + `access-entries` scenarios × pulumi/terraform, all six phases
each (VALIDATE → DEPLOY → VERIFY-OUT → VERIFY-RES → DESTROY → VERIFY-CLN).
Verifier: `aa_e2e/verify/big_query_dataset.go` (bigquery v2 datasets.get +
location posture). Published `e2e/prerequisite.yaml` serves the
GcpBigQueryTable chain. Zero orphans (post-run dataset list empty).

---

**Audit completed at:** 2026-07-05 12:15:00
**Reference:** `architecture/deployment-component.md`
