apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcprouternat
spec:
  # Composed scenarios: the registry installs the VPC -> subnetwork ->
  # address chain before every NAT create. Routers and NATs themselves
  # deploy in seconds; the chain dominates the runtime.
  tier: 1
  # pending_proof: the spec grew the router BGP surface (redesigned into
  # router_bgp), NAT64, resource-manager tags, and deletion_policy — the
  # prior green proof describes code that no longer exists.
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
