# Audit Report: GcpKmsKeyIamMember

**Audit Date:** 2026-07-10 00:29:07
**Component Kind:** GcpKmsKeyIamMember
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpkmskeyiammember/v1alpha1/`
**Enum Value:** 692
**ID Prefix:** gcpkim

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpKmsKeyIamMember = 692` exists in `cloud_resource_kind.proto` — the first slot of the KMS/security sub-band (692–699), directly after `GcpKmsKeyRing` 690 / `GcpKmsKey` 691
- Enum value 692 is in the GCP range (600–799)
- `id_prefix` `gcpkim` is unique across the registry
- `kind_meta` includes provider, version, id_prefix, and `prerequisites: [GcpKmsKeyRing, GcpKmsKey, GcpServiceAccount]` (the full chain exercised live by the E2E scenario)

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- Folder exists at `catalog/gcp/gcpkmskeyiammember/v1alpha1/`
- Folder name is the lowercase enum name; `v1/` subfolder present

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- `spec.proto` (4.9 KB): four fields — `crypto_key_id` (required, FK `GcpKmsKey.status.outputs.key_id` — byte-exact one of the three formats the provider's KMS ID parser accepts), `role` (required, FK `GcpIamCustomRole.status.outputs.name`), `member` (required, FK `GcpServiceAccount.status.outputs.member`), optional `condition` — a 100% model of the released `google_kms_crypto_key_iam_member` (schema probed against the released v6.50.0 tag; `condition` confirmed GA in the released source, superseding the stale doc-page Beta annotation). Field comments teach the CMEK service-agent pattern, IAM inheritance down the ring hierarchy, additive semantics, and immutability.
- `api.proto`, `stack_input.proto`, `stack_outputs.proto` present; outputs echo the resolved grant tuple + policy `etag`.
- All four `.pb.go` stubs generated and current.
- `spec_test.go` (5.2 KB): 14 cases (CMEK-agent/viewer/custom-role/conditional positives; missing-field, condition-coherence, max-len, kind-literal negatives) — all pass (`go test` green).
- Validation messages: constraints are simple `required`/`max_len` (protovalidate default messages); the deploy-time format checks live in both modules with actionable error strings.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `module/locals.go`, `module/iam_member.go`, `module/outputs.go` present; comments explain the additive contract, immutability, the tri-format key identifier, the normalize-only-on-import output note, and the no-project/location-argument contract.
- Root `iac/pulumi/main.go` declares `package main`; no `main/`/`entrypoint/` subdir; `Pulumi.yaml` + `Makefile` canonical.
- Release-equivalent build passes: `go build -o /dev/null ./apis/.../v1/iac/pulumi`.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- `variables.tf` (3.0 KB, typed spec object with three validation blocks mirroring the Pulumi module's deploy-time checks incl. the tri-format `crypto_key_id` regex), `provider.tf` (`google ~> 6.0` + aliased `google-beta`), `locals.tf`, `main.tf` (richly commented — CMEK least-privilege + DAG-edge rationale), `outputs.tf` (4 outputs matching `stack_outputs.proto`).
- `tofu init -backend=false && tofu validate` green; real-converter `planton tofu plan` green on the hack manifest.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Scope source | `spec.crypto_key_id` | `var.spec.crypto_key_id` | ✅ MATCH |
| Resource naming basis | n/a (grant creates no named cloud object) | n/a | ✅ MATCH |
| Labels | none (IAM policies carry no labels) | none | ✅ MATCH |
| Selectors | n/a | n/a | ✅ MATCH |
| Spec feature coverage | all 4 fields incl. condition.description | same | ✅ MATCH |
| Deploy-time validation | tri-format key regex + member regex + deleted: rejection | identical acceptance in `variables.tf` validations | ✅ MATCH |
| Dependency ordering | single resource | single resource | ✅ MATCH |
| Stack outputs → proto | 4/4 populated (`crypto_key_id` echoes the configured identifier — provider normalizes only on import, noted in both modules) | 4/4 populated | ✅ MATCH |

Output parity verified three ways: the `pkg/outputs/conformance_test.go` case (green), `planton validate-outputs` (generic reflection path, no override), and live E2E VERIFY-OUT reporting 4/4 proto fields populated on BOTH engines — with the conditioned binding present and matched at policy version 3.

**Effect on score:** none — engines in parity.

---

### 6–8. Documentation and Supporting Files

✅ **Passed:**

- `docs/README.md` (3.7 KB): the universal CMEK prerequisite, why key-scoped beats ring/project-scoped (least privilege + the first-deploy IAM-propagation race closed by the DAG edge), additive-only stopping line, lazily-created service agents sharp edge, ring-scope note.
- `README.md` (7.1 KB) + `catalog-page.md`: quick starts (literal + composed), full configuration reference, member-format table, key/ring/project scoping guidance, outputs, related components.
- `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` (validates through the CLI).

**Scores:** 13.18% + 13.09% + 10.09% — all full.

---

### 10. Presets (5.00%)

✅ **Passed:**

- 3 preset pairs: `01-storage-cmek-grant`, `02-workload-key-user`, `03-conditional-key-access` — each YAML has a companion `.md`; correct `apiVersion`/`kind`; `my-` names; `StringValueOrRef` wrapper forms; every field exists in `spec.proto`; no duplicate ranks.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` green; zero `gaps[]` entries for this kind (the spec carries no secret-bearing fields — grants are policy metadata; the key id is an exempt-class resource identifier arriving as a plain reference field). The GCP baseline remains empty.

---

## Live E2E

Scenario `conditional-cmek-grant` (ring → key → service account prerequisite chain; `crypto_key_id` ← the key's `key_id` output, `member` ← the account's `member` output; the grant carries an IAM condition, live-proving the conditioned-binding arm): **live dual-engine green on `planton-e2e`** (~1m55s/engine incl. the chain), deploy → getIamPolicy verify → destroy → absence verified, zero orphans (the run-scoped ring/key prerequisites remain as inert, free objects per the undeletable-resource contract; the key's versions destroy-scheduled at teardown).

---

**Audit completed at:** 2026-07-10 00:29:07
**Reference:** See `architecture/deployment-component.md` for ideal state definition
