apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpvertexainotebook
spec:
  # Composed scenario: VPC -> subnetwork chain before the notebook create.
  # Workbench instances take ~5-15 minutes per engine; batch runs with a
  # go test -timeout of at least 60m.
  tier: 1
  # The prior green predates the current spec shape (deletion_policy
  # modeled from the spec instead of the Pulumi-side DELETE pin, the
  # widened accelerator/hyperdisk value sets) — awaits live re-proof.
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
