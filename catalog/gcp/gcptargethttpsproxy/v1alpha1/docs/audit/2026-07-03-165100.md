# Audit Report: GcpTargetHttpsProxy

**Audit Date:** 2026-07-03 16:51:00
**Component Kind:** GcpTargetHttpsProxy
**Provider:** gcp
**Component Path:** `catalog/gcp/gcptargethttpsproxy/v1alpha1/`
**Enum Value:** 601
**ID Prefix:** gcpthsp

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpTargetHttpsProxy = 601`; `id_prefix` `gcpthsp` unique; `prerequisites: [GcpUrlMap, GcpManagedSslCertificate]` drives the composed E2E chain.

### Protobuf API Definitions

- Full released 6.50.0 GA surface on plain `google ~> 6.0`: required `url_map` ref; all three certificate mechanisms — `ssl_certificates` (repeated ref → `GcpManagedSslCertificate.self_link`, max 15), `certificate_manager_certificates` (repeated ref → `GcpCertManagerCert.certificate_name`), `certificate_map` — with the single-certificate-source CEL enforcing what GCP rejects at the API; `ssl_policy` and `server_tls_policy` as un-defaulted refs (their kinds land later with one-line `default_kind` follow-ups); `quic_override` (middleware default NONE = GCP default); `tls_early_data` (all four 0-RTT modes; immutable); keep-alive; `proxy_bind`.
- **Deliberately unmodeled:** `deletion_policy` (absent from the released 6.x line), `timeouts`. Recorded in `docs/README.md`.
- 22-case spec test suite green, including the three pairwise certificate-mechanism conflicts and the TD server-TLS-only shape.

### IaC Modules (both engines)

- Both enable `compute.googleapis.com` first; ambient-project contract; empty-list/empty-string → omit semantics identical on both sides; repeated refs flattened to literal values.
- Release-equivalent Pulumi build green; `tofu validate` + offline `planton tofu plan` (2 to add, certificate list and SSL policy inspected in the plan) through the real tfvars converter.

### Cross-Engine IaC Parity (Category 5b gate)

Identical naming basis, full spec coverage, identical outputs (`self_link`, `proxy_name`, `proxy_id`, `fingerprint`). Conformance case in `pkg/outputs` (4/4 both engines). No PARITY-EXCEPTIONs. **PARITY ✅**

### Secure-by-Default (Category 11 gate)

No secret-bearing fields — certificates attach by REFERENCE (self-links / resource names), never as key material; the private key never touches this spec. `planton secret-coverage --check` green. **COVERAGE ✅**

### Supporting Files, Presets, Docs, E2E

- 3 presets (managed-cert frontend, certificate-map SaaS, mTLS server-TLS-policy) — all pass `planton validate`.
- Composed live E2E green on BOTH engines (Pulumi 4m45s, Terraform 4m35s) on the 5-prerequisite chain including a PROVISIONING managed certificate (attachment-before-activation proven live); verifier asserts URL-map + TLS-input wiring; zero orphans after teardown.
- The kind's repeated-ref scenario also drove the E2E ref resolver's repeated-`StringValueOrRef` support (unit-tested in the shared runner).
