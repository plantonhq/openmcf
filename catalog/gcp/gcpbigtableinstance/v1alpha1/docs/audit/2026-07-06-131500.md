# Audit Report: GcpBigtableInstance

**Audit Date:** 2026-07-06 13:15:00
**Component Kind:** GcpBigtableInstance
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpbigtableinstance/v1alpha1/`
**Enum Value:** 635
**ID Prefix:** gcpbt

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.44% | 4.44% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.34% | 13.34% | ✅ |
| Documentation - User-Facing | 13.33% | 13.33% | ✅ |
| Supporting Files | 13.33% | 13.33% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Detailed Findings

### 1–2. Registry and Structure

✅ Enum 635 (data sub-band); `id_prefix: gcpbt`; full anatomy including a
new `e2e/` (minimal + autoscaling scenarios, published `prerequisite.yaml`
for the table chain, all cloud-side names `${E2E_RUN_ID}`-scoped).

### 3. Protobuf API Definitions

✅ Spec at the released 6.50.0 floor: clusters (zone/num_nodes XOR
autoscaling with CPU + storage targets, SSD/HDD, CMEK per cluster, node
scaling factor), display name, deletion_protection (optional bool default
TRUE), force_destroy. The rebuild added user `labels` (the released
resource supports them; the spec had none). Recorded skips
(released-schema-probe verified): `edition` (ENTERPRISE/ENTERPRISE_PLUS)
and `tags` — main-only; `instance_type` — deprecated (DEV/PROD distinction
being removed). 52-case spec test green.

### 4–5. IaC Modules

✅ Both engines: Bigtable Admin API enablement (disable_on_destroy=false),
`planton-ai_*` labels with user labels merged beneath — the TF module's
legacy unprefixed label keys (`resource`, `resource-name`, …) are fixed to
the shared key set, closing the live label-parity break. Canonical
Pulumi.yaml (stale `binary:` gone); `~> 6.0`.

### 5b. Cross-Engine IaC Parity (GATE)

**PARITY ✅**:

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project source | omit when empty | `null` fallback | ✅ |
| Naming basis | `instance_name` | same | ✅ |
| Labels | user beneath `planton-ai_*` | same merge order | ✅ |
| Deletion protection | explicit, nil→TRUE | `optional(bool, true)` sent | ✅ |
| Cluster arms | fixed XOR autoscaling, 0-means-server-default storage target | identical translation | ✅ |
| Outputs | instance_id (full path) + instance_name | same | ✅ |

`validate-outputs` green both module dirs; conformance case green.

### 6–11. Documentation, Presets, Coverage

✅ Docs/README/catalog timeless with the recorded-skips ledger; 3 preset
pairs (dev-single-node / ha-production / enterprise-encrypted) validate;
site catalog page refreshed. No secret-bearing fields — coverage gate
green.

---

## Critical Gaps

None.

## Validation Evidence

- Spec tests 52/52; release-equivalent Pulumi build; `tofu validate` +
  offline converter plan; `secret-coverage --check`;
  `validate-refs --check`; `validate-outputs` both module dirs;
  conformance case; all manifests validate.
- **Live dual-engine E2E green on `planton-e2e`**: minimal (single-node)
  and autoscaling (scale bounds + CPU target) scenarios — Pulumi 50s,
  Terraform 2m55s across both scenarios; create → READY + cluster posture
  verify → destroy → verify gone. Zero orphans.

**Audit completed at:** 2026-07-06-131500
**Reference:** `architecture/deployment-component.md`
