apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpcloudarmorpolicy
spec:
  # Leaf security policy with explicit default rule (~1–2 minutes).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
