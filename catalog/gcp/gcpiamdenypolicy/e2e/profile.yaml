apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpiamdenypolicy
spec:
  # Live E2E is conditional on org-level roles/iam.denyAdmin, which
  # creating IAM deny policies requires even for project-attached
  # policies — a permission the standalone test project's credentials
  # deliberately lack. The offline bar (spec suite, both engines'
  # builds, offline plans) is the shipped evidence until an org-scoped
  # credential path exists; validated_provisioners stays empty until the
  # live lanes actually run.
  tier: 2
  status: deferred
  deferred_reason: creating IAM deny policies requires org-level roles/iam.denyAdmin, which the standalone test project's credentials deliberately lack; offline plans only
  validated_provisioners: []
  timeout_minutes: 15
