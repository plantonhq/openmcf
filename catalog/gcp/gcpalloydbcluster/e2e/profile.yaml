apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpalloydbcluster
spec:
  # Composed scenarios: the registry installs the full private-services-access
  # chain before every cluster create. AlloyDB primary clusters are slow
  # (~10–20 min each); run the package with a generous go test -timeout.
  tier: 1
  # pending_proof: the spec grew the full 7.43 surface (deletion_protection
  # modeled with spec default TRUE, cluster/primary deletion policies,
  # restore provenance, Dataplex, user labels, the primary's public-IP/PSC/
  # pooling/zone surface) — the prior green described code that no longer
  # exists. Offline-proven-only arms (live proof needs fixtures this lane
  # records): restore sources (need a source cluster/backup), the primary's
  # PSC arm (needs a PSC cluster), connection pooling live behavior.
  status: pending_proof
  timeout_minutes: 60
