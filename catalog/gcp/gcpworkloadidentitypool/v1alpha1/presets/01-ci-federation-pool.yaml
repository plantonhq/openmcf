apiVersion: gcp.planton.dev/v1alpha1
kind: GcpWorkloadIdentityPool
metadata:
  name: ci-federation-pool
spec:
  projectId:
    value: "<gcp-project-id>"
  workloadIdentityPoolId: ci-federation # replace: 4-32 chars; lowercase letters, digits, hyphens
  displayName: CI Federation
  description: Keyless federation boundary for CI/CD pipelines
