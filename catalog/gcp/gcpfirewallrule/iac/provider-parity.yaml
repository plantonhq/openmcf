# Provider-parity manifest: this kind's recorded judgment against the pinned
# google provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  google_compute_firewall:
    mappings:
      # Renames: the spec says what each field IS, where the provider
      # inherits terser API names.
      - spec: spec.rule_name
        arg: name
      - spec: spec.project_id
        arg: project
      # ONE spec list feeds BOTH provider block families: spec.action
      # selects whether the rules render as allow or deny blocks, and each
      # entry's protocol/ports map name-for-name below either prefix.
      - spec: spec.rules
        arg: allow
      - spec: spec.rules
        arg: deny
      # The provider nests create-time tags under a params block; the spec
      # uses the catalog-wide resource_manager_tags vocabulary.
      - spec: spec.resource_manager_tags
        arg: params.resource_manager_tags

specExclusions:
  - field: spec.action
    reason: >-
      selector, not payload: ALLOW/DENY decides which of the provider's
      mutually-exclusive allow/deny block families the shared spec.rules
      list renders into (the provider enforces ExactlyOneOf on the two
      blocks; the enum makes that choice explicit in the manifest)
