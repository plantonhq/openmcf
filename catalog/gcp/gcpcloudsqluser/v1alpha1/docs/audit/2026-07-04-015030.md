# Audit Report: GcpCloudSqlUser

**Audit Date:** 2026-07-04 01:50:30
**Component Kind:** GcpCloudSqlUser
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpcloudsqluser/v1alpha1/`
**Enum Value:** 638
**ID Prefix:** `gcpsqlusr`

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Critical Gaps

None.

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ `GcpCloudSqlUser = 638` registered (`provider: gcp`, `version: v1`,
`id_prefix: "gcpsqlusr"` — unique registry-wide) with
`prerequisites: [GcpCloudSql]` for automatic instance-chain installation in
E2E.

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ Correct hierarchy and lowercase naming; `v1/` present.

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Proto files:** `api.proto` (903 B), `spec.proto` (5.3 KB),
`stack_input.proto` (402 B), `stack_outputs.proto` (510 B). Spec models
`google_sql_user` at the ~> 6.0 floor: `project_id` ref, required
`instance` ref (GcpCloudSql → `instance_name`), `user_name`, sensitive
`password`, `type` (BUILT_IN default via platform defaulting +
CLOUD_IAM_USER / CLOUD_IAM_SERVICE_ACCOUNT / CLOUD_IAM_GROUP), MySQL-only
`host`, per-user `password_policy`. CEL: IAM types must not set a password;
`password_policy` is BUILT_IN-only; expiration duration format explained in
plain English with the regex in a comment.

Recorded skips (documented in `docs/README.md`): `database_roles` and
`iam_email` are not on the released 6.50.0 floor; `password_wo` skipped by
design (write-only is a TF-plan mechanic — Planton's secret handling owns
this).

✅ **Stubs:** all `.pb.go` files regenerated.

✅ **Tests:** `spec_test.go` (4.7 KB);
`go test ./catalog/gcp/gcpcloudsqluser/v1alpha1/` — **ok**.

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ `module/main.go`, `module/locals.go`, `module/outputs.go`,
`module/user.go`; root `main.go` is `package main`; `Pulumi.yaml` +
`Makefile`; release-equivalent build passes. `password` flows through
`pulumi.ToSecret` so the credential is encrypted in Pulumi state and never
exported. Comments explain the auth families, in-place password rotation,
and the PostgreSQL `cloudsql.iam_authentication` flag prerequisite for IAM
types.

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ All five files present; `tofu validate` — **Success**; provider pinned
`~> 6.0`; `password` handled as sensitive input, empty → null so IAM users
carry no password argument at all.

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs` (dry-run: 2/2 proto fields
populated, zero unmapped).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project fallback | omit arg when empty | empty → null → provider default | ✅ MATCH |
| Resource naming basis | `spec.user_name` | `var.spec.user_name` | ✅ MATCH |
| Labels | none (`google_sql_user` has no labels surface) | none | ✅ MATCH |
| Spec feature coverage | all 11 fields honored | all 11 fields honored | ✅ MATCH |
| Secret handling | `pulumi.ToSecret(password)`; never exported | sensitive var; never exported | ✅ MATCH |
| `type` default | platform defaulting stamps `BUILT_IN` at manifest load (`internal/manifest` ApplyDefaults); module passes it through | variable default `BUILT_IN` as converter safety; provider diff-suppresses the explicit form | ✅ MATCH |
| `host` (MySQL-only) | omitted when empty | empty → null | ✅ MATCH |
| `password_policy` | nil-guarded block, optional int via ptr | dynamic block, null-guarded | ✅ MATCH |
| API enablement | intentionally none (parent instance owns it; documented in both) | intentionally none | ✅ MATCH |
| Stack outputs → proto | `user_name`, `instance_name` | same, 2/2 populated | ✅ MATCH |

**Effect on score:** none.

---

### 6–8. Documentation & Supporting Files

✅ `docs/README.md` (3.2 KB) — auth-family landscape (BUILT_IN vs IAM),
MySQL truncation behavior for IAM principals, recorded skips with reasons.
✅ `README.md` (4.6 KB) — user-facing with rotation guidance.
✅ `iac/pulumi/README.md`, `iac/tf/README.md`, `iac/hack/manifest.yaml` all present.

**Scores:** 13.18%, 13.09%, 10.09% — full credit.

---

### 9. Nice to Have (10%)

✅ `BUILD.bazel` files generated.

---

### 10. Presets (5.00%)

✅ Two ranked pairs (`01-application-user`, `02-iam-service-account-user`),
companion `.md` files, no duplicate ranks, `my-` names, both pass
`planton validate`, `apiVersion`/`kind` match `api.proto`.

**Score:** 5.00% / 5.00%

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

| Field path | Classification | Note |
|-----------|----------------|------|
| `spec.password` | covered | `(sensitive) = true`; ToSecret in Pulumi state |
| `spec.password_policy.password_expiration_duration` | exempt | truthful reason: an expiry duration, not credential material |

Zero `gaps[]` entries prefixed `GcpCloudSqlUser:`; `--check` passes.

---

## Live E2E Status

`e2e/scenarios/minimal.yaml` deploys a BUILT_IN user onto a prerequisite
instance chain. Verifier: `aa_e2e/verify/cloud_sql_user.go` (sqladmin
users.get; asserts the credential is never present in outputs). Dual-engine
live run tracked in the session wrap-up.

---

**Audit completed at:** 2026-07-04 01:50:30
**Reference:** `architecture/deployment-component.md`
