# Provider-parity manifest: this kind's recorded judgment against the pinned
# google provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  google_firestore_database:
    mappings:
      # Renames: the spec says what each field IS, where the provider
      # inherits terser API names.
      - spec: spec.database_name
        arg: name
      - spec: spec.project_id
        arg: project
      # CMEK is one key reference; the provider nests it in a single-field
      # block.
      - spec: spec.kms_key_name
        arg: cmek_config.kms_key_name
      # The provider calls resource-manager tags plain `tags` here; the
      # spec uses the catalog-wide resource_manager_tags vocabulary.
      - spec: spec.resource_manager_tags
        arg: tags

  google_project_service:
    internal: >-
      API enablement plumbing -- the module enables firestore.googleapis.com
      with fixed arguments (disable_on_destroy false so destroying one
      database never disables the API project-wide); not spec surface.
