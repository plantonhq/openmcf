apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcppubsubtopic
spec:
  # Fast control-plane object. The schema-validated scenario composes the
  # GcpPubSubSchema prerequisite live (schema FK resolution proof).
  tier: 1
  # The prior green predates the current spec shape (deletion_policy,
  # revision-range pinning refs, the ai_inference transform arm,
  # resource-manager tags) — awaits live re-proof.
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
