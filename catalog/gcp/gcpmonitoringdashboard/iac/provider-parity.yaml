# Provider-parity manifest: this kind's recorded judgment against the pinned
# google provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name (dashboard_json and deletion_policy
# auto-match that way). Every exclusion carries its reason: omission is a
# decision, never an accident.

resources:
  google_monitoring_dashboard:
    mappings:
      - spec: spec.project_id
        arg: project

  google_project_service:
    internal: >-
      API enablement plumbing -- the module enables
      monitoring.googleapis.com with fixed arguments (disable_on_destroy
      false so destroying one dashboard never disables monitoring
      project-wide); not spec surface.
