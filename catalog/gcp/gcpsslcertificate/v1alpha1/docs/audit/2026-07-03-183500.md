# Audit Report: GcpSslCertificate

**Audit Date:** 2026-07-03 18:35:00
**Component Kind:** GcpSslCertificate
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpsslcertificate/v1alpha1/`
**Enum Value:** 712
**ID Prefix:** gcpsslcert

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpSslCertificate = 712` in the 710–719 networking/LB overflow block; `id_prefix` `gcpsslcert` unique (full-registry scan); no prerequisites (true leaf). Kind map regenerated.

### Protobuf API Definitions

- Full released 6.50.0 GA surface on plain `google ~> 6.0` (GA and beta released schemas verified identical): `project_id` ref → GcpProject, RFC1035 `certificate_name` (shared-namespace-with-managed-certs documented), dual-scope `region` (empty = global, set = regional — schemas field-for-field identical), immutable `description`, required PEM `certificate` chain, required PEM `private_key`.
- PEM-framing CEL on both material fields catches the classic swap mistakes pre-deploy (key pasted into the certificate field and vice versa); GCP validates cryptographic validity at deploy.
- `private_key` is `(sensitive)`; `certificate` deliberately is NOT — public handshake material presented to every client (the TF provider's sensitive flag on it is a plan-display concern, not a secrecy contract; documented in the field comment and docs).
- **Deliberately unmodeled:** `private_key_wo`/`private_key_wo_version` (absent from released 6.50.0), `name_prefix` (Planton metadata-driven naming owns names; rotation is a new resource with a versioned name), `deletion_policy` (catalog-wide skip). Recorded in `docs/README.md`.
- 20-case spec test suite green (PKCS#8/PKCS#1/SEC1 key framings, swap mistakes, missing material, name/region/description bounds, kind/api_version constants).

### IaC Modules — Pulumi

- `compute.SSLCertificate` / `compute.RegionSslCertificate` branch on `spec.region`; `private_key` wrapped in `pulumi.ToSecret` (encrypted in state); Compute API enablement with `disable_on_destroy=false`; ambient-project contract honored; `certificate_id` int → string; region output exports the plain spec region name; the key never appears in outputs.
- Release-equivalent build green.

### IaC Modules — Terraform

- Same two resources under count guards on plain `google ~> 6.0`; variable validations mirror the PEM-framing CEL; `tofu validate` + offline `planton tofu plan` through the real tfvars converter green (plan inspected: certificate + API service; both material fields render as sensitive values in the plan).

### Cross-Engine IaC Parity — PARITY ✅

- Field-by-field: name, project, description, certificate, private_key, region — identical send/omit semantics in both engines. Outputs identical keys and semantics (`self_link`, `certificate_name`, `certificate_id` stringified, `expire_time`, plain-name `region`); the private key surfaces in NEITHER engine's outputs. Zero PARITY-EXCEPTIONs.
- `validate-outputs` dry-runs fully populated against BOTH module dirs; `pkg/outputs` conformance case added.

### Secure-by-Default Coverage — COVERAGE ✅

- `private_key` `(sensitive)` — the kind's only secret; `secret-coverage --check` green.

### E2E

- Leaf scenario uploads a THROWAWAY self-signed keypair checked into the manifest (CN=e2e-test.invalid, 10-year expiry, trusted by nothing — clearly commented as test material, not a real secret). Live dual-engine: Pulumi 41.8s, Terraform 49.8s — both green, zero orphans. Verifier asserts existence AND a non-empty `expire_time` (GCP parsed real chain material). `e2e/prerequisite.yaml` published; consumed live by the target HTTPS proxy hardened-frontend scenario via explicit `valueFrom.kind`.
