# Audit Report: GcpSpannerInstance

**Audit Date:** 2026-07-05 09:33:50
**Component Kind:** GcpSpannerInstance
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpspannerinstance/v1alpha1/`
**Enum Value:** 633
**ID Prefix:** gcpspn

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

**Legend:** ✅ Complete | ⚠️ Partial | ❌ Missing

---

## Detailed Findings

### 1. Cloud Resource Registry (4.44%)

✅ **Passed:**

- Enum entry `GcpSpannerInstance = 633` in the GCP band (600–799), data sub-band
- `id_prefix: gcpspn` unique across the registry
- `kind_meta` carries provider, version, id_prefix

**Score:** 4.44% / 4.44%

---

### 2. Folder Structure (4.44%)

✅ **Passed:**

- `catalog/gcp/gcpspannerinstance/v1alpha1/` with the full anatomy

**Score:** 4.44% / 4.44%

---

### 3. Protobuf API Definitions (22.20%)

✅ **Passed:**

- Four protos present and substantial (`spec.proto` 14.8 KB with dense, LLM-grade field comments)
- All four `.pb.go` stubs regenerated
- Every CEL message is a complete user-facing sentence (capacity XOR, FREE_INSTANCE arms, autoscaling-limits unit coherence, asymmetric-override bounds)
- `spec_test.go`: 60 cases, all passing (`go test` green)

**Score:** 22.20% / 22.20%

---

### 4. IaC Modules - Pulumi (13.32%)

✅ **Passed:**

- `module/main.go`, `locals.go`, `outputs.go`, `spanner_instance.go` present
- Root `main.go` declares `package main`; no `main/`/`entrypoint/` subdir
- `Pulumi.yaml` canonical (runtime go, no `binary:` option); `Makefile` present
- Release-equivalent build passes (`go build -o /dev/null ./…/iac/pulumi`)
- Authoring comments explain intent (ambient-project contract, bridged-provider config path normalization, capacity semantics, force_destroy trade-off)

**Score:** 13.32% / 13.32%

---

### 5. IaC Modules - Terraform (4.24%)

✅ **Passed:**

- All five files present; `variables.tf` uses converter-contract plain-string refs
- `tofu validate` green; offline `planton tofu plan` through the real tfvars converter renders all fields correctly
- Authoring comments match the Pulumi module's bar

**Score:** 4.24% / 4.24%

---

### 5b. Cross-Engine IaC Parity (Critical GATE)

**Parity verdict: PARITY ✅**

Verified outputs with `planton validate-outputs` on BOTH module dirs (full 4-field population, zero unmapped). Live dual-engine E2E green (minimal + autoscaling on both engines).

| Dimension | Pulumi | Terraform | Verdict |
|-----------|--------|-----------|---------|
| Project / scope source | omit arg when spec empty → provider default | `null` fallback → provider default | ✅ MATCH |
| Resource naming basis | `instance_name` → `metadata.name` fallback | same | ✅ MATCH |
| Labels (keys + values) | user labels first, then `planton-ai_*` platform keys | identical literals, identical merge order | ✅ MATCH |
| Spec feature coverage | all 12 spec fields incl. asymmetric autoscaling | same | ✅ MATCH |
| Dependency ordering | `DependsOn(spanner API service)` | `depends_on` | ✅ MATCH |
| Stack outputs → proto | instance_id/instance_name/state/config (config path-stripped for the bridged provider's fully qualified form) | same shape from resolved attributes | ✅ MATCH |

**Effect on score:** none — engines in parity. The bridged provider's fully-qualified-config-path behavior is normalized in the Pulumi module with an explanatory comment (found live, fixed, re-proven live).

---

### 6–10. Documentation, Supporting Files, Presets

✅ **Passed:**

- `docs/README.md` (5.4 KB): provider floor table, recorded skips with reasons (`deletion_policy`, instance-config/partition Tier-2, IAM trios), immutability and composition sections
- `README.md` (4 KB) + `catalog-page.md` (6.4 KB), timeless, no history narration
- `iac/pulumi/README.md`, `iac/tf/README.md`, canonical `iac/hack/manifest.yaml` (validates)
- 3 preset pairs (free-instance / regional-production / autoscaling incl. asymmetric example), every YAML passes `planton validate`
- BUILD.bazel files regenerated via gazelle

---

### 11. Secure-by-Default Sensitive Coverage (Critical GATE)

**Coverage verdict: COVERAGE ✅**

`planton secret-coverage --check` green; zero `gaps[]` for `GcpSpannerInstance:` (the spec carries no secret-bearing fields — capacity, topology, and label configuration only).

---

## Critical Gaps

None.

## Validation Evidence

- Spec tests 60/60; release-equivalent Pulumi build; `tofu validate` + offline plan (fields inspected); `validate-refs --check`; `secret-coverage --check`; `validate-outputs` dry-runs 4/4 on BOTH module dirs; `pkg/outputs` conformance case green
- **Live dual-engine E2E green on `planton-e2e`**: minimal 34s/38s, autoscaling 43s/49s (Pulumi/Terraform) — zero orphans after teardown

**Audit completed at:** 2026-07-05-093350
**Reference:** `architecture/deployment-component.md`
