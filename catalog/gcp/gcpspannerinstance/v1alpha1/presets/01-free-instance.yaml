apiVersion: gcp.planton.dev/v1alpha1
kind: GcpSpannerInstance
metadata:
  name: dev-spanner
spec:
  # project_id omitted: falls back to the provider's default project.
  # Replace with a projectId value or valueFrom ref to target another project.
  config: regional-us-central1
  displayName: Dev Spanner
  instanceType: FREE_INSTANCE
