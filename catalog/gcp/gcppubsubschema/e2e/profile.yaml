apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcppubsubschema
spec:
  # Leaf scenario: the schema is a pure control-plane object — create,
  # posture-verify (type + definition), destroy in seconds.
  #
  # Live-proven 2026-08-12 on both engines under the apply-idempotency
  # gate (full lifecycle incl. deletion_policy DELETE echo clean); the
  # revision_id output's first live consumption is proven on the topic's
  # schema-validated scenario.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 20
