apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpcloudschedulerjob
spec:
  # Fast control-plane object. The pubsub-target scenario composes the
  # GcpPubSubTopic prerequisite live (topic FK resolution proof through
  # the fully qualified topic_id path); the http-target scenario composes
  # the GcpServiceAccount prerequisite (OIDC token FK).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
