apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpspannerdatabase
spec:
  # Composed scenario: deploys the GcpSpannerInstance prerequisite
  # (100 processing units) then creates a database on it. The scenario
  # sets deletionProtection: false so the ephemeral DESTROY can run.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]  # live-proven 2026-08-12: full lifecycle + apply-idempotency on both engines
  timeout_minutes: 30
