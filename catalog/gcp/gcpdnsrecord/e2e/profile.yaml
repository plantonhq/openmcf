apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpdnsrecord
spec:
  # A-record leaf on a chained public zone (~1–3 minutes).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
