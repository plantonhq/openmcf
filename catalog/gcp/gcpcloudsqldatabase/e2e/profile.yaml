apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpcloudsqldatabase
spec:
  # Composed scenario: deploys the GcpCloudSql prerequisite (which itself
  # pulls the service networking chain) then creates a database on it.
  tier: 1
  # Live-proven 2026-08-13 on both engines at the total-accounting spec:
  # minimal scenario (database on the public prerequisite instance +
  # explicit deletionPolicy DELETE) x 2 engines, all phases green under
  # the apply-idempotency gate.
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 30
