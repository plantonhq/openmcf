# Audit Report: GcpUrlMap

**Audit Date:** 2026-07-03 14:05:00
**Component Kind:** GcpUrlMap
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpurlmap/v1alpha1/`
**Enum Value:** 627
**ID Prefix:** gcpum

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpUrlMap = 627`; `id_prefix` `gcpum` unique; `prerequisites: [GcpBackendService]` drives composed E2E chain transitively through health check.

### Protobuf API Definitions

- Full released GA routing tree on plain `google ~> 6.0`: default target exclusivity, host rules, path matchers, path rules, route rules with match filters (incl. `path_template_match`), weighted backends, rewrites (incl. route-rules-only `path_template_rewrite`), redirects, header actions, CORS, fault injection, retry/timeout/mirror, custom error response policies, test blocks.
- **Deliberately unmodeled:** route-action `cache_policy` (exists on provider clone main and Pulumi SDK but absent from released TF 6.50.0 GA/beta — released-TF floor governs both engines). Regional URL map recorded as separate future kind.
- CEL enforces provider context divergences (default target exclusivity, `strip_query` requiredness, weighted-backend requiredness per context). `default_service` and path/route `service` are un-defaulted `StringValueOrRef` (multi-target: backend service OR backend bucket).
- Spec test suite green.

### IaC Modules (both engines)

- Shared nested messages reused across route-action contexts; both enable Compute API first; ambient-project contract.
- Release-equivalent Pulumi build, `tofu validate`, `planton validate-outputs` green.

### Cross-Engine IaC Parity (Category 5b gate)

Identical naming, full spec coverage at released floor, identical outputs (`self_link`, `url_map_name`, `map_id`, `fingerprint`). Conformance case in `pkg/outputs`. **PARITY ✅**

### Secure-by-Default (Category 11 gate)

No secret-bearing fields. **COVERAGE ✅**

### Supporting Files, Presets, Docs, E2E

- 3 presets (host/path fanout, weighted canary, apex redirect); composed E2E scenario with explicit `valueFrom.kind` on `default_service`; verifier registered.
