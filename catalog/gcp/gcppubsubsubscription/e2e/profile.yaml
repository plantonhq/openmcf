apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcppubsubsubscription
spec:
  # Fast control-plane object composed on the GcpPubSubTopic prerequisite
  # chain (schema -> topic -> subscription): the transitive-FK proof.
  tier: 1
  # The prior green predates the current spec shape (deletion_policy,
  # the ai_inference transform arm, resource-manager tags) — awaits
  # live re-proof.
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
