# Audit Report: GcpBackendBucket

**Audit Date:** 2026-07-03 10:35:00
**Component Kind:** GcpBackendBucket
**Provider:** gcp
**Component Path:** `catalog/gcp/gcpbackendbucket/v1alpha1/`
**Enum Value:** 624
**ID Prefix:** gcpbb

---

## Overall Completion Score

**Score: 100%**

```
██████████ 100% Complete
```

**Status:** Fully Complete

---

## Summary by Category

| Category | Weight | Score | Status |
|----------|--------|-------|--------|
| Cloud Resource Registry | 4.44% | 4.44% | ✅ |
| Folder Structure | 4.44% | 4.44% | ✅ |
| Protobuf API Definitions | 22.20% | 22.20% | ✅ |
| IaC Modules - Pulumi | 13.32% | 13.32% | ✅ |
| IaC Modules - Terraform | 4.24% | 4.24% | ✅ |
| Cross-Engine IaC Parity | gate | PARITY ✅ | ✅ |
| Documentation - Research | 13.18% | 13.18% | ✅ |
| Documentation - User-Facing | 13.09% | 13.09% | ✅ |
| Supporting Files | 10.09% | 10.09% | ✅ |
| Presets | 5.00% | 5.00% | ✅ |
| Nice to Have | 10.00% | 10.00% | ✅ |
| Secure-by-Default Coverage | gate | COVERAGE ✅ | ✅ |

---

## Critical Gaps

None.

---

## Detailed Findings

### Cloud Resource Registry

- Enum entry `GcpBackendBucket = 624` in the networking/LB sub-band; `id_prefix` `gcpbb` unique; `prerequisites: [GcpGcsBucket]` — the first LB-family kind with a registered prerequisite chain. Kind map regenerated.

### Protobuf API Definitions

- All four protos with dense comments. FK wiring: `bucket_name` → `GcpGcsBucket(status.outputs.bucket_id)` (the origin, mutable for blue/green swaps), `edge_security_policy` → `GcpCloudArmorPolicy(status.outputs.policy_self_link)`, `project_id` → `GcpProject`. The bucket-flavor `cdn_policy` is fully modeled (11 sub-fields incl. negative caching per-code TTLs, cache key policy, bypass headers).
- CEL enforces cache-mode/TTL coherence pre-deploy (USE_ORIGIN_HEADERS forbids TTLs; FORCE_CACHE_ALL forbids max_ttl — both of which GCP would otherwise silently strip), the CDN↔INTERNAL_MANAGED incompatibility, negative-caching code whitelist, and header-form validation. All messages user-facing quality.
- 20-case spec test green.

### IaC Modules (both engines)

- Both create the backend bucket plus one signed-URL-key resource per configured key (folded per the granularity rule: never FK-referenced, max 3, lifecycle owned by the bucket), enable the Compute Engine API first, apply the ambient-project contract, and treat TTL 0 as unset (API defaults). Pulumi marks key material secret in state (`pulumi.ToSecret`); neither engine surfaces key values in outputs. Provider pin `~> 6.0` + google-beta declared (surface is GA — verified identical in beta). Richly commented, incl. the Pulumi `queryStringWhitelists` naming quirk.
- Release-equivalent Pulumi build, `tofu validate`, and offline `planton tofu plan` green.

### Cross-Engine Parity (Category 5b gate)

Walked all parity dimensions: naming basis (`backend_bucket_name` → `metadata.name` fallback), full spec coverage both sides, identical `""`→absent and `0`→unset normalization, identical API-enablement dependency edge, identical outputs (`self_link`, `backend_bucket_name`, `bucket_name`). `planton validate-outputs` dry-run: 3/3 populated. Conformance case added and green. **No PARITY-EXCEPTION needed.** **PARITY ✅**

### Secure-by-Default (Category 11 gate)

`signed_url_keys[].key_value` annotated `(sensitive) = true` — anyone holding it can mint valid signed URLs. `planton secret-coverage --check` green. **COVERAGE ✅**

### Supporting Files, Presets, Docs

- `iac/hack/manifest.yaml` validates; 3 presets (CDN static assets, plain origin, signed-URL private CDN) with companion docs, all validating; README + catalog page + research doc complete, incl. recorded skip reasons (`deletion_policy`, `params.resource_manager_tags`, beta-only backend-bucket IAM trio).

### Live E2E

- Dual-engine composed scenario with a GcpGcsBucket prerequisite (bucket name carries `${E2E_RUN_ID}` — GCS names are globally unique across all of GCP): Pulumi 65s, Terraform 95s — prerequisite deploy → FK resolution → create → compute API verify (incl. origin wiring) → destroy → 404 verify → prerequisite teardown. Zero orphans confirmed by post-run `gcloud compute backend-buckets list` and `gcloud storage ls`.
