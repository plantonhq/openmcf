apiVersion: gcp.planton.dev/v1alpha1
kind: GcpManagedSslCertificate
metadata:
  name: example-com-cert
spec:
  projectId:
    value: "<gcp-project-id>"
  description: TLS for example.com apex and www
  domains:
    - example.com
    - www.example.com
