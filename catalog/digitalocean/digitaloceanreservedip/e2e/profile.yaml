apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceanreservedip
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. THREE scenarios: `minimal` (unassigned IPv4 -- the shape
  # that BILLS ~$5/month prorated while it exists; its destroy is the
  # billing stop and the zero-orphan sweep is doubly binding), `assigned`
  # (IPv4 inline assignment to a region-pinned droplet fixture -- the
  # provider's genuinely mutable droplet_id arm), and `ipv6-assigned`
  # (proves BOTH folded v6 resources: the reservation and the separate
  # assignment resource). The two assignment lanes each deploy one
  # region-pinned droplet fixture (~pennies/hour).
  # Watch items for the proof lane: the upstream v6 delete swallows
  # non-404 errors (inverted error check, verified in provider source), so
  # only the verifier's live absence check proves the destroy; v4
  # re-pointing/unassign are in-place updates the six-phase runner cannot
  # exercise (no update phase).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
