package module

const (
	// OpReservedIpAddress is the reserved address itself (the resource's
	// API identity).
	OpReservedIpAddress = "reserved_ip_address"
	// OpUrn is the reservation's DigitalOcean uniform resource name.
	OpUrn = "urn"
)
