apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceancontainerregistry
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. TWO scenarios: `minimal` (starter tier, no region --
  # DigitalOcean chooses) and `full` (basic tier + explicit region + minted
  # docker credentials: write access, 1-hour expiry). HARD CONSTRAINT for the
  # proof lane: a DigitalOcean account holds exactly ONE registry, so the two
  # scenarios (and the two engines) can never overlap -- run every lane
  # strictly serially, and any pre-existing registry in the test account
  # collides with all of them. Registry names are globally unique across ALL
  # DigitalOcean accounts, so scenario names carry ${E2E_RUN_ID}. Registries
  # create in seconds; the starter tier is free, basic is ~$5/mo prorated.
  # Watch on first live run: the blind import round-trip addresses the
  # registry by NAME, and the docker-credentials resource is EXCLUDED from
  # the round-trip (defective importer at the pin -- see the import map).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
