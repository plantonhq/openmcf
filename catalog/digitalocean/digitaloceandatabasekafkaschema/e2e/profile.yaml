apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceandatabasekafkaschema
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map with the importer exclusion, presets, README/catalog/GUIDE).
  # Live dual-engine proof has not yet run. ONE scenario: `minimal` IS the
  # kind's total surface (all four arguments required, all create-only) --
  # an Avro subject on the SHARED Kafka manifest-path fixture that lives in
  # the DigitalOceanDatabaseKafkaTopic kind's folder. The lane deploys TWO
  # clusters (the PostgreSQL registry prerequisite plus the Kafka fixture).
  # SERIALIZATION: the shared fixture means this kind's lanes and the
  # topic kind's lanes must run serially, never in parallel.
  # Import round-trip: EXCLUDED (recorded in the import map) -- the
  # upstream importer is broken at the pin (restores only cluster_id,
  # never the subject). Watch item for the proof lane: schema evolution is
  # destroy+recreate that DROPS all subject versions -- verify the destroy
  # phase leaves no registry residue.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 45
