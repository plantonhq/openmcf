package module

const (
	// OpClusterId is the UUID of the Kafka cluster whose registry holds
	// the subject.
	OpClusterId = "cluster_id"
	// OpSubjectName is the registered subject's name (its API identity).
	OpSubjectName = "subject_name"
)
