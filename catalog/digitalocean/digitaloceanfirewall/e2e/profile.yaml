apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceanfirewall
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. TWO scenarios: `minimal` is rules-rich with no targeting
  # (both directions, icmp without port_range, the "all" shorthand, a port
  # range, IPv4+IPv6 CIDRs); `full` proves droplet targeting through the
  # droplet_ids REFERENCE (the Droplet fixture rides the e2e-prerequisites
  # annotation and installs from that kind's minimal scenario, ~1 minute
  # and ~$0.009/hour) plus tag targeting and a rule-level tag source.
  # Deliberately not in the live lanes: rule-level source/destination
  # droplet ids (same reference class the top-level droplet_ids arm
  # proves), kubernetes ids (needs a DOKS fixture -- ~10-minute creates,
  # the slow class), and load balancer uids (needs an LB fixture; identical
  # SVOR wiring). Watch on first live run: firewall tags are expected to be
  # created implicitly (the dispositions ledger's recorded judgment) -- if
  # the API instead rejects an unknown tag, re-target the tag arms through
  # a pre-created tag and record the lesson. Firewalls are free API objects
  # that create in seconds; the fixture droplet costs pennies.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
