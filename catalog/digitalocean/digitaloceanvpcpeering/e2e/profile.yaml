apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceanvpcpeering
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. ONE scenario: `minimal` IS the kind's total create
  # surface (name + exactly two VPCs). It is the catalog's FIRST
  # dual-fixture scenario of one kind: vpc_1 rides the registry
  # prerequisite's install fixture, vpc_2 rides a scenario-declared extra
  # VPC instance -- watch the fixture resolution order on first live run.
  # The peering object is free; create/delete each wait up to 2 minutes
  # (provider-enforced timeouts), and the provider retries deletes on the
  # 403 DigitalOcean returns while a peering is still settling.
  # Watch item for the proof lane: renaming is the kind's only in-place
  # update and the six-phase runner has no update phase.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
