package module

const (
	// OpPeeringId is the peering connection's UUID (its API identity and
	// import id).
	OpPeeringId = "peering_id"
	// OpStatus is the peering's lifecycle status at apply time
	// (UPPERCASE, e.g. ACTIVE).
	OpStatus = "status"
)
