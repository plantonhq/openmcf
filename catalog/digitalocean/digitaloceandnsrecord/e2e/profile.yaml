apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceandnsrecord
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. THREE scenarios, all riding the DigitalOceanDnsZone
  # registry prerequisite (installed from that kind's minimal scenario):
  # `minimal` is an A record proving lifecycle + the domain reference;
  # `full` is an SRV record proving the priority/weight/port presence trio
  # and the trailing-dot value normalization the import tolerates; `caa`
  # proves the flags/tag class including the explicit-zero flags quirk.
  # Records are free API objects that create in seconds. Watch on first
  # live run: the blind import round-trip needs the TWO-part id
  # ("{domain},{record_id}" -- both stack outputs), and the record verifier
  # is the OutputsVerifier shape reading record_id + domain together.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
