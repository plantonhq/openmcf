# Provider-parity manifest: this kind's recorded judgment against the pinned
# digitalocean provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.
resources:
  digitalocean_droplet:
    mappings:
      - spec: spec.droplet_name
        arg: name
      # The VPC reference is a StringValueOrRef FK to the DigitalOceanVpc
      # kind.
      - spec: spec.vpc
        arg: vpc_uuid
      - spec: spec.enable_ipv6
        arg: ipv6
      - spec: spec.enable_backups
        arg: backups
      # The spec inverts the provider default: monitoring is on unless
      # disabled, so the module maps disable_monitoring = !monitoring.
      - spec: spec.disable_monitoring
        arg: monitoring
