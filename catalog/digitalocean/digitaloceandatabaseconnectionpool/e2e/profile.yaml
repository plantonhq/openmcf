apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceandatabaseconnectionpool
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. TWO scenarios on the PostgreSQL cluster registry
  # prerequisite: `minimal` is a transaction-mode pool authenticating as
  # the cluster's built-in doadmin user (no user fixture needed);
  # `inbound-user` omits the user, proving DigitalOcean's inbound-user pool
  # shape (clients bring their own credentials; the password output is
  # legitimately empty). Pool operations are seconds; the cluster fixture
  # (~5 min, ~$0.02/hour) dominates the lane time.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 35
