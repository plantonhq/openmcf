# Provider-parity manifest: this kind's recorded judgment against the pinned
# digitalocean provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. The nested maintenance_policy,
# control_plane_firewall, cluster_autoscaler_configuration, sso, and the
# nine addon-toggle surfaces all match by name, so six renames and one
# synthesized leaf are the whole story.
resources:
  digitalocean_kubernetes_cluster:
    mappings:
      - spec: spec.cluster_name
        arg: name
      - spec: spec.kubernetes_version
        arg: version
      # The VPC reference is a StringValueOrRef FK to the DigitalOceanVpc
      # kind.
      - spec: spec.vpc
        arg: vpc_uuid
      - spec: spec.highly_available
        arg: ha
      # The spec nests the cluster's inline default pool; its leaves mirror
      # the provider's node_pool block.
      - spec: spec.default_node_pool
        arg: node_pool
      # The provider names the repeated taint block in the singular.
      - spec: spec.default_node_pool.taints
        arg: node_pool.taint
    exclusions:
      - arg: node_pool.name
        reason: synthesized by the module -- the inline default pool has no independent identity of its own
