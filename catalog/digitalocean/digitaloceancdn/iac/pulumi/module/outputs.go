package module

const (
	// OpCdnId is the CDN endpoint's UUID (its API identity and import id).
	OpCdnId = "cdn_id"
	// OpEndpoint is the fully-qualified domain name the CDN serves content
	// from.
	OpEndpoint = "endpoint"
)
