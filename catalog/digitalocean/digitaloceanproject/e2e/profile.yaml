apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceanproject
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. TWO scenarios: `minimal` is a bare project proving the
  # lifecycle and the provider's purpose default; `full` adds description/
  # purpose/environment and membership by REFERENCE to a Droplet fixture's
  # urn output (annotation-composed; ~1 min create, ~$0.009/hour), whose
  # destroy proves the relocate-then-delete path -- the droplet must
  # survive in the account's default project, and the zero-orphan sweep
  # must look for it THERE. Deliberately not in the live lanes: is_default
  # (flipping the shared test account's default project mutates state
  # every other lane inherits -- the shared-live-state deferral class).
  # Projects are free API objects; the droplet fixture dominates lane time.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
