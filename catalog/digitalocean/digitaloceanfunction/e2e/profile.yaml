apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceanfunction
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests,
  # both engine builds, tofu validate green, checker-green parity
  # manifest, import map, presets, README/catalog/GUIDE). Live
  # dual-engine proof has not yet run. ONE scenario: `hello-world`
  # clones DigitalOcean's public sample-functions-nodejs-helloworld
  # repo over plain git (no GitHub connection, no deploy_on_push).
  # Runtime, memory, timeout, and schedules live in the repo's
  # project.yml -- they are not on the spec and must not be. Import
  # round-trips the digitalocean_app UUID via function_id (there is
  # no standalone Functions resource). Git-source App Platform
  # deploys run a real build; budget ~10 minutes per engine.
  # basic-xxs-equivalent functions billing is cents for a short lane.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 25
