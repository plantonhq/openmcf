apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceanvpc
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map re-verified, presets, README/catalog/GUIDE). Live dual-engine
  # proof has not yet run. TWO scenarios: `minimal` (region only -- proves
  # the DigitalOcean-assigned-range path and the ip_range output read-back)
  # and `full` (description + explicit /24). VPCs are free API objects
  # creating in seconds. NOTE: this kind's e2e/prerequisite.yaml is the
  # install fixture other kinds' lanes deploy (region nyc3, 10.60.0.0/24) --
  # it is deliberately separate from these scenarios and must keep its shape.
  # The provider serializes VPC creation per region internally, so parallel
  # lanes in one region queue up rather than fail. Watch on first live run:
  # minimal's DigitalOcean-assigned range must land in the ip_range output
  # with no plan diff (Optional+Computed read-back).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
