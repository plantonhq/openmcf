apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceankubernetesnodepool
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. TWO scenarios, both riding the registry prerequisite edge
  # to DigitalOceanKubernetesCluster (the cluster's minimal scenario is its
  # install profile, which itself composes the VPC fixture): `minimal` is a
  # fixed one-node pool on the cheapest slug; `full` proves autoscaling
  # bounds, node labels, a PreferNoSchedule taint, and pool tags.
  # Deliberately not in the live lanes: gpu_partition_mode (GPU sizes are
  # the expensive class AND a Pulumi SDK v4.49.0 gap -- Terraform wires it,
  # Pulumi fails loudly if set). The pool itself creates in a few minutes
  # and bills only its nodes (~$0.018/hour on s-1vcpu-2gb); the fixture
  # cluster dominates the lane's wall clock (~5-10 min create).
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 40
