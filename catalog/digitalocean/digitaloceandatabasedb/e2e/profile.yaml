apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: digitaloceandatabasedb
spec:
  # pending_proof: authored to the full offline bar (spec/CEL tests, both
  # engine builds, tofu validate green, checker-green parity manifest,
  # import map, presets, README/catalog/GUIDE). Live dual-engine proof has
  # not yet run. ONE scenario: `minimal` -- an additional logical database
  # on the PostgreSQL cluster registry prerequisite. The resource has
  # exactly two create-only arguments, so the minimal lane IS total
  # coverage. Database operations are seconds; the cluster fixture (~5 min,
  # ~$0.02/hour) dominates the lane time.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 35
