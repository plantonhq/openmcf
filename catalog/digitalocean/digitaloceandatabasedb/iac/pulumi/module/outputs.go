package module

const (
	// OpClusterId is the UUID of the cluster the logical database lives in.
	OpClusterId = "cluster_id"
	// OpDatabaseName is the logical database's name (its API identity).
	OpDatabaseName = "database_name"
)
