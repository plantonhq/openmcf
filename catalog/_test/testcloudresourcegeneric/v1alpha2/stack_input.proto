syntax = "proto3";

package dev.planton._test.testcloudresourcegeneric.v1alpha2;

import "catalog/_test/testcloudresourcegeneric/v1alpha2/api.proto";

// TestCloudResourceGenericStackInput at v1alpha2 -- provider-free on
// purpose, exactly like v1alpha1 (see that version's stack_input.proto).
message TestCloudResourceGenericStackInput {
  // The target test resource to "deploy".
  TestCloudResourceGeneric target = 1;
}
