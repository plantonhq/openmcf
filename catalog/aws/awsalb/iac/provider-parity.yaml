# Provider-parity manifest: this kind's recorded judgment against the pinned
# aws provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.
#
# aws_lb serves three load balancer types behind one schema. This kind IS
# the application type, so every network-only argument below is excluded
# with that reason -- the surface is modeled where it functions, on AwsNlb.
# Type applicability is read from the provider source at the pin (the
# create/update paths gate these attributes on the network type) and from
# AWS's own API contract (the SubnetMapping members are documented
# "[Network Load Balancers]"), never from documentation memory.

resources:
  aws_lb:
    mappings:
      # The spec says what each flag IS; the provider prefixes verbs.
      - spec: spec.delete_protection_enabled
        arg: enable_deletion_protection
      - spec: spec.http2_enabled
        arg: enable_http2
      - spec: spec.waf_fail_open_enabled
        arg: enable_waf_fail_open
      - spec: spec.zonal_shift_enabled
        arg: enable_zonal_shift
      - spec: spec.xff_client_port_enabled
        arg: enable_xff_client_port
      - spec: spec.tls_version_and_cipher_suite_headers_enabled
        arg: enable_tls_version_and_cipher_suite_headers
      # The spec carries the unit suffix the provider leaves implicit.
      - spec: spec.idle_timeout_seconds
        arg: idle_timeout
      - spec: spec.client_keep_alive_seconds
        arg: client_keep_alive
      # The one-field capacity-reservation block flattened to its unit count.
      - spec: spec.minimum_load_balancer_capacity_units
        arg: minimum_load_balancer_capacity.capacity_units
      # The one-field IPAM block flattened to its pool id.
      - spec: spec.ipv4_ipam_pool_id
        arg: ipam_pools.ipv4_ipam_pool_id
    exclusions:
      # Identity and type are the kind's own contract.
      - arg: load_balancer_type
        reason: fixed to "application" -- this kind IS the application load balancer
      - arg: name
        reason: derived from metadata.name (truncated deterministically to AWS's 32-char limit) -- never free-form user surface
      - arg: name_prefix
        reason: alternative naming form; names always derive from metadata.name
      # Placement: the spec models plain subnet lists (provider ExactlyOneOf
      # with subnet_mapping). For the application type the mapping form adds
      # nothing -- AWS's API contract scopes every SubnetMapping member
      # except subnet_id to network load balancers.
      - arg: subnet_mapping.subnet_id
        reason: alternative form to subnets (provider ExactlyOneOf); for application load balancers a mapping carries only the subnet id, which spec.subnets already expresses
      - arg: subnet_mapping.allocation_id
        reason: network-type only per AWS's API contract (EIP allocation for NLB nodes) -- modeled on AwsNlb
      - arg: subnet_mapping.ipv6_address
        reason: network-type only per AWS's API contract (per-node IPv6 address) -- modeled on AwsNlb
      - arg: subnet_mapping.private_ipv4_address
        reason: network-type only per AWS's API contract (per-node private IPv4 for internal NLBs) -- modeled on AwsNlb
      # Block-presence semantics.
      - arg: access_logs.enabled
        reason: derived by the module from the access_logs block's presence -- a declared block is an enabled block
      - arg: connection_logs.enabled
        reason: derived by the module from the connection_logs block's presence -- a declared block is an enabled block
      - arg: health_check_logs.enabled
        reason: derived by the module from the health_check_logs block's presence -- a declared block is an enabled block
      # Network-type-only surface (modeled on AwsNlb).
      - arg: dns_record_client_routing_policy
        reason: network-type only (NLB DNS zonal affinity) -- modeled on AwsNlb
      - arg: enable_cross_zone_load_balancing
        reason: network-type only (always on and immutable for application load balancers) -- modeled on AwsNlb
      - arg: enable_prefix_for_ipv6_source_nat
        reason: network-type only (UDP-listener IPv6 source NAT) -- modeled on AwsNlb
      - arg: enforce_security_group_inbound_rules_on_private_link_traffic
        reason: network-type only (PrivateLink inbound-rule enforcement) -- modeled on AwsNlb
      - arg: secondary_ips_auto_assigned_per_subnet
        reason: network-type only (NLB node secondary IPs) -- modeled on AwsNlb
      # Hardware-tied surface.
      - arg: customer_owned_ipv4_pool
        reason: Outposts-only surface -- requires a physical Outposts rack; deliberately unmodeled
      # Tag machinery.
      - arg: tags
        reason: wired by the module from metadata (the Name tag plus the planton.ai identity keys) -- never free-form user surface
      - arg: tags_all
        reason: provider-side merge of resource and provider-default tags -- reflects tags, carries no configuration of its own

  aws_route53_record:
    # Instantiated per spec.dns.hostnames entry: a deliberately narrow
    # alias-A convenience helper pointing hostnames at the ALB. The full
    # Route53 record surface (routing policies, TTLs, record values) is
    # modeled by the AwsRoute53DnsRecord kind -- this helper stays narrow on
    # purpose.
    specRoot: spec.dns
    mappings:
      - spec: spec.dns.route53_zone_id
        arg: zone_id
      - spec: spec.dns.hostnames
        arg: name
    exclusions:
      - arg: type
        reason: fixed to "A" -- the helper creates alias A records only
      - arg: alias.name
        reason: wired by the module to the ALB's dns_name -- never user surface
      - arg: alias.zone_id
        reason: wired by the module to the ALB's canonical hosted zone id -- never user surface
      - arg: alias.evaluate_target_health
        reason: fixed false -- health evaluation only changes behavior under failover/weighted policies, which this helper deliberately omits
      - arg: allow_overwrite
        reason: fixed true -- the module owns the alias record and adopts one left by a prior partial apply instead of failing the create
      - arg: ttl
        reason: alias records carry no TTL (Route53 derives it from the target) -- full record surface belongs to AwsRoute53DnsRecord
      - arg: records
        reason: alias records carry no record values -- full record surface belongs to AwsRoute53DnsRecord
      - arg: health_check_id
        reason: routing-policy surface -- the helper is a plain alias; full record surface belongs to AwsRoute53DnsRecord
      - arg: set_identifier
        reason: routing-policy surface -- the helper is a plain alias; full record surface belongs to AwsRoute53DnsRecord
      - arg: multivalue_answer_routing_policy
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: failover_routing_policy.type
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: latency_routing_policy.region
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: weighted_routing_policy.weight
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: cidr_routing_policy.collection_id
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: cidr_routing_policy.location_name
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geolocation_routing_policy.continent
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geolocation_routing_policy.country
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geolocation_routing_policy.subdivision
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geoproximity_routing_policy.aws_region
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geoproximity_routing_policy.bias
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geoproximity_routing_policy.local_zone_group
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geoproximity_routing_policy.coordinates.latitude
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord
      - arg: geoproximity_routing_policy.coordinates.longitude
        reason: routing-policy surface -- full record surface belongs to AwsRoute53DnsRecord

  aws_wafv2_web_acl_association:
    # One optional association: the binding of the ALB to its (at most one)
    # WAFv2 web ACL, a setting of the protected resource.
    exclusions:
      - arg: resource_arn
        reason: wired by the module to the ALB it creates -- never user surface

specExclusions:
  - field: spec.dns.enabled
    reason: gates whether the alias records are created at all -- a module switch, not a provider argument
