# Account-Wide PII Masking

Data protection for every log group in the region: emails and card numbers are audited and masked at ingest, so a leaked debug line never stores plaintext PII. Add `selectionCriteria` to scope it while rolling out.
