# Field Indexing for App Logs

Index the fields your Logs Insights queries filter on (`requestId`, `customerId`, `sourceIp`) across every log group in the region — indexed filters scan less and cost less. Free to define; savings show up on query bills. Field-index policies always apply account-wide: AWS accepts selection criteria only on subscription-filter policies.
