# Field Indexing for App Logs

Index the fields your Logs Insights queries filter on (`requestId`, `customerId`, `sourceIp`) across every `/app*` log group — indexed filters scan less and cost less. Free to define; savings show up on query bills.
