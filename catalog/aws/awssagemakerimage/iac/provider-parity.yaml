# Provider-parity manifest: this kind's recorded judgment against the pinned
# aws provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  aws_sagemaker_image:
    exclusions:
      - arg: image_name
        reason: wired by the module from metadata.name -- the component's name IS the image name
      - arg: tags
        reason: wired by the module from metadata (the canonical identity tag map) -- never free-form user surface
      - arg: tags_all
        reason: provider-side merge of resource and provider-default tags -- reflects tags, carries no configuration of its own

  aws_sagemaker_image_version:
    specRoot: spec.versions
    mappings:
      # v6 per-resource region override -- the module wires every
      # resource to spec.region.
      - arg: region
        spec: spec.region
    exclusions:
      - arg: image_name
        reason: wired by the module to the folded parent image; never user surface
