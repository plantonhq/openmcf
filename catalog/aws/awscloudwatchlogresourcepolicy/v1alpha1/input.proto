syntax = "proto3";

package dev.planton.aws.awscloudwatchlogresourcepolicy.v1alpha1;

import "catalog/aws/awscloudwatchlogresourcepolicy/v1alpha1/api.proto";
import "catalog/aws/provider.proto";

// AwsCloudwatchLogResourcePolicyStackInput is the input for the IaC
// modules that manage a CloudWatch Logs resource policy.
message AwsCloudwatchLogResourcePolicyStackInput {
  // target is the AwsCloudwatchLogResourcePolicy resource to deploy.
  AwsCloudwatchLogResourcePolicy target = 1;
  // provider_config contains the AWS provider credentials and configuration.
  dev.planton.aws.AwsProviderConfig provider_config = 2;
}
