# EventBridge Log Delivery Grant

Lets EventBridge rules target CloudWatch log groups under `/aws/events/*` — the grant the "target a log group" feature silently requires. Both principals cover the direct and vended delivery paths.
