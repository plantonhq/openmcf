# Route53 Query Logging Grant

The grant Route53 needs before zone query logging works (query logs only land in us-east-1 — deploy this policy there). One policy covers every zone logging under `/aws/route53/*`.
