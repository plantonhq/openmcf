# Advanced Threat Detection (Alert First)

DNS Firewall Advanced heuristics — algorithmically generated domains and DNS tunneling — in ALERT mode. Run alongside query logging until the hit pattern is understood, then flip actions to BLOCK.
