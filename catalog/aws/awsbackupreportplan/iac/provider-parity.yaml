# Provider-parity manifest: this kind's recorded judgment against the pinned
# aws provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  aws_backup_report_plan:
    mappings:
      # v6 per-resource region override -- the module wires every resource
      # to spec.region.
      - arg: region
        spec: spec.region
      # AWS report plan names forbid hyphens (stricter than metadata.name
      # conventions), so the name is an explicit spec field.
      - arg: name
        spec: spec.report_plan_name
      # The provider's report_delivery_channel block is the spec's
      # delivery_channel (the "report_" prefix is redundant inside the
      # kind).
      - arg: report_delivery_channel.s3_bucket_name
        spec: spec.delivery_channel.s3_bucket_name
      - arg: report_delivery_channel.s3_key_prefix
        spec: spec.delivery_channel.s3_key_prefix
      - arg: report_delivery_channel.formats
        spec: spec.delivery_channel.formats
    exclusions:
      - arg: tags
        reason: wired by the module from metadata (the canonical identity tag map) -- never free-form user surface
      - arg: tags_all
        reason: provider-side merge of resource and provider-default tags -- reflects tags, carries no configuration of its own
