apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: awssagemakermodelregistry
spec:
  # A model package group under live E2E: two free, seconds-scale lanes
  # (create waits Completed; delete waits gone - both ~1 min).
  # Verification is DescribeModelPackageGroup keyed on
  # model_package_group_name (status Completed) plus the description
  # echo on the full lane.
  #
  # Deliberately not exercised live: the resource-policy arm (a valid
  # policy document needs a real account principal - unblock: an
  # account-id token in the harness's E2E_ENV vocabulary, then extend
  # the full-surface scenario and assert GetModelPackageGroupPolicy) -
  # offline-proven via e2e/manifest.yaml.
  #
  # pending_proof: forged this session to the full offline bar -- Q36
  # proves it live.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
