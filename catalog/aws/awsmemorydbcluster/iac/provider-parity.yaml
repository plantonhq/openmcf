# Provider-parity manifest: this kind's recorded judgment against the pinned
# aws provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  aws_memorydb_cluster:
    exclusions:
      - arg: name
        reason: wired by the module from metadata.name -- the cluster name is platform naming, never free-form spec surface
      - arg: name_prefix
        reason: provider auto-naming (random suffix) -- unused by design; the cluster name is metadata.name on both engines
      - arg: tags
        reason: wired by the module from metadata (user labels merged beneath the Name tag and planton.ai identity keys) -- never free-form user surface
      - arg: tags_all
        reason: provider-side merge of resource and provider-default tags -- reflects tags, carries no configuration of its own

  aws_memorydb_parameter_group:
    # The managed group for inline spec.parameters -- pure glue (a named
    # parameter list), created only when the spec brings parameters. The
    # family stays a spec field: MemoryDB parameter families are chosen by
    # the user alongside the engine/version pin.
    mappings:
      # The provider nests entries under a "parameter" block; the spec IS
      # that list.
      - spec: spec.parameters
        arg: parameter
      - spec: spec.parameter_group_family
        arg: family
    exclusions:
      - arg: name
        reason: wired by the module ("<cluster>-params") -- the group is owned by exactly one cluster
      - arg: name_prefix
        reason: provider auto-naming -- unused by design; the group name derives from the cluster's
      - arg: description
        reason: wired by the module (a fixed management notice) -- carries no configuration
      - arg: tags
        reason: wired by the module from metadata -- never free-form user surface
      - arg: tags_all
        reason: provider-side merge of resource and provider-default tags -- reflects tags, carries no configuration of its own

  aws_memorydb_subnet_group:
    # The subnet group is pure glue (a named list of subnets), managed only
    # when the spec brings raw subnet_ids; subnet_ids exact-matches
    # spec.subnet_ids under the default root.
    exclusions:
      - arg: name
        reason: wired by the module from metadata.name -- the group is owned by exactly one cluster
      - arg: name_prefix
        reason: provider auto-naming -- unused by design; the group name is the cluster's
      - arg: description
        reason: wired by the module (a fixed management notice) -- carries no configuration
      - arg: tags
        reason: wired by the module from metadata -- never free-form user surface
      - arg: tags_all
        reason: provider-side merge of resource and provider-default tags -- reflects tags, carries no configuration of its own
