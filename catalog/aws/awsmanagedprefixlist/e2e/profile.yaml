apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: awsmanagedprefixlist
spec:
  # A standalone IPv4 list with three described entries - no fixtures,
  # free at AWS, seconds per lifecycle phase.
  #
  # Deliberately not exercised live:
  #   - IPv6 lists -- same API shape as IPv4 with a different family
  #     enum; the spec's family-match CEL is the load-bearing guard.
  #     Unblock: a second scenario with an IPv6 CIDR set.
  #
  # PROVEN live 2026-08-25 (Q45): both engines through the full
  # six-phase runner + the Terraform blind import round-trip under
  # the armed apply-idempotency gate.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 15
