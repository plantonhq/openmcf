# Office Networks

The canonical use: every office and VPN egress range in one described list, capacity 10 for growth. Reference the `pl-` id from bastion and dashboard security groups — when an office moves, edit one entry here and every rule follows.
