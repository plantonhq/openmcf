# Python Shared Utilities

The everyday layer: your team's common Python code (logging setup, tracing, config loaders) published once and attached by every function. Both architectures declared so Graviton and x86 functions filter it correctly in the console. Point `code.bucket` at your artifact bucket and let the pipeline stage the zip.
