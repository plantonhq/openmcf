# Local Zone Edge Cache

Object storage in a metro Local Zone (Los Angeles here) for media and rendering workloads that need single-digit latency to on-prem or edge compute. Requires the Local Zone opt-in on the account; the redundancy class names the zone type explicitly.
