# Provider-parity manifest: this kind's recorded judgment against the pinned
# aws provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  aws_s3_directory_bucket:
    mappings:
      # v6 per-resource region override -- the module wires every resource
      # to spec.region.
      - arg: region
        spec: spec.region
      # The location block carries the zone identity.
      - arg: location.name
        spec: spec.zone_id
      - arg: location.type
        spec: spec.zone_type
    exclusions:
      - arg: bucket
        reason: DERIVED by the modules as "{metadata.name}--{zone_id}--x-s3" (the AWS-mandated format) -- a hand-assembled spec field could disagree with the location block
      - arg: type
        reason: module-pinned to Directory -- the provider's BucketType enum holds exactly that one value
      - arg: tags
        reason: wired by the module from metadata (the canonical identity tag map) -- never free-form user surface
