# Database Data Volume

An encrypted 200 GiB gp3 with tuned IOPS and throughput, attached at `/dev/sdf` — the self-managed-database baseline. The volume outlives the instance: replace the EC2 host and re-attach the same disk.
