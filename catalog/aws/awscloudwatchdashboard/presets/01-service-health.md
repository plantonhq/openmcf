# Service Health Dashboard

The incident-first view for one service: a markdown header plus error-count and p95-latency graphs side by side. Point the metric dimensions at your service (the example charts a Lambda function) and add rows as the service grows — every edit applies in place.
