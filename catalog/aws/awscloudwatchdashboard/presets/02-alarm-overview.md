# Alarm Overview Dashboard

The on-call landing page: an alarm-status strip across the top (wire in your AwsCloudwatchAlarm ARNs) and a Logs Insights error tail beneath it. Swap the log group and query for your service's error signature.
