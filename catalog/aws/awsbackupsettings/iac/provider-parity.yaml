# Provider-parity manifest: this kind's recorded judgment against the pinned
# aws provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  aws_backup_global_settings:
    mappings:
      # The account-wide arm nests the provider's one map under
      # spec.global.
      - arg: global_settings
        spec: spec.global.settings
  aws_backup_region_settings:
    mappings:
      # v6 per-resource region override -- the module wires every resource
      # to spec.region.
      - arg: region
        spec: spec.region
      - arg: resource_type_opt_in_preference
        spec: spec.region_settings.resource_type_opt_in_preference
      - arg: resource_type_management_preference
        spec: spec.region_settings.resource_type_management_preference
