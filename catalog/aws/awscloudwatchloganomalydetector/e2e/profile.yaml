apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: awscloudwatchloganomalydetector
spec:
  # A detector over the log-group fixture: enabled, five-minute
  # cadence, the 7-day visibility boundary. The fixture group carries
  # zero traffic so per-GB analysis bills nothing (~$0).
  #
  # Deliberately not exercised live:
  #   - kms_key_id -- a replace-on-change encryption pin; the default
  #     AWS-owned encryption proves the lifecycle. Unblock: an
  #     AwsKmsKey fixture via scenario annotations.
  #
  # PROVEN live 2026-08-25 (Q44): both engines through the full
  # six-phase runner + the Terraform blind import round-trip under the
  # armed apply-idempotency gate; first-pass green, zero defects.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 20
