# Encrypted Multi-Index Store

One KMS-encrypted bucket, two models: full-size document embeddings (1024) and compact code embeddings (256) as separate indexes — different dimensions are ALWAYS different indexes. Encryption is fixed for life; the key is chosen here, once.
