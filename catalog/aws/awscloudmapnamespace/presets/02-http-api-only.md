# HTTP (API-Only) Namespace

Discovery without DNS: consumers call DiscoverInstances with the namespace's `http_name` and filter on custom attributes. The shape for worker fleets and anything that should never be resolvable.
