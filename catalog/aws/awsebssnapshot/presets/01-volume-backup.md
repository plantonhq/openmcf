# Volume Backup

The pre-change checkpoint: snapshot the data volume before an upgrade or migration, restore in minutes if it goes sideways. Reference an AwsEbsVolume's `volume_id` output to wire it into a chart.
