# Consumer-scoped AwsIamRole install profile for the fixture recorder
# the framework evaluates under (see awsconfigrecorder.yaml).
apiVersion: aws.planton.dev/v1alpha1
kind: AwsIamRole
metadata:
  name: planton-oss-e2e-awsbkf-role
  id: planton-oss-e2e-awsbkf-role
  org: planton-oss
  env: e2e
  labels:
    managed-by: planton-e2e
    e2e-component: awsiamrole
  annotations:
    planton.dev/e2e: "true"
  tags:
    - planton-e2e
spec:
  region: us-east-1
  description: Planton E2E role AWS Config assumes for the framework kind's fixture recorder
  managedPolicyArns:
    - value: arn:aws:iam::aws:policy/service-role/AWS_ConfigRole
  inlinePolicies:
    configBucketDelivery:
      Version: "2012-10-17"
      Statement:
        - Effect: Allow
          Action:
            - s3:PutObject
          Resource: "arn:aws:s3:::planton-oss-e2e-awsbkf-history/*"
          Condition:
            StringEquals:
              s3:x-amz-acl: bucket-owner-full-control
        - Effect: Allow
          Action:
            - s3:GetBucketAcl
          Resource: "arn:aws:s3:::planton-oss-e2e-awsbkf-history"
  trustPolicy:
    Version: "2012-10-17"
    Statement:
      - Effect: Allow
        Principal:
          Service: config.amazonaws.com
        Action: sts:AssumeRole
