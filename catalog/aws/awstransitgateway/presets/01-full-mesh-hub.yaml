apiVersion: aws.planton.dev/v1alpha1
kind: AwsTransitGateway
metadata:
  org: example-org
  env: prod
  name: full-mesh-hub
  id: full-mesh-hub-id
spec:
  region: <aws-region>
  description: Full-mesh hub - every attachment reaches every other attachment
