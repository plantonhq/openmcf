apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: awsssmpatchbaseline
spec:
  # A patch baseline under live E2E on ROCKY_LINUX - deliberately an
  # OS with no managed nodes in the lane account, so the full surface
  # (both approval-arm styles incl. approve_after_days=0, global
  # filter, patch lists, BLOCK action, a governed patch group, and the
  # DEFAULT DESIGNATION) exercises real API paths while touching
  # nothing that patches. Destroy proves the designation's
  # restore-AWS-default revert. ~$0: baselines/groups/designations are
  # free.
  #
  # Deliberately not exercised live:
  #   - sources (alternative repos) -- Rocky consumes yum repo
  #     definitions AWS stores verbatim; the canonical manifest renders
  #     the arm offline. Unblock: rides any future full-surface re-run
  #     (storing a syntactically valid unreachable repo is safe).
  #   - available_security_updates_compliance_status -- Windows Server
  #     managed nodes only, per AWS. Unblock: a WINDOWS-baseline lane.
  #
  # pending_proof: forged this session to the full offline bar -- Q42
  # proves it live.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
