apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: awsnetworkacl
spec:
  # A subnet firewall on the VPC/subnet fixtures: deny + allow rules in
  # both directions (named AND numeric protocols - the normalization
  # assertion), an ICMP rule, one subnet association. Free at AWS.
  #
  # Deliberately not exercised live:
  #   - IPv6 rules -- the fixture VPC carries no IPv6 block. Unblock: an
  #     IPv6-enabled VPC fixture.
  #
  # PROVEN live 2026-08-25 (Q45): both engines through the full
  # six-phase runner + the Terraform blind import round-trip under
  # the armed apply-idempotency gate.
  tier: 1
  status: green
  validated_provisioners: [pulumi, terraform]
  timeout_minutes: 20
