apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: awsinternetgateway
spec:
  # Composed AWS topology under live E2E: the harness deploys a (gateway-free)
  # AwsVpc prerequisite, resolves the gateway's vpc_id from its outputs, then runs
  # deploy -> DescribeInternetGateways verify -> destroy -> verify-destroyed on
  # both provisioners against the AWS test account via keyless SSO/OIDC credentials.
  tier: 1
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 10
  # 2026-08-13 tag-convergence sweep: the Pulumi identity-tag map gained the
  # canonical Name key, converging on the key set the Terraform module (and
  # this badge's live proof) already sends. Declared-set equality is now
  # machine-enforced by hack/guards/ensure_cross_engine_tag_keys.sh; the
  # change class is live-re-proven once via Q31 (awssqsqueue represents it).
