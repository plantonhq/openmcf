# Provider-parity manifest: this kind's recorded judgment against the pinned
# aws provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  aws_kinesis_stream_consumer:
    exclusions:
      - arg: name
        reason: wired by the module from metadata.name -- the consumer name is platform naming, never free-form spec surface
      - arg: tags
        reason: wired by the module from metadata (planton.ai identity keys) -- never free-form user surface
      - arg: tags_all
        reason: provider-side merge of resource and provider-default tags -- reflects tags, carries no configuration of its own

  # The folded resource policy -- AWS models it as a separate resource-policy
  # API keyed by the consumer ARN; it has no identity of its own and follows
  # the consumer's lifecycle. (The stream itself carries its own
  # resource_policy for stream-level grants.)
  aws_kinesis_resource_policy:
    mappings:
      - spec: spec.resource_policy
        arg: policy
    exclusions:
      - arg: resource_arn
        reason: wired by the module to the ARN of the consumer this kind creates -- never free-form user surface
