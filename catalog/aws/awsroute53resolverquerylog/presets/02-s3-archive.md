# S3 Archive Destination

Compliance-grade retention at S3 prices, queryable with Athena. The bucket needs the documented resolver-logging bucket policy — an association against an unwritable bucket FAILS asynchronously after a clean apply.
