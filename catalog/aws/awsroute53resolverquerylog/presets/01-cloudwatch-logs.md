# CloudWatch Logs Destination

Interactive DNS visibility: queries land in a CloudWatch log group for Logs Insights and alarms. Same-account log groups need no extra permissions. Watch the volume — busy VPCs generate serious query traffic.
