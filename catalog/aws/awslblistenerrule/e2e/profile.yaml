apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: awslblistenerrule
spec:
  # A listener rule under live E2E rides the full family chain: the harness
  # deploys AwsVpc -> two AwsSubnets -> AwsAlb -> AwsLbTargetGroup ->
  # AwsLbListener, then runs deploy -> DescribeRules verify -> destroy ->
  # verify-destroyed on both provisioners. The rule itself creates in
  # seconds; the chain dominates the runtime.
  tier: 1
  status: green
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 45
  # 2026-08-13 tag-convergence sweep: the Pulumi identity-tag map gained the
  # canonical Name key, converging on the key set the Terraform module (and
  # this badge's live proof) already sends. Declared-set equality is now
  # machine-enforced by hack/guards/ensure_cross_engine_tag_keys.sh; the
  # change class is live-re-proven once via Q31 (awssqsqueue represents it).
