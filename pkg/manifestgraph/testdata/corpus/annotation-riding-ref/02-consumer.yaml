apiVersion: _test.planton.dev/v1alpha2
kind: TestCloudResourceGeneric
metadata:
  name: consumer
  env: dev
spec:
  requiredRef:
    value: literal
  annotatedRef:
    valueFrom:
      name: producer
