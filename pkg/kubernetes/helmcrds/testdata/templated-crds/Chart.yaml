apiVersion: v2
name: templated-crds
description: Test fixture. A chart that TEMPLATES its CRDs release-owned behind a switch, with release-derived values inside them (the OpenTelemetry-operator shape).
type: application
version: 1.2.3
appVersion: "1.0.0"
