# =============================================================================
# Content Distribution Release (Reusable Workflow)
# =============================================================================
# Packages Planton content into six zips and uploads them to Cloudflare R2
# (downloads.planton.dev). These zips decouple downstream consumers (e.g. the
# Planton upgrade-planton script) from needing a local Planton checkout.
#
# Every artifact is built by its OWN job so the Actions UI shows exactly what
# a release ships -- the job list IS the artifact inventory. The five
# source-file zips share one matrix job (each entry displayed by artifact
# name); the catalog bundle has a dedicated job because it is a compiled,
# verified artifact with its own toolchain (Go + buf), not a file collection.
#
# Artifacts created:
#   presets.zip         -- Preset YAML + MD, kind enum proto
#   iac-source.zip      -- IaC module source (.go, .tf, .md, .yaml)
#   catalog-pages.zip   -- Per-component catalog-page.md
#   proto-source.zip    -- Raw proto source (spec, api, stack_input, stack_outputs)
#   reference-pack.zip  -- Component reference pack (generated reference pages,
#                          indexes, cross-reference graph, commons, GUIDE.md /
#                          patterns wisdom layer)
#   catalog-bundle.zip  -- The catalog as DATA: buf-built descriptor set
#                          (schemas + validation rules + kind registry),
#                          conversion specs, presets, checksummed manifest.
#                          Verified against the compiled registry BEFORE
#                          upload; a non-conformant bundle fails the release.
#
# Upload path: s3://downloads-dot-planton-dot-dev-bucket/releases/{tag}/content/{name}.zip
# Public URL:  https://downloads.planton.dev/releases/{tag}/content/{name}.zip
#
# Called by: .github/workflows/release.yaml (after CLI release creates the release)
# =============================================================================

name: release.content

on:
  workflow_call:
    secrets:
      CLOUDFLARE_R2_ACCESS_KEY_ID:
        required: true
      CLOUDFLARE_R2_SECRET_ACCESS_KEY:
        required: true
      CLOUDFLARE_R2_ENDPOINT:
        required: true

permissions:
  contents: read

env:
  R2_BUCKET: downloads-dot-planton-dot-dev-bucket

jobs:
  # One matrix entry per source-file zip, displayed by artifact name.
  # fail-fast is off: the artifacts are independent, so one broken selector
  # must not cancel the other uploads.
  zip:
    name: ${{ matrix.zip }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        zip:
          - presets
          - iac-source
          - catalog-pages
          - proto-source
          - reference-pack
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Package ${{ matrix.zip }}.zip
        run: bash tools/ci/release/package_content.sh "${GITHUB_REF_NAME}" "${{ matrix.zip }}"

      - name: Upload to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.CLOUDFLARE_R2_ENDPOINT }}
        run: |
          R2_KEY="releases/${GITHUB_REF_NAME}/content/${{ matrix.zip }}.zip"
          aws s3 cp "${{ matrix.zip }}.zip" "s3://${R2_BUCKET}/${R2_KEY}" \
            --endpoint-url "${AWS_ENDPOINT_URL}"
          echo "uploaded: https://downloads.planton.dev/${R2_KEY}"

  catalog-bundle:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Setup buf
        uses: bufbuild/buf-action@v1
        with:
          setup_only: true

      - name: Build and verify the catalog bundle
        run: |
          make build-catalog-bundle tag="${GITHUB_REF_NAME}"
          make verify-catalog-bundle

      - name: Upload to R2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.CLOUDFLARE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.CLOUDFLARE_R2_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.CLOUDFLARE_R2_ENDPOINT }}
        run: |
          R2_KEY="releases/${GITHUB_REF_NAME}/content/catalog-bundle.zip"
          aws s3 cp "build/catalog-bundle.zip" "s3://${R2_BUCKET}/${R2_KEY}" \
            --endpoint-url "${AWS_ENDPOINT_URL}"
          echo "uploaded: https://downloads.planton.dev/${R2_KEY}"
