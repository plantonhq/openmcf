apiVersion: kubernetes.planton.dev/v1
kind: KubernetesCertManager
metadata:
  name: cert-manager
spec:
  namespace:
    value: cert-manager
  createNamespace: true
  workloadIdentity:
    gke:
      serviceAccountEmail:
        value: "<gsa-name>@<project-id>.iam.gserviceaccount.com"
