apiVersion: kubernetes.planton.dev/v1
kind: KubernetesCertManager
metadata:
  name: cert-manager
spec:
  namespace:
    value: cert-manager
  createNamespace: true
  workloadIdentity:
    eks:
      roleArn:
        value: "arn:aws:iam::<account-id>:role/<cert-manager-dns01-role>"
