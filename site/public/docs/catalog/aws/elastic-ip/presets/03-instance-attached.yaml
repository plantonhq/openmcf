apiVersion: aws.planton.dev/v1alpha1
kind: AwsElasticIp
metadata:
  name: ${name}
spec:
  region: <aws-region>
  instance:
    valueFrom:
      kind: AwsEc2Instance
      name: <ec2-instance-name>
      fieldPath: status.outputs.instance_id
