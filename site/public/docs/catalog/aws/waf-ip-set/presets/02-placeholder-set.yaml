apiVersion: aws.planton.dev/v1
kind: AwsWafIpSet
metadata:
  org: example-org
  env: prod
  name: partner-allowlist
  id: partner-allowlist-id
spec:
  region: <aws-region>
  scope: REGIONAL
  ipAddressVersion: IPV4
  addresses: []
  description: Partner integration ranges — populate before go-live
