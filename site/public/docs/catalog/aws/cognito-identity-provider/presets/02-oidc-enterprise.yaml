apiVersion: aws.planton.dev/v1
kind: AwsCognitoIdentityProvider
metadata:
  name: ${name}
  org: ${org}
  env: ${env}
  id: ${id}
  labels:
    planton.dev/provisioner: pulumi
    planton.dev/stack: ${env}.AwsCognitoIdentityProvider.${name}
spec:
  region: <aws-region>
  userPoolId:
    valueFrom:
      kind: AwsCognitoUserPool
      name: <user-pool-name>
      fieldPath: status.outputs.user_pool_id
  providerName: CorpSSO
  providerType: OIDC
  oidc:
    clientId: <oidc-client-id>
    clientSecret: <oidc-client-secret>
    oidcIssuer: https://sso.example.com
    authorizeScopes: "openid email profile"
  attributeMapping:
    email: email
    username: sub
    given_name: given_name
    family_name: family_name
