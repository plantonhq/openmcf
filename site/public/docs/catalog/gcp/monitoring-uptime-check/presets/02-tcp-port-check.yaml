apiVersion: gcp.planton.dev/v1alpha1
kind: GcpMonitoringUptimeCheck
metadata:
  name: broker-tcp
spec:
  timeout: 10s
  period: 60s
  monitoredResource:
    type: uptime_url
    labels:
      host: broker.example.com
  tcpCheck:
    port: 5672
