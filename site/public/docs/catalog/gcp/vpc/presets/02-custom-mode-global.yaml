apiVersion: gcp.planton.dev/v1
kind: GcpVpcNetwork
metadata:
  name: my-global-vpc
spec:
  projectId:
    value: my-gcp-project-123
  networkName: my-global-vpc
  autoCreateSubnetworks: false  # custom mode
  routingMode: GLOBAL  # Cloud Routers advertise routes across all regions
