apiVersion: azure.planton.dev/v1
kind: AzurePostgresqlFlexibleServer
metadata:
  name: dev-postgres
spec:
  region: eastus
  resourceGroup:
    value: <resource-group-name>
  # Replace: lowercase letters, digits, and hyphens, globally unique -- it
  # becomes {name}.postgres.database.azure.com.
  serverName: myorg-dev-postgres
  administratorLogin: <admin-login>
  administratorPassword:
    value: <admin-password>
  # Burstable compute: the cheapest way to run a real PostgreSQL server.
  # Burstable SKUs do not support high availability or read replicas.
  skuName: B_Standard_B1ms
  databases:
    # Replace with your application database name (letters first, then
    # letters/digits/hyphens/underscores).
    - name: appdb
  firewallRules:
    # 0.0.0.0-0.0.0.0 admits Azure-internal services only (not the
    # internet); add your workstation or CI ranges as separate rules.
    - name: allow-azure-services
      startIpAddress: "0.0.0.0"
      endIpAddress: "0.0.0.0"
  tags:
    cost-center: <cost-center>
