apiVersion: auth0.planton.dev/v1alpha1
kind: Auth0Role
metadata:
  name: my-basic-role
  org: example-org
  env: production
spec:
  name: Basic
  description: A role whose permissions are managed separately
