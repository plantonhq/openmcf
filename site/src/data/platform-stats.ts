/**
 * Platform-wide statistics used across marketing pages, feature pages,
 * solutions pages, and the landing page. Single source of truth — update
 * these values here when counts change.
 */
export const PLATFORM_STATS = {
  DEPLOYMENT_MODULE_COUNT: '700+',
  CLOUD_PROVIDER_COUNT: '8',
  INFRA_CHART_COUNT: '50+',
} as const;
