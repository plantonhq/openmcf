apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: GCP RAG Vector Search
spec:
  selector:
    kind: platform
  description: Production vector search for RAG in one deploy — a streaming Vertex AI Vector Search index that upserts embeddings within seconds, the serving endpoint and deployed index that answer nearest-neighbor queries, an embeddings bucket for batch backfills, and a dedicated query identity with exactly the role it needs. One toggle moves serving from the public endpoint onto your VPC's private peering, with reserved-range pinning and optional JWT auth. The retrieval layer of a RAG stack, wired the way Vertex AI actually expects.
  iconUrl: https://assets.planton.ai/apis/infrahub/cloudresource/v1/assets/provider/gcp/gcpvertexaiendpoint/v1/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/gcp/rag-vector-search
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/gcp/rag-vector-search/README.md
