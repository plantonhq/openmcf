apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: GCP Spanner Application
spec:
  selector:
    kind: platform
  description: The Cloud Spanner backend for an application that cannot lose data or go down for maintenance — a provisioned instance sized by processing units (or managed autoscaling), a database with point-in-time recovery, and an explicit cron-driven backup schedule with real retention. The pieces every Spanner production checklist asks for, already wired in the right order and guarded against accidental deletion.
  iconUrl: https://assets.planton.ai/apis/infrahub/cloudresource/v1/assets/provider/gcp/gcpspannerinstance/v1/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/gcp/spanner-application
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/gcp/spanner-application/README.md
