apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: GCP Terraform State Backend
spec:
  selector:
    kind: platform
  description: A hardened GCS state backend for Terraform and OpenTofu — versioned, never public, with bounded version history, an optional customer-managed encryption key, and an optional dedicated state-access service account. Deploy it once, point your backend block (or your Planton StateBackend) at the bucket, and state handling is solved.
  iconUrl: https://assets.planton.ai/apis/connect/terraformbackendproviderconnection/v1/assets/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/gcp/terraform-state-backend
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/gcp/terraform-state-backend/README.md
