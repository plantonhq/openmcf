apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: AWS CI/CD Pipeline
spec:
  selector:
    kind: platform
  description: Push-to-production delivery on AWS — a CodePipeline V2 wired from your Git repository through a Docker-enabled CodeBuild project into a private ECR repository, with least-privilege roles, a hardened artifact bucket, and an optional rolling deploy to ECS.
  iconUrl: https://assets.planton.ai/apis/infrahub/cloudresource/v1/assets/provider/aws/awscodepipeline/v1/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/aws/ci-cd-pipeline
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/aws/ci-cd-pipeline/README.md
