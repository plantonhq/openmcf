apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: AWS Data Lakehouse
spec:
  selector:
    kind: platform
  description: A queryable data lake on S3 — Glue-cataloged storage with cost-tiering that never strands data, an Athena workgroup with a per-query spend guard, and an optional Firehose ingestion stream that lands partitioned data ready to query.
  iconUrl: https://assets.planton.ai/apis/infrahub/cloudresource/v1/assets/provider/aws/awsathenaworkgroup/v1/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/aws/data-lakehouse
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/aws/data-lakehouse/README.md
