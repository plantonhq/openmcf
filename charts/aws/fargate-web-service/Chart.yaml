apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: AWS Fargate Web Service
spec:
  selector:
    kind: platform
  description: Production container service on ECS Fargate — load-balanced, health-checked, request-count autoscaled, with an ECR repository and least-privilege IAM roles, plus HTTPS, WAF, and DNS toggles.
  iconUrl: https://assets.planton.ai/apis/infrahub/cloudresource/v1/assets/provider/aws/awsecsservice/v1/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/aws/fargate-web-service
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/aws/fargate-web-service/README.md
