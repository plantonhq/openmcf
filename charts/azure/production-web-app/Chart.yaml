apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: Production Web App
spec:
  selector:
    kind: platform
  description: A customer-facing web application from edge to database -- a VNet-integrated Linux App Service on PremiumV3, a private PostgreSQL Flexible Server, RBAC-mode Key Vault, WAF-fronted Azure Front Door with origin lockdown, and the observability pack (diagnostics, outside-in availability test, paging alert). Optional custom-domain wiring self-completes Front Door validation through DNS records.
  iconUrl: https://assets.planton.ai/apis/infrahub/cloudresource/v1/assets/provider/azure/azurelinuxwebapp/v1/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/azure/production-web-app
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/azure/production-web-app/README.md
