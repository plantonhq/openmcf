apiVersion: infra-hub.planton.ai/v1
kind: InfraChart
metadata:
  name: Azure AKS Baseline
spec:
  selector:
    kind: platform
  description: A production AKS cluster that scales itself between the bounds you set and backs itself up on a schedule — private nodes behind one NAT gateway, keyless workload identity, TLS, DNS, and ingress already wired.
  iconUrl: https://assets.planton.ai/apis/infrahub/cloudresource/v1/assets/provider/azure/azureakscluster/v1/logo.svg
  isReady: true
  webLinks:
    chartWebUrl: https://github.com/plantonhq/planton/tree/main/charts/azure/kubernetes/aks-baseline
    readmeRawUrl: https://raw.githubusercontent.com/plantonhq/planton/refs/heads/main/charts/azure/kubernetes/aks-baseline/README.md
